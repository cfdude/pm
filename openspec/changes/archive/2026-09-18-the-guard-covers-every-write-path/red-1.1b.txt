WHAT THIS CAPTURE ACTUALLY SHOWS — corrected at Gate 2 (finding G2-M3), and not re-run.

This file was presented as the RED for task 1.1b. It is a MODULE-LOAD SyntaxError: the test file could
not be imported at all, because `scripts/lib/gate-guard.mjs` did not yet export `WRITE_SHAPE_LABELS`.
red-1.1.txt, red-1.1b.txt and red-1.3.txt are therefore byte-identical apart from timings, and the
tests each one names NEVER RAN. The capture proves the export was absent. It does NOT prove that
task 1.1b's assertions discriminate — which is what a RED is for.

Re-capturing it honestly is not available: against an engine with no such export, a module-load
failure IS the truthful output of this test file, and any capture that ran the named tests would
have to be taken against an engine that already has the GREEN. Weakening the test to make it import
would be worse than the missing evidence. So the discrimination evidence is supplied the only way it
can be — by MUTATION against the shipped engine, one row neutered at a time, in
red-1.x-mutation-evidence.txt. Each mutant below reddens exactly the assertions this task added:

  M1  the record row deleted ................ 3 fail, incl. 2.1 "destroying the conductor record is
                                              blocked through the hook, trailing globs included"
  M2  trailing-glob alternatives dropped .... 4 fail, incl. 1.3's single-definition-site scan
  M3  the record match made a PREFIX match .. 3 fail, incl. "removing the state lock stays runnable"
  M4  unquoteOnce neutered .................. 3 fail, incl. 1.1b's predicate assertions
  M9  the engine predicate always false ..... 1 fail — 1.1b, and ONLY 1.1b, which is the point
  M9b the predicate's variable form dropped . 1 fail — 1.1b
  M18 the command-word list emptied ......... 3 fail
  M18b only `rsync` removed ................. 1 fail (1.3's source scan) on the engine as reviewed —
                                              task 4.8 adds the behavioural row that now fails too

  M9c the predicate's CALL SITE deleted ..... 0 fail, AND THAT IS EXPECTED AND RECORDED. No
                                              command-word row is reachable from a `node`-led
                                              segment (the arm reads the leading word, always the
                                              runtime), so the call site changes no command's
                                              outcome. This is the structural fact the
                                              `gate-integrity` delta already carries as a forward
                                              commitment, and the reason the predicate is exported
                                              and asserted directly.
  M10c requirePlatformFlag reordered ........ 0 fail; the documented fail-open surface is unchanged
                                              by the move, so nothing observable differs.

Evidence source: Gate 2 lens 1's mutation battery, run against the shipped engine in a real clone at
65d3e7e. The original capture follows unchanged below.

================================================================================
file:///Users/robsherman/Documents/Repos/pm/scripts/test/gate-guard-write-paths.test.mjs:17
import { writeShape, isEngineInvocation, WRITE_SHAPE_LABELS } from "../lib/gate-guard.mjs";
                                         ^^^^^^^^^^^^^^^^^^
SyntaxError: The requested module '../lib/gate-guard.mjs' does not provide an export named 'WRITE_SHAPE_LABELS'
    at #asyncInstantiate (node:internal/modules/esm/module_job:463:21)
    at async ModuleJob.run (node:internal/modules/esm/module_job:561:5)
    at async node:internal/modules/esm/loader:650:26
    at async asyncRunEntryPointWithESMLoader (node:internal/modules/run_main:101:5)

Node.js v26.8.2
✖ scripts/test/gate-guard-write-paths.test.mjs (37.570625ms)
ℹ tests 1
ℹ suites 0
ℹ pass 0
ℹ fail 1
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 41.049584

✖ failing tests:

test at scripts/test/gate-guard-write-paths.test.mjs:1:1
✖ scripts/test/gate-guard-write-paths.test.mjs (37.570625ms)
  'test failed'
