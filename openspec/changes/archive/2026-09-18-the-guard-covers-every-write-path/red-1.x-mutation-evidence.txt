Gate 2 lens 1's mutation battery against the SHIPPED engine — the discrimination evidence that
red-1.1.txt, red-1.1b.txt and red-1.3.txt could not carry, because each of those is a module-load
SyntaxError in which the named tests never ran (finding G2-M3).

Method: one row of the closed shape list, or one predicate, neutered at a time in a clone at
65d3e7e; the full `scripts/test/gate-guard-write-paths.test.mjs` run against each; DIED = at least
one test failed, and the failing test names follow. LIVED = no test failed.

The two LIVED lines are expected and are recorded rather than fixed — see red-1.1b.txt for why the
predicate's CALL SITE is structurally undetectable, which is a forward commitment in the
`gate-integrity` delta rather than a gap.

Two further LIVED mutants — MSEG (only the first segment scanned) and M19 (the hook's whole-command
read reduced to line one) — WERE gaps, and were closed by task 4.7. Their post-fix runs are in
red-4.6-mutation-evidence.txt.

================================================================================
DIED  M1 record row deleted — fail=3 :: 1.1 every recognized write shape is matched | 1.1 the return value carries no text taken from the command | 2.1 destroying the conductor record is blocked through the hook, trailing globs included
DIED  M2 trailing-glob alternatives dropped — fail=4 :: 1.1 every recognized write shape is matched | 1.1 the return value carries no text taken from the command | 1.3 REGRESSION GUARD: the closed shape list has exactly one definition site
DIED  M3 record match becomes a prefix match — fail=3 :: 1.1 no read-only command, lock remedy or engine invocation is matched | 2.3 a read-only Bash command is not blocked while a reconcile is owed | 2.3 removing the state lock stays runnable through the hook, glob spelling included
DIED  M4 unquoteOnce neutered — fail=3 :: 1.1 every recognized write shape is matched | 1.1 the return value carries no text taken from the command | 1.1b the engine-invocation predicate recognizes both spellings pm emits
DIED  M5 fd-duplication exclusion removed — fail=2 :: 1.1 no read-only command, lock remedy or engine invocation is matched | 2.3 a read-only Bash command is not blocked while a reconcile is owed | 1.1 no read-only command, lock remedy or engine invocation is matched
DIED  M6 device-path exclusion removed — fail=3 :: 1.1 no read-only command, lock remedy or engine invocation is matched | 2.3 a read-only Bash command is not blocked while a reconcile is owed | 2.7 the refresh block covers a Bash write on the same terms, and names the shape
DIED  M7 arrow exclusion removed — fail=3 :: 1.1 no read-only command, lock remedy or engine invocation is matched | 2.3 a read-only Bash command is not blocked while a reconcile is owed | 2.3 every engine invocation the gate names as its own exit stays runnable
DIED  M8 command-less Bash payload allowed — fail=2 :: 2.8c a payload naming Bash with no readable command blocks | 2.5 a Bash payload with no readable command does not inherit the exemption | 2.8c a payload naming Bash with no readable command blocks
DIED  M8b tool_input non-object Bash allowed — fail=2 :: 2.8c a payload naming Bash with no readable command blocks | 2.5 a Bash payload with no readable command does not inherit the exemption | 2.8c a payload naming Bash with no readable command blocks
DIED  M9 engine predicate always false — fail=1 :: 1.1b the engine-invocation predicate recognizes both spellings pm emits | 1.1b the engine-invocation predicate recognizes both spellings pm emits
DIED  M9b engine predicate: variable form unrecognized — fail=1 :: 1.1b the engine-invocation predicate recognizes both spellings pm emits | 1.1b the engine-invocation predicate recognizes both spellings pm emits
LIVED M9c engine predicate call site deleted — fail=0
DIED  M10 exemption applied unconditionally before the load — fail=8 :: 2.1 a heredoc redirection is blocked while a reconcile is owed, and the block names the shape | 2.1 an in-place stream editor is blocked | 2.1 the block message carries a label, not the command
DIED  M10b exemption not applied at the load (removed) — fail=1 :: 2.5 every remedy the unreadable-state message prints stays runnable | 2.5 every remedy the unreadable-state message prints stays runnable
LIVED M10c requirePlatformFlag moved after the exemption decision+load — fail=0
DIED  M11 no-clobber operator alternative dropped — fail=2 :: 1.1 every recognized write shape is matched | 1.1 the return value carries no text taken from the command | 1.1 every recognized write shape is matched
DIED  M11b segments split on every pipe — fail=2 :: 1.1 every recognized write shape is matched | 1.1 the return value carries no text taken from the command | 1.1 every recognized write shape is matched
DIED  M12 redirection arm skipped for engine segments — fail=3 :: 1.1b the exemption covers the command-word arm only — a redirection still blocks | 1.1b an UNFILLED command template blocks, and the filled command passes | 2.3 every engine invocation the gate names as its own exit stays runnable
DIED  M14 tracker arm ignores the shape (blocks any Bash) — fail=1 :: 2.7 the refresh block covers a Bash write on the same terms, and names the shape | 2.7 the refresh block covers a Bash write on the same terms, and names the shape
DIED  M15 reconcile arm gated by the flag — fail=7 :: 2.1 a heredoc redirection is blocked while a reconcile is owed, and the block names the shape | 2.1 an in-place stream editor is blocked | 2.1 the block message carries a label, not the command
DIED  M16 label replaced by command text — fail=7 :: 1.1 every recognized write shape is matched | 1.1 the return value carries no text taken from the command | 1.1b the exemption covers the command-word arm only — a redirection still blocks
DIED  M18b only rsync removed from the command-word list — fail=1 :: 1.3 REGRESSION GUARD: the closed shape list has exactly one definition site | 1.3 REGRESSION GUARD: the closed shape list has exactly one definition site
DIED  M17 in-place long spelling unrecognized — fail=2 :: 1.1 every recognized write shape is matched | 1.1 the return value carries no text taken from the command | 1.1 every recognized write shape is matched
DIED  M18 command-word shapes emptied — fail=3 :: 1.1 every recognized write shape is matched | 1.1 the return value carries no text taken from the command | 1.3 REGRESSION GUARD: the closed shape list has exactly one definition site
LIVED MSEG only the first segment is scanned — fail=0
LIVED M19 whole-command read reduced to first line — fail=0
porcelain after: ""
