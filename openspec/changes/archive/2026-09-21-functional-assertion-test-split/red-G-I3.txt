G-I3 RED — the hook was not held to "does not run the functional half".

REGRESSION-GUARD style: the new test passes the moment it exists, so the RED is the MUTATION, made in
a copy of this working tree at .../g2fix-47/gi3/pm. Nothing was weakened to make anything pass.

MUTANT — the hook's runner gains the triggered half:
  `if node --test --test-isolation=none scripts/test/assert/*.test.mjs >"$tmpfile" 2>&1; then`
  → the same line with `scripts/test/functional/*.test.mjs` appended to the file list.

The fixture's scripts/test/functional/ holds a marker test that calls assert.fail("the hook ran the
functional half"), with its assertion twin in the fixture's assert half (a functional id with no twin
is refused by the drift script, so without the twin this would be testing the wrong refusal).

  $ node --test --test-name-pattern="G-I3 the hook does NOT run" scripts/test/functional/conductor-09.test.mjs
  ✖ G-I3 the hook does NOT run the functional half — a marker file there is never picked up
    AssertionError: the hook must run only the passing assertion half:
      drift: ok — 3 tracked test files, 1 functional ids, 3 staged paths
      ✖ G-I3 MARKER the hook must never run this functional file
        AssertionError: the hook ran the functional half

Before this fix the same edit left EVERYTHING green — the whole assertion half (1237) and the hook's
own functional tests — which is what the Gate 2 reviewer measured.

CONTROL — in this working tree, with the mutation reverted: the G-I3 test passes
(1/1, "pre-commit: 2/2 passing" in the fixture, no marker in the output).
