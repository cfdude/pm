0.47.0 task 7.2 — DATA REFERENCES ARE CALL SITES. Verified in a COPY of the repository
(scratchpad/apply-47/run-5x/mutate/pm), holding the record a real `certify functional` +
`certify sweeps` produced in the repository itself. The working tree was never touched.

== the record as certified ==
  engine-source  (trigger, covers=1)
  scripts/conductor.mjs  (module, covers=1, rows=12)
  scripts/lib/commit-watch.mjs  (module, covers=0)
  scripts/lib/constants.mjs  (module, covers=12)
  scripts/lib/created-at.mjs  (module, covers=1)
  scripts/lib/git.mjs  (module, covers=6)
  scripts/lib/subcommands.mjs  (module, covers=3)
  scripts/lib/tool-currency.mjs  (module, covers=2)
  scripts/lib/worktree-hygiene.mjs  (module, covers=1)

-- baseline: the record against the tree it was written over --
$ node scripts/test/drift.mjs --root $PWD
drift: ok — 133 tracked test files, 46 functional ids, 0 staged paths
exit=0

== 1. RENAME one module in the gateway set: the record is REFUSED, not silently not-covering ==
$ node scripts/test/drift.mjs --root $PWD
drift: ABORT — the suite's two halves have drifted apart

the certified module 'scripts/lib/git-renamed.mjs' changed (scripts/lib/git-renamed.mjs), and no record entry covers this content. Run `node scripts/test/certify.mjs functional` to certify the new content.
the certified module 'engine-source' changed (scripts/lib/git-renamed.mjs), and the record covers DIFFERENT content — it was written before this change. Run `node scripts/test/certify.mjs sweeps` to certify the new content.
the record holds an entry for 'scripts/lib/git.mjs': the record certifies an id that is no longer in the certified set — a module was renamed or removed, and a record keyed on the old name reads as a certification of nothing. Delete the entry or re-run `node scripts/test/certify.mjs functional`.

drift: the checks are enrolment, twin coverage, diff coupling and record freshness.
drift: `node scripts/test/certify.mjs functional|sweeps` produces the record a freshness refusal names.
exit=1

== 2. DELETE one conformance ROW: the conductor.mjs entry is REFUSED, BY THE ROW ==
  (removed one ROWS entry; the FILE and every other id are untouched)
$ node scripts/test/drift.mjs --root $PWD
drift: ABORT — the suite's two halves have drifted apart

the record's entry for 'scripts/conductor.mjs' names conformance — "an unknown verb — the global usage" in its covers: a conformance ROW the record was written over is gone; deleting a row deletes the only observation of that refusal class, and the record must not keep claiming it. Re-run `node scripts/test/certify.mjs functional`.

drift: the checks are enrolment, twin coverage, diff coupling and record freshness.
drift: `node scripts/test/certify.mjs functional|sweeps` produces the record a freshness refusal names.
exit=1

== 3. DELETE a functional test file a module's covers names ==
$ node scripts/test/drift.mjs --root $PWD
drift: ABORT — the suite's two halves have drifted apart

the record's entry for 'scripts/lib/git.mjs' names tool-currency in its covers: the record names a test id that no longer exists in either half or the sweep bucket — the entry would read as covered by a test nobody can run. Re-run `node scripts/test/certify.mjs functional`.

drift: the checks are enrolment, twin coverage, diff coupling and record freshness.
drift: `node scripts/test/certify.mjs functional|sweeps` produces the record a freshness refusal names.
exit=1

== restored: accepted again ==
drift: ok — 133 tracked test files, 46 functional ids, 1 staged paths
exit=0
