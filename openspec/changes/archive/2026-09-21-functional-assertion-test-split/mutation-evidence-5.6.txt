0.47.0 task 5.6 — MUTATION EVIDENCE for the source-reading guards.

5.6's own verify line: "each guard still fails on a deliberate mutation of the thing it guards — a
repair that only makes it green is the failure this list exists to catch." So each guard below was
mutated and the run recorded. EVERY MUTATION WAS APPLIED IN A COPY OF THE REPOSITORY
(scratchpad/apply-47/run-5x/mutate/pm, a full `cp -R` including .git), never in the working tree,
and each was reverted with `git checkout -- <file>` in the copy before the next one.

A GREEN result under a mutation is recorded as a FINDING rather than dropped: it means the guard
does not discriminate on that arm, and one of them was a real defect (see #3).

================================================================
1. conductor-25 — the dispatch-object reader and its `}[cmd]` anchor
   MUTATION (guard): `const end = body.indexOf("}[cmd]");` → `/*MUTATED*/`
   RESULT: RED. 16 tests, 15 pass, 1 fail —
     ✖ gh-85: every dispatched verb declares whether it mutates the working tree
   The reader's own `assert.notEqual(end, -1)` fires, which is the assertion that exists so a
   reader that finds nothing cannot go green against an EMPTY verb set.

2. verb-surface — the dispatch-table regex
   MUTATION (guard): `const m = src.match(/\(\{\n([\s\S]*?)\n\s*\}\[cmd\]/m);` → `/*MUTATED*/`
   RESULT: RED. 8 tests, 5 pass, 3 fail —
     ✖ VERB_POSITIONALS is set-equal to the dispatch table
     ✖ Every dispatched verb refuses an undeclared flag and writes nothing
     ✖ A help token after every verb's working invocation prints help and writes nothing
   Three of the file's checks are built on the derived set, so a broken derivation is caught rather
   than silently shrinking the sweep to nothing.

3. conductor-13 — "update-epic holds no openspec-lane archive condition of its own"
   MUTATION 1 (guarded thing): alias the imported symbol — `archiveGate as archiveGateX` in the
   import AND `archiveGate(epic, {` → `archiveGateX(epic, {`.
   RESULT: **GREEN — A FINDING, AND IT WAS A REAL DEFECT.** The guard read the RAW source and
   matched `/archiveGate\(/`, and the verb's own prose calls the gate by name:
     scripts/lib/update-epic.mjs:561  // The REPLACEMENT RULE, one level down from archiveGate()'s …
   so the guard was satisfied by a COMMENT. It could not have failed for the reason it claims: the
   call it is supposed to be watching could be deleted and the guard would still pass.
   REPAIR: the read drops comment-only lines before matching (`codeLines()`), and the hand-rolled
   character-level stripper tried first is DELIBERATELY NOT used — measured on this same file it
   desyncs on a regex literal holding a quote, and every comment after that point passes through
   VERBATIM, which is a false NEGATIVE; the line-oriented form cannot desync.
   MUTATION 2 (guarded thing, after the repair): same alias mutation.
   RESULT: RED. 20 tests, 19 pass, 1 fail —
     ✖ update-epic holds no openspec-lane archive condition of its own
   (unmutated: 20/20 pass). The repair is mutation-verified rather than merely green.

4. save-report-surface — the saveState() call-site scan
   MUTATION (guard): `sites.push({ file, line: n + 1, captured, exemptReason, … });` → `void captured; void exemptReason;`
   RESULT: RED. 5 tests, 4 pass, 1 fail —
     ✖ the scan finds the engine's saveState call sites at all — a stripper that ate the source
       passes vacuously otherwise
   The vacuity check is what fires, which is the half of this guard that makes the other assertions
   mean anything.

5. hermetic-git — the narrowed predicate
   MUTATION (guard): drop the exclusion —
     `return /["']git["']/.test(src) && !/functional-harness\.mjs|helpers\.mjs|hermetic-git\.mjs/.test(src);`
     → `return /["']git["']/.test(src);`
   RESULT: RED. 3 tests, 2 pass, 1 fail —
     ✖ every FUNCTIONAL test file that mentions git imports the hermetic module, directly or
       through its harness
   The narrowing 5.7 made is load-bearing and not a no-op: without the exclusion the guard fires on
   the harness-importing files, which is exactly the shape the narrowing was written for.

6. output-interpolations — DIRECT_OUTPUT_CALLS
   MUTATION 6a (guard, whole list): the whole regex → `/(?!)\./g` (matches nothing).
   RESULT: RED. 25 tests, 24 pass, 1 fail —
     ✖ mutant (Gate 2 W-I2): render's PROJECT_MD — a path under CLAUDE_PROJECT_DIR — printed raw
       is UNCLASSIFIED, though render() is judged sink-flow as a whole
   MUTATION 6b (guard, ONE arm): remove ONLY `|fs\.writeSync` from the alternation.
   RESULT: **GREEN — FINDING (minor).** 25/25 pass. The `fs.writeSync` arm is not independently
   discriminated by any test: the file's own `fs.writeSync` case exercises the NOT_OUTPUT_CALLS
   exclusion (`fs\.(?!writeSync\b)…`), not this inclusion. The list as a whole IS load-bearing
   (6a); this arm is redundant belt-and-braces today.

7. output-text-integrity — the refusal-text predicate `String(err.stderr||"") + String(err.stdout||"")`
   MUTATION 7a (guard): drop the stdout half.
   RESULT: GREEN. 11 tests, 11 pass.
   MUTATION 7b (guarded thing): `die()` writes `invocation().stdout` instead of
   `invocation().stderr` (scripts/lib/command-exit.mjs), predicate intact.
   RESULT: GREEN. 11 tests, 11 pass.
   MUTATION 7c (both): 7a + 7b together.
   RESULT: GREEN. 11 tests, 11 pass.
   FINDING (minor, and NOT a defect in the guard): the stdout half is not reachable today. The
   pattern is LINE-ANCHORED (`/^conductor: FORGED$/m`) and the engine escapes the injected newline
   before the text is written at all, so routing refusals to the other stream changes nothing. The
   predicate is a SUPERSET of what the subject needs — a guard checking more than it must is not
   weakened — and the discrimination on this file's real subject was proved separately:
   MUTATION 7d (guarded thing): `escapeControls` neutered to identity (scripts/lib/constants.mjs),
   predicate intact.
   RESULT: RED. 1 fail. This file watches what it claims to watch (an unescaped control character
   reaching rendered output); the `+ stdout` half is the part that cannot be isolated as
   discriminating, and that is recorded rather than papered over.

8. assert-half-has-no-spawn — the guard over the half it walks
   MUTATION (a file in the walked directory): append to scripts/test/assert/conductor-05.test.mjs
     import { spawnSync } from "node:child_process";
     test("__mutant", () => { spawnSync("git", ["status"]); });
   RESULT: RED. 2 tests, 1 pass, 1 fail —
     ✖ 5.2 the assertion half spawns no child process and runs no git
     + 'conductor-05.test.mjs reaches node:child_process — a child process in the assertion half'
   The refusal NAMES the file, which is the requirement rather than merely failing.

================================================================
SUMMARY

  6 of the 8 guards were RED under a mutation of the thing they guard, one of them only after a
  REPAIR that the mutation itself exposed (#3, a guard a comment could satisfy).
  2 arms are GREEN under a mutation and are recorded as findings rather than dropped:
  #6b (a redundant alternation arm) and #7 (a superset predicate whose extra half is unreachable
  today). Neither weakens the guard it belongs to; both are stated so the next reader does not
  mistake them for coverage.

  NOTHING in this file was applied to the repository's working tree. The only change 5.6 makes to
  the repository is the conductor-13 repair in #3.
