0.47.0 task 1.3 — MUTATION EVIDENCE, both halves.

A guard whose failure mode is 'the whole process died' proves nothing, and a conformance row that cannot
discriminate is a green light nobody checked. So each was mutated deliberately and the failure recorded.

================================================================
MUTATION A — 1.2's exit guard, against a re-introduced inline exit
  $ printf '
function __mutantExit() { process.exit(1); }
' >> scripts/lib/rank.mjs
  $ node --test scripts/test/no-inline-exit.test.mjs

  AssertionError [ERR_ASSERTION]: an executable process.exit() kills the assertion half's ONE shared process. Every refusal must go through die() in lib/command-exit.mjs, which writes the message and throws CommandExit. The CLI tail assigns process.exitCode, which this guard does not match.
  + actual - expected
  
  + [
  +   'lib/rank.mjs:119: function __mutantExit() { process.exit(1); }'
  + ]
  - []
  
      at TestContext.<anonymous> (file:///Users/robsherman/Documents/Repos/pm/scripts/test/no-inline-exit.test.mjs:127:10)

  VERDICT: the GUARD went red (2 of 3 tests; the engine-scan test failed and named the site).
  The suite did not die — which is the point: this guard is on the assertion half's side of the
  failure the change exists to remove.

================================================================
MUTATION B — 1.1's conformance set, against one neutered refusal class
  $ sed -i '' 's/^      return 1;$/      return 0;/' scripts/conductor.mjs   # the pre-dispatch refusal's status
  $ node --test scripts/test/conformance.test.mjs

✖ conformance: a command-line refusal — an undeclared flag on a dispatched verb — the returned status equals the exited status (394.558542ms)
✖ conformance: nothing the engine prints for an invocation reaches the process's own streams (367.0385ms)
ℹ tests 15
ℹ pass 13
ℹ fail 2
✖ conformance: a command-line refusal — an undeclared flag on a dispatched verb — the returned status equals the exited status (394.558542ms)
✖ conformance: nothing the engine prints for an invocation reaches the process's own streams (367.0385ms)

  VERDICT: the mutated class's row went red, and it is the ONLY class row that did. The second
  failure is the no-leak test, which drives the SAME refusal and asserts its status too — a
  second observation of one mutation, not a second class. Every other row stayed green, which is
  what "the set DISCRIMINATES" means: it is not merely a set that can fail.

================================================================
MUTATION C — task 3.5, the captured root the assertion half must not observe

  $ # constants.mjs, temporarily:
  $ const __CAPTURED_ROOT = process.env.CLAUDE_PROJECT_DIR || process.cwd();   // MUTANT
  $ export const engineRoot = () => __CAPTURED_ROOT;
  $ node --test scripts/test/per-call-roots.test.mjs
  ✖ 3.1 two roots in one process: the first initialized, the second not — each call lands under its own
  ✖ 3.1 the argument list is the one the caller passed, not the running process's
  ✖ 3.6 changedFiles() strips the prefix of the root THAT invocation was given
  ℹ tests 3   pass 0   fail 3

  VERDICT: the guard FAILS, it does not flake, and it names the property rather than a symptom —
  2 of 3 tests are 3.1's own. Reverted immediately; the unmutated run is 3/3 passing, recorded above
  the mutation.

================================================================
NAMED, NOT DONE IN THIS COMMIT

  - conductor-38's static guard still scans for a big stdout write followed by `process.exit(`. It
    passes (there are none left in scripts/lib, and conductor.mjs's five are gone too, so it is now
    green against an EMPTY set on the engine side). Re-pointing it is task 2.6's, where the last of
    the exits it watches disappear from the CLI's own control flow.
  - 3.3/3.4's guards are done; the two that 2.5 changes (the activity log's instrumentation) are not.
