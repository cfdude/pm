G-I4 RED — the assertion half ran the REAL git binary, and no guard could see it.

THE DEFECT. scripts/test/assert/conductor-33.test.mjs called appendEvents
(scripts/lib/activity-log.mjs) DIRECTLY, outside main(), with CLAUDE_PROJECT_DIR set on the process.
`invocation()` therefore answered with the live PROCESS_CONTEXT, `gitOps()` built the real gateway,
and the detach probe inside appendEvents ran `git symbolic-ref --quiet HEAD` — a real git process in
the half whose contract is that it runs no git. Nothing in the offending FILE contains a spawn call,
so 5.2's source scan (over the half's files) and 4.1's derivation (over the engine's modules) were both
blind to it.

RED — the full assertion half, with the new PATH shim installed and nothing else changed
(the shim counts and an exit listener fails the process; it does not otherwise alter behaviour):

  $ node --test --test-isolation=none scripts/test/assert/*.test.mjs
  ASSERT HALF EXIT=1
  ℹ tests 1240   ℹ pass 1240   ℹ fail 0
  assertion half: 1 REAL 'git' invocation(s) reached the shim — the half must run no git
    git symbolic-ref --quiet HEAD

Every test passed and the HALF failed: that is the guard working, and it is exactly why the earlier
`git` shim that merely exited 128 proved nothing (the reviewer's shim, and this one's behaviour on the
call, are indistinguishable to the engine — a Popen failure is tolerated).

GREEN — the same call driven under an installed invocation (withAssertInvocation, this half's
double), in this working tree:

  $ node --test --test-isolation=none scripts/test/assert/*.test.mjs
  ASSERT HALF EXIT=0
  ℹ tests 1240   ℹ pass 1240   ℹ fail 0
  (0 occurrences of "REAL 'git' invocation")

MUTANT — the direct call restored in a copy at .../g2fix-47/gi4/pm, everything else identical:

  $ node --test --test-isolation=none scripts/test/assert/*.test.mjs
  ASSERT HALF EXIT=1
  ℹ tests 1240   ℹ pass 1240   ℹ fail 0
  assertion half: 1 REAL 'git' invocation(s) reached the shim
    git symbolic-ref --quiet HEAD

The exit listener therefore holds the property for the WHOLE half (including files that run after the
guard test itself); the guard test asserts the same fact directly at the point it runs, plus that the
shim is first on PATH.
