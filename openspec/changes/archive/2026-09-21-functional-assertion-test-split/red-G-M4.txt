G-M4 RED — the conformance twin asserted nothing about stream leaks.

WHAT WAS MISSING. `scripts/test/assert/conformance.test.mjs`'s "nothing the engine prints for an
invocation reaches the process's own streams" checked only that the call produced output and returned
a number — an engine that ALSO wrote to `process.stderr` satisfied it. The real check existed only in
the functional twin (`withLeakWatch`), so the per-commit half held the property not at all.

THE MUTANT — `die()` gains one line writing the refusal to `process.stderr` as well as to the
invocation's, in a copy at .../g2fix-47/gm4/pm:

  scripts/lib/command-exit.mjs
    export function die(message, code = 1) {
      invocation().stderr.write(message);
  +   process.stderr.write(message); // G-M4 mutant

  $ node --test --test-name-pattern="nothing the engine prints" scripts/test/assert/conformance.test.mjs
  ✖ conformance: nothing the engine prints for an invocation reaches the process's own streams
    AssertionError: a refused invocation wrote to the process's own streams:
      "conductor: epic 'ghost' not found\n"

WHY THE SECOND CASE IS IN THE TEST. The unknown-verb refusal does NOT catch this mutant — it is
refused before dispatch and never reaches `die()` — so a leak test that used only that case would
have been green against a leaking refusal path. The die()-borne refusal (`update-epic ghost …`, after
`init`) is the one that fires, which is asserted here rather than left to the reader.

CONTROL — the same test in this working tree: 1 test, 1 passing, `leaked === ""` for both the
successful and the refused invocation.

The capture is in `fixtures/harness.mjs`'s `invokeEngine` — the two writers are patched-and-forwarded
for the duration of the call and the result is returned as `leaked` — so BOTH halves get it and the
functional twin's own capture is unchanged.
