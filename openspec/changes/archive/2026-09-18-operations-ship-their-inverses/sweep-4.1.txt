CALL-SITE COMPLETENESS SWEEP — task 4.1. Every list below is rg output at sweep time, never typed.
Run: 2026-09-18T05:32:45Z, at c7a288d.

════ A. rg -ni "pre-authoriz|preauthoriz" scripts/ commands/ skills/ agents/ hooks/ ════
agents/hierarchy-child-executor.md
commands/epic.md
commands/status.md
scripts/lib/add-epic.mjs
scripts/lib/autonomy.mjs
scripts/lib/constants.mjs
scripts/lib/gate-guard.mjs
scripts/lib/integrity.mjs
scripts/lib/rules.mjs
scripts/test/autonomy-revocation.test.mjs
scripts/test/conductor-03.test.mjs
scripts/test/conductor-13.test.mjs
scripts/test/conductor-16.test.mjs
scripts/test/conductor-31.test.mjs
scripts/test/emitted-invocations.test.mjs
scripts/test/fixtures/state-pre-disposition-walk.json
scripts/test/output-text-integrity.test.mjs
scripts/test/stored-value-integrity.test.mjs
skills/conductor/SKILL.md
-- per-file hits (source only) --
   1 scripts/lib/rules.mjs:    "   a. Already pre-authorized in the preflight — either an exact `action` match or the",
   1 scripts/lib/rules.mjs:    "   `set-autonomy <id> --preauthorize \"<action>:<reason>\"` / `--context \"<note>\"`, and only",
   1 scripts/lib/rules.mjs:    "   `--preauthorize \"category:<filesystem|network|schema|external-api>:<reason>\"` — see the",
   1 scripts/lib/rules.mjs:      "   was a safety surface: pre-authorization grants accumulated with no revoke, so turning",
   1 scripts/lib/integrity.mjs:    title: "an autonomy pre-authorization whose action and category are both empty",
   1 scripts/lib/integrity.mjs:        getAutonomy(e).preAuthorized.forEach((g, i) => {
   1 scripts/lib/integrity.mjs:            `autonomy.preAuthorized[${i}] names neither an action nor a category. A grant that names ` +
   1 scripts/lib/gate-guard.mjs:  // pre-authorized. The escape hatch is not optional here: an agent that is offline,
   1 scripts/lib/constants.mjs:export const KNOWN_PREAUTHORIZE_CATEGORIES = ["filesystem", "network", "schema", "external-api"];
   1 scripts/lib/constants.mjs:// is not a breaking change for existing preAuthorized entries.
   1 scripts/lib/constants.mjs:// Default category taxonomy for the `--preauthorize "category:<name>:<reason>"` shorthand —
   1 scripts/lib/constants.mjs:  // THE INVERSE `--preauthorize` NEVER SHIPPED (operations-ship-their-inverses). TWO flags and not
   1 scripts/lib/constants.mjs:  // one colon-packed value, and that is forced rather than stylistic: `--preauthorize` splits on the
   1 scripts/lib/constants.mjs:  { flag: "preauthorize", commands: ["set-autonomy"], repeats: true },
   1 scripts/lib/autonomy.mjs:import { KNOWN_AUTONOMY_LEVELS, KNOWN_PREAUTHORIZE_CATEGORIES, escapeControls } from "./constants.mjs";
   1 scripts/lib/autonomy.mjs:const DEFAULT_AUTONOMY = Object.freeze({ level: "off", preAuthorized: [], context: [], notifications: [] });
   1 scripts/lib/autonomy.mjs:/** `set-autonomy <id> [--level off|autonomous] [--preauthorize "<action>:<reason>"]
   1 scripts/lib/autonomy.mjs: *  to preAuthorized/context/notifications, never clobber) except --level, which replaces.
   1 scripts/lib/autonomy.mjs: *  A `--preauthorize` value starting with "category:" is stored as a category-based grant
   1 scripts/lib/autonomy.mjs: *  `preAuthorized[]` carrying this stamp — on the precedent `linkOnce()`'s `superseded` and
   1 scripts/lib/autonomy.mjs: *  `--preauthorize` value pasted back. Whatever `--preauthorize` stored is exactly what
   1 scripts/lib/autonomy.mjs: *  [--preauthorize "category:<name>:<reason>"] [--context "<note>"] [--notify "<what>"]` —
   1 scripts/lib/autonomy.mjs: *  (`{ action, reason, grantedAt }`, no `category` field) — see KNOWN_PREAUTHORIZE_CATEGORIES
   1 scripts/lib/autonomy.mjs:  return (autonomy && Array.isArray(autonomy.preAuthorized) ? autonomy.preAuthorized : [])
   1 scripts/lib/autonomy.mjs:  for (const s of (f.preauthorize || [])) {
   1 scripts/lib/autonomy.mjs:  // THE REVOKE RUNS BEFORE THE GRANTS BELOW, so `--revoke X --preauthorize "X:<new reason>"` in one
   1 scripts/lib/autonomy.mjs:    preAuthorized: Array.isArray(a.preAuthorized) ? a.preAuthorized : [],
   1 scripts/lib/autonomy.mjs:    const matches = a.preAuthorized.filter(g => namesGrant(g, target));
   1 scripts/lib/autonomy.mjs:    a.preAuthorized = a.preAuthorized.map(g =>
   1 scripts/lib/autonomy.mjs:    a.preAuthorized = [...a.preAuthorized, entry];

════ A2. the NARROWER pattern, run as the task demands, to show what it misses ════
agents/hierarchy-child-executor.md:18:- Its `autonomy` block from `.conductor/state.json` — `preAuthorized` actions, `context` notes
agents/hierarchy-child-executor.md:50:a. An action already covered by `preAuthorized`? → proceed, note it.
scripts/lib/autonomy.mjs:117:  const a = { ...getAutonomy(epic) };
scripts/lib/autonomy.mjs:13:// getAutonomy() is the ONLY place that should read epic.autonomy directly; everywhere
scripts/lib/autonomy.mjs:135:    // `.conductor/state.json` byte-identical: nothing is saved and `epic.autonomy` is never
scripts/lib/autonomy.mjs:144:    const matches = a.preAuthorized.filter(g => namesGrant(g, target));
scripts/lib/autonomy.mjs:16:const DEFAULT_AUTONOMY = Object.freeze({ level: "off", preAuthorized: [], context: [], notifications: [] });
scripts/lib/autonomy.mjs:162:    a.preAuthorized = a.preAuthorized.map(g =>
scripts/lib/autonomy.mjs:17:export function getAutonomy(epic) {
scripts/lib/autonomy.mjs:18:  const a = epic.autonomy;
scripts/lib/autonomy.mjs:185:      a.preAuthorized = [...a.preAuthorized, entry];
scripts/lib/autonomy.mjs:2:// Per-epic autonomy read (getAutonomy) and the set-autonomy CLI verb. Circular with
scripts/lib/autonomy.mjs:211:    a.preAuthorized = [...a.preAuthorized, entry];
scripts/lib/autonomy.mjs:22:    preAuthorized: Array.isArray(a.preAuthorized) ? a.preAuthorized : [],
scripts/lib/autonomy.mjs:220:  epic.autonomy = a;
scripts/lib/autonomy.mjs:29: *  `preAuthorized[]` carrying this stamp — on the precedent `linkOnce()`'s `superseded` and
scripts/lib/autonomy.mjs:3:// lib/render.mjs (setAutonomy calls render(); render() calls getAutonomy()) and needs
scripts/lib/autonomy.mjs:50:  return (autonomy && Array.isArray(autonomy.preAuthorized) ? autonomy.preAuthorized : [])
scripts/lib/autonomy.mjs:77: *  to preAuthorized/context/notifications, never clobber) except --level, which replaces.
scripts/lib/briefing.mjs:108:    const autonomous = getAutonomy(active).level === "autonomous" ? ", 🤖 autonomous" : "";
scripts/lib/briefing.mjs:7:import { getAutonomy } from "./autonomy.mjs";
scripts/lib/constants.mjs:1351:// is not a breaking change for existing preAuthorized entries.
scripts/lib/integrity.mjs:29:import { getAutonomy, grantLabel } from "./autonomy.mjs";
scripts/lib/integrity.mjs:651:        getAutonomy(e).preAuthorized.forEach((g, i) => {
scripts/lib/integrity.mjs:654:            `autonomy.preAuthorized[${i}] names neither an action nor a category. A grant that names ` +
scripts/lib/render.mjs:12:import { getAutonomy } from "./autonomy.mjs";
scripts/lib/render.mjs:128:    const autonomous = getAutonomy(e).level === "autonomous" ? " 🤖" : "";
scripts/lib/render.mjs:3:// lib/active-pointer.mjs (staleMarker), lib/autonomy.mjs (getAutonomy), and
scripts/test/fixtures/state-pre-disposition-walk.json:1020:        "preAuthorized": [
scripts/test/fixtures/state-pre-disposition-walk.json:106:        "preAuthorized": [
scripts/test/fixtures/state-pre-disposition-walk.json:1114:        "preAuthorized": [
scripts/test/fixtures/state-pre-disposition-walk.json:1241:        "preAuthorized": [
scripts/test/fixtures/state-pre-disposition-walk.json:1272:        "preAuthorized": [
scripts/test/fixtures/state-pre-disposition-walk.json:1304:        "preAuthorized": [
scripts/test/fixtures/state-pre-disposition-walk.json:138:        "preAuthorized": [
scripts/test/fixtures/state-pre-disposition-walk.json:1402:        "preAuthorized": [
scripts/test/fixtures/state-pre-disposition-walk.json:170:        "preAuthorized": [
scripts/test/fixtures/state-pre-disposition-walk.json:232:        "preAuthorized": [],
scripts/test/fixtures/state-pre-disposition-walk.json:258:        "preAuthorized": [
scripts/test/fixtures/state-pre-disposition-walk.json:290:        "preAuthorized": [],
scripts/test/fixtures/state-pre-disposition-walk.json:316:        "preAuthorized": [],
scripts/test/fixtures/state-pre-disposition-walk.json:342:        "preAuthorized": [],
scripts/test/fixtures/state-pre-disposition-walk.json:368:        "preAuthorized": [
scripts/test/fixtures/state-pre-disposition-walk.json:3742:        "preAuthorized": [
scripts/test/fixtures/state-pre-disposition-walk.json:400:        "preAuthorized": [
scripts/test/fixtures/state-pre-disposition-walk.json:432:        "preAuthorized": [],
scripts/test/fixtures/state-pre-disposition-walk.json:4474:        "preAuthorized": [],
scripts/test/fixtures/state-pre-disposition-walk.json:458:        "preAuthorized": [],
scripts/test/fixtures/state-pre-disposition-walk.json:484:        "preAuthorized": [],
scripts/test/fixtures/state-pre-disposition-walk.json:510:        "preAuthorized": [],
scripts/test/fixtures/state-pre-disposition-walk.json:5161:          "text": "SIX INSTANCES, not one — grooming pass 2026-09-07 found five more, ranked by consequence. (1) SAFETY: set-autonomy grants are append-only with no revoke — scripts/lib/autonomy.mjs:79,:89 do a.preAuthorized = [...a.preAuthorized, entry], and --level off leaves preAuthorized intact, so re-enabling autonomy silently restores every prior grant including one made in error. CLAUDE.md's execution-time decision rule branches on 'already pre-authorized -> proceed', so this is a safety surface. The in-repo precedent is exact: --attribute-commit (append) got --withdraw-commit + --withdrawal-reason when gh-140 shipped; the pattern was applied to commits and not to grants. (2) set-tracker --remove is secondary-only (tracker.mjs:56 sits inside if (role === 'secondary')), so on the primary path --remove is a declared, accepted flag that is silently ignored and a configured primary cannot be unconfigured. (3) gh-159's bare-invocation READ shipped on one verb of five: set-gate-guard reads (gate-guard.mjs:15-16); set-review-mode (review-mode.mjs:22-25), set-activity-log (activity-log.mjs:59-62) and set-tracker (tracker.mjs:115) all exit 1; set-lane-routing reads only by accident, falling through to a no-op saveState and printing a COUNT rather than the value. (4) eight nullable fields on update-epic with one clear path. (5) no story-removal path — --add-story adds, --done/--wont-do dispose, and a story added by typo can only be closed by recording a false --wont-do. ALSO: --clear-links is all-or-nothing while set-lane-routing has per-item --remove; same collection shape, two answers, one engine. AND THE KEY MECHANISM: gh-66's disposition claims 'a test now fails CI if any source-artifact field is settable at creation but not on the update surfaces' — scripts/test/conductor-23.test.mjs tests set-at-creation against set-on-update and NEVER tests unset. The guard was built along the axis the sweep already looks at, which is exactly why a call-site sweep cannot find this class."
scripts/test/fixtures/state-pre-disposition-walk.json:536:        "preAuthorized": [],
scripts/test/fixtures/state-pre-disposition-walk.json:562:        "preAuthorized": [],
scripts/test/fixtures/state-pre-disposition-walk.json:587:        "preAuthorized": [
scripts/test/fixtures/state-pre-disposition-walk.json:629:        "preAuthorized": [
scripts/test/fixtures/state-pre-disposition-walk.json:683:        "preAuthorized": [
scripts/test/fixtures/state-pre-disposition-walk.json:720:        "preAuthorized": [
scripts/test/fixtures/state-pre-disposition-walk.json:770:        "preAuthorized": [
scripts/test/fixtures/state-pre-disposition-walk.json:834:        "preAuthorized": [
scripts/test/fixtures/state-pre-disposition-walk.json:865:        "preAuthorized": [
scripts/test/fixtures/state-pre-disposition-walk.json:896:        "preAuthorized": [
scripts/test/fixtures/state-pre-disposition-walk.json:927:        "preAuthorized": [
scripts/test/fixtures/state-pre-disposition-walk.json:958:        "preAuthorized": [
scripts/test/fixtures/state-pre-disposition-walk.json:989:        "preAuthorized": [

════ B. rg -n "carriedTo|carried-to" scripts/ ════
scripts/lib/archive-gate.mjs:258:  // echoed value is user data — a title `moved --carried-to later` is one quoted word — and reading flags
scripts/lib/archive-gate.mjs:260:  // REAL echoed carry flag cannot collide: `--carried-to` and `--reason` are both dropped from the echo.
scripts/lib/archive-gate.mjs:272: *  nothing blocks. Carried to the disposition's own `carriedTo`, the one input a stored record gives
scripts/lib/archive-gate.mjs:275:  const carriedTo = (epic && epic.disposition && epic.disposition.carriedTo) || undefined;
scripts/lib/archive-gate.mjs:276:  return deliveredObligations(epic, { carriedTo }).map(o => {
scripts/lib/archive-gate.mjs:344: *   handoff                      no outstanding work (outstandingSummary), unless `carriedTo` names
scripts/lib/archive-gate.mjs:347: * Each entry: `{variant, kind, detect(epic, {carriedTo}) → {detail, items} | null, remedy(epic) →
scripts/lib/archive-gate.mjs:428:    detect(epic, { carriedTo } = {}) {
scripts/lib/archive-gate.mjs:429:      if (carriedTo) return null;
scripts/lib/archive-gate.mjs:435:    // ticked in its own file, and moved work is recorded with `--carried-to` on the archive itself.
scripts/lib/archive-gate.mjs:440:    // `--carried-to`). EVERY printer offering `--outcome delivered` for such an epic appends these flags
scripts/lib/archive-gate.mjs:446:      ? [] : ["--carried-to <epicId>", "--reason \"<which tasks moved>\""]),
scripts/lib/archive-gate.mjs:469: * archiveGate() (for a REQUESTED `delivered`, passing the request's `--carried-to`, since the epic
scripts/lib/archive-gate.mjs:471: * `delivered`, passing the stored `disposition.carriedTo`). So this function does NOT test the
scripts/lib/archive-gate.mjs:472: * outcome, and takes `carriedTo` as an argument rather than reading a disposition.
scripts/lib/archive-gate.mjs:478:export function deliveredObligations(epic, { carriedTo } = {}) {
scripts/lib/archive-gate.mjs:481:    const found = o.detect(epic, { carriedTo });
scripts/lib/archive-gate.mjs:607:  const failing = outcome === "delivered" ? deliveredObligations(epic, { carriedTo: request.carriedTo }) : [];
scripts/lib/archive-gate.mjs:676:    // be written and the only key was `--carried-to`, naming a receiver for work that was
scripts/lib/archive-gate.mjs:682:        `--carried-to <epicId> --reason "<which stories moved>" instead.`
scripts/lib/archive-gate.mjs:683:      : `Either record where the work went with --carried-to <epicId> --reason "<which tasks ` +
scripts/lib/archive-gate.mjs:693:    disposition: agentDisposition({ outcome, reason, carriedTo: request.carriedTo,
scripts/lib/briefing.mjs:230:      const carried = d.carriedTo ? ` — carried to \`${d.carriedTo}\`` : "";
scripts/lib/briefing.mjs:285:  const handoffs = epics.filter(e => e.disposition && e.disposition.carriedTo);
scripts/lib/briefing.mjs:289:      L.push(`  • \`${e.id}\` carried work to \`${e.disposition.carriedTo}\`` +
scripts/lib/briefing.mjs:291:      L.push(`      \`${e.disposition.carriedTo}\` inherited it from \`${e.id}\``);
scripts/lib/constants.mjs:535:  { flag: "carried-to", key: "disposition", commands: ["update-epic"], write: "custom",
scripts/lib/disposition.mjs:10://   { outcome, reason?, recordedAt, recordedBy?, carriedTo? }
scripts/lib/disposition.mjs:132:export function agentDisposition({ outcome, reason, carriedTo, recordedAt, corrects } = {}) {
scripts/lib/disposition.mjs:139:  if (nonEmpty(carriedTo)) record.carriedTo = carriedTo;
scripts/lib/disposition.mjs:271:      (outcomeOf(e) !== "unknown" || e.disposition.reason || e.disposition.carriedTo))
scripts/lib/disposition.mjs:358: *  record holds zero story-level moves, the epic-level `--carried-to` handoff already names a
scripts/lib/disposition.mjs:7:// declined deferral, a release exclusion, and a handoff (`carriedTo`, naming the receiving
scripts/lib/integrity.mjs:593:     *  self-referential `carriedTo` SATISFIES the archive gate's handoff obligation while conveying
scripts/lib/integrity.mjs:601:     *  self-reference is no less false in a deferral than in a `carriedTo`.
scripts/lib/integrity.mjs:610:          "relationship to another record and conveys nothing; a `carriedTo` of this shape satisfies " +
scripts/lib/integrity.mjs:769:          // (`--carried-to`), unless they are ticked in the task source first (Gate 2 E-I5).
scripts/lib/links.mjs:299: *  release's `deferred[]`, an archive disposition's `carriedTo`, a deferral assertion's
scripts/lib/links.mjs:320: *  of the disposition's reference rule (`--carried-to` and a deferral assertion's epic), so a
scripts/lib/links.mjs:379:      add(e.id, `epic \`${e.id}\` disposition.carriedTo`, e.disposition.carriedTo,
scripts/lib/links.mjs:380:        () => { delete e.disposition.carriedTo; });
scripts/lib/links.mjs:387:        add(e.id, `epic \`${e.id}\` disposition.superseded.carriedTo`, prior.carriedTo,
scripts/lib/links.mjs:388:          () => { delete prior.carriedTo; });
scripts/lib/links.mjs:403:    // being HISTORICAL exempts it from nothing — the same ruling `disposition.superseded.carriedTo`
scripts/lib/remove-epic.mjs:71:  // `carriedTo` and a deferral assertion's `deferrals[]` dangling after group 14 added them:
scripts/lib/render.mjs:110:    const to = e.disposition && e.disposition.carriedTo;
scripts/lib/render.mjs:114:    const carriedOut = e.disposition && e.disposition.carriedTo
scripts/lib/render.mjs:115:      ? [`carried-to→${e.disposition.carriedTo}`] : [];
scripts/lib/render.mjs:237:      const carried = d.carriedTo ? ` (carried to \`${d.carriedTo}\`)` : "";
scripts/lib/update-epic.mjs:117: *  refusal opens "cannot archive", and its remedies name `--carried-to`/`--outcome`/`--reason`,
scripts/lib/update-epic.mjs:144:  const carriedToOf = (e) => (e.disposition && e.disposition.carriedTo) || undefined;
scripts/lib/update-epic.mjs:145:  const before = deliveredObligations(snapshot, { carriedTo: carriedToOf(snapshot) });
scripts/lib/update-epic.mjs:146:  const after = deliveredObligations(next, { carriedTo: carriedToOf(next) });
scripts/lib/update-epic.mjs:288:    process.stderr.write(`usage: conductor.mjs update-epic <id> [--title T] [--external-id X] [--external-url U] [--parent P] [--status S] [--priority P] [--lane openspec|superpowers|claude-code|decision|external] [--plan <path>] [--spec <path>] [--link \"<${linkTypeVocabulary()}>:<epic>[:<reason>]\"] [--clear-links] [--clear <field>] [--review-mode off|standard|thorough] [--add-story \"<title>\"] [--story <n> --done|--wont-do "<reason>"] [--attribute-commit <sha>] [--withdraw-commit <sha> --withdrawal-reason \"<why>\"] [--withdraw-gate-review 1|2 --withdrawal-reason \"<why>\"] [--outcome ${AGENT_OUTCOMES.join("|")}] [--reason \"<why>\"] [--correct-disposition \"<why the recorded one was wrong>\"] [--carried-to <epicId>] [--deferral \"<epicId>:<section>\" (or ::)] [--declined-deferral \"<what>::<why not>\"] [--no-deferrals] [--description D] [--notes \"<text>\"] [--external-updated-at <iso>]\n`);
scripts/lib/update-epic.mjs:296:  // `--outcome`, `--reason`, `--carried-to`, `--base-sha` and the rest were never checked here
scripts/lib/update-epic.mjs:699:  // `--reason` and `--carried-to` are recorded only inside that same branch, and supplied without
scripts/lib/update-epic.mjs:703:  const supplied = ["outcome", "reason", "carried-to", "deferral", "declined-deferral", "no-deferrals"]
scripts/lib/update-epic.mjs:80:  "status", "outcome", "reason", "carried-to", "correct-disposition",
scripts/lib/update-epic.mjs:920:  // BEFORE the gate decides, because a self-referential `--carried-to` SATISFIES the handoff
scripts/lib/update-epic.mjs:928:  // `--carried-to ""` never arrives: requireFlagValues() already refuses a blank value-bearing flag
scripts/lib/update-epic.mjs:945:    const carried = str(f["carried-to"]);
scripts/lib/update-epic.mjs:948:      if (bad) refuseReference("--carried-to", carried, bad);
scripts/lib/update-epic.mjs:964:      carriedTo: str(f["carried-to"]), deferralAssertion: asserted, correction,

════ C. rg -n "deferralAssertion|deferrals\[\]" scripts/ ════
scripts/lib/archive-gate.mjs:579:  if (!epic.deferralAssertion && !request.deferralAssertion) {
scripts/lib/archive-gate.mjs:695:    deferralAssertion: request.deferralAssertion };
scripts/lib/constants.mjs:519:  { flag: "deferral", key: "deferralAssertion", commands: ["update-epic"], repeats: true, write: "custom",
scripts/lib/constants.mjs:525:  { flag: "declined-deferral", key: "deferralAssertion", commands: ["update-epic"], repeats: true, write: "custom",
scripts/lib/constants.mjs:532:  { flag: "no-deferrals", key: "deferralAssertion", commands: ["update-epic"], write: "custom", valueless: true },
scripts/lib/disposition.mjs:286:export function deferralAssertion({ deferrals = [], declined = [], assertedAt } = {}) {
scripts/lib/links.mjs:300: *  `deferrals[]` — and none was swept, so removing an epic a release had deferred left
scripts/lib/links.mjs:391:    const da = e.deferralAssertion;
scripts/lib/links.mjs:393:      add(e.id, `epic \`${e.id}\` deferralAssertion.deferrals[]`, d && d.epic,
scripts/lib/remove-epic.mjs:71:  // `carriedTo` and a deferral assertion's `deferrals[]` dangling after group 14 added them:
scripts/lib/update-epic.mjs:16:import { deferralAssertion, isEngineStamped, isStoryDisposed, outcomeOf, storyDisposition, storyDispositionError } from "./disposition.mjs";
scripts/lib/update-epic.mjs:235:    deferrals: snapshot.deferralAssertion ? "asserted" : "placeholder",
scripts/lib/update-epic.mjs:717:    ? deferralAssertion({
scripts/lib/update-epic.mjs:964:      carriedTo: str(f["carried-to"]), deferralAssertion: asserted, correction,
scripts/lib/update-epic.mjs:975:    if (verdict.deferralAssertion) epic.deferralAssertion = verdict.deferralAssertion;

════ D. rg -n "detourStack" scripts/ ════
scripts/lib/activity-log.mjs:179:  const st = state && Array.isArray(state.detourStack) ? state.detourStack : [];
scripts/lib/activity-log.mjs:266:  const beforeDepth = before && Array.isArray(before.detourStack) ? before.detourStack.length : 0;
scripts/lib/activity-log.mjs:267:  const afterDepth = after && Array.isArray(after.detourStack) ? after.detourStack.length : 0;
scripts/lib/briefing.mjs:149:  if (state.detourStack.length) {
scripts/lib/briefing.mjs:150:    L.push(`DETOUR STACK — ${state.detourStack.length} paused (LIFO, resume top first):`);
scripts/lib/briefing.mjs:151:    for (let i = state.detourStack.length - 1; i >= 0; i--) {
scripts/lib/briefing.mjs:152:      const f = state.detourStack[i];
scripts/lib/detour-stack.mjs:127:  if ((state.detourStack || []).some(fr => fr && fr.pausedEpic === id)) {
scripts/lib/detour-stack.mjs:149:  state.detourStack = Array.isArray(state.detourStack) ? state.detourStack : [];
scripts/lib/detour-stack.mjs:150:  state.detourStack.push({
scripts/lib/detour-stack.mjs:16:// `reconcileNeeded`. `rg detourStack scripts/` finds only readers. The pop itself was step 2's
scripts/lib/detour-stack.mjs:212:  const stack = Array.isArray(state.detourStack) ? state.detourStack : [];
scripts/lib/detour-stack.mjs:235:  state.detourStack = stack;
scripts/lib/detour-stack.mjs:337:  const frames = Array.isArray(state.detourStack) ? state.detourStack : [];
scripts/lib/detour-stack.mjs:351:  state.detourStack = frames;
scripts/lib/epic-progress.mjs:160:    (state.detourStack || []).filter(f => f && f.reconcileOnResume).map(f => f.pausedEpic)
scripts/lib/epic-progress.mjs:164:  const pausedByAnyFrame = new Set((state.detourStack || []).filter(f => f).map(f => f.pausedEpic));
scripts/lib/links.mjs:242:  return ((state && state.detourStack) || []).some(f => f && f.pausedEpic === epicId && f.reconcileOnResume);
scripts/lib/links.mjs:284:  if (state.detourStack && state.detourStack.length) {
scripts/lib/links.mjs:285:    const top = state.detourStack[state.detourStack.length - 1];
scripts/lib/links.mjs:411:  for (const f of (state && state.detourStack) || []) {
scripts/lib/links.mjs:429: *     record (dated snapshot, 2026-08-29): an EMPTY `detourStack`, zero `may-invalidate` links,
scripts/lib/links.mjs:459:  for (const f of (state && state.detourStack) || []) {
scripts/lib/reconciler-writeback.mjs:85:  if ((state.detourStack || []).some(fr => fr && fr.pausedEpic === id && fr.spawnedDetour === detourId)) {
scripts/lib/render.mjs:83:  if (!state.detourStack.length) {
scripts/lib/render.mjs:88:    state.detourStack.forEach((f, i) => {
scripts/lib/state.mjs:231:  if (Object.prototype.hasOwnProperty.call(s, "detourStack") && !Array.isArray(s.detourStack)) {
scripts/lib/state.mjs:232:    return "its `detourStack` member is present and is not an array";
scripts/lib/state.mjs:38:  return { version: 1, active: null, epics: [], detourStack: [] };
scripts/lib/subcommands.mjs:442:    ? [ctx.detourId, ...[...(state.detourStack || [])].reverse().map(fr => fr && fr.pausedEpic)]
scripts/lib/subcommands.mjs:569:  const pausedArtifacts = (state.detourStack || [])
scripts/lib/update-epic.mjs:968:      frame: (state.detourStack || []).find(fr => fr && fr.pausedEpic === id),

════ E. rg -n "isArmed|ownedDetours|reconcileOnResume|reconcileNeeded" scripts/ ════
scripts/conductor.mjs:47: *                  `reconcileNeeded` in the SAME write — the frame is gone before reconciliation
scripts/lib/active-pointer.mjs:10:import { ownedDetours } from "./links.mjs";
scripts/lib/active-pointer.mjs:29: *  It is SET AT THE TRANSITION, never derived from current state — the law `reconcileNeeded`
scripts/lib/active-pointer.mjs:63:  if (!e || e.reconcileNeeded !== true) return;
scripts/lib/active-pointer.mjs:64:  const owed = ownedDetours(e);
scripts/lib/add-epic.mjs:420:    status, role: "epic", lane, links, reconcileNeeded: false,
scripts/lib/add-many.mjs:132:    // a batch never supplies (`role`, `reconcileNeeded`). Everything else is copied by the
scripts/lib/add-many.mjs:139:      role: "epic", lane: e.lane, links: [], reconcileNeeded: false,
scripts/lib/briefing.mjs:110:    if (active.reconcileNeeded)
scripts/lib/briefing.mjs:166:      if (f.reconcileOnResume)
scripts/lib/detour-stack.mjs:10:// `reconcileOnResume`, no write-conflict guard, no read-back verification, and no record that
scripts/lib/detour-stack.mjs:111:  const reconcileOnResume = yes;
scripts/lib/detour-stack.mjs:147:  const alreadyOwed = paused.reconcileNeeded === true;
scripts/lib/detour-stack.mjs:148:  paused.reconcileNeeded = alreadyOwed || reconcileOnResume;
scripts/lib/detour-stack.mjs:155:    reconcileOnResume,
scripts/lib/detour-stack.mjs:16:// `reconcileNeeded`. `rg detourStack scripts/` finds only readers. The pop itself was step 2's
scripts/lib/detour-stack.mjs:161:  linkOnce(paused, "may-invalidate", detourId, reason, { arm: reconcileOnResume });
scripts/lib/detour-stack.mjs:170:  const owedBefore = ownedDetours(paused);
scripts/lib/detour-stack.mjs:171:  const pushReport = reconcileOnResume
scripts/lib/detour-stack.mjs:20:// describe — reconcileArchived() clearing `reconcileNeeded` on any epic with no live frame that was
scripts/lib/detour-stack.mjs:236:  if (frame.reconcileOnResume) epic.reconcileNeeded = true;
scripts/lib/detour-stack.mjs:261:  if (resumed.reconcileNeeded === true) {
scripts/lib/detour-stack.mjs:270:    const owed = ownedDetours(resumed);
scripts/lib/detour-stack.mjs:273:      `conductor: RECONCILE GATE — '${escapeControls(pausedEpic)}' carries reconcileNeeded` +
scripts/lib/detour-stack.mjs:314: *  at its verdict transition — `ownedDetours(epic).length > 0 || liveReconcileFrame(state, id)` —
scripts/lib/detour-stack.mjs:325:  // all, it re-raises `reconcileNeeded` from the frame it popped. The one rule they genuinely share —
scripts/lib/detour-stack.mjs:357:      .find(l => isArmed(l) && l.epic === detourId && !l.reconciled);
scripts/lib/detour-stack.mjs:359:      link.reconcileOnResume = false;
scripts/lib/detour-stack.mjs:366:    if (epic.reconcileNeeded === true) {
scripts/lib/detour-stack.mjs:367:      epic.reconcileNeeded = ownedDetours(epic).length > 0 || liveReconcileFrame(state, id);
scripts/lib/detour-stack.mjs:37:import { deferralHistory, deferralNote, isArmed, liveReconcileFrame, ownedDetours } from "./links.mjs";
scripts/lib/detour-stack.mjs:376:      (frame.reconcileOnResume
scripts/lib/detour-stack.mjs:65:    link.reconcileOnResume = arm === true;
scripts/lib/detour-stack.mjs:74:  found.reconcileOnResume = true;
scripts/lib/detour-stack.mjs:84: *  `reconcileOnResume` to be written "deliberately rather than by hand", and a default is the
scripts/lib/detour-stack.mjs:87: *  from a considered one, and `reconcileOnResume` is the flag whose whole documented property is
scripts/lib/epic-progress.mjs:11:import { isArmed, isUnmigrated } from "./links.mjs";
scripts/lib/epic-progress.mjs:150:  // reconcileNeeded is a genuine state-TRANSITION flag, not a pure function of current state:
scripts/lib/epic-progress.mjs:160:    (state.detourStack || []).filter(f => f && f.reconcileOnResume).map(f => f.pausedEpic)
scripts/lib/epic-progress.mjs:168:      if (!e.reconcileNeeded) { e.reconcileNeeded = true; changed = true; }
scripts/lib/epic-progress.mjs:169:    } else if (e.reconcileNeeded === true) {
scripts/lib/epic-progress.mjs:177:      if (!pausedByAnyFrame.has(e.id) && !links.some(l => isArmed(l) || isUnmigrated(l))) {
scripts/lib/epic-progress.mjs:178:        e.reconcileNeeded = false; changed = true;
scripts/lib/epic-progress.mjs:340:      links: [], reconcileNeeded: false,
scripts/lib/gate-guard.mjs:316: *  reconcile (`reconcileNeeded` — see reconcileArchived()'s comment for why this can be
scripts/lib/gate-guard.mjs:318: *  gate-guard-default-on-reconcile change, an epic with `reconcileNeeded: true` is ALWAYS
scripts/lib/gate-guard.mjs:369:  // epic carrying a stale `reconcileNeeded` could wedge Edit/Write/NotebookEdit — and, since the
scripts/lib/gate-guard.mjs:387:  if (active.reconcileNeeded) {
scripts/lib/gate-guard.mjs:42:    out.push("  `reconcileNeeded` blocks Edit/Write/NotebookEdit until a verdict is recorded, and");
scripts/lib/gate-guard.mjs:53:      : (live.reconcileNeeded
scripts/lib/links.mjs:211:// Every `may-invalidate` link carries an explicit ARMING RECORD, `reconcileOnResume` (the frame's
scripts/lib/links.mjs:215:// `reconcileNeeded` flag, so a verdict can be required to name the detour the obligation is owed
scripts/lib/links.mjs:219:export function isArmed(link) {
scripts/lib/links.mjs:220:  return !!link && link.type === "may-invalidate" && link.reconcileOnResume === true;
scripts/lib/links.mjs:229:    !Object.prototype.hasOwnProperty.call(link, "reconcileOnResume");
scripts/lib/links.mjs:233: *  Reads link keys and verdicts only — never `reconcileNeeded`, the flag it feeds. */
scripts/lib/links.mjs:234:export function ownedDetours(epic) {
scripts/lib/links.mjs:236:    .filter(l => isArmed(l) && !l.reconciled && typeof l.epic === "string")
scripts/lib/links.mjs:242:  return ((state && state.detourStack) || []).some(f => f && f.pausedEpic === epicId && f.reconcileOnResume);
scripts/lib/links.mjs:266:      l.reconcileOnResume = e.reconcileNeeded === true && !l.reconciled && l.epic !== e.id && ids.has(l.epic);
scripts/lib/links.mjs:278:  return !!epic && epic.reconcileNeeded === true &&
scripts/lib/links.mjs:279:    (Array.isArray(epic.links) ? epic.links : []).some(l => isArmed(l) || isUnmigrated(l));
scripts/lib/links.mjs:371:      if (e.reconcileNeeded === true && (isArmed(l) || isUnmigrated(l))) {
scripts/lib/links.mjs:95:    if (at === -1) { merged.push(l.type === "may-invalidate" ? { ...l, reconcileOnResume: false } : l); continue; }
scripts/lib/migrations.mjs:101:    note: "give every may-invalidate link an explicit reconcileOnResume arming record",
scripts/lib/reconciler-writeback.mjs:108:  const stillOwed = ownedDetours(epic).length > 0 || liveReconcileFrame(state, id);
scripts/lib/reconciler-writeback.mjs:109:  epic.reconcileNeeded = correction ? (epic.reconcileNeeded === true && stillOwed) : stillOwed;
scripts/lib/reconciler-writeback.mjs:50:  const owed = ownedDetours(epic);
scripts/lib/reconciler-writeback.mjs:57:  //     names: a verdict against a new armed detour would otherwise set the flag from ownedDetours()
scripts/lib/reconciler-writeback.mjs:79:  if (!isArmed(link)) {
scripts/lib/reconciler-writeback.mjs:9:import { isArmed, isUnmigrated, liveReconcileFrame, ownedDetours } from "./links.mjs";
scripts/lib/render.mjs:139:      `${e.status}${outcome}${e.reconcileNeeded ? " ⚠" : ""}${miss}${autonomous}${staleMarker(e)}`, progress, links));
scripts/lib/render.mjs:65:    if (active.reconcileNeeded) {
scripts/lib/render.mjs:89:      md.push(tableRow(String(i + 1), `\`${f.pausedEpic}\``, f.reason, `\`${f.spawnedDetour || "-"}\``, f.reconcileOnResume ? "⚠ yes" : "no"));
scripts/lib/rules.mjs:668:    "   resumes the epic and writes `reconcileNeeded` in the SAME write, which is what makes the",
scripts/lib/rules.mjs:670:    "   `reconcileOnResume`, run the reconcile gate (reconciler agent) BEFORE writing code,",
scripts/lib/rules.mjs:676:    "   `--reconcile` and already popped, and `reconcileNeeded` clears only when no such detour is",
scripts/lib/subcommands.mjs:720:      links: [], reconcileNeeded: false,
scripts/lib/subcommands.mjs:758:      pushEpic(state, { id, title: id, priority: "P?", status: "untriaged", role: "epic", lane: "openspec", links: [], reconcileNeeded: false });
scripts/lib/subcommands.mjs:832:    pushEpic(state, { id, title, priority: "P?", status: "untriaged", role: "epic", lane: "superpowers", planPath, links: [], reconcileNeeded: false });
scripts/lib/update-epic.mjs:19:import { holdsOwedReconcileRecord, linkTypeVocabulary, mergeLinks, ownedDetours, storedEpicIdError } from "./links.mjs";
scripts/lib/update-epic.mjs:447:      const owed = ownedDetours(epic);
scripts/lib/verb-effects.mjs:100:  "pop-detour": { effect: "mutates", writes: "state.json (the popped frame, the resumed epic's status and reconcileNeeded, and the active pointer), .conductor/honcho-memories.log, plus render()'s writes" },
scripts/lib/verb-effects.mjs:99:  "drop-detour": { effect: "mutates", writes: "state.json (the named epic's detour-stack frame, the may-invalidate link's arming record and drop stamp, and reconcileNeeded), plus render()'s writes — never the epic's status and never the active pointer" },

════════════════════════════════════════════════════════════════════════════════
VERDICTS — where each rule holds, where it does not, and why every omission is one
════════════════════════════════════════════════════════════════════════════════

A. GRANTS — every reader must honour the revocation stamp.
   The WIDE pattern was the point. `getAutonomy|epic\.autonomy|preAuthorized` (section A2)
   returns FOUR source hits and ZERO in scripts/lib/rules.mjs — the file that emits the
   instruction text asserting the defect this change removes AND the execution-time decision
   rule a revoked grant must not satisfy. Run narrow only and both would have been missed.
   HOLDS:
     autonomy.mjs getAutonomy()    — still the ONLY direct reader of `epic.autonomy`. Unchanged.
     autonomy.mjs liveGrants()     — the ONE predicate for "this grant authorises something":
                                     named, and not revoked. Task 1.6.
     autonomy.mjs re-arm report    — reads liveGrants(). Task 1.6.
     integrity.mjs grant check     — reads getAutonomy() + grantLabel(), the same predicate. 2.6.
     rules.mjs :216-218            — the falsified claim, reworded to past measured evidence. 1.11.
     rules.mjs decision rule (a)   — now says a REVOKED grant and a grant naming nothing cover
                                     nothing. The THIRD surface epic-autonomy names. 1.11.
   DOES NOT HOLD, each justified:
     render.mjs:128, briefing.mjs:108 — read `level` ONLY, for a 🤖 marker. No surface renders the
       grants at all, which is the mechanical reason the silent restore was silent. Nothing here
       can honour or ignore a revocation, so there is nothing to bind. Adding a grants column is a
       separate ask (design Risks) and is deliberately not smuggled in.
     gate-guard.mjs:316 — the word "pre-authorized" appears in PROSE about the guard's escape
       hatch. It reads no grant.
     add-epic.mjs, constants.mjs — the flag registry and the category vocabulary. Declarations.
     agents/hierarchy-child-executor.md:50 — THE ONLY ACTOR OUTSIDE THE ENGINE THAT ACTS ON A
       GRANT, and its step (a) still says "an action already covered by preAuthorized? → proceed"
       with nothing about a revoked or empty one. NOT FIXED HERE BY TASK ORDER, not by omission:
       it is task 5.4, explicitly scheduled after Gate 2 by the change's own task list, and it is
       named in this sweep so the gate can check the task exists rather than trusting it. Until it
       lands, a revoke is honoured by every engine reader and by one agent reader short.

B. carriedTo — writers, readers AND REMOVERS.
   WRITER:  disposition.mjs agentDisposition() (the one construction site) ← archiveGate() ←
            update-epic, which now VALIDATES before the gate decides (2.2).
   READERS: briefing.mjs:230/:285-291, render.mjs:110/:114/:237, archive-gate.mjs:275/:429,
            update-epic.mjs:144-146. All read a value the write path has already refused if it
            were empty, unknown or self. Unchanged.
   REMOVERS: links.mjs:379 (`disposition.carriedTo`) and :387 (`disposition.superseded.carriedTo`).
            BOTH are enumerated and both are covered by the read-time checks. The write-time
            refusal covers only :379's value, and that is correct rather than a gap: a value
            reaches `superseded.carriedTo` by being MOVED there by `--correct-disposition`, never
            by being supplied. Validating it at the correction would refuse a correction because
            the record it is correcting is wrong — the opposite of what the correction is for.
            The two read-time checks see both, which is the half that can.

C. deferralAssertion / deferrals[] — same three roles.
   WRITER:  disposition.mjs deferralAssertion() ← update-epic's `pairs()`, now validated (2.4).
   READERS: archive-gate.mjs:579 (the assertion's presence), briefing/render via the epic record.
   REMOVER: links.mjs:393. Enumerated; covered by both read-time checks.
   NOT REACHED, deliberately: `--declined-deferral`'s `<what>` (constants.mjs:525,
   update-epic declinedPairs()). It is free text and NEVER an epic id. A validation applied there
   would be this change's own defect class turned inward, and a regression guard asserts a
   `<what>` that looks like an unknown epic id still records.

D. detourStack — every reader, and which ones a dropped frame changes the answer for.
   CHANGES ANSWER, correctly:
     render.mjs:83-89        the stack table loses the row. Correct — the pause ended.
     briefing.mjs:149-166    the DETOUR STACK block loses it. Correct.
     links.mjs:284 detourContext()          no longer "inside a detour" if that was the last frame.
     links.mjs:411, :459 epicReferences()   the frame's two `kind:"frame"` rows go, which is what
                                            unblocks `remove-epic` (3.5).
     epic-progress.mjs:160-164 the heal      neither re-arms nor re-clears, because the drop cleared
                                            the flag in the SAME write. Asserted by test (3.6), and
                                            a mutation run proved the assertion had to read the
                                            drop's OWN output to distinguish it from the heal's.
     reconciler-writeback.mjs:85            check 3 no longer refuses "still paused for" — correct:
                                            the pause is over. Check 2 refuses instead, on the
                                            disarmed link, asserted by 3.9.
     subcommands.mjs:442 attributionCandidates, :569 pausedArtifacts — the dropped epic stops being
                                            a commit-attribution candidate and its artifacts stop
                                            counting as paused. Correct: it is not paused.
     detour-stack.mjs:127 push-detour's already-on-stack refusal — the epic can be pushed again.
                                            Correct; the frame is gone.
     detour-stack.mjs:212 pop-detour        the frame beneath becomes the top. The whole point.
     state.mjs:231 — a shape check only.
   *** FINDING — activity-log.mjs:266-268 ***
     `diffEvents` classifies a stack transition by DEPTH alone: `afterDepth < beforeDepth` emits
     `kind: "detour-pop"`. A drop reduces depth, so a drop is logged as a POP. Verified in-process
     against the shipped function on 2026-09-18 with a BURIED drop: `{kind:"detour-pop",
     epic:null, depth:1}`. Ending a pause and resuming one differ in status, in the active pointer
     and in the obligation, so the kind is wrong. A SECOND, pre-existing defect sits on the same
     line: `frameEpic()` reads `f.epic || f.epicId || f.id` and a frame carries `pausedEpic`, so
     EVERY detour event — push and pop, before this change — carries `epic: null`.
     NOT FIXED HERE, and the justification is scope rather than difficulty: activity-log.mjs is
     absent from this change's Impact list, no requirement of any of its three specs binds the
     log, and the record is not misleading to a reader (every event carries `verb: "drop-detour"`).
     ROUTED, not noted: the existing planned epic `activity-log-detour-events-lose-epic` already
     owns the `epic: null` half — found by `triage`, which is why no second epic was registered —
     and it now carries a third instance in its notes, with the repro and the suggested shape
     (a `detour-drop` kind keyed on WHICH frame left, not on depth). This is the epic the archive
     in 6.2 must name in its `--deferral` assertion.

E. isArmed / ownedDetours / reconcileOnResume / reconcileNeeded — every consumer of the
   obligation the drop ends.
   THE RULE: "is anything still owed" has ONE spelling and the drop uses it —
   `ownedDetours(epic).length > 0 || liveReconcileFrame(state, id)`, imported from links.mjs,
   identical to reconciler-writeback.mjs:108. Not a rule written fresh.
   HOLDS / unchanged by the drop:
     links.mjs:219 isArmed          the disarmed link reads false, which is the whole mechanism.
     links.mjs:234 ownedDetours     stops naming the dropped detour, keeps naming any other.
     links.mjs:278 holdsOwedReconcileRecord — false once the flag is cleared, which is what stops
                                    `--clear-links` and `remove-epic` refusing (3.5).
     links.mjs:371 epicReferences   the `owed-reconcile` undroppable row becomes an ordinary
                                    droppable link row. Correct — nothing is owed.
     reconciler-writeback.mjs:79    check 2 refuses a verdict against the dropped detour, through
                                    its EXISTING arm. Asserted on that arm's own words (3.9).
     gate-guard.mjs:387             stops blocking once the flag is false, which the MODIFIED
                                    gate-integrity requirement states outright.
     active-pointer.mjs:63          the drop moves no pointer, so its warning never fires here.
     epic-progress.mjs:177          covered in D.
     links.mjs:266 stampReconcileKeys — the 0.44.0 migration. It stamps only UNMIGRATED links
                                    (no `reconcileOnResume` key). A dropped link HAS the key, set
                                    false, so the migration cannot re-arm it. Checked, not assumed.
     migrations.mjs:101             the same migration's registry row. Declaration only.
