EMITTED-REMEDY SWEEP — task 4.1b. Every command line THIS change emits, run through the
sibling's SHIPPED Bash write-shape scanner (writeShape(), scripts/lib/gate-guard.mjs:194).
The sibling landed FIRST, so its own 4.1b could not see any of these.
Run: 2026-09-18T05:35:32Z, at c7a288d.

════ ENUMERATION at sweep time — rg -n '\Wdrop-detour|--revoke|--revoke-reason' scripts/ commands/ skills/ agents/ ════
commands/epic.md:860:   closed by shipping the inverse — `set-autonomy <id> --revoke` — and the evidence is kept rather
commands/status.md:348:   closed by shipping the inverse — `set-autonomy <id> --revoke` — and the evidence is kept rather
scripts/conductor.mjs:153:const USAGE = "usage: conductor.mjs init|render|brief|snapshot|commit-nudge|sync|log-detour|retract-detour|push-detour|pop-detour|drop-detour|honcho-memory|add-epic|add-many|update-epic|remove-epic|reorder|set-active|clear-active|set-tracker|set-lane-routing|suggest-lane|triage|set-autonomy|record-reconcile|record-gate-review|record-cross-spec-review|record-tracker-refresh|set-review-mode|release|set-gate-guard|gate-guard|lesson-advice|plan-hierarchy|claim|unclaim|owners|activity|set-activity-log|purge-logs|verify-worktrees|verify-state|verify-specs|integrity|changesets|recover-created-at|unconsidered-outcomes|upgrade|changelog|rules|write-rules|rules-target\n";
scripts/conductor.mjs:298:  "drop-detour": dropDetour,
scripts/conductor.mjs:42: *   drop-detour    the INVERSE push-detour never shipped. Removes the frame naming an epic
scripts/lib/archive-gate.mjs:539:      orNoRemedy(() => `\`drop-detour ${printedId(epic.id)} --reason "<why it is not coming back>"\``) +
scripts/lib/autonomy.mjs:103:      "conductor: --revoke-reason explains a revocation, and this invocation revokes nothing — " +
scripts/lib/autonomy.mjs:104:      "pass --revoke \"<action>\" (or \"category:<name>\") alongside it. Nothing was written.\n");
scripts/lib/autonomy.mjs:120:  // THE REVOKE RUNS BEFORE THE GRANTS BELOW, so `--revoke X --preauthorize "X:<new reason>"` in one
scripts/lib/autonomy.mjs:139:        "conductor: --revoke requires --revoke-reason \"<why>\" — the revocation is kept on the " +
scripts/lib/autonomy.mjs:54:/** Read a `--revoke` value as the grant identity it names, through the IDENTICAL first-colon split
scripts/lib/autonomy.mjs:58: *  `--revoke` names. */
scripts/lib/autonomy.mjs:92:      "[--revoke \"<action>\" --revoke-reason \"<why>\"] " +
scripts/lib/constants.mjs:819:  { flag: "reason", commands: ["push-detour", "drop-detour", "retract-detour"], requires: REASON_REQUIRES },
scripts/lib/constants.mjs:928:  "drop-detour": EPIC_ID,
scripts/lib/detour-stack.mjs:284:const DROP_USAGE = "usage: conductor.mjs drop-detour <pausedEpicId> --reason \"<why>\"\n";
scripts/lib/detour-stack.mjs:286:/** `drop-detour <pausedEpicId> --reason "<why>"` — THE INVERSE `push-detour` NEVER SHIPPED.
scripts/lib/detour-stack.mjs:332:  requireFlagValues("drop-detour", f);
scripts/lib/detour-stack.mjs:371:  const saved = saveState(state, { verb: "drop-detour" });
scripts/lib/rules.mjs:218:      "   all of them. It was closed by shipping the inverse — `set-autonomy <id> --revoke` — and",
scripts/lib/rules.mjs:765:    "      revoke shipped can still hold and which no `--revoke` can name. Both authorise nothing",
scripts/lib/verb-effects.mjs:99:  "drop-detour": { effect: "mutates", writes: "state.json (the named epic's detour-stack frame, the may-invalidate link's arming record and drop stamp, and reconcileNeeded), plus render()'s writes — never the epic's status and never the active pointer" },
skills/conductor/SKILL.md:159:**`drop-detour <paused-id> --reason "<why>"`** END a frame for an epic that is NOT coming back —
skills/conductor/SKILL.md:491:   closed by shipping the inverse — `set-autonomy <id> --revoke` — and the evidence is kept rather
a redirection into a file  archive-gate.mjs:539 UNFILLED template (as printed)
                        drop-detour p --reason "<why it is not coming back>"
null                    archive-gate.mjs:539 FILLED, as an engine invocation
                        node "$ENGINE" drop-detour p --reason "stale frame"
null                    archive-gate.mjs:539 FILLED, bare verb
                        drop-detour p --reason "stale frame"
a redirection into a file  detour-stack.mjs:284 DROP_USAGE (unfilled)
                        conductor.mjs drop-detour <pausedEpicId> --reason "<why>"
null                    detour-stack.mjs:284 DROP_USAGE, filled + engine
                        node "$ENGINE" drop-detour p --reason "not coming back"
a redirection into a file  autonomy.mjs:92 usage (unfilled)
                        conductor.mjs set-autonomy <id> [--revoke "<action>" --revoke-reason "<why>"]
null                    autonomy.mjs:92 filled + engine
                        node "$ENGINE" set-autonomy e --revoke "rm -rf build/" --revoke-reason "no longer regenerated"
null                    autonomy.mjs:103-104 refusal remedy, filled
                        node "$ENGINE" set-autonomy e --revoke "category:filesystem" --revoke-reason "scratch moved"
null                    autonomy.mjs:139 refusal remedy, filled
                        node "$ENGINE" set-autonomy e --revoke drop-table --revoke-reason "unsafe"
a redirection into a file  rules.mjs:218 / SKILL / epic.md / status.md
                        set-autonomy <id> --revoke
null                    detour-stack.mjs no-frame refusal names
                        pop-detour
null                    integrity grant-check detail (prints NO invocation)
                        autonomy.preAuthorized[0] names neither an action nor a category.
null                    update-epic reference refusal (prints NO invocation)
                        cannot archive: --carried-to "x": no epic in this record carries that id
null                    a quoted rm -rf INSIDE a --preauthorize argument
                        node "$ENGINE" set-autonomy e --preauthorize "rm -rf build/:it is regenerated"
null                    and inside a --revoke argument
                        node "$ENGINE" set-autonomy e --revoke "rm -rf build/" --revoke-reason "no"

════════════════════════════════════════════════════════════════════════════════
VERDICT PER CASE — stays runnable, or accepted as blocked
════════════════════════════════════════════════════════════════════════════════

FOUR blocks, all of them UNFILLED TEMPLATES, all ACCEPTED. They are `<id>`/`<why>`
placeholders whose `>` the scanner's redirection arm matches. Every one:
  - removes nothing that would otherwise run. An unfilled template is ALREADY broken at the
    shell, where `<why>` is an input redirection and `<id>` a syntax error. The block refuses a
    command that could not have worked;
  - has a FILLED spelling that passes. Verified above, not assumed: every filled form of every
    command this change emits returns `null` — bare, and as `node "$ENGINE" …`.

  archive-gate.mjs:539   `drop-detour <id> --reason "<why it is not coming back>"`   blocked as
                         a redirection while unfilled; filled → null. ACCEPTED.
  detour-stack.mjs:284   DROP_USAGE, same shape. ACCEPTED.
  autonomy.mjs:92        the set-autonomy usage line, same shape. ACCEPTED.
  rules.mjs:218 (+ the three mirrors)  `set-autonomy <id> --revoke` — a NAME, not an invocation:
                         the prose says which flag closed the finding. ACCEPTED.

THE CLASS IS PRE-EXISTING AND pm-WIDE, AND IT BELONGS TO THE SIBLING, NOT HERE. It is not
introduced by this change: pm emits the identical `<…>`-carrying spelling today at
briefing.mjs, autonomy.mjs's pre-existing usage line, gate-review-writeback.mjs and
argv-surface.mjs, none of which this change touches. It is a consequence of the sibling's
scope and is named as an accepted class in ITS design D7. Fixing it at the emitting site here
would file one pm-wide class in two places and fix it in neither. Re-derived at sweep time
rather than trusted from the design's prediction, and the prediction held.

NOTHING ELSE THIS CHANGE EMITS IS BLOCKED. No remedy it prints is a redirection, an in-place
editor, an `rm` of the record, or any other shape in the closed list. Two cases were checked
BECAUSE they look dangerous and are not:
  - `rm -rf build/` inside a quoted `--preauthorize` argument → null. It is not a
    segment-leading command word, which is the arm that matches.
  - the same string inside a `--revoke` argument → null, for the same reason. This is the
    spelling the revoke's own repro uses, so the check is not hypothetical.

THE TWO NEW REFUSALS THAT PRINT NO INVOCATION AT ALL, stated so their silence is not read as
an omission: `integrity`'s `grant-names-nothing` detail and `update-epic`'s reference refusals
print prose only. The grant check prints none BY DESIGN — a revoke cannot name an empty value
and a grants clear-all is deletion, which `epic-autonomy` rules out — and that is asserted by a
Layer B builder declared `prints: "none"`, so a remedy added there later fails the suite
rather than going unswept.
