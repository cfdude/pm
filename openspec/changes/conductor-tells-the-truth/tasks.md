# tasks

> **Verification bar for this change.** This release exists partly because tasks were ticked green
> while their files were never staged (#121) and because checks passed while measuring nothing.
> Every task below names a verification that can actually fail. Where a verification runs against
> **live data in this repository** it names the data. No verification inspects the working tree
> where the diff is what matters.
>
> **Every implementation task names the files it changes** — task 16.2 asserts those files appear in
> that task's commit, and it has nothing to check against otherwise.
>
> Baseline: `node --test scripts/test/*.test.mjs`, **335 passing**, all green before any commit.
> Zero dependencies — Node 18+ built-ins only (`CLAUDE.md`).

## 1. The shared flag registry — the chokepoint four capabilities grow, grown ONCE and early

> `UPDATE_EPIC_FLAGS` (`update-epic.mjs:15`) is a literal 11-element array and an unlisted flag
> exits 1. Four capabilities in this release add flags to epic-writing commands; grown four times,
> whichever lands first rejects the others' flags by name. This group lands before any of them.

- [ ] 1.1 Add the single `EPIC_FLAGS` declaration to `scripts/lib/constants.mjs` — one entry per flag carrying its name, which commands accept it (`add-epic` / `update-epic` / `add-many`), whether it repeats, and the state key it writes — seeded with exactly today's `update-epic` and `add-epic` flag surfaces; verify with a unit test asserting the projection derived from it is set-equal to today's 11-element `UPDATE_EPIC_FLAGS` literal, so seeding changes no behavior
- [ ] 1.2 Replace the literal in `scripts/lib/update-epic.mjs` so `UPDATE_EPIC_FLAGS` becomes a projection of that declaration (files: `update-epic.mjs`, `constants.mjs`); verify the full existing suite still reports 335 passing and that `update-epic <id> --bogus` still exits non-zero naming `--bogus` alongside the known flags
- [ ] 1.3 Make `REPEATABLE_FLAGS` (`add-epic.mjs:16`) a projection of the same declaration's `repeats` attribute rather than a second literal (design §7 — leaving it a literal reproduces the absent-edit class inside its own fix); verify with a test that marks a flag `repeats: true` in the registry ONLY and asserts `parseFlags` accumulates it into an array with no edit to `add-epic.mjs`
- [ ] 1.4 Give `add-epic` its first flag allowlist, derived from the same declaration, rejecting an unsupported flag by name with the supported flags alongside and writing nothing — this is issue #79 exactly (files: `add-epic.mjs`); verify with a test asserting `add-epic --id x --bogus v` exits non-zero and `state.epics` is unchanged, and a second asserting `--notes "y"` now either persists or is rejected, never the current exit-0-write-nothing
- [ ] 1.5 Replace `add-many`'s fixed key copy (`add-many.mjs:61-70`) with a loop over the same declaration, with `--external-updated-at` ↔ `externalUpdatedAt` as the one flag→key mapping rule, rejecting an unknown batch key by name (files: `add-many.mjs`); verify with a 3-entry batch whose second entry carries an unpersisted key — exits non-zero naming the key and `state.epics` gains ZERO epics, so the valid entries are not created
- [ ] 1.6 Add the flag-coverage check that reads the **documented** surface at check time — `update-epic`'s usage line in `update-epic.mjs:27` and the flag names in `commands/epic.md` — invoking the command once per documented flag and asserting the value reads back from state, never a list transcribed into the test (a registry-driven check passes vacuously on exactly the omission it exists to catch); verify by deleting one flag from the registry while leaving it in the usage line — the check must fail naming that flag

## 2. The disposition record — one shape, four scopes

- [ ] 2.1 New module `scripts/lib/disposition.mjs` importing `constants.mjs` only: the record shape `{outcome, reason?, recordedAt, recordedBy?, carriedTo?}`, the vocabulary `delivered|killed|superseded|abandoned|unknown`, and the fixed `recordedBy` literals `archive-drift-heal`, `archive-backfill`, `migration`; verify with unit tests asserting each non-`delivered` outcome with `reason: "   "` is rejected with nothing written, and `delivered` with no reason accepted
- [ ] 2.2 Export `outcomeOf(epic)` as the ONE reader every consumer and every test uses — `outcome` is not a flat field on the epic and never becomes a `status` value, `KNOWN_STATUSES` is untouched (design §3); verify with a test asserting `outcomeOf({})` returns `unknown`, plus a source-scan test asserting no module under `scripts/lib/` reads `.outcome` directly off an epic
- [ ] 2.3 Render a recorded disposition — outcome, reason, and when it was recorded — in `PROJECT.md` and the briefing without re-reading any prose artifact (files: `render.mjs`, `briefing.mjs`); verify with a fixture epic archived `outcome: killed` with a reason — both surfaces carry the reason and `state.json` alone answers why

## 3. Outstanding work, and the `<!-- pm:lifecycle -->` declaration

- [ ] 3.1 Add `outstandingWork(epic)` to `scripts/lib/epic-progress.mjs` beside `epicProgress()` as the single definition, and give `epicProgress()` an excluded count so the rendered bar and every guard use the same arithmetic; verify with a 13-item source, 12 ticked, thirteenth marked — progress renders `12/12` and `outstandingWork` returns 0, not `12/13` with a footnote
- [ ] 3.2 Exclude exactly the tasks whose task LINE carries the literal `<!-- pm:lifecycle -->`, never a following line, and infer exclusion from nothing else — not wording, not the commands the text names, not file position (files: `epic-progress.mjs`); verify with two tests that fail any text matcher — a task reading `run /opsx:archive <this change>` with NO marker counts normally, and a real task describing implementing archiving also counts
- [ ] 3.3 Keep `exists` from `countCheckboxes()` as the missing-source discriminator rather than `total > 0`, so a present source whose every task is excluded does not collapse into the no-progress-source state (files: `epic-progress.mjs`); verify with a fixture whose every task carries the marker — no missing-source warning is emitted, and the test fails if the discriminator is switched to `total > 0`
- [ ] 3.4 Repoint every consumer of raw checkbox counts at `outstandingWork()` — `PROJECT.md` rendering, the briefing, `/pm:next`, and every guard in this release that refuses because work remains (files: `render.mjs`, `briefing.mjs`, `subcommands.mjs`, `archive-gate.mjs`); verify with a test asserting the count named in an archive refusal is byte-identical to the count `PROJECT.md` renders for the same epic
- [ ] 3.5 Present checkbox-derived progress as **claimed** completion on all three surfaces — `PROJECT.md`, the briefing and `/pm:next` (files: `render.mjs`, `briefing.mjs`, `subcommands.mjs`); verify with a test asserting each of the three outputs carries the claimed-completion wording for a fully ticked epic, so `12/12` is not read as evidence of delivery

## 4. Gate verdicts — lane normalization, checkable evidence, two vocabularies

- [ ] 4.1 Normalize `(epic.lane || "openspec")` at ALL THREE strict `=== "openspec"` sites in one commit — the archive guard (`update-epic.mjs:106`), `missing()` (`epic-progress.mjs:193`), and `record-gate-review`'s lane refusal (`gate-review-writeback.mjs:37`); verify with three tests over a lane-less epic — the `delivered` archive with no Gate 2 is refused, the epic appears in the dangling-change check, and `record-gate-review` accepts a verdict — each failing if its own site alone is reverted
- [ ] 4.2 Add `--base-sha`, `--head-sha` and `--reviewer` to `record-gate-review`, stored as separate fields on `gateReview.gateN` (files: `gate-review-writeback.mjs`, `constants.mjs`, `commands/epic.md`); verify by recording a pass with all three and reading each back from `state.json` as a field, not from prose in a note
- [ ] 4.3 Refuse a `pass` verdict missing either sha, naming the missing evidence and writing nothing; a `fail` may omit the range (files: `gate-review-writeback.mjs`); verify with `--verdict pass` and no `--head-sha` — exits non-zero and `state.json` is byte-identical before and after
- [ ] 4.4 Load pre-existing `{verdict, reviewedAt, note?}` verdicts unchanged and report them as carrying no checkable evidence — never deleted, rewritten, or treated as a verified pass (files: `gate-review-writeback.mjs`, `render.mjs`, `briefing.mjs`); verify with a checked-in legacy-shaped state fixture that loads and renders "no checkable evidence" rather than a pass
- [ ] 4.5 Split the verdict vocabularies: `KNOWN_GATE_VERDICTS` (`gate-review-writeback.mjs:10`) stays `["pass","fail"]` for the agent, and a separate storage-side list admits `ungated`; verify with a test asserting `record-gate-review <id> --gate 2 --verdict ungated` exits non-zero naming the accepted verdicts — a test that fails the moment `ungated` is widened into the single `--verdict` allowlist
- [ ] 4.6 Give `gate1` a reader: display its verdict, its recorded evidence and whether it is stale in `PROJECT.md` and the briefing alongside Gate 2 (files: `render.mjs`, `briefing.mjs`); verify with a fixture epic carrying ONLY a `gate1` verdict — both surfaces name it, where today the sole reader of `gateReview` anywhere in the engine is `update-epic.mjs:107` reading `gate2`

## 5. Commit attribution and verdict staleness

- [ ] 5.1 Add `--attribute-commit <sha>` to `update-epic`, appending each hash to `epic.attributedCommits` in the order given, declared in the group-1 registry (files: `update-epic.mjs`, `constants.mjs`); verify a single hash appends and reads back from `state.json`
- [ ] 5.2 Register `--attribute-commit` in `REPEATABLE_FLAGS` (`add-epic.mjs:16`) as well as the shared allowlist — verified live: `parseFlags` (`add-epic.mjs:17-28`) OVERWRITES a non-repeatable flag on each occurrence, so `--title a --title b` exits 0 and silently keeps `b`, and without this two attributed hashes silently become one; verify with a test running `update-epic <id> --attribute-commit <a> --attribute-commit <b>` in ONE invocation and asserting `attributedCommits` is `[a, b]` — the test reports length 1 if the flag is dropped from the repeatable set
- [ ] 5.3 Initialize `attributedCommits: []` at epic creation on every creation path — `add-epic`, `add-many`, and the inward registration recipe — because absent and empty are different claims (files: `add-epic.mjs`, `add-many.mjs`); verify a newly created epic has the key present and empty while a checked-in 0.26.0 state fixture has no key at all
- [ ] 5.4 Implement staleness in `scripts/lib/archive-gate.mjs` using `git merge-base --is-ancestor` through `git.mjs` (which already shells out at `git.mjs:10`): a `pass` whose `headSha` is a strict ancestor of the LAST entry of `attributedCommits` is stale, must not satisfy the `delivered` archive gate, and renders as stale; verify with a two-commit fixture repo — the refusal names the recorded `headSha` and the attributed commits it does not cover, and the same test with `headSha` equal to the last entry passes even after unrelated commits move repository `HEAD`
- [ ] 5.5 Handle the three no-attribution states distinctly: an ABSENT array is unverifiable and not refused, an EMPTY array asserts nothing attributed and is not refused but renders differently, and where git is unavailable the verdict reports unverifiable and the archive is not refused on staleness grounds; verify with three fixtures each archiving successfully and each producing a different rendering — a single collapsed "no attribution" branch fails two of the three

## 6. The archive gate and the archive-drift heal

- [ ] 6.1 New module `scripts/lib/archive-gate.mjs` importing `constants.mjs`, `epic-progress.mjs`, `disposition.mjs`, `git.mjs`, imported BY `update-epic.mjs` rather than growing the inline refusal at `update-epic.mjs:106-115` — that inline placement is exactly how the current refusal came to bind 1 of 3 archive paths (design §1); verify with a source-scan test asserting `update-epic.mjs` contains no openspec-lane archive condition of its own
- [ ] 6.2 The interactive archive verb requires an `outcome` from `delivered|killed|superseded|abandoned`, refusing both an absent outcome and an agent-supplied `unknown` while naming the permitted outcomes, with `--outcome` and `--reason` DECLARED IN THE GROUP-1 REGISTRY — `UPDATE_EPIC_FLAGS` rejects an unlisted flag and exits 1 before any gate logic runs (files: `constants.mjs`, `archive-gate.mjs`, `update-epic.mjs`); verify with two tests — no `--outcome`, and `--outcome unknown` — each exiting non-zero with `state.json` byte-identical and the status unchanged
- [ ] 6.3 Bind the Gate 2 demand to `outcome: delivered` ONLY: a `delivered` archive of an openspec-lane epic with no passing `gate2` is refused, while `killed`/`superseded`/`abandoned` archive on their required reason with no verdict demanded or invented (files: `archive-gate.mjs`); verify with the flagship case — a 47-task change killed at Gate 1 with no code written archives successfully and its reason reads back from `state.json`
- [ ] 6.4 Deferral-assertion gate: the interactive verb refuses until the agent has asserted durably against the epic what the change's deferrals are — including "none" — each asserted deferral either registered as a `planned`/`queued` epic carrying the originating change id and the artifact section, or recorded as declined with a reason — the assertion persists to `epic.deferralAssertion` `{assertedAt, deferrals: [], declined: []}` and its flag is DECLARED IN THE GROUP-1 REGISTRY (files: `constants.mjs`, `archive-gate.mjs`, `update-epic.mjs`, `disposition.mjs`); verify with a test asserting the refusal names the MISSING ASSERTION and contains no deferral list, plus a source-scan test asserting no module reads change artifacts to identify deferrals — a prose scanner is prohibited and would make the message a guess
- [ ] 6.5 Handoff gate: a `delivered` archive of an epic with outstanding work (the group-3 quantity, never raw checkboxes) requires a `carried-to` reference naming the receiving epic with the moved tasks as the reason, and the refusal states the same count the record renders AND names BOTH remedies, with `--carried-to` DECLARED IN THE GROUP-1 REGISTRY (files: `constants.mjs`, `archive-gate.mjs`, `update-epic.mjs`); verify with a test asserting the refusal string contains the literal token `<!-- pm:lifecycle -->` as well as the handoff instruction, plus a second asserting a fully delivered change whose only unticked item is a MARKED archive task archives with no handoff demanded
- [ ] 6.6 Render the handoff on BOTH ends — the archiving epic shows it carried work out, the receiving epic shows what it inherited — in `PROJECT.md` and the briefing, using the existing free-form link vocabulary with no new link type (files: `render.mjs`, `briefing.mjs`); verify with a fixture handoff where both epics' rendered rows carry the relationship
- [ ] 6.7 Make `reconcileArchived()` (`epic-progress.mjs`) write ONE record at the moment it flips a status: `gateReview.gate2 = {verdict: "ungated", reviewedAt, recordedBy: "archive-drift-heal"}` with NO `reviewer` field, and in the same write `{outcome: "unknown", recordedAt, recordedBy: "archive-drift-heal"}`, never overwriting an existing `gate2` — the rules bind the FUNCTION, so all four call sites inherit it (`migrations.mjs:66`, `render.mjs:30`/`:34`, `subcommands.mjs:200`/`:204`, `subcommands.mjs:252`); verify with one test per call site asserting identical records after the flip, plus a test that a pre-existing `gate2` survives untouched
- [ ] 6.8 Exempt the archive-drift heal and the archive backfill registration from the outcome refusal, the deferral assertion and the handoff demand — both reflect disk and receive no disposition from anyone (files: `archive-gate.mjs`, `epic-progress.mjs`, `subcommands.mjs`); verify with a fixture archived change carrying 12 unticked tasks that heals to `archived` and registers with those counts intact and no refusal on either path
- [ ] 6.9 Outcome-invariant test over the WHOLE epic list after an upgrade run in a repo where a change already sits under `openspec/changes/archive/` with an unhealed epic: every `status: "archived"` epic reads a non-null `outcomeOf()`; verify the test FAILS if the heal's own disposition stamp is removed and only the migration stamps outcomes — `migrations.mjs:66` runs the MIGRATIONS loop, then the heal, then stamps `pmVersion`, so a heal-flipped epic would otherwise be permanently outcome-less

## 7. Migration `0.27.0`

- [ ] 7.1 Add the `0.27.0` `MIGRATIONS` entry to `scripts/lib/migrations.mjs` — additive, idempotent, backward-compatible, keyed to the release per `CLAUDE.md`; verify by running `/pm:upgrade` twice against a fixture and asserting the second run is a byte-identical no-op (the no-op-save requirement means the revision must not bump either)
- [ ] 7.2 Stamp tracker direction: `github-issues` primary → `inward`, every other primary → `outward` (NOT `both` — a Jira tracker today receives only the outward section, so `both` would grant inward pull no repo has ever had), every secondary → `inward`, guarded on `!t.direction` so an explicitly set direction is never overwritten; verify with a test asserting a jira primary is stamped `outward`, a `github-issues` primary `inward`, an explicitly-`both` tracker unchanged, and the rules block emitted after the upgrade identical to the one emitted before it
- [ ] 7.3 Stamp `outcome` on EVERY archived epic regardless of lane — `delivered` only where a passing Gate 2 exists, `unknown` everywhere else — with `recordedBy: "migration"` and `recordedAt` preferring the epic's existing `completedAt`, falling back to the migration timestamp; verify against LIVE data in this repository: after `/pm:upgrade` all 68 archived epics carry an outcome, exactly 3 read `delivered` (the 3 openspec-lane epics that hold the only passing Gate 2 verdicts) and 65 read `unknown` — a lane-scoped implementation leaves 65 unstamped and fails the invariant test in 6.9
- [ ] 7.4 Assert the three NEGATIVE migration requirements, each with its own check: the migration MUST NOT add `attributedCommits` to any pre-existing epic, MUST NOT write `archiveBackfilledAt`, and MUST NOT overwrite an existing `disposition` or `direction`; verify with a test asserting a pre-existing epic has NO `attributedCommits` KEY after upgrade — the assertion that catches a uniformity-minded `[]` implementation, which would convert the staleness gate's one forgiven case into a repo-wide false claim
- [ ] 7.5 Confirm no migration is written for `externalUpdatedAt`, `trackerRefreshNeeded` or `revision`, and that a 0.26.0 state file loads and behaves unchanged UN-upgraded (absent `direction` resolves through the group-9 fallback, absent disposition reads `unknown`, absent `attributedCommits` reads unverifiable, absent `revision` is 0 per `state.mjs:68`); verify with a checked-in 0.26.0 fixture asserting the rules block for a jira primary is byte-identical to 0.26.0's output
- [ ] 7.6 Document the git-is-the-rollback procedure for users — commit `state.json` before upgrading, `git restore .conductor/state.json` then `/pm:status` to re-render, and that rolling back state does NOT require rolling back the engine (files: `commands/upgrade.md`, `README.md`); verify by executing the documented sequence against a fixture repo end to end and confirming `PROJECT.md` re-renders from the restored state

## 8. Archive reconciliation and the one-time backfill

- [ ] 8.1 Make `sync` walk `openspec/changes/archive/` in addition to `openspec/changes/`, registering an archived change with no epic directly at `status: "archived"`, while `reconcileArchived()` continues to CREATE nothing (files: `subcommands.mjs`, `epic-progress.mjs`); verify with a fixture holding 24 archived directories of which 8 have no epic — exactly 8 epics are registered — plus a test asserting `reconcileArchived()` never appends to `state.epics`
- [ ] 8.2 Derive registration identity from the date-prefix-stripped change id, the same way `isArchived()` derives it (`epic-progress.mjs:50-56`) (files: `subcommands.mjs`); verify with `archive/2026-08-01-port-domain-health-system` against an existing `port-domain-health-system` epic — no new epic — plus a re-run immediately after a backfill adding zero epics and modifying zero, and a change registered while active that later archives resolving to ONE epic
- [ ] 8.3 Read a backfilled epic's real counts from its archived artifacts rather than returning `{done:0,total:0}` the way `epicProgress()` does for an archived epic with a missing source (`epic-progress.mjs:143`, `:150`) (files: `subcommands.mjs`, `epic-progress.mjs`); verify with a fixture reproducing `log-collector-not-applicable` registered with 12 of its tasks unticked — its progress reflects those 12 rather than `0/0` or an em dash — and a fully ticked archived change registering as complete and distinguishable from it
- [ ] 8.4 Have the ENGINE stamp every backfilled epic unconditionally with `outcome: unknown` and `recordedBy: "archive-backfill"` on its disposition — never conditioned on task counts, lane or any other property, and with no CLI flag that lets the agent apply it (files: `subcommands.mjs`, `disposition.mjs`); verify with a fixture holding one fully ticked and one 12-unticked backfilled change — both stamped identically — plus a test asserting no flag on any command writes `recordedBy`
- [ ] 8.5 Write NO `gateReview.gate2` entry on a backfilled epic at all (design §10 — an `ungated` entry there is a permanent, unclearable condition against essentially every archived change: 68 archived here, 3 with a passing Gate 2) (files: `subcommands.mjs`); verify with a test asserting a backfilled epic has no `gate2` key and that the ungated-archive notice names none of them
- [ ] 8.6 Add `archiveBackfilledAt` at the state root as a PRESENCE marker — nothing is ever compared against it — so the first run announces the count and the ids it registered and writes the field, and later runs register only changes archived since with no announcement and no re-registration (files: `subcommands.mjs`, `state.mjs`); verify with three tests: first run announces and writes the field; a run against state already carrying it announces nothing and adds zero; a state file with no field loads unchanged, backfills once, and does not repeat

## 9. Integrity checks — read-only, six checks plus the standing notice

> Day-one finding set for this repository, measured: 4 dual-lane pairs, 1 zero-ticked epic outside
> the collision set, 1 range-vs-note mismatch. Every check below can fail against today's engine.

- [ ] 9.1 New module `scripts/lib/integrity.mjs` importing `constants.mjs`, `epic-progress.mjs`, `disposition.mjs`, `git.mjs`, plus an `integrity` subcommand dispatched at the bottom of `conductor.mjs` and documented in `commands/status.md` and the `conductor` skill (design §16 — a new `commands/*.md` instead would need a `docs/parity-ledger.json` claim in the SAME commit); verify with a test asserting `state.json` is byte-identical before and after `integrity` runs against a state carrying findings, and that it blocks no command
- [ ] 9.2 Apply the scope rule for the completion-shaped checks — exclude an epic whose outcome is `killed`/`superseded`/`abandoned`, and one stamped `recordedBy: "archive-backfill"`, and NOTHING else, so `unknown` stays IN scope (files: `integrity.mjs`); verify with a test asserting a killed 47-task epic with everything unticked produces no finding while an `unknown`-outcome zero-ticked epic does — scoping to `delivered` makes the checks inert, measurable here: 68 archived, 3 `delivered` after the migration
- [ ] 9.3 Check: a verdict's range does not contain the commits its note cites, reporting the epic, the recorded range and the uncontained shas (files: `integrity.mjs`, `git.mjs`); verify against LIVE data in this repository — the check reports `platform-parity-mechanism`, whose Gate 2 records `d168b1e..04c54c8` while its note cites `c63efc1` and `3cba2e9`, both descendants of that range
- [ ] 9.4 Check: a gate recorded as bookkeeping rather than review, on TWO arms — `reviewedAt` falls after the epic's merge commit (defined as the LAST hash in `attributedCommits`; where that array is absent or empty this arm does not apply) OR the two gates' `reviewedAt` values fall within 60 seconds of each other (files: `integrity.mjs`); verify with a fixture reproducing the audited instance (both gates 83 s after the squash-merge, 47 ms apart, no notes) reported by the second arm, and a fixture with a spec review and an implementation review hours apart producing nothing
- [ ] 9.5 Check: an archived epic with zero ticked tasks, gated on `total > 0` and NOT on `done === 0` (files: `integrity.mjs`); verify against LIVE data — the check reports exactly the four epics archived at `0/17`, `0/99`, `0/37` and `0/34`, including `2026-07-29-platform-aware-rules-block`, which is not in the dual-lane collision set; a `done === 0` implementation reports 65+ source-less archived epics and fails this test
- [ ] 9.6 Check: one change id registered under two lanes, keyed on the DATE-PREFIX-STRIPPED id (the same normalization `isArchived()` applies), reporting each pair and the lanes each holds and asserting nothing about intent (files: `integrity.mjs`); verify against LIVE data — exactly 4 pairs: `conductor-mjs-module-split`, `platform-parity-mechanism`, `epic-hierarchy-orchestration` and `edd-harness-agent-behavior-testing`, each openspec-lane against its date-prefixed superpowers-lane registration; a literal-equality implementation reports 0 while claiming 4 exist and fails this test
- [ ] 9.7 Have the dual-lane finding name #64/#69 — `sync` registering a plan file as a second epic — as the likely cause, so a reader gets the known explanation alongside the pair (files: `integrity.mjs`); verify with a test asserting the finding text for each of the 4 live pairs cites those issue numbers
- [ ] 9.9 Check: a heal-archived epic that DID pass Gate 2 reads as `unknown` — a `delivered`-shaped record wearing an `unknown` outcome, i.e. a passing `gate2` alongside a disposition stamped `archive-drift-heal` (files: `integrity.mjs`); verify with a fixture that records a passing Gate 2 then moves the change on disk so the heal flips the status — the check reports it, and nothing else does, because it gets no `ungated` entry and its outcome is honestly `unknown`
- [ ] 9.10 Check: a `delivered` epic with a passing Gate 2 whose attribution array is PRESENT and EMPTY — the shape of *the agent ignored the `--attribute-commit` obligation* (files: `integrity.mjs`); verify with two fixtures — the empty-array epic is reported, the absent-array epic is not
- [ ] 9.11 Check: an openspec-lane epic archived with a passing Gate 2 and no `gate1` verdict is reported as a finding, never a refusal, because Gate 1 gates code and the code has already been written by archive time (files: `integrity.mjs`); verify the archive proceeds and the finding appears
- [ ] 9.12 Check: a directory under `openspec/changes/archive/` corresponding to no epic in `state.epics`, reported as an unrecorded archived change with registration explicitly out of scope for the check (files: `integrity.mjs`); verify with a fixture unregistered archive directory that is reported and for which no epic is created
- [ ] 9.13 Emit the ungated-archive notice in the briefing RECOMPUTED from `state.json` on every composition and never consumed on delivery, with a real Gate 2 carrying evidence superseding the `ungated` entry and clearing the notice while the superseded entry stays readable (files: `briefing.mjs`, `integrity.mjs`); verify with two consecutive briefings both naming the epic — the deliberate opposite of the contention warning — and a third after a real passing verdict that does not, with `state.json` still showing the epic was archived ungated
- [ ] 9.14 Run `integrity` against this repository's own live state and confirm the day-one finding set is exactly 4 dual-lane pairs, 1 zero-ticked epic outside the collision set, and 1 range-vs-note mismatch, with no backfilled epic named as an ungated archive; verify by executing the command and recording the finding set in the change's record — a larger set means a check is firing on the classes 9.2 excludes

> **Ruled 2026-08-23 — 9.8 is cut, `design.md` stands.** The proposal to add a machine-readable
> `deliberate` field was mine, from a POLISH finding about the dual-lane check reporting four
> known-good pairs on day one. `design.md` § Risks is right to refuse it: the field would be a
> fifth disposition scope invented with no motivating case beyond suppressing one check's output,
> and all four pairs are the same deferred bug (#64/#69 — `sync` registering a plan file as a
> second epic). Building schema to quiet a symptom of a bug we chose not to fix this release is
> worse than the noise. **9.7 ships** — the check names #64/#69 as the likely cause, which gives
> the reader the actionable fact without new state. When #64/#69 lands, the four pairs disappear
> on their own.

## 10. Tracker direction — two predicates, computed once, consumed by every emitter

- [ ] 10.1 Add `outwardApplies(tracker)` and `inwardProcedureEmittable(tracker)` to `scripts/lib/constants.mjs` — the only module both `rules.mjs` and `briefing.mjs` already reach, and `briefing.mjs` must never start importing from `rules.mjs` — with the fallback `github-issues` → `inward`, everything else → `outward` holding independently of the migration because `/pm:upgrade` lags the plugin update by design; verify with unit tests over the full truth table including a `github-issues` tracker with no `repo`, where direction resolves `inward` while `inwardProcedureEmittable` is FALSE
- [ ] 10.2 Add `set-tracker --direction <inward|outward|both>`, rejecting an invalid value and pinning a secondary tracker to `inward` (files: `tracker.mjs`, `commands/tracker.md`); verify `--direction sideways` exits non-zero with `state.tracker` unchanged, and `--role secondary … --direction outward` exits non-zero with `state.secondaryTrackers` unchanged
- [ ] 10.3 Capture the new-tracker `inward` default BEFORE the merge in `setTracker()` — `tracker.mjs:61` merges `{...(state.tracker || {})}`, so a naive `if (!t.direction) t.direction = "inward"` inside the writer silently switches OFF outward mirroring for every existing Jira repo — using `const isNew = !(state.tracker && state.tracker.system)`, and apply the identical fix in `upsertSecondaryTracker` (files: `tracker.mjs`); verify with a test running `set-tracker --intent paused:todo` against a direction-less jira primary and asserting NO `direction` field is written and only `statusIntent` changed
- [ ] 10.4 Replace the vendor test at `rules.mjs:203` so the outward "External tracker sync" section is emitted iff `outwardApplies` and the inward section iff `inwardProcedureEmittable` (files: `rules.mjs`); verify with byte-identity tests against 0.26.0 output for both un-upgraded shapes — a jira primary with no `direction` gets the outward section only, a `github-issues` primary with a `repo` and no `direction` gets the inward section only
- [ ] 10.5 Make the inward section vendor-neutral — `github-issues` keeps its literal `gh issue list --repo <repo>` step, any other system gets the "list open items in `<system>` (`<scope>`) with your own tooling" phrasing the secondary path already emits — and instruct deduplication on `externalUrl`, never bare `externalId` (files: `rules.mjs`); verify a `{system: "jira", projectKey: "JOB", direction: "inward"}` fixture emits an inward section naming `jira` and `externalUrl` and no outward section
- [ ] 10.6 Emit NO inward section for a scope-less tracker on both the un-upgraded and the migrated path, with no line naming an unfilled scope placeholder anywhere in the block (files: `rules.mjs`, `constants.mjs`); verify `{system: "github-issues"}` with no `repo` — tested both with no `direction` and with `direction: "inward"` — emits neither section, and that adding a `projectKey` to `{system: "jira", direction: "inward"}` turns the inward section on
- [ ] 10.7 Gate the "Sync after completing tracker-linked work" reminder on at least one configured tracker having an emittable inward procedure, so it stops citing "the writeback steps above" that the same block never emits — this is deliberate emitted-output change #1 of exactly two (files: `rules.mjs`); verify three fixtures: outward-only → reminder absent; scope-less inward primary → reminder absent; scoped inward primary → reminder present with every referenced writeback step actually emitted in the same block
- [ ] 10.8 Gate the brief's `TRACKER SYNC` outward drift line on `outwardApplies` (`briefing.mjs:115` gates it on a tracker merely EXISTING today, which is why a repo receives a briefing demanding outward action for 29 epics under a rules block with no outward instructions) and its freshness line on `inwardProcedureEmittable` AND a count greater than zero (files: `briefing.mjs`); verify an inward `github-issues` fixture with unmirrored queued epics has no "not yet in github-issues" line while the identical fixture set `outward` does
- [ ] 10.9 Gate the brief's "N tracker(s) configured — consider `/pm:sync`" nudge (`briefing.mjs:136`) on an emittable inward procedure — deliberate emitted-output change #2 (files: `briefing.mjs`); verify an outward-only repo gets no nudge while a repo with an outward primary and one secondary does
- [ ] 10.10 Add the emitter-coherence test matrix: for each of `inward`/`outward`/`both` crossed with a `github-issues` and a `jira` tracker, assert the rules block containing an outward procedure and the brief demanding outward action have the SAME truth value; plus the scope-less case where the inward section, the freshness line, the sync nudge, the completion-sync reminder and `/pm:sync`'s external read are all false together; verify with those 7 fixture cases plus a source-scan test asserting no emitter recomputes direction from `system`, `repo` or `direction` locally
- [ ] 10.11 Make `/pm:sync`'s tracker instructions follow direction — the inward branch (pull new items, compare each linked item's tracker-side updated timestamp against the epic's watermark, read the movers) instructed only where an inward procedure is emittable; under `outward` no external read at all, local OpenSpec/Superpowers registration only; under `both`, both (files: `commands/sync.md`, `subcommands.mjs`, `rules.mjs`); verify the instruction text emitted for an outward-only repo contains no step that reads or lists items from that tracker
- [ ] 10.12 Make the emitted inward registration recipe RUN as written — supply the required `--id` it currently omits, derive that id deterministically from the external item so the same item yields the same epic id across repos and sessions, and state the derivation in the recipe (files: `rules.mjs`, `commands/sync.md`); verify by executing the emitted command verbatim with placeholders filled from a real issue — exits 0 and the epic exists — then running it a second time and getting the SAME id, refused as a duplicate rather than a second epic under an invented slug
- [ ] 10.13 Take the mirrored item's lane from `suggest-lane` (`lane-routing.mjs`) instead of the hardcoded `--lane claude-code`, permitting the agent to override with a reason recorded on the epic (files: `rules.mjs`, `commands/sync.md`); verify the emitted recipe text contains no `--lane claude-code` literal and names lane routing — this ships the call-site half of #114 only, and #114 stays open
- [ ] 10.14 Update the two MODIFIED tracker-sync behaviors: primary `set-tracker` merges preserving every field not named and never writes `state.secondaryTrackers`, with section choice from `direction` not `system`; secondary inward pull dedups on `externalUrl` when both sides carry one and never on bare `externalId` (files: `tracker.mjs`, `rules.mjs`, `subcommands.mjs`); verify with the two-repos test — `acme/market-intelligence#42` and `acme/risk-engine#42` register as two DISTINCT epics

## 11. Freshness watermark, the single activation door, the refresh gate

- [ ] 11.1 Add `externalUpdatedAt` — the tracker's OWN updated timestamp as of the last content read, never a local clock — declaring `--external-updated-at` in the group-1 registry so `update-epic`, `add-epic` and `add-many` all accept it (files: `constants.mjs`, `update-epic.mjs`, `add-epic.mjs`, `add-many.mjs`); verify registration with the item's timestamp reads back, a bulk-created entry carries it identically, and the emitted `/pm:sync` list step contains no watermark write, so listing cannot advance it
- [ ] 11.2 Route `add-many` through `activate()` (`active-pointer.mjs:13-23`) instead of constructing epics inline and pushing them onto `state.epics` (`add-many.mjs:61-71`), making it the fourth caller alongside `active-pointer.mjs:55`, `update-epic.mjs:136` and `add-epic.mjs:222` (files: `add-many.mjs`); verify with a test — failing today — that a batch entry at `active` status becomes the single active epic and demotes any other epic still at `active`
- [ ] 11.3 Set the tracker-refresh obligation INSIDE `activate()`, keyed on `externalId` being present (provenance, never direction) and never derived from current state, keeping `add-epic.mjs:222`'s freshly-read exemption (files: `active-pointer.mjs`, `add-epic.mjs`); verify with one test per activation path — `set-active`, `update-epic --status active`, `add-epic --status active`, `add-many` batch — asserting an identical obligation from all four, plus an epic created active in the same command that read the item owing nothing
- [ ] 11.4 Surface the refresh debt in the brief and `PROJECT.md` so a compacted session re-learns it, and emit the local-source re-read instruction (plan document, or OpenSpec proposal plus tasks) for an epic with no external id, recording nothing in state for that case (files: `briefing.mjs`, `render.mjs`, `rules.mjs`); verify a tracker-linked active epic shows the debt on both surfaces while a local-origin active epic owes no tracker refresh and receives the local instruction instead
- [ ] 11.5 Add the refresh-verdict writeback verb requiring BOTH a verdict (`unchanged` | `material-change`) and the item's updated timestamp, persisting verdict, summary, when it was recorded and the watermark, and clearing the obligation (files: new `scripts/lib/tracker-refresh-writeback.mjs`, `conductor.mjs`, `commands/sync.md`); verify three tests — a verdict with no timestamp exits non-zero writing nothing, a verdict on an epic with no external id exits non-zero writing nothing, and a `material-change` verdict advances `externalUpdatedAt` and clears the obligation
- [ ] 11.6 Make the mechanical PreToolUse block for an outstanding tracker refresh honor the repo's gate-guard setting while leaving the unconditional reconcile block untouched by `set-gate-guard off` (files: `gate-guard.mjs`, `commands/gate-guard.md`); verify with two tests — the refresh block fires with the guard on and not with it off, and the reconcile block fires with the guard both on and off
- [ ] 11.7 Emit `N tracker-linked epics never re-read since mirroring` (external id present, no watermark) only when an inward procedure is emittable AND the count is greater than zero, and never a claim about how many linked items have newer remote activity, which would require a network call the engine is forbidden to make (files: `briefing.mjs`); verify a fixture with 3 such epics produces the line, the line disappears once every linked epic has a watermark, and a test asserts no brief output for ANY tracker configuration contains a remote-activity claim

## 12. Epic annotation surface

- [ ] 12.1 Add `description` (a single durable string, replaced when set again) and `notes` (an append-only array of `{at, actor, text}`, `actor` recorded and not interpreted) as first-class epic fields with flags on `add-epic` and `update-epic`, declared in the group-1 registry (files: `constants.mjs`, `add-epic.mjs`, `update-epic.mjs`); verify setting a description leaves notes unchanged, appending a second note preserves the first entry's text verbatim, and replacing the description afterwards leaves every note present
- [ ] 12.2 Add `update-epic --lane`, validated against `KNOWN_LANES` exactly as creation validates it, and `--plan` setting the plan path (files: `update-epic.mjs`, `constants.mjs`); verify changing a lane leaves start time, gate verdict, links, stories and list position unchanged; an invalid lane exits non-zero naming the valid lanes with the epic unchanged; a plan attaches to an epic created without one
- [ ] 12.3 Ship `--clear-links` as a named flag and turn the valueless `--link` into a non-zero exit naming the flag, with the clearing form named in `update-epic`'s usage line and `commands/epic.md` (files: `update-epic.mjs`, `commands/epic.md`); verify `--clear-links` empties links while leaving title, status, priority, stories and gate verdict unchanged, and that `--link` with no value exits non-zero rather than silently emptying the array as `update-epic.mjs:50-56` does today
- [ ] 12.4 Diagnose a misplaced `--id` as `update-epic`'s first token — either accept it as an alias for the positional id and perform the update, or exit non-zero with a message naming `--id` and showing `update-epic <id> ...`, never a bare usage dump (#71) (files: `update-epic.mjs`); verify with an assertion on the MESSAGE TEXT — `update-epic --id my-epic --priority P1` either updates `my-epic` or produces a message containing both `--id` and `update-epic <id>`, so today's bare usage line fails it — plus `update-epic --priority P1` with no id in any form erroring that the id is required positionally
- [ ] 12.5 Surface a backlog epic's description in the rendered project record, which today shows an epic's id and not even its title (files: `render.mjs`); verify an epic registered `--status later` with a description is recoverable from `PROJECT.md` alone without opening `.conductor/state.json`

## 13. The contention warning latches

- [ ] 13.1 Replace `consumeConflictWarning()`'s log rotation (`write-conflicts.mjs:73-77`) with a sidecar LATCH marker file beside the log — never a field in `state.json`, which is the file whose write just failed — and change `briefing.mjs:63` from `conflictCount() === CONFLICT_WARN_THRESHOLD` to `count >= threshold && !latched`, with `clearConflicts()` (already called on any successful write, `state.mjs:130`) removing both the log and the latch (files: `write-conflicts.mjs`, `briefing.mjs`); verify by REPLACING the existing `conductor-12.test.mjs` case "a count ABOVE the threshold does not warn — the rule is equality, not >=" with its inverse: a 0→7 burst between two briefings warns exactly ONCE, and the new test fails if the latch is removed because the warning then repeats every briefing
- [ ] 13.2 Stop rotating the log to `.prev` on consume, so the path the warning tells the reader to open still exists and a continuing run of contention does not re-cross the threshold and warn again (files: `write-conflicts.mjs`); verify by updating `conductor-12.test.mjs`'s "consuming the warning preserves the evidence it points at" to assert the log is NOT renamed, plus a test that a successful state write re-arms the warning so a later run of contention warns once more
- [ ] 13.3 Stop `snapshot()` (`subcommands.mjs:78`) passing `consume: true` — PreCompact writes `.conductor/brief.txt`, which nothing reads back — leaving `brief()` (`subcommands.mjs:65`) as the only consumer and `render()` (`render.mjs:133`) passing no `consume` as it already does (files: `subcommands.mjs`); verify with a test where a PreCompact snapshot written while the threshold is crossed does not consume and the next SessionStart briefing still carries the warning — the test fails if `consume: true` is left on `snapshot()`
- [ ] 13.4 Confirm the four write-guard behaviors 0.26.0 already shipped stay covered and unbroken by 13.1–13.3 — the stale-write refusal, the hook retry-then-skip with its sidecar entry, the no-op save, and `--force` writing strictly greater than both the read and the on-disk revision; verify the named cases in `scripts/test/conductor-12.test.mjs` still pass after this group lands, including "`--force` must never rewind the revision below what's on disk"

## 14. Release planning — #125's slice (the 20th issue; this whole group is separable and cuttable)

- [ ] 14.1 Add `state.releases = [{id, intent, target?, deferred: []}]` as first-class objects with a CLI verb to create one, with membership recorded ONE-WAY as `epic.release` (at most one) so the two can never disagree and the engine proposes no membership (files: `constants.mjs`, new `scripts/lib/releases.mjs`, `conductor.mjs`, `commands/status.md`); verify "what is in this release" is answerable from `state.json` alone, and that adding, re-prioritizing or archiving epics changes membership for none of them
- [ ] 14.2 Record an exclusion in `state.releases[].deferred[]` using the group-2 disposition record with its required reason, leaving the excluded epic in the backlog rather than ending it (files: `disposition.mjs`, the release module); verify a deferral reason stored against the epic/release pair survives the release closing, and that a queued epic never associated renders as NEITHER in the release nor deferred from it
- [ ] 14.3 Render a release as "`<release>`: N epics, M deferred" in `PROJECT.md` and the briefing with each deferral's reason reachable from the record (files: `render.mjs`, `briefing.mjs`); verify a release with 12 members and 3 deferrals renders `12 epics, 3 deferred` on both surfaces and the three reasons read back from `state.json`
- [ ] 14.4 Record THIS release's own plan against a `0.27.0` release object, including every "excluded because…" judgment in `proposal.md`'s scope note and #125's own cut-line; verify `/pm:status` shows the `0.27.0` release line with its member and deferred counts, so the release's plan survives outside this conversation transcript — the exact failure this change exists to fix

## 15. The gate procedure pm emits (instruction layer — no engine work)

> These bind the text **pm owns and emits** — the managed `CLAUDE.md` rules block (`rules.mjs`), the
> `conductor` skill, and the command docs — because a change's `tasks.md` is authored by the
> `openspec` plugin, which pm neither owns nor writes. Group 16 separately performs the sweep and
> the commit audit on THIS change.

- [ ] 15.1 Add the call-site completeness sweep to the emitted gate procedure as a NUMBERED REQUIRED TASK ITEM, not a prose bullet — for every rule, guard or invariant a change introduces or modifies, enumerate all call sites of the guarded thing, state where the rule holds and where it does not, and justify each omission (files: `rules.mjs`, `skills/conductor/SKILL.md`, `commands/*.md`); verify with a test asserting the emitted string carries it as a numbered required item with the enumeration named concretely — the test fails if it is downgraded to a bullet, which the audit measured at 3/15 adoption against 14/14
- [ ] 15.2 Add commit-based verification to the same three emitted surfaces as a numbered required task item, stating explicitly that reading the working tree does not count as verification and requiring the files a task claims to change to be asserted present in that task's commit (files: `rules.mjs`, `skills/conductor/SKILL.md`, `commands/*.md`); verify with a test asserting the emitted text contains both the numbered item and the explicit working-tree statement
- [ ] 15.3 Add the `<!-- pm:lifecycle -->` obligation to the emitted rules block and brief — mark a lifecycle-bookkeeping task with the literal token at the moment the task source is authored OR AMENDED, naming the self-referential archive task as the case that always qualifies (files: `rules.mjs`, `briefing.mjs`, `skills/conductor/SKILL.md`); verify with a test asserting both surfaces contain the literal token and cover amendment of a source that predates this capability
- [ ] 15.4 Add the `--attribute-commit` obligation to the emitted instructions — attribute each commit to its epic at the moment the commit is made, naming the flag, naming the per-task conventional commit of an OpenSpec apply loop as the always-qualifying case, and covering work already in flight (files: `rules.mjs`, `skills/conductor/SKILL.md`, `commands/epic.md`); verify with a test asserting each of the three surfaces names the flag
- [ ] 15.5 Ensure no emitted surface presents removing the record as a way to end an epic, a story, a deferral or a release exclusion — every such text names the disposition path with its required reason — while `remove-epic` stays available and ungated for an epic registered in error (files: `rules.mjs`, `skills/conductor/SKILL.md`, `commands/*.md`); verify with a test scanning the rules block, the skill and every command doc for a removal suggestion in an ending context and asserting none, plus a test that `remove-epic` still works on an epic carrying no work

## 16. Applying this release's own remedies to itself

- [ ] 16.1 Perform #115's **call-site completeness sweep on this change**: for every rule, guard and invariant it introduces or modifies — the archive transition, openspec-lane normalization, `activate()`, the shared flag registry, the direction predicates, the outstanding-work definition, the disposition stamp, the latch — enumerate ALL call sites of the guarded thing, state where the rule holds and where it does not, and justify each omission; verify by delivering the written enumeration (rule → call-site list → held / not held → justification) committed with the change, with every "not held" carrying a justification — a rule whose call sites are not enumerated fails this task
- [ ] 16.2 Perform #121's **verification against the commit**: for every task in this list, run `git show --stat <that task's sha>` and assert every file the task claims to change appears in that commit — explicitly NOT the working tree; verify by delivering the per-task file-vs-commit table, where a task whose claimed file is absent from its commit fails even though the working tree holds the edit and the whole suite passes
- [ ] 16.3 Exercise both emitted obligations on this release itself — amend this `tasks.md` so its archive task carries `<!-- pm:lifecycle -->`, and attribute this change's commits with `update-epic --attribute-commit <sha>` as each is made; verify this change's own epic reaches archive with outstanding work of zero and a NON-empty `attributedCommits`, so neither the handoff refusal (6.5) nor the empty-attribution integrity check (9.10) fires on it

## 17. Release discipline, docs, and archive

- [ ] 17.1 Edit the stale `## Purpose` in `openspec/specs/tracker-sync/spec.md` — it still says "full bidirectional mirror, or the `github-issues` inward-only special case", and a delta's Purpose is never merged, so nothing fails if this is forgotten — replacing it with the direction-governed description; verify `rg -c "full bidirectional mirror" openspec/specs/tracker-sync/spec.md` returns 0
- [ ] 17.2 Run the Gate 2 implementation review over the full `BASE..HEAD` diff with a fresh-context reviewer and record it with `record-gate-review <id> --gate 2 --verdict pass --base-sha <a> --head-sha <b> --reviewer "<identity>"`; verify the verdict reads back from `state.json` with both shas as fields and that the recorded `headSha` equals the last entry of the epic's `attributedCommits`, so 5.4's staleness gate does not refuse the archive
- [ ] 17.3 Bump `.claude-plugin/plugin.json` to `0.27.0` and add the `CHANGELOG.md` entry naming every user-visible change — including the new-tracker `inward` default as a behavior REVERSAL and both deliberate emitted-output changes for existing repos (files: `.claude-plugin/plugin.json`, `CHANGELOG.md`); verify `/pm:changelog` renders the 0.27.0 delta and the version matches the `MIGRATIONS` key added in 7.1
- [ ] 17.4 Update `README.md` for every user-visible change, stating the new-tracker `inward` default explicitly as a behavior change with its one-line remedy `set-tracker --system jira --direction outward`; verify `rg "direction" README.md` finds the default and the remedy, and that a reader can find the disposition, integrity and attribution verbs there
- [ ] 17.5 Update the Mintlify site (`pm-plugin.dev`) in the SAME PR cycle per `CLAUDE.md`, following the `mintlify-doc-sync` skill — the Changelog page, Real Numbers, and every page touching trackers, epics, gates or archiving, with the `inward` default named; verify by the skill's own live-verification step against the deployed pages
- [ ] 17.6 Update `commands/epic.md`, `commands/sync.md`, `commands/tracker.md`, `commands/status.md`, `commands/upgrade.md` and `skills/conductor/SKILL.md` for every new flag and writeback verb, claiming any NEW file in `docs/parity-ledger.json` in the same commit (design §16 — as proposed, no new claimed path is needed); verify `node --test scripts/test/parity.test.mjs` passes
- [ ] 17.7 Run the whole suite green — `node --test scripts/test/*.test.mjs` — before every commit, with no `--no-verify` anywhere; verify the final run reports `fail 0` and a total strictly greater than the 335 baseline, so this change demonstrably added coverage rather than only passing what already existed
- [ ] 17.8 Close the resolved issues and confirm the two partial ones stay OPEN — #71, #79, #80, #88, #102, #103, #107, #109, #110, #113, #115, #116, #117, #118, #119, #120, #121, #122, #124 and #125's slice close; #66 and #114 must NOT be closed; verify `gh issue list --repo cfdude/pm --state open` still lists #66 and #114 after the close pass, and lists none of the nineteen
- [ ] 17.9 <!-- pm:lifecycle --> Run `/opsx:archive conductor-tells-the-truth`, then archive the epic with `outcome: delivered`; verify the change directory appears under `openspec/changes/archive/`, `/pm:status` shows the epic `archived` carrying `outcome: delivered`, and `integrity` reports no new finding against it
