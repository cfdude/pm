# tasks

> **Verification bar for this change.** This release exists partly because tasks were ticked green
> while their files were never staged (#121) and because checks passed while measuring nothing.
> Every task below names a verification that can actually fail. Where a verification runs against
> **live data in this repository** it names the data. No verification inspects the working tree
> where the diff is what matters.
>
> **Every implementation task carries an explicit `(files: …)` clause** naming the non-test files
> its commit must contain — task 16.2 asserts exactly those files appear in that task's commit, and
> it has nothing to check against otherwise. A task whose commit also adds a test or fixture file
> does not need to enumerate it; 16.2 checks the claimed files are present, never that no other file
> is. **The exemption list is closed and named here**: `13.4`, `17.5`, `17.7` and `17.8` produce no
> file of their own (a confirmation run, an off-repo docs site, a test run, a tracker close pass).
> 16.2 skips exactly those four and **fails** any other task that names no files — an unstated
> exemption is the silent hole this task exists to close.
>
> Baseline: `node --test scripts/test/*.test.mjs`, **335 passing**, all green before any commit.
> Zero dependencies — Node 18+ built-ins only (`CLAUDE.md`).
>
> **Live-state snapshot, measured 2026-08-23 at `a49aadf`** — every count below is a dated reading of
> `.conductor/state.json`, never an assertion a task may depend on. `137` epics, `69` archived, `3`
> carrying a passing Gate 2 (`multi-tracker-primary-secondary-support`, `conductor-mjs-module-split`,
> `platform-parity-mechanism`), of which only the first also carries a `gate1`. `0` archived epics
> carry any disposition today. `1` directory under `openspec/changes/archive/`, registered. This
> change's own epic is `active`, so 17.9 makes it the 70th archived epic. **Tasks assert relative
> properties — "every archived epic carries an outcome", "exactly those with a passing Gate 2 read
> `delivered`" — and quote a count only as a dated snapshot.** The spec files still quote `68`
> archived from an earlier reading; that is a stale snapshot in prose, not a requirement, and no task
> below asserts it.

## 1. The shared flag registry — the chokepoint four capabilities grow, grown ONCE and early

> `UPDATE_EPIC_FLAGS` (`update-epic.mjs:15`) is a literal 11-element array and an unlisted flag
> exits 1. Four capabilities in this release add flags to epic-writing commands; grown four times,
> whichever lands first rejects the others' flags by name. This group lands before any of them.

- [x] 1.1 Add the single `EPIC_FLAGS` declaration to `scripts/lib/constants.mjs` — one entry per flag carrying its name, which commands accept it (`add-epic` / `update-epic` / `add-many`), whether it repeats, and the state key it writes — seeded with exactly today's `update-epic` and `add-epic` flag surfaces (files: `scripts/lib/constants.mjs`); verify with a unit test asserting the projection derived from it is set-equal to today's 11-element `UPDATE_EPIC_FLAGS` literal, so seeding changes no behavior
- [x] 1.2 Replace the literal in `scripts/lib/update-epic.mjs` so `UPDATE_EPIC_FLAGS` becomes a projection of that declaration (files: `scripts/lib/update-epic.mjs`, `scripts/lib/constants.mjs`); verify the full existing suite still reports 335 passing and that `update-epic <id> --bogus` still exits non-zero naming `--bogus` alongside the known flags
- [x] 1.3 Make `REPEATABLE_FLAGS` (`add-epic.mjs:16`) the UNION of its surviving literal and the registry's `repeats: true` entries, not a wholesale projection (files: `scripts/lib/add-epic.mjs`, `scripts/lib/constants.mjs`); **justification, verified live** — of the seven entries in that literal only `link` is an epic flag: `intent` belongs to `set-tracker`, `preauthorize`/`context`/`notify` to `set-autonomy`, and `add`/`remove` to `set-lane-routing`, because `parseFlags` is shared by nearly every subcommand, so a registry seeded with epic flags alone (1.1) would silently break `set-tracker --intent a:b --intent c:d`; the union keeps those six literal and lets any capability in this release make an epic flag repeat from the registry only; verify with two tests — one marking a flag `repeats: true` in the registry ONLY and asserting `parseFlags` accumulates it into an array with no edit to `add-epic.mjs`, and one asserting `set-tracker --intent a:b --intent c:d` still accumulates both, which fails the moment the literal is replaced rather than unioned
- [x] 1.4 Give `add-epic` its first flag allowlist, derived from the same declaration, rejecting an unsupported flag by name with the supported flags alongside and writing nothing — this is issue #79 exactly (files: `scripts/lib/add-epic.mjs`, `scripts/lib/constants.mjs`); verify with a test asserting `add-epic --id x --bogus v` exits non-zero and `state.epics` is unchanged, and a second asserting `--notes "y"` now either persists or is rejected, never the current exit-0-write-nothing
- [x] 1.5 Replace `add-many`'s fixed key copy (`add-many.mjs:61-70`) with a loop over the same declaration, with `--external-updated-at` ↔ `externalUpdatedAt` as the one flag→key mapping rule, rejecting an unknown batch key by name (files: `scripts/lib/add-many.mjs`, `scripts/lib/constants.mjs`); verify with a 3-entry batch whose second entry carries an unpersisted key — exits non-zero naming the key and `state.epics` gains ZERO epics, so the valid entries are not created
- [x] 1.6 Add the flag-coverage check that reads the **documented** surface at check time — `update-epic`'s usage line in `update-epic.mjs:27` and the flag names in `commands/epic.md` — invoking the command once per documented flag and asserting the value reads back from state, never a list transcribed into the test (a registry-driven check passes vacuously on exactly the omission it exists to catch) (files: `scripts/test/conductor-13.test.mjs`); verify by deleting one flag from the registry while leaving it in the usage line — the check must fail naming that flag

## 2. The disposition record — one shape, four scopes

- [x] 2.1 New module `scripts/lib/disposition.mjs` importing `constants.mjs` only: the record shape `{outcome, reason?, recordedAt, recordedBy?, carriedTo?}`, the vocabulary `delivered|killed|superseded|abandoned|unknown`, and the fixed `recordedBy` literals `archive-drift-heal`, `archive-backfill`, `add-epic`, `add-many`, `migration` — the five engine-stamp tokens `gate-integrity` and `epic-disposition` enumerate, with an **agent-supplied** record defined as one carrying NO `recordedBy` at all (files: `scripts/lib/disposition.mjs`); verify with unit tests asserting each non-`delivered` outcome with `reason: "   "` is rejected with nothing written, `delivered` with no reason accepted, and the token set asserted exactly — a test that fails if a sixth token is added without a rule, or if any of the five is dropped
- [x] 2.2 Export `outcomeOf(epic)` as the ONE reader every consumer and every test uses — `outcome` is not a flat field on the epic and never becomes a `status` value, `KNOWN_STATUSES` is untouched (design §3) — alongside `isEngineStamped(disposition)`, which returns true iff `recordedBy` holds one of the five literals and is the ONLY way any consumer tells an engine stamp from an agent's record (files: `scripts/lib/disposition.mjs`); verify with a test asserting `outcomeOf({})` returns `unknown`, a test asserting `isEngineStamped` is false for a record with no `recordedBy` and true for each of the five tokens, plus a source-scan test asserting no module under `scripts/lib/` reads `.outcome` or `.recordedBy` directly off an epic
- [ ] 2.3 Render a recorded disposition — outcome, reason, and when it was recorded — in `PROJECT.md` and the briefing without re-reading any prose artifact (files: `scripts/lib/render.mjs`, `scripts/lib/briefing.mjs`); verify with a fixture epic archived `outcome: killed` with a reason — both surfaces carry the reason and `state.json` alone answers why

## 3. The archive-gate module, outstanding work, and the `<!-- pm:lifecycle -->` declaration

> **3.1 creates `archive-gate.mjs` at the head of this group, not inside group 6.** Three later tasks
> write into that module before group 6 runs — 3.5 repoints a consumer at it, 5.4 implements
> staleness in it, and every group-6 task lives in it — so creating it in group 6 would make 16.2
> fail on 3.5 and 5.4 by construction: their claimed file would not exist in their own commit.

- [ ] 3.1 New module `scripts/lib/archive-gate.mjs` importing `constants.mjs`, `epic-progress.mjs`, `disposition.mjs` (group 2) and `git.mjs`, imported BY `update-epic.mjs` rather than growing the inline refusal at `update-epic.mjs:106-115` — that inline placement is exactly how the current refusal came to bind 1 of 5 archive paths (design §1) (files: `scripts/lib/archive-gate.mjs`, `scripts/lib/update-epic.mjs`); verify with a source-scan test asserting `update-epic.mjs` contains no openspec-lane archive condition of its own, plus a test asserting the module's exported gate entry point is reachable from `update-epic` and returns a refusal object rather than calling `process.exit` itself
- [ ] 3.2 Add `outstandingWork(epic)` to `scripts/lib/epic-progress.mjs` beside `epicProgress()` as the single definition, and give `epicProgress()` an excluded count so the rendered bar and every guard use the same arithmetic (files: `scripts/lib/epic-progress.mjs`); verify with a 13-item source, 12 ticked, thirteenth marked — progress renders `12/12` and `outstandingWork` returns 0, not `12/13` with a footnote
- [ ] 3.3 Exclude exactly the tasks whose task LINE carries the literal `<!-- pm:lifecycle -->`, never a following line, and infer exclusion from nothing else — not wording, not the commands the text names, not file position (files: `scripts/lib/epic-progress.mjs`); verify with two tests that fail any text matcher — a task reading `run /opsx:archive <this change>` with NO marker counts normally, and a real task describing implementing archiving also counts
- [ ] 3.4 Keep `exists` from `countCheckboxes()` as the missing-source discriminator rather than `total > 0`, so a present source whose every task is excluded does not collapse into the no-progress-source state (files: `scripts/lib/epic-progress.mjs`); verify with a fixture whose every task carries the marker — no missing-source warning is emitted, and the test fails if the discriminator is switched to `total > 0`
- [ ] 3.5 Repoint every consumer of raw checkbox counts at `outstandingWork()` — `PROJECT.md` rendering, the briefing, `/pm:next`, and every guard in this release that refuses because work remains (files: `scripts/lib/render.mjs`, `scripts/lib/briefing.mjs`, `scripts/lib/subcommands.mjs`, `scripts/lib/archive-gate.mjs`); verify with a test asserting the count named in an archive refusal is byte-identical to the count `PROJECT.md` renders for the same epic
- [ ] 3.6 Present checkbox-derived progress as **claimed** completion on all three surfaces — `PROJECT.md`, the briefing and `/pm:next` (files: `scripts/lib/render.mjs`, `scripts/lib/briefing.mjs`, `scripts/lib/subcommands.mjs`); verify with a test asserting each of the three outputs carries the claimed-completion wording for a fully ticked epic, so `12/12` is not read as evidence of delivery

## 4. Gate verdicts — lane normalization, checkable evidence, two vocabularies

- [ ] 4.1 Normalize `(epic.lane || "openspec")` at ALL THREE strict `=== "openspec"` sites that exist today, in one commit — the archive guard (`update-epic.mjs:106`), `missing()` (`epic-progress.mjs:193`), and `record-gate-review`'s lane refusal (`gate-review-writeback.mjs:37`) — exporting the single predicate `isOpenspecLane(epic)` from `constants.mjs` so that **the fourth site, the heal's bypass-half lane test created in 6.8, consumes it rather than re-deriving** (files: `scripts/lib/constants.mjs`, `scripts/lib/update-epic.mjs`, `scripts/lib/epic-progress.mjs`, `scripts/lib/gate-review-writeback.mjs`); verify with three tests over a lane-less epic — the `delivered` archive with no Gate 2 is refused, the epic appears in the dangling-change check, and `record-gate-review` accepts a verdict — each failing if its own site alone is reverted, PLUS a source-scan test asserting no module under `scripts/lib/` compares a `.lane` value to the literal `"openspec"` without either normalizing an absent lane inline or going through `isOpenspecLane`, so a fifth strict site added later (6.8 included) fails this test rather than passing silently
- [ ] 4.2 Add `--base-sha`, `--head-sha` and `--reviewer` to `record-gate-review`, stored as separate fields on `gateReview.gateN`, declared in the group-1 registry where they touch an epic-writing surface (files: `scripts/lib/gate-review-writeback.mjs`, `scripts/lib/constants.mjs`, `commands/epic.md`); verify by recording a pass with all three and reading each back from `state.json` as a field, not from prose in a note
- [ ] 4.3 Refuse a `pass` verdict missing either sha, naming the missing evidence and writing nothing; a `fail` may omit the range (files: `scripts/lib/gate-review-writeback.mjs`); verify with `--verdict pass` and no `--head-sha` — exits non-zero and `state.json` is byte-identical before and after
- [ ] 4.4 Load pre-existing `{verdict, reviewedAt, note?}` verdicts unchanged and report them as carrying no checkable evidence — never deleted, rewritten, or treated as a verified pass, and never mined for a range by parsing the note (files: `scripts/lib/gate-review-writeback.mjs`, `scripts/lib/render.mjs`, `scripts/lib/briefing.mjs`); verify with a checked-in legacy-shaped state fixture that loads and renders "no checkable evidence" rather than a pass, plus a source-scan test asserting no module extracts a sha or a `a..b` range from a `note` string
- [ ] 4.5 Split the verdict vocabularies: `KNOWN_GATE_VERDICTS` (`gate-review-writeback.mjs:10`) stays `["pass","fail"]` for the agent, and a separate storage-side list admits `ungated` (files: `scripts/lib/gate-review-writeback.mjs`, `scripts/lib/constants.mjs`); verify with a test asserting `record-gate-review <id> --gate 2 --verdict ungated` exits non-zero naming the accepted verdicts — a test that fails the moment `ungated` is widened into the single `--verdict` allowlist
- [ ] 4.6 Preserve the superseded entry when a real verdict lands on a gate that already holds one: `recordGateReview()` today does a wholesale overwrite (`gate-review-writeback.mjs:46`), so an `ungated` entry the heal wrote is destroyed by the verdict that supersedes it, and `gate-integrity`'s "the superseded entry MUST remain readable" has no writer at all — move the prior entry to `gateReview.gateN.superseded` (a single nested record, itself never carrying a further `superseded`) as part of the same write, for any prior entry and not only an `ungated` one (files: `scripts/lib/gate-review-writeback.mjs`); verify with a test recording a passing Gate 2 over an `ungated` entry and asserting the epic reads `gate2.verdict === "pass"` while `gate2.superseded` still holds `{verdict: "ungated", recordedBy: "archive-drift-heal"}` — this is the shape 9.13's third assertion reads, and the test fails against today's overwrite
- [ ] 4.7 Give `gate1` a reader: display its verdict and its recorded evidence in `PROJECT.md` and the briefing alongside Gate 2 — staleness rendering is 5.4's, which exports the predicate and covers both gates, and is deliberately NOT claimed here because that predicate does not exist until group 5 (files: `scripts/lib/render.mjs`, `scripts/lib/briefing.mjs`); verify with a fixture epic carrying ONLY a `gate1` verdict — both surfaces name it, where today the sole reader of `gateReview` anywhere in the engine is `update-epic.mjs:107` reading `gate2`

## 5. Commit attribution and verdict staleness

- [ ] 5.1 Add `--attribute-commit <sha>` to `update-epic`, appending each hash to `epic.attributedCommits` in the order given, declared in the group-1 registry (files: `scripts/lib/update-epic.mjs`, `scripts/lib/constants.mjs`); verify a single hash appends and reads back from `state.json`
- [ ] 5.2 Mark `--attribute-commit` `repeats: true` in the group-1 registry so 1.3's union carries it into `REPEATABLE_FLAGS` (`add-epic.mjs:16`) (files: `scripts/lib/constants.mjs`); verified live: `parseFlags` (`add-epic.mjs:17-28`) OVERWRITES a non-repeatable flag on each occurrence, so `--title a --title b` exits 0 and silently keeps `b`, and without this two attributed hashes silently become one; verify with a test running `update-epic <id> --attribute-commit <a> --attribute-commit <b>` in ONE invocation and asserting `attributedCommits` is `[a, b]` — the test reports length 1 if the flag is dropped from the repeatable set
- [ ] 5.3 Initialize `attributedCommits: []` at epic creation on every creation path — `add-epic`, `add-many`, and the inward registration recipe — because absent and empty are different claims (files: `scripts/lib/add-epic.mjs`, `scripts/lib/add-many.mjs`); verify a newly created epic has the key present and empty while a checked-in 0.26.0 state fixture has no key at all
- [ ] 5.4 Implement staleness in `scripts/lib/archive-gate.mjs` (created in 3.1) using `git merge-base --is-ancestor` through `git.mjs` (which already shells out at `git.mjs:10`): a `pass` whose `headSha` is a strict ancestor of the LAST entry of `attributedCommits` is stale and must not satisfy the `delivered` archive gate — and **export the staleness predicate so it renders as stale wherever the verdict is displayed**, which `render.mjs` and `briefing.mjs` reach by importing it directly, neither module importing `archive-gate.mjs` today (files: `scripts/lib/archive-gate.mjs`, `scripts/lib/git.mjs`, `scripts/lib/render.mjs`, `scripts/lib/briefing.mjs`); verify with a two-commit fixture repo — the refusal names the recorded `headSha` and the attributed commits it does not cover, the same test with `headSha` equal to the last entry passes even after unrelated commits move repository `HEAD`, and both rendered surfaces carry the stale marking for the stale fixture and omit it for the fresh one — **for `gate1` as well as `gate2`**, since 4.7 renders Gate 1's verdict and evidence but leaves its staleness to this task
- [ ] 5.5 Handle the three no-attribution states distinctly: an ABSENT array is unverifiable and not refused, an EMPTY array asserts nothing attributed and is not refused but renders differently, and where git is unavailable the verdict reports unverifiable and the archive is not refused on staleness grounds (files: `scripts/lib/archive-gate.mjs`, `scripts/lib/render.mjs`); verify with three fixtures each archiving successfully and each producing a different rendering — a single collapsed "no attribution" branch fails two of the three
- [ ] 5.6 Assert the engine half of the archive-move exclusion — an epic whose attribution array ends at its last DELIVERY commit is not refused as stale after its change directory has been archived and that move has been committed, because the move was never attributed and the recorded `headSha` is still the last entry; the engine classifies nothing and appends exactly the hashes it is given, so this is a behavior assertion over 5.4's gate, and the emitted-text half is 15.4 (files: `scripts/lib/archive-gate.mjs`); verify with a fixture repo where the archive-move commit is a descendant of `headSha` and is deliberately NOT attributed — the `delivered` archive succeeds — plus its inverse where the move IS attributed and the archive is refused as stale, which is exactly the trap 15.4's emitted exclusion exists to keep an agent out of

## 6. The archive gate, the archive-drift heal, and the creation paths

> `gate-integrity` now names **five** paths that can leave an epic at `status: "archived"` — the
> interactive verb, the archive-drift heal, the archive backfill registration, and the two
> archived-at-creation paths (`add-epic --status archived`, an `add-many` batch entry at
> `archived`). Every rule in this group states which of the five it binds. The module these tasks
> write into is created in 3.1; the disposition shape they stamp comes from 2.1.

- [ ] 6.1 The interactive archive verb requires an `outcome` from `delivered|killed|superseded|abandoned`, refusing both an absent outcome and an agent-supplied `unknown` while naming the permitted outcomes, with `--outcome` and `--reason` DECLARED IN THE GROUP-1 REGISTRY — `UPDATE_EPIC_FLAGS` rejects an unlisted flag and exits 1 before any gate logic runs (files: `scripts/lib/constants.mjs`, `scripts/lib/archive-gate.mjs`, `scripts/lib/update-epic.mjs`); verify with two tests — no `--outcome`, and `--outcome unknown` — each exiting non-zero with `state.json` byte-identical and the status unchanged
- [ ] 6.2 Accept an epic whose status is ALREADY `archived` on the interactive verb: run the full archive gate on that invocation and record the disposition, never refusing the call as a no-op and never treating "the status did not change" as grounds to skip the gate — and read every gate input from the RECORD (the Gate 2 verdict and its evidence, the group-3 outstanding-work quantity, the deferral assertion, the attribution array), never from `openspec/changes/<id>/`, which has already moved under `archive/` by the time this call happens (files: `scripts/lib/archive-gate.mjs`, `scripts/lib/update-epic.mjs`); **this is the only moment the documented workflow can record `delivered`** — `/opsx:archive` moves the directory, the heal flips the status and stamps `unknown`, and a verb that refused the follow-up call would leave every openspec epic at `unknown`; verify with two tests — the full documented sequence (record a passing Gate 2 with evidence → move the change on disk → run the heal → run the verb with `--status archived --outcome delivered`) ends at `outcome: delivered` with the passing `gate2` intact and no `ungated` entry anywhere in `gateReview`, and a second where the epic's `gate2` is `ungated` and the same call is REFUSED exactly as a first-time archive would be, so acceptance is not a waiver
- [ ] 6.3 Implement the replacement rule and its refusal, told apart by `recordedBy` and by nothing else (2.2's `isEngineStamped`): an agent's disposition through the interactive verb REPLACES an engine-stamped one — outcome, reason and timestamp together, the replacing record carrying no `recordedBy` of its own so it is not replaceable again — and is REFUSED against an agent-supplied one, naming the recorded outcome and when it was recorded (files: `scripts/lib/disposition.mjs`, `scripts/lib/archive-gate.mjs`, `scripts/lib/update-epic.mjs`); **this rule runs opposite to its two neighbours and must not be generalized from them** — the heal may not overwrite an existing `gate2` (6.8) and the migration may not overwrite an existing `disposition` (7.4), both engine paths overwriting an agent's work, whereas this is an agent correcting a record the engine wrote because nobody was asked; verify with three tests — `delivered` replaces `{outcome: "unknown", recordedBy: "archive-drift-heal"}` leaving NO `recordedBy` on the result, `delivered` replaces a `recordedBy: "migration"` stamp (without which all 65-plus migration-stamped archived epics are frozen at `unknown`), and a record carrying no `recordedBy` is refused with `state.json` byte-identical — the second test fails the moment 7.4's never-overwrite rule is widened past the migration
- [ ] 6.4 Bind the Gate 2 demand to `outcome: delivered` ONLY: a `delivered` archive of an openspec-lane epic with no passing `gate2` is refused, while `killed`/`superseded`/`abandoned` archive on their required reason with no verdict demanded or invented (files: `scripts/lib/archive-gate.mjs`); verify with the flagship case — a 47-task change killed at Gate 1 with no code written archives successfully and its reason reads back from `state.json`
- [ ] 6.5 Deferral-assertion gate: the interactive verb refuses until the agent has asserted durably against the epic what the change's deferrals are — including "none" — each asserted deferral either registered as a `planned`/`queued` epic carrying the originating change id and the artifact section, or recorded as declined with a reason — the assertion persists to `epic.deferralAssertion` `{assertedAt, deferrals: [], declined: []}` and its flag is DECLARED IN THE GROUP-1 REGISTRY (files: `scripts/lib/constants.mjs`, `scripts/lib/archive-gate.mjs`, `scripts/lib/update-epic.mjs`, `scripts/lib/disposition.mjs`); verify with a test asserting the refusal names the MISSING ASSERTION and contains no deferral list, plus a source-scan test asserting no module reads change artifacts to identify deferrals — a prose scanner is prohibited and would make the message a guess
- [ ] 6.6 Handoff gate: a `delivered` archive of an epic with outstanding work (the group-3 quantity, never raw checkboxes) requires a `carried-to` reference naming the receiving epic with the moved tasks as the reason, and the refusal states the same count the record renders AND names BOTH remedies, with `--carried-to` DECLARED IN THE GROUP-1 REGISTRY (files: `scripts/lib/constants.mjs`, `scripts/lib/archive-gate.mjs`, `scripts/lib/update-epic.mjs`); verify with a test asserting the refusal string contains the literal token `<!-- pm:lifecycle -->` as well as the handoff instruction, plus a second asserting a fully delivered change whose only unticked item is a MARKED archive task archives with no handoff demanded
- [ ] 6.7 Render the handoff on BOTH ends — the archiving epic shows it carried work out, the receiving epic shows what it inherited — in `PROJECT.md` and the briefing, using the existing free-form link vocabulary with no new link type (files: `scripts/lib/render.mjs`, `scripts/lib/briefing.mjs`); verify with a fixture handoff where both epics' rendered rows carry the relationship
- [ ] 6.8 Make `reconcileArchived()` (`epic-progress.mjs`) write ONE record at the moment it flips a status, whose **two halves bind different sets of epics**: the DISPOSITION half — `{outcome: "unknown", recordedAt, recordedBy: "archive-drift-heal"}` — binds EVERY lane, because the outcome invariant admits no lane exception; the BYPASS half — `gateReview.gate2 = {verdict: "ungated", reviewedAt, recordedBy: "archive-drift-heal"}` with NO `reviewer` field — binds **openspec-lane epics only**, because `record-gate-review` refuses a verdict to any other lane, so an `ungated` entry on a `claude-code` or `superpowers` epic would be a standing condition with no clearing path in the engine at all; the lane test here is the FOURTH site deciding openspec-lane membership and MUST use 4.1's `isOpenspecLane` rather than a strict comparison; never overwrite an existing `gate2`; the rules bind the FUNCTION, so all four call sites inherit them (`migrations.mjs:66`, `render.mjs:30`/`:34`, `subcommands.mjs:200`/`:204`, `subcommands.mjs:252`) (files: `scripts/lib/epic-progress.mjs`, `scripts/lib/disposition.mjs`); verify with one test per call site asserting identical records after the flip, a test that a healed `superpowers`-lane epic carries the disposition and NO `gate2` key, a test that a healed lane-less epic DOES receive the bypass half, and a test that a pre-existing `gate2` survives untouched — plus 4.1's source scan, which fails if this lane test is written strict
- [ ] 6.9 Stamp the two ARCHIVED-AT-CREATION paths in the engine — `add-epic --status archived` and an `add-many` batch entry carrying `status: "archived"` — each leaving the created epic with `{outcome: "unknown", recordedAt, recordedBy: "<add-epic|add-many>"}` and, exactly like the backfill and for the identical reason, NO `gateReview.gate2` entry; creation at any other status writes no disposition at all, because nothing has ended; the stamp binds every lane and applies whether or not the change exists on disk; where the archive backfill (8.4) registers THROUGH a creation path the disposition carries `archive-backfill` and never the inner creation token, because every exemption elsewhere in this release keys on that token (files: `scripts/lib/add-epic.mjs`, `scripts/lib/add-many.mjs`, `scripts/lib/disposition.mjs`); verify with four tests — `add-epic --status archived` reads `recordedBy: "add-epic"` with no `gate2` key, an `add-many` archived entry reads `recordedBy: "add-many"` (so a rule applied to one command is visibly absent from the other), an epic created at `queued` carries no disposition record at all, and a backfilled epic routed through a creation path still reads `archive-backfill`; today all four assertions fail, because both commands construct the epic inline and `archived` is already a member of `KNOWN_STATUSES`
- [ ] 6.10 Exempt the archive-drift heal, the archive backfill registration and the two archived-at-creation paths from the outcome refusal, the deferral assertion and the handoff demand — all four supply no disposition and receive no named receiver from anyone, and all four reflect a record rather than a judgment (files: `scripts/lib/archive-gate.mjs`, `scripts/lib/epic-progress.mjs`, `scripts/lib/subcommands.mjs`, `scripts/lib/add-epic.mjs`, `scripts/lib/add-many.mjs`); verify with a fixture archived change carrying 12 unticked tasks that heals to `archived` and registers with those counts intact and no refusal, plus an `add-epic --status archived` and an `add-many` archived entry that each succeed with no `--outcome`, no deferral assertion and no `--carried-to` — four paths, four assertions, each failing on its own
- [ ] 6.11 Outcome-invariant test over the WHOLE epic list after an upgrade run in a repo where a change already sits under `openspec/changes/archive/` with an unhealed epic, asserting **the record rather than the reader's default**, in three clauses: (a) every `status: "archived"` epic carries a `disposition` OBJECT with a non-null `outcome` AND a `recordedAt`; (b) `recordedBy` is present and holds one of 2.1's five literals for every engine-stamped disposition, and is ABSENT on an agent-supplied one (6.3), so the two are distinguishable without prose; (c) the heal-flipped epic in the fixture reads `recordedBy: "archive-drift-heal"` specifically (files: `scripts/test/conductor-13.test.mjs`); verify the test FAILS when 6.8's disposition stamp is removed and only 7.3's migration stamps outcomes — `migrations.mjs:66` runs the MIGRATIONS loop, THEN the heal, then stamps `pmVersion`, and the migration never replays, so a heal-flipped epic is otherwise permanently outcome-less — and confirm it also fails against today's engine, where zero archived epics carry any disposition; asserting only that `outcomeOf()` is non-null would be satisfied by 2.2's `"unknown"` default with every stamp in this release deleted, which is the defect this task exists to catch

## 7. Migration `0.27.0`

- [ ] 7.1 Add the `0.27.0` `MIGRATIONS` entry to `scripts/lib/migrations.mjs` — additive, idempotent, backward-compatible, keyed to the release per `CLAUDE.md` (files: `scripts/lib/migrations.mjs`); verify by running `/pm:upgrade` twice against a fixture and asserting the second run is a byte-identical no-op (the no-op-save requirement means the revision must not bump either)
- [ ] 7.2 Stamp tracker direction: `github-issues` primary → `inward`, every other primary → `outward` (NOT `both` — a Jira tracker today receives only the outward section, so `both` would grant inward pull no repo has ever had), every secondary → `inward`, guarded on `!t.direction` so an explicitly set direction is never overwritten (files: `scripts/lib/migrations.mjs`); verify with a test asserting a jira primary is stamped `outward`, a `github-issues` primary `inward`, an explicitly-`both` tracker unchanged, and the rules block emitted after the upgrade identical to the one emitted before it
- [ ] 7.3 Stamp `outcome` on EVERY archived epic regardless of lane — `delivered` only where a passing Gate 2 exists, `unknown` everywhere else — with `recordedBy: "migration"` and `recordedAt` preferring the epic's existing `completedAt`, falling back to the migration timestamp (files: `scripts/lib/migrations.mjs`, `scripts/lib/disposition.mjs`); verify against LIVE data in this repository, **asserted relatively so the numbers cannot rot**: after `/pm:upgrade`, EVERY archived epic carries an outcome, the set reading `delivered` is exactly the set carrying a passing `gate2`, and every other archived epic reads `unknown` — measured 2026-08-23 as 69 archived, 3 `delivered`, 66 `unknown`, quoted as a dated snapshot and asserted nowhere; a lane-scoped implementation leaves every non-openspec archived epic unstamped and fails 6.11's invariant
- [ ] 7.4 Assert the three NEGATIVE migration requirements, each with its own check: **the migration** MUST NOT add `attributedCommits` to any pre-existing epic, **the migration** MUST NOT write `archiveBackfilledAt`, and **the migration** MUST NOT overwrite an existing `disposition` or `direction` (files: `scripts/lib/migrations.mjs`); **all three bind the migration and nothing else** — the disposition clause in particular MUST NOT be generalized into a repo-wide never-overwrite rule, which would kill 6.3's replacement rule and freeze every migration-stamped epic at `unknown` forever; verify with a test asserting a pre-existing epic has NO `attributedCommits` KEY after upgrade (the assertion that catches a uniformity-minded `[]` implementation, which would convert the staleness gate's one forgiven case into a repo-wide false claim), a test asserting an epic that already carries an agent-supplied disposition is untouched by the migration, and a test asserting the interactive verb can still replace a `recordedBy: "migration"` stamp AFTER the migration has run — which fails if the never-overwrite rule leaks out of the migration
- [ ] 7.5 Confirm no migration is written for `externalUpdatedAt`, `trackerRefreshNeeded` or `revision`, and that a 0.26.0 state file loads and behaves unchanged UN-upgraded (absent `direction` resolves through the group-10 fallback, absent disposition reads `unknown`, absent `attributedCommits` reads unverifiable, absent `revision` is 0 per `state.mjs:68`) (files: `scripts/test/fixtures/state-0.26.0.json`); verify with that checked-in 0.26.0 fixture asserting the rules block for a jira primary is byte-identical to 0.26.0's output
- [ ] 7.6 Document the git-is-the-rollback procedure for users — commit `state.json` before upgrading, `git restore .conductor/state.json` then `/pm:status` to re-render, and that rolling back state does NOT require rolling back the engine (files: `commands/upgrade.md`, `README.md`); verify by executing the documented sequence against a fixture repo end to end and confirming `PROJECT.md` re-renders from the restored state

## 8. Archive reconciliation and the one-time backfill

- [ ] 8.1 Make `sync` walk `openspec/changes/archive/` in addition to `openspec/changes/`, registering an archived change with no epic directly at `status: "archived"`, while `reconcileArchived()` continues to CREATE nothing (files: `scripts/lib/subcommands.mjs`, `scripts/lib/epic-progress.mjs`); verify with a fixture holding 24 archived directories of which 8 have no epic — exactly 8 epics are registered — plus a test asserting `reconcileArchived()` never appends to `state.epics`
- [ ] 8.2 Derive registration identity from the date-prefix-stripped change id, the same way `isArchived()` derives it (`epic-progress.mjs:50-56`) (files: `scripts/lib/subcommands.mjs`); verify with `archive/2026-08-01-port-domain-health-system` against an existing `port-domain-health-system` epic — no new epic — plus a re-run immediately after a backfill adding zero epics and modifying zero, and a change registered while active that later archives resolving to ONE epic
- [ ] 8.3 Read a backfilled epic's real counts from its archived artifacts rather than returning `{done:0,total:0}` the way `epicProgress()` does for an archived epic with a missing source (`epic-progress.mjs:143`, `:150`) (files: `scripts/lib/subcommands.mjs`, `scripts/lib/epic-progress.mjs`); verify with a fixture reproducing `log-collector-not-applicable` registered with 12 of its tasks unticked — its progress reflects those 12 rather than `0/0` or an em dash — and a fully ticked archived change registering as complete and distinguishable from it
- [ ] 8.4 Have the ENGINE stamp every backfilled epic unconditionally with `outcome: unknown` and `recordedBy: "archive-backfill"` on its disposition — never conditioned on task counts, lane or any other property, with no CLI flag that lets the agent apply it, and carrying that token in preference to any creation-path token where the backfill registers through a creation path (6.9) (files: `scripts/lib/subcommands.mjs`, `scripts/lib/disposition.mjs`); verify with a fixture holding one fully ticked and one 12-unticked backfilled change — both stamped identically — plus a test asserting no flag on any command writes `recordedBy`
- [ ] 8.5 Write NO `gateReview.gate2` entry on a backfilled epic at all (design §10 — an `ungated` entry there is a permanent, unclearable condition against essentially every archived change, and the ratio is what makes it unclearable: measured 2026-08-23, 69 archived epics in this repository against 3 carrying a passing Gate 2) (files: `scripts/lib/subcommands.mjs`); verify with a test asserting a backfilled epic has no `gate2` key and that the ungated-archive notice names none of them — asserted as "no backfilled epic is named", never as a count
- [ ] 8.6 Add `archiveBackfilledAt` at the state root as a PRESENCE marker — nothing is ever compared against it — so the first run announces the count and the ids it registered and writes the field, and later runs register only changes archived since with no announcement and no re-registration (files: `scripts/lib/subcommands.mjs`, `scripts/lib/state.mjs`); verify with three tests: first run announces and writes the field; a run against state already carrying it announces nothing and adds zero; a state file with no field loads unchanged, backfills once, and does not repeat

## 9. Integrity checks — read-only, six checks plus the standing notice

> **Day-one finding set for this repository, per check, measured 2026-08-23 at `a49aadf`** — the
> full enumeration and the rule that every finding be EXPLAINED rather than counted lives in 9.14.
> Some checks have live candidates and some have none; a check with none is fixture-verified, which
> is not the same as a check that passed.

- [ ] 9.1 New module `scripts/lib/integrity.mjs` importing `constants.mjs`, `epic-progress.mjs`, `disposition.mjs`, `git.mjs`, plus an `integrity` subcommand dispatched at the bottom of `conductor.mjs` and documented in `commands/status.md` and the `conductor` skill (design §16 — a new `commands/*.md` instead would need a `docs/parity-ledger.json` claim in the SAME commit) (files: `scripts/lib/integrity.mjs`, `scripts/conductor.mjs`, `commands/status.md`, `skills/conductor/SKILL.md`); verify with a test asserting `state.json` is byte-identical before and after `integrity` runs against a state carrying findings, and that it blocks no command
- [ ] 9.2 Apply the scope rule for the completion-shaped checks — exclude an epic whose outcome is `killed`/`superseded`/`abandoned`, and one stamped `recordedBy: "archive-backfill"`, and NOTHING else, so `unknown` stays IN scope (files: `scripts/lib/integrity.mjs`); verify with a test asserting a killed 47-task epic with everything unticked produces no finding while an `unknown`-outcome zero-ticked epic does, plus a test asserting that scoping to `delivered` empties the candidate set — measurable on this repository, where after 7.3's migration only the epics carrying a passing Gate 2 read `delivered` (3 of 69 archived, measured 2026-08-23), so a `delivered`-only implementation makes every check below inert; the assertion is "the `delivered` set is a strict subset of the archived set and the checks have candidates outside it", not either count
- [ ] 9.3 Check: a verdict's range does not contain the commits its note cites, reporting the epic, the recorded range and the uncontained shas, reading the range from the `baseSha`/`headSha` FIELDS only and never parsing a range out of the note (4.4) (files: `scripts/lib/integrity.mjs`, `scripts/lib/git.mjs`); verify with a fixture epic reproducing `platform-parity-mechanism`'s exact shape — `{baseSha: "d168b1e", headSha: "04c54c8", note: "…c63efc1…3cba2e9…"}` — evaluated against THIS repository's real git history, where all four shas exist and `c63efc1` and `3cba2e9` are verifiably NOT ancestors of `04c54c8`, so the containment computation is real and the test fails if the ancestry logic is inverted or absent; **this check has zero live candidates today and that is not a gap** — no verdict in `state.json` carries structured sha fields, because 4.2 introduces them and 4.4 forbids rewriting the three legacy verdicts, so the live instance the spec cites exists only as prose in a note and is reproduced here as a fixture rather than mined from it (9.14 records the explained zero)
- [ ] 9.4 Check: a gate recorded as bookkeeping rather than review, on TWO arms — `reviewedAt` falls after the epic's merge commit (defined as the LAST hash in `attributedCommits`; where that array is absent or empty this arm does not apply) OR the two gates' `reviewedAt` values fall within 60 seconds of each other (files: `scripts/lib/integrity.mjs`); verify with a fixture reproducing the audited instance (both gates 83 s after the squash-merge, 47 ms apart, no notes) reported by the second arm, and a fixture with a spec review and an implementation review hours apart producing nothing; **both arms are fixture-verified with zero live candidates, and the reason is recorded** — arm 1 cannot apply because no epic carries an attribution array before this release, and arm 2 finds nothing because the one live epic holding two gate verdicts recorded them 22 minutes apart (measured 2026-08-23)
- [ ] 9.5 Check: an archived epic with zero ticked tasks, gated on `total > 0` and NOT on `done === 0` (files: `scripts/lib/integrity.mjs`); verify against LIVE data — the check reports exactly the **five** epics archived at `0/17`, `0/99`, `0/37`, `0/34` and `0/39` (`2026-07-14-epic-hierarchy-orchestration`, `2026-07-21-conductor-mjs-module-split`, `2026-07-26-edd-harness-agent-behavior-testing`, `2026-07-29-platform-aware-rules-block`, `2026-08-18-state-write-conflict-guard`), of which **two** — `2026-07-29-platform-aware-rules-block` and `2026-08-18-state-write-conflict-guard`, the latter archived during this change's own session — are outside the dual-lane collision set 9.6 reports, so this check has live candidates that are not artifacts of another finding; a `done === 0` implementation reports every source-less archived epic instead and fails this test; the five counts are stable only because **none of the seven files in `docs/superpowers/plans/` carries a `<!-- pm:lifecycle -->` marker today** (verified 2026-08-23 by scanning the repository), which 3.2's excluded count and 15.3's marking obligation could both change — the test therefore asserts the identified SET, and any later marker that moves a count must move this task with it
- [ ] 9.6 Check: one change id registered under two lanes, keyed on the DATE-PREFIX-STRIPPED id (the same normalization `isArchived()` applies), reporting each pair and the lanes each holds and asserting nothing about intent (files: `scripts/lib/integrity.mjs`); verify against LIVE data — exactly 4 pairs, each a date-prefixed `superpowers`-lane registration against an un-prefixed sibling: `conductor-mjs-module-split` (openspec), `platform-parity-mechanism` (openspec), `epic-hierarchy-orchestration` (**decision**, not openspec) and `edd-harness-agent-behavior-testing` (**decision**, not openspec) — the lanes are asserted as read from state rather than assumed uniform; a literal-equality implementation reports 0 while claiming 4 exist and fails this test
- [ ] 9.7 Have the dual-lane finding name #64/#69 — `sync` registering a plan file as a second epic — as the likely cause, so a reader gets the known explanation alongside the pair (files: `scripts/lib/integrity.mjs`); verify with a test asserting the finding text for each of the 4 live pairs cites those issue numbers
- [ ] 9.9 Check: a heal-archived epic that DID pass Gate 2 reads as `unknown` — a `delivered`-shaped record wearing an `unknown` outcome, i.e. a passing `gate2` alongside a disposition stamped `archive-drift-heal` (files: `scripts/lib/integrity.mjs`); verify with a fixture that records a passing Gate 2 then moves the change on disk so the heal flips the status — the check reports it, and nothing else does, because it gets no `ungated` entry and its outcome is honestly `unknown`; the remediation is 6.2's interactive verb run on the already-archived epic with 6.3's replacement, so the finding names that call as the fix; **zero live candidates**, because 7.3's migration stamps `recordedBy: "migration"` rather than `archive-drift-heal` on every pre-existing archived epic
- [ ] 9.10 Check: a `delivered` epic with a passing Gate 2 whose attribution array is PRESENT and EMPTY — the shape of *the agent ignored the `--attribute-commit` obligation* (files: `scripts/lib/integrity.mjs`); verify with two fixtures — the empty-array epic is reported, the absent-array epic is not; **zero live candidates**, because 7.4 forbids the migration from adding the array to any pre-existing epic, so every archived epic reads absent rather than empty
- [ ] 9.11 Check: an openspec-lane epic archived with a passing Gate 2 and no `gate1` verdict is reported as a finding, never a refusal, because Gate 1 gates code and the code has already been written by archive time (files: `scripts/lib/integrity.mjs`); verify the archive proceeds and the finding appears, and verify against LIVE data that the check reports exactly **two** epics — `conductor-mjs-module-split` and `platform-parity-mechanism`, the two of the three passing-Gate-2 epics that carry no `gate1` (only `multi-tracker-primary-secondary-support` does), neither excluded by 9.2's scope rule
- [ ] 9.12 Check: a directory under `openspec/changes/archive/` corresponding to no epic in `state.epics`, reported as an unrecorded archived change with registration explicitly out of scope for the check (files: `scripts/lib/integrity.mjs`); verify with a fixture unregistered archive directory that is reported and for which no epic is created; **zero live candidates** — this repository holds one archive directory, `2026-07-19-multi-tracker-primary-secondary-support`, and it is registered
- [ ] 9.13 Emit the ungated-archive notice in the briefing RECOMPUTED from `state.json` on every composition and never consumed on delivery, with a real Gate 2 carrying evidence superseding the `ungated` entry and clearing the notice while the superseded entry stays readable at `gateReview.gate2.superseded` (the field 4.6 writes) (files: `scripts/lib/briefing.mjs`, `scripts/lib/integrity.mjs`); verify with two consecutive briefings both naming the epic — the deliberate opposite of the contention warning — and a third after a real passing verdict that does not, with `state.json` still showing `gate2.superseded.verdict === "ungated"` so the record that the epic was archived ungated survives; the third assertion fails without 4.6, which is why that task is a prerequisite rather than a nicety
- [ ] 9.14 Run `integrity` against this repository's own live state and record the day-one finding set **per check, with every finding explained** — not compared against a total (files: `openspec/changes/conductor-tells-the-truth/integrity-day-one.md`); the enumeration measured 2026-08-23 at `a49aadf`, to be re-measured at the moment the command first runs rather than copied: 9.6 → 4 dual-lane pairs; 9.5 → 5 zero-ticked archived epics, 2 of them outside the collision set; 9.11 → 2 openspec-lane epics with a passing Gate 2 and no `gate1`; 9.3, 9.4, 9.9, 9.10 and 9.12 → 0 each, every zero carrying the reason its task records (no structured sha fields exist yet; no attribution arrays exist yet and the one two-gate epic is 22 minutes apart; the migration stamps `migration` not `archive-drift-heal`; the migration adds no attribution array; the single archive directory is registered); and no backfilled epic named as an ungated archive; **verify by executing the command, writing the per-check enumeration into the change's record, and giving every finding an explanation — a finding with no explanation fails this task, and so does a check whose live count differs from the enumeration above without the difference being explained**; counting alone is what let a check measure nothing in the first place

> **Ruled 2026-08-23 — 9.8 is cut, `design.md` stands.** The proposal to add a machine-readable
> `deliberate` field was mine, from a POLISH finding about the dual-lane check reporting four
> known-good pairs on day one. `design.md` § Risks is right to refuse it: the field would be a
> fifth disposition scope invented with no motivating case beyond suppressing one check's output,
> and all four pairs are the same deferred bug (#64/#69 — `sync` registering a plan file as a
> second epic). Building schema to quiet a symptom of a bug we chose not to fix this release is
> worse than the noise. **9.7 ships** — the check names #64/#69 as the likely cause, which gives
> the reader the actionable fact without new state. When #64/#69 lands, the four pairs disappear
> on their own.

## 10. Tracker direction — two predicates, computed once, consumed by every emitter

- [ ] 10.1 Add `outwardApplies(tracker)` and `inwardProcedureEmittable(tracker)` to `scripts/lib/constants.mjs` — the only module both `rules.mjs` and `briefing.mjs` already reach, and `briefing.mjs` must never start importing from `rules.mjs` — with the fallback `github-issues` → `inward`, everything else → `outward` holding independently of the migration because `/pm:upgrade` lags the plugin update by design (files: `scripts/lib/constants.mjs`); verify with unit tests over the full truth table including a `github-issues` tracker with no `repo`, where direction resolves `inward` while `inwardProcedureEmittable` is FALSE
- [ ] 10.2 Add `set-tracker --direction <inward|outward|both>`, rejecting an invalid value and pinning a secondary tracker to `inward` (files: `scripts/lib/tracker.mjs`, `commands/tracker.md`); verify `--direction sideways` exits non-zero with `state.tracker` unchanged, and `--role secondary … --direction outward` exits non-zero with `state.secondaryTrackers` unchanged
- [ ] 10.3 Capture the new-tracker `inward` default BEFORE the merge in `setTracker()` — `tracker.mjs:61` merges `{...(state.tracker || {})}`, so a naive `if (!t.direction) t.direction = "inward"` inside the writer silently switches OFF outward mirroring for every existing Jira repo — using `const isNew = !(state.tracker && state.tracker.system)`, and apply the identical fix in `upsertSecondaryTracker` (files: `scripts/lib/tracker.mjs`); verify with a test running `set-tracker --intent paused:todo` against a direction-less jira primary and asserting NO `direction` field is written and only `statusIntent` changed
- [ ] 10.4 Replace the vendor test at `rules.mjs:203` so the outward "External tracker sync" section is emitted iff `outwardApplies` and the inward section iff `inwardProcedureEmittable` (files: `scripts/lib/rules.mjs`); verify with byte-identity tests against 0.26.0 output for both un-upgraded shapes — a jira primary with no `direction` gets the outward section only, a `github-issues` primary with a `repo` and no `direction` gets the inward section only
- [ ] 10.5 Make the inward section vendor-neutral — `github-issues` keeps its literal `gh issue list --repo <repo>` step, any other system gets the "list open items in `<system>` (`<scope>`) with your own tooling" phrasing the secondary path already emits — and instruct deduplication on `externalUrl`, never bare `externalId` (files: `scripts/lib/rules.mjs`); verify a `{system: "jira", projectKey: "JOB", direction: "inward"}` fixture emits an inward section naming `jira` and `externalUrl` and no outward section
- [ ] 10.6 Emit NO inward section for a scope-less tracker on both the un-upgraded and the migrated path, with no line naming an unfilled scope placeholder anywhere in the block (files: `scripts/lib/rules.mjs`, `scripts/lib/constants.mjs`); verify `{system: "github-issues"}` with no `repo` — tested both with no `direction` and with `direction: "inward"` — emits neither section, and that adding a `projectKey` to `{system: "jira", direction: "inward"}` turns the inward section on
- [ ] 10.7 Gate the "Sync after completing tracker-linked work" reminder on at least one configured tracker having an emittable inward procedure, so it stops citing "the writeback steps above" that the same block never emits — this is deliberate emitted-output change #1 of exactly two (files: `scripts/lib/rules.mjs`); verify three fixtures: outward-only → reminder absent; scope-less inward primary → reminder absent; scoped inward primary → reminder present with every referenced writeback step actually emitted in the same block
- [ ] 10.8 Gate the brief's `TRACKER SYNC` outward drift line on `outwardApplies` (`briefing.mjs:115` gates it on a tracker merely EXISTING today, which is why a repo receives a briefing demanding outward action for 29 epics under a rules block with no outward instructions) and its freshness line on `inwardProcedureEmittable` AND a count greater than zero (files: `scripts/lib/briefing.mjs`); verify an inward `github-issues` fixture with unmirrored queued epics has no "not yet in github-issues" line while the identical fixture set `outward` does
- [ ] 10.9 Gate the brief's "N tracker(s) configured — consider `/pm:sync`" nudge (`briefing.mjs:136`) on an emittable inward procedure — deliberate emitted-output change #2 (files: `scripts/lib/briefing.mjs`); verify an outward-only repo gets no nudge while a repo with an outward primary and one secondary does
- [ ] 10.10 Add the emitter-coherence test matrix: for each of `inward`/`outward`/`both` crossed with a `github-issues` and a `jira` tracker, assert the rules block containing an outward procedure and the brief demanding outward action have the SAME truth value; plus the scope-less case where the inward section, the freshness line, the sync nudge, the completion-sync reminder and `/pm:sync`'s external read are all false together (files: `scripts/test/conductor-14.test.mjs`); verify with those 7 fixture cases plus a source-scan test asserting no emitter recomputes direction from `system`, `repo` or `direction` locally
- [ ] 10.11 Make `/pm:sync`'s tracker instructions follow direction — the inward branch (pull new items, compare each linked item's tracker-side updated timestamp against the epic's watermark, read the movers) instructed only where an inward procedure is emittable; under `outward` no external read at all, local OpenSpec/Superpowers registration only; under `both`, both (files: `commands/sync.md`, `scripts/lib/subcommands.mjs`, `scripts/lib/rules.mjs`); verify the instruction text emitted for an outward-only repo contains no step that reads or lists items from that tracker
- [ ] 10.12 Make the emitted inward registration recipe RUN as written — supply the required `--id` it currently omits, derive that id deterministically from the external item so the same item yields the same epic id across repos and sessions, and state the derivation in the recipe (files: `scripts/lib/rules.mjs`, `commands/sync.md`); verify by executing the emitted command verbatim with placeholders filled from a real issue — exits 0 and the epic exists — then running it a second time and getting the SAME id, refused as a duplicate rather than a second epic under an invented slug
- [ ] 10.13 Take the mirrored item's lane from `suggest-lane` (`lane-routing.mjs`) instead of the hardcoded `--lane claude-code`, permitting the agent to override with a reason recorded on the epic (files: `scripts/lib/rules.mjs`, `commands/sync.md`); verify the emitted recipe text contains no `--lane claude-code` literal and names lane routing — this ships the call-site half of #114 only, and #114 stays open
- [ ] 10.14 Update the two MODIFIED tracker-sync behaviors: primary `set-tracker` merges preserving every field not named and never writes `state.secondaryTrackers`, with section choice from `direction` not `system`; secondary inward pull dedups on `externalUrl` when both sides carry one and never on bare `externalId` (files: `scripts/lib/tracker.mjs`, `scripts/lib/rules.mjs`, `scripts/lib/subcommands.mjs`); verify with the two-repos test — `acme/market-intelligence#42` and `acme/risk-engine#42` register as two DISTINCT epics

## 11. Freshness watermark, the single activation door, the refresh gate

- [ ] 11.1 Add `externalUpdatedAt` — the tracker's OWN updated timestamp as of the last content read, never a local clock — declaring `--external-updated-at` in the group-1 registry so `update-epic`, `add-epic` and `add-many` all accept it (files: `scripts/lib/constants.mjs`, `scripts/lib/update-epic.mjs`, `scripts/lib/add-epic.mjs`, `scripts/lib/add-many.mjs`); verify registration with the item's timestamp reads back, a bulk-created entry carries it identically, and the emitted `/pm:sync` list step contains no watermark write, so listing cannot advance it
- [ ] 11.2 Route `add-many` through `activate()` (`active-pointer.mjs:13-23`) instead of constructing epics inline and pushing them onto `state.epics` (`add-many.mjs:61-71`), making it the fourth caller alongside `active-pointer.mjs:55`, `update-epic.mjs:136` and `add-epic.mjs:222` (files: `scripts/lib/add-many.mjs`); verify with a test — failing today — that a batch entry at `active` status becomes the single active epic and demotes any other epic still at `active`
- [ ] 11.3 Set the tracker-refresh obligation INSIDE `activate()`, keyed on `externalId` being present (provenance, never direction) and never derived from current state, keeping `add-epic.mjs:222`'s freshly-read exemption (files: `scripts/lib/active-pointer.mjs`, `scripts/lib/add-epic.mjs`); verify with one test per activation path — `set-active`, `update-epic --status active`, `add-epic --status active`, `add-many` batch — asserting an identical obligation from all four, plus an epic created active in the same command that read the item owing nothing
- [ ] 11.4 Surface the refresh debt in the brief and `PROJECT.md` so a compacted session re-learns it, and emit the local-source re-read instruction (plan document, or OpenSpec proposal plus tasks) for an epic with no external id, recording nothing in state for that case (files: `scripts/lib/briefing.mjs`, `scripts/lib/render.mjs`, `scripts/lib/rules.mjs`); verify a tracker-linked active epic shows the debt on both surfaces while a local-origin active epic owes no tracker refresh and receives the local instruction instead
- [ ] 11.5 Add the refresh-verdict writeback verb requiring BOTH a verdict (`unchanged` | `material-change`) and the item's updated timestamp, persisting verdict, summary, when it was recorded and the watermark, and clearing the obligation (files: `scripts/lib/tracker-refresh-writeback.mjs`, `scripts/conductor.mjs`, `commands/sync.md`) — no `docs/parity-ledger.json` claim is needed: the ledger walks `commands/`, `agents/`, `skills/`, `hooks/` and `.claude-plugin/` only, and this task adds no file under any of them; verify three tests — a verdict with no timestamp exits non-zero writing nothing, a verdict on an epic with no external id exits non-zero writing nothing, and a `material-change` verdict advances `externalUpdatedAt` and clears the obligation
- [ ] 11.6 Make the mechanical PreToolUse block for an outstanding tracker refresh honor the repo's gate-guard setting while leaving the unconditional reconcile block untouched by `set-gate-guard off` (files: `scripts/lib/gate-guard.mjs`, `commands/gate-guard.md`); verify with two tests — the refresh block fires with the guard on and not with it off, and the reconcile block fires with the guard both on and off
- [ ] 11.7 Emit `N tracker-linked epics never re-read since mirroring` (external id present, no watermark) only when an inward procedure is emittable AND the count is greater than zero, and never a claim about how many linked items have newer remote activity, which would require a network call the engine is forbidden to make (files: `scripts/lib/briefing.mjs`); verify a fixture with 3 such epics produces the line, the line disappears once every linked epic has a watermark, and a test asserts no brief output for ANY tracker configuration contains a remote-activity claim

## 12. Epic annotation surface

- [ ] 12.1 Add `description` (a single durable string, replaced when set again) and `notes` (an append-only array of `{at, actor, text}`, `actor` recorded and not interpreted) as first-class epic fields with flags on `add-epic` and `update-epic`, declared in the group-1 registry (files: `scripts/lib/constants.mjs`, `scripts/lib/add-epic.mjs`, `scripts/lib/update-epic.mjs`); verify setting a description leaves notes unchanged, appending a second note preserves the first entry's text verbatim, and replacing the description afterwards leaves every note present
- [ ] 12.2 Add `update-epic --lane`, validated against `KNOWN_LANES` exactly as creation validates it, and `--plan` setting the plan path (files: `scripts/lib/update-epic.mjs`, `scripts/lib/constants.mjs`); verify changing a lane leaves start time, gate verdict, links, stories and list position unchanged; an invalid lane exits non-zero naming the valid lanes with the epic unchanged; a plan attaches to an epic created without one
- [ ] 12.3 Ship `--clear-links` as a named flag and turn the valueless `--link` into a non-zero exit naming the flag, with the clearing form named in `update-epic`'s usage line and `commands/epic.md` (files: `scripts/lib/update-epic.mjs`, `scripts/lib/constants.mjs`, `commands/epic.md`); verify `--clear-links` empties links while leaving title, status, priority, stories and gate verdict unchanged, and that `--link` with no value exits non-zero rather than silently emptying the array as `update-epic.mjs:50-56` does today
- [ ] 12.4 Diagnose a misplaced `--id` as `update-epic`'s first token — either accept it as an alias for the positional id and perform the update, or exit non-zero with a message naming `--id` and showing `update-epic <id> ...`, never a bare usage dump (#71) (files: `scripts/lib/update-epic.mjs`); verify with an assertion on the MESSAGE TEXT — `update-epic --id my-epic --priority P1` either updates `my-epic` or produces a message containing both `--id` and `update-epic <id>`, so today's bare usage line fails it — plus `update-epic --priority P1` with no id in any form erroring that the id is required positionally
- [ ] 12.5 Surface a backlog epic's description in the rendered project record, which today shows an epic's id and not even its title (files: `scripts/lib/render.mjs`); verify an epic registered `--status later` with a description is recoverable from `PROJECT.md` alone without opening `.conductor/state.json`

## 13. The contention warning latches

- [ ] 13.1 Replace `consumeConflictWarning()`'s log rotation (`write-conflicts.mjs:73-77`) with a sidecar LATCH marker file beside the log — never a field in `state.json`, which is the file whose write just failed — and change `briefing.mjs:63` from `conflictCount() === CONFLICT_WARN_THRESHOLD` to `count >= threshold && !latched`, with `clearConflicts()` (already called on any successful write, `state.mjs:130`) removing both the log and the latch (files: `scripts/lib/write-conflicts.mjs`, `scripts/lib/briefing.mjs`); verify by REPLACING the existing `conductor-12.test.mjs` case "a count ABOVE the threshold does not warn — the rule is equality, not >=" with its inverse: a 0→7 burst between two briefings warns exactly ONCE, and the new test fails if the latch is removed because the warning then repeats every briefing
- [ ] 13.2 Stop rotating the log to `.prev` on consume, so the path the warning tells the reader to open still exists and a continuing run of contention does not re-cross the threshold and warn again (files: `scripts/lib/write-conflicts.mjs`); verify by updating `conductor-12.test.mjs`'s "consuming the warning preserves the evidence it points at" to assert the log is NOT renamed, plus a test that a successful state write re-arms the warning so a later run of contention warns once more
- [ ] 13.3 Stop `snapshot()` (`subcommands.mjs:78`) passing `consume: true` — PreCompact writes `.conductor/brief.txt`, which nothing reads back — leaving `brief()` (`subcommands.mjs:65`) as the only consumer and `render()` (`render.mjs:133`) passing no `consume` as it already does (files: `scripts/lib/subcommands.mjs`); verify with a test where a PreCompact snapshot written while the threshold is crossed does not consume and the next SessionStart briefing still carries the warning — the test fails if `consume: true` is left on `snapshot()`
- [ ] 13.4 Confirm the four write-guard behaviors 0.26.0 already shipped stay covered and unbroken by 13.1–13.3 — the stale-write refusal, the hook retry-then-skip with its sidecar entry, the no-op save, and `--force` writing strictly greater than both the read and the on-disk revision (**no files — a confirmation run over existing coverage; one of the four tasks 16.2 exempts**); verify the named cases in `scripts/test/conductor-12.test.mjs` still pass after this group lands, including "`--force` must never rewind the revision below what's on disk"

## 14. Release planning — #125's slice (the 20th issue; this whole group is separable and cuttable)

> **Cutting this group also cuts three `epic-disposition` requirements it solely implements** — "A
> release is a named grouping of epics", "Exclusion from a release is recorded with a reason" and
> "Release membership and exclusions render". No other group touches them, so cutting group 14
> means removing those three requirements from the change rather than shipping them unimplemented.

- [ ] 14.1 Add `state.releases = [{id, intent, target?, deferred: []}]` as first-class objects with a CLI verb to create one, with membership recorded ONE-WAY as `epic.release` (at most one) so the two can never disagree and the engine proposes no membership (files: `scripts/lib/constants.mjs`, `scripts/lib/releases.mjs`, `scripts/conductor.mjs`, `commands/status.md`) — documented in the EXISTING `commands/status.md`, so no `docs/parity-ledger.json` claim is needed; verify "what is in this release" is answerable from `state.json` alone, and that adding, re-prioritizing or archiving epics changes membership for none of them
- [ ] 14.2 Record an exclusion in `state.releases[].deferred[]` using the group-2 disposition record with its required reason, leaving the excluded epic in the backlog rather than ending it (files: `scripts/lib/disposition.mjs`, `scripts/lib/releases.mjs`); verify a deferral reason stored against the epic/release pair survives the release closing, and that a queued epic never associated renders as NEITHER in the release nor deferred from it
- [ ] 14.3 Render a release as "`<release>`: N epics, M deferred" in `PROJECT.md` and the briefing with each deferral's reason reachable from the record (files: `scripts/lib/render.mjs`, `scripts/lib/briefing.mjs`); verify a release with 12 members and 3 deferrals renders `12 epics, 3 deferred` on both surfaces and the three reasons read back from `state.json`
- [ ] 14.4 Record THIS release's own plan against a `0.27.0` release object, including every "excluded because…" judgment in `proposal.md`'s scope note and #125's own cut-line (files: `.conductor/state.json`); verify `/pm:status` shows the `0.27.0` release line with its member and deferred counts, so the release's plan survives outside this conversation transcript — the exact failure this change exists to fix

## 15. The gate procedure pm emits (instruction layer — no engine work)

> These bind the text **pm owns and emits** — the managed `CLAUDE.md` rules block (`rules.mjs`), the
> `conductor` skill, and the command docs — because a change's `tasks.md` is authored by the
> `openspec` plugin, which pm neither owns nor writes. Group 16 separately performs the sweep and
> the commit audit on THIS change.

- [ ] 15.1 Add the call-site completeness sweep to the emitted gate procedure as a NUMBERED REQUIRED TASK ITEM, not a prose bullet — for every rule, guard or invariant a change introduces or modifies, enumerate all call sites of the guarded thing, state where the rule holds and where it does not, and justify each omission (files: `scripts/lib/rules.mjs`, `skills/conductor/SKILL.md`, `commands/epic.md`, `commands/status.md`); verify with a test asserting the emitted string carries it as a numbered required item with the enumeration named concretely — the test fails if it is downgraded to a bullet, which the audit measured at 3/15 adoption against 14/14
- [ ] 15.2 Add commit-based verification to the same three emitted surfaces as a numbered required task item, stating explicitly that reading the working tree does not count as verification and requiring the files a task claims to change to be asserted present in that task's commit (files: `scripts/lib/rules.mjs`, `skills/conductor/SKILL.md`, `commands/epic.md`, `commands/status.md`); verify with a test asserting the emitted text contains both the numbered item and the explicit working-tree statement
- [ ] 15.3 Add the `<!-- pm:lifecycle -->` obligation to the emitted rules block and brief — mark a lifecycle-bookkeeping task with the literal token at the moment the task source is authored OR AMENDED, naming the self-referential archive task as the case that always qualifies (files: `scripts/lib/rules.mjs`, `scripts/lib/briefing.mjs`, `skills/conductor/SKILL.md`); verify with a test asserting both surfaces contain the literal token and cover amendment of a source that predates this capability
- [ ] 15.4 Add the `--attribute-commit` obligation to the emitted instructions — attribute each commit to its epic at the moment the commit is made, naming the flag, naming the per-task conventional commit of an OpenSpec apply loop as the always-qualifying case, and covering work already in flight — **and state the exclusion in the same text**: the commit that moves `openspec/changes/<id>/` under `archive/`, and any commit that only relocates or deletes a change's artifacts rather than implementing its work, is lifecycle bookkeeping and MUST NOT be attributed, because the move lands after the reviewed range by construction and attributing it makes the epic's own Gate 2 stale at the instant the archive gate reads it (files: `scripts/lib/rules.mjs`, `skills/conductor/SKILL.md`, `commands/epic.md`); verify with a test asserting each of the three surfaces names the flag AND names the archive move as a commit not to attribute — the exclusion is agent-declared exactly like `<!-- pm:lifecycle -->`, so the engine classifies nothing and 5.6 asserts the behavioral half against the gate
- [ ] 15.5 Ensure no emitted surface presents removing the record as a way to end an epic, a story, a deferral or a release exclusion — every such text names the disposition path with its required reason — while `remove-epic` stays available and ungated for an epic registered in error (files: `scripts/lib/rules.mjs`, `skills/conductor/SKILL.md`, `commands/epic.md`, `commands/status.md`); verify with a test scanning the rules block, the skill and every command doc for a removal suggestion in an ending context and asserting none, plus a test that `remove-epic` still works on an epic carrying no work

## 16. Applying this release's own remedies to itself

- [ ] 16.1 Perform #115's **call-site completeness sweep on this change**: for every rule, guard and invariant it introduces or modifies — the archive transition across all FIVE paths, openspec-lane normalization across all FOUR sites, `activate()`, the shared flag registry, the direction predicates, the outstanding-work definition, the disposition stamp and its replacement rule, the latch — enumerate ALL call sites of the guarded thing, state where the rule holds and where it does not, and justify each omission (files: `openspec/changes/conductor-tells-the-truth/call-site-sweep.md`); verify by delivering the written enumeration (rule → call-site list → held / not held → justification) committed with the change, with every "not held" carrying a justification — a rule whose call sites are not enumerated fails this task
- [ ] 16.2 Perform #121's **verification against the commit**: for every task in this list, run `git show --stat <that task's sha>` and assert every file named in that task's `(files: …)` clause appears in that commit — explicitly NOT the working tree; a task that names no files **fails**, with exactly four stated exemptions — `13.4`, `17.5`, `17.7`, `17.8`, named in this document's preamble, which produce no file of their own (files: `openspec/changes/conductor-tells-the-truth/commit-verification.md`); verify by delivering the per-task file-vs-commit table covering every task in this list, where a task whose claimed file is absent from its commit fails even though the working tree holds the edit and the whole suite passes, and where an unclaimed, unexempted task fails on that ground alone
- [ ] 16.3 Exercise both emitted obligations on this release itself — amend this `tasks.md` so its archive task carries `<!-- pm:lifecycle -->` (17.9 already does), and attribute this change's commits with `update-epic --attribute-commit <sha>` as each is made while NOT attributing the archive-move commit per 15.4 (files: `.conductor/state.json`); verify this change's own epic reaches archive with outstanding work of zero and a NON-empty `attributedCommits` whose last entry equals the `headSha` 17.2 recorded, so neither the handoff refusal (6.6), the staleness gate (5.4) nor the empty-attribution integrity check (9.10) fires on it

## 17. Release discipline, docs, and archive

- [ ] 17.1 Edit the stale `## Purpose` in `openspec/specs/tracker-sync/spec.md` — it still says "full bidirectional mirror, or the `github-issues` inward-only special case", and a delta's Purpose is never merged, so nothing fails if this is forgotten — replacing it with the direction-governed description (files: `openspec/specs/tracker-sync/spec.md`); verify `rg -c "full bidirectional mirror" openspec/specs/tracker-sync/spec.md` returns 0
- [ ] 17.2 Run the Gate 2 implementation review over the full `BASE..HEAD` diff with a fresh-context reviewer and record it with `record-gate-review <id> --gate 2 --verdict pass --base-sha <a> --head-sha <b> --reviewer "<identity>"` (files: `.conductor/state.json`); verify the verdict reads back from `state.json` with both shas as fields and that the recorded `headSha` equals the last entry of the epic's `attributedCommits`, so 5.4's staleness gate does not refuse the archive
- [ ] 17.3 Bump `.claude-plugin/plugin.json` to `0.27.0` and add the `CHANGELOG.md` entry naming every user-visible change — including the new-tracker `inward` default as a behavior REVERSAL and both deliberate emitted-output changes for existing repos (files: `.claude-plugin/plugin.json`, `CHANGELOG.md`); verify `/pm:changelog` renders the 0.27.0 delta and the version matches the `MIGRATIONS` key added in 7.1
- [ ] 17.4 Update `README.md` for every user-visible change, stating the new-tracker `inward` default explicitly as a behavior change with its one-line remedy `set-tracker --system jira --direction outward` (files: `README.md`); verify `rg "direction" README.md` finds the default and the remedy, and that a reader can find the disposition, integrity and attribution verbs there
- [ ] 17.5 Update the Mintlify site (`pm-plugin.dev`) in the SAME PR cycle per `CLAUDE.md`, following the `mintlify-doc-sync` skill — the Changelog page, Real Numbers, and every page touching trackers, epics, gates or archiving, with the `inward` default named (**no files in this repo — the docs site is off-repo; one of the four tasks 16.2 exempts**); verify by the skill's own live-verification step against the deployed pages
- [ ] 17.6 Update `commands/epic.md`, `commands/sync.md`, `commands/tracker.md`, `commands/status.md`, `commands/upgrade.md` and `skills/conductor/SKILL.md` for every new flag and writeback verb, claiming any NEW file in `docs/parity-ledger.json` in the same commit (design §16 — **as proposed, no new claimed path is needed**: every new module lands under `scripts/lib/`, which the parity walk does not cover, and every doc edited here already exists and is already claimed) (files: `commands/epic.md`, `commands/sync.md`, `commands/tracker.md`, `commands/status.md`, `commands/upgrade.md`, `skills/conductor/SKILL.md`); verify `node --test scripts/test/parity.test.mjs` passes
- [ ] 17.7 Run the whole suite green — `node --test scripts/test/*.test.mjs` — before every commit, with no `--no-verify` anywhere (**no files — a test run; one of the four tasks 16.2 exempts**); verify the final run reports `fail 0` and a total strictly greater than the 335 baseline, so this change demonstrably added coverage rather than only passing what already existed
- [ ] 17.8 Close the resolved issues and confirm the two partial ones stay OPEN — #71, #79, #80, #88, #102, #103, #107, #109, #110, #113, #115, #116, #117, #118, #119, #120, #121, #122, #124 and #125's slice close; #66 and #114 must NOT be closed (**no files — a tracker close pass; one of the four tasks 16.2 exempts**); verify `gh issue list --repo cfdude/pm --state open` still lists #66 and #114 after the close pass, and lists none of the nineteen
- [ ] 17.9 <!-- pm:lifecycle --> Run `/opsx:archive conductor-tells-the-truth`, let the archive-drift heal flip the epic, then record the real disposition on the already-archived epic (6.2) with `outcome: delivered`, which replaces the heal's engine stamp (6.3) — and do NOT attribute the archive-move commit (15.4) (files: `openspec/changes/archive/`, `.conductor/state.json`); verify the change directory appears under `openspec/changes/archive/`, `/pm:status` shows the epic `archived` carrying `outcome: delivered` with NO `recordedBy` on that record, its passing `gate2` intact with no `ungated` entry anywhere in `gateReview`, and `integrity` reports no new finding against it
