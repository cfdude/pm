G-M1 RED — the drift refusal mislabelled the engine-source trigger as a "module", and its trigger
branch was dead code.

A genuine RED, not a mutation: the new test fails against the pre-fix source. The pre-fix code was
restored in a copy at .../g2fix-47/gm12/pm (`noun: set.get(entryId).kind`), and nothing else changed.

  $ node --test --test-name-pattern="G-M1" scripts/test/assert/drift-script.test.mjs
  ✖ G-M1 the refusal says WHICH KIND of thing changed — the trigger is not a module
    AssertionError: the engine-source entry is a TRIGGER, not a module

WHY: `certifiedSet()` maps an id to the FILES it covers — an ARRAY — so `.kind` on it is always
`undefined`, `describeRefusal`'s `r.noun === "trigger"` branch never fired, and an edit that moved the
sweep bucket's subject printed "the certified module 'engine-source' changed". The noun now comes from
the entry's own `kind` when the record has one, and from the same derivation `runFor()` uses when it
does not — the second case is covered by its own assertion, because `entry` is then undefined.

  $ node --test scripts/test/assert/drift-script.test.mjs     # pre-fix
  ℹ tests 20  ℹ pass 18  ℹ fail 2      # G-M1 and G-M2, and nothing else
