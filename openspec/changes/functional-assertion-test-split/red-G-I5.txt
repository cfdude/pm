G-I5 RED — the gateway call-site guard failed open, in both halves.

REGRESSION-GUARD style: both guards pass the moment they exist, so the RED is the MUTATION, made in a
copy of this working tree at .../g2fix-47/gi5/pm. Nothing was weakened to make anything pass.

WHY IT WAS OPEN. Both guards derived spawn sites with `(execFileSync|execSync)\(\s*"git` — two of the
six child-process entry points, no aliased binding, and nothing for a program passed as an expression.

MUTANT A — `import { spawnSync } from "node:child_process";` + `spawnSync("git", ["rev-parse","HEAD"])`
appended to scripts/lib/changelog.mjs:

  $ node --test scripts/test/functional/git-gateway-guard.test.mjs
  ✖ gateway: no engine module outside the gateway spawns git
    AssertionError: a git call site outside the injected gateway is a call site the assertion half's
      double cannot answer — it must be given an operation in scripts/lib/git-gateway.mjs
  $ node --test scripts/test/assert/git-gateway-guard.test.mjs
  ✖ gateway: no engine module outside the gateway spawns git — the call sites are DERIVED, not listed
    AssertionError: every git invocation must live in lib/git-gateway.mjs …

MUTANT B — the same site written through an ALIASED binding
(`import { execFileSync as __x } from "node:child_process"; __x("git", ["rev-parse","HEAD"])`):
both guards fail the same way (1 failing test each, by the same assertion).

Under the OLD pattern both mutants left BOTH guards green — 4/4 and 5/5, exactly as the Gate 2
reviewer measured — because neither the name `spawnSync`/`__x` nor an aliased call matched.

THE FALSE CLAIM, corrected in the same commit. The old exclusion assertion said
`tool-currency.mjs: execFileSync("openspec"` was "the engine's only non-git spawn". It was already
false: `scripts/lib/self-hosting.mjs` calls `spawnSync(process.execPath, …)`, which a pattern
requiring a string literal after the paren cannot see. The non-git set is now asserted BY IDENTITY
(two entries, both named in design D4) instead of by a length bound over one file.

CONTROL — the same two guards in the unmutated working tree:
  functional: tests 5, pass 5, fail 0     assertion: tests 6, pass 6, fail 0
