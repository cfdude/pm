G-I1 RED — the pre-commit floor had no test in the FIRING direction, and the "never a superset"
invariant was unheld.

Both are REGRESSION-GUARD style fixes (the tasks.md vocabulary for 3.5 / 4.1 / 5.2 / 6.1): the new
tests pass the moment they exist, so the RED here is the MUTATION — each mutant was made in a copy of
this working tree and each one kills the check it is aimed at. Nothing was weakened to make a test
pass; the two mutations are exactly the two the Gate 2 reviewer reported as invisible.

MUTANT 1 — neuter the floor: `if [ -n "$total" ] && [ -n "$declared" ] && [ "$total" -lt "$declared" ]`
           → `if false; then`   in a COPY at .../g2fix-47/gi1/pm

  $ node --test --test-name-pattern="G-I1 the floor FIRES" scripts/test/functional/conductor-09.test.mjs
  ✖ G-I1 the floor FIRES when the runner's glob cannot reach a file the half still declares
    AssertionError: the floor must abort the commit: the file is declared and never ran. Output was:
      drift: ok — 2 tracked test files, 0 functional ids, 2 staged paths
      pre-commit: 1/1 passing
    actual: 0, expected: 0, operator: 'notStrictEqual'

  The fixture's shape is real, not doctored: `git ls-files 'scripts/test/assert/*.test.mjs'` matches
  `/` and a leading dot (so scripts/test/assert/.collapsed.test.mjs IS declared) while /bin/sh's
  expansion of the SAME pattern matches neither (so the runner never runs it) — total < declared by
  construction. With the floor live it aborts; neutered, the commit goes through 1/1 passing.

MUTANT 2 — the forbidden superset: `declared=$(git ls-files 'scripts/test/assert/*.test.mjs'` →
           `declared=$(git ls-files 'scripts/test/assert/*.test.mjs' 'scripts/test/functional/*.test.mjs'`
           in the same copy.

  $ node --test --test-name-pattern="pre-commit exists, is executable" scripts/test/assert/conductor-09.test.mjs
  ✖ .githooks/pre-commit exists, is executable, and runs the assertion half and the drift script
    AssertionError: expected the hook text NOT to match /declared=\$\(git ls-files[^\n]*functional/

  Before this fix the same edit left EVERYTHING green — the assertion twin's shape check was a prefix
  `assert.match`, which the superset line still satisfied — while in the real repository it aborts
  every commit that has a functional half at all (design D8's reproduced `total=6 declared=15`).

CONTROL — both new tests, both mutations reverted, in this working tree: 3 passing, 0 failing (the
G-I1 fixture test, the exact-runner-line assertion and the two doesNotMatch assertions).
