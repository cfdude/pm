{
  "hooks": {
    "SessionStart": [
      {
        "comment": "PM CONDUCTOR: Inject the project briefing (active epic, detour stack, next-up) on startup, resume, and AFTER auto-compaction (source=compact). No-ops silently in projects that have not run /pm:init.",
        "matcher": "startup|resume|compact",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/conductor.mjs\" brief --platform claude-code"
          }
        ]
      }
    ],
    "PreCompact": [
      {
        "comment": "PM CONDUCTOR: PreCompact cannot inject context, so snapshot the freshest state (re-render PROJECT.md + write .conductor/brief.txt) right before the window collapses.",
        "matcher": "auto|manual",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/conductor.mjs\" snapshot --platform claude-code"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "comment": "PM CONDUCTOR: Fires on EVERY Bash call by design — commit-nudge decides whether a commit happened by OBSERVING the repo (a HEAD watermark plus the reflog action), not by reading the command text, so it must see every call to keep the watermark truthful. It re-renders the index and nudges to update epic status / pop a finished detour only when a commit actually landed. Non-blocking, and silent in projects that have not run /pm:init.",
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/conductor.mjs\" commit-nudge --platform claude-code"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "comment": "PM CONDUCTOR: gate guard — on by default for any epic with reconcileNeeded:true (unconditional, `set-gate-guard off` does not bypass it). Blocks Edit/Write/NotebookEdit while the active epic still owes a reconcile after a detour POP. Dormant until /pm:init.",
        "matcher": "Edit|Write|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/conductor.mjs\" gate-guard --platform claude-code"
          }
        ]
      }
    ]
  }
}
