# PROJECT — Conductor Index

> GENERATED by the `pm` plugin — do not hand-edit. Source of truth is
> `.conductor/state.json` (ordering, detours, links) + OpenSpec `tasks.md` (stories).
> Regenerate: `/pm:status` (or `node scripts/conductor.mjs render`).
> Last rendered: 2026-09-25T02:51:58.210Z

## Now

**`node-support-policy`** — Node support policy: support the oldest non-EOL Node major (floor 22 today, 24 after 2027-04-30); CI matrix 22/24/26; retire Node 18 pin, stale actions (v4 on EOL node20 runtime), and old-Node workarounds; correct every 'Node 18+' claim (epic, P1) — 22/50 stories · 1 lifecycle

## Detour stack

_Empty — no work is paused._

## Epics

> Progress is claimed completion — ticked checkboxes, not verified delivery.

| Priority | Epic | Lane | Role | Status | Progress | Links |
|----------|------|------|------|--------|----------|-------|
| P0 | `conductor-tells-the-truth` | openspec | epic | archived · delivered 🤖 | — | - |
| P0 | `gh-110-gate2-bypassed-by-archive-hook` | claude-code | epic | archived · delivered · corrected (was delivered) | — | - |
| P0 | `gh-122-gate-records-unreliable-both-ways` | claude-code | epic | archived · delivered | — | - |
| P1 | `backlog-grooming-pass` | openspec | epic | planned | ⚠ tasks.md missing | relates-to→epic-registration-clock |
| P1 | `detached-head-is-not-a-workspace` | openspec | epic | archived · delivered | — | relates-to→gh-cfdude-pm-175; supersedes→gh-cfdude-pm-175 |
| P1 | `disposition-backfill-undispositioned-archive` | openspec | epic | archived · delivered | — | - |
| P1 | `epic-registration-clock` | openspec | epic | archived · delivered | — | - |
| P1 | `every-verb-refuses-what-it-does-not-read` | openspec | epic | archived · delivered | — | supersedes→every-verb-validates-its-argv |
| P1 | `functional-assertion-test-split` | openspec | epic | archived · delivered | 47/47 stories · 1 lifecycle | relates-to→comprehensive-code-review-after-0-43-0 |
| P1 | `gates-bind-to-verified-evidence` | openspec | epic | archived · delivered | — | supersedes→reconcile-gate-bypasses; supersedes→commit-shas-stored-unresolved; supersedes→gate-staleness-reads-only-last-attribution |
| P1 | `gh-cfdude-pm-175` | openspec | epic | archived · superseded | — | - |
| P1 | `node-support-policy` | openspec | epic | active | 22/50 stories · 1 lifecycle | - |
| P1 | `state-file-refuses-to-guess` | openspec | epic | archived · delivered | — | supersedes→state-json-unparseable-loads-as-empty; supersedes→state-write-race-loses-updates; supersedes→claim-ttl-overflow-crashes-readers; supersedes→rules-block-writer-destroys-hand-content |
| P1 | `unit-rung-and-fixture-snapshots` | openspec | epic | archived · delivered | — | supersedes→functional-assertion-test-split |
| P1 | `commit-shas-stored-unresolved` | superpowers | epic | archived · superseded | — | relates-to→comprehensive-code-review-after-0-43-0; relates-to→gate-staleness-reads-only-last-attribution |
| P1 | `comprehensive-code-review-after-0-43-0` | superpowers | epic | archived · delivered | — | - |
| P1 | `every-verb-validates-its-argv` | superpowers | epic | archived · superseded | — | relates-to→comprehensive-code-review-after-0-43-0; relates-to→gh-cfdude-pm-187 |
| P1 | `gh-cfdude-pm-169` | superpowers | epic | archived · delivered | — | relates-to→gh-66-update-epic-missing-flags; relates-to→gh-70-link-validation-gap |
| P1 | `gh-cfdude-pm-174` | superpowers | epic | archived · delivered | — | relates-to→epic-registration-clock |
| P1 | `gh-cfdude-pm-180` | superpowers | epic | archived · delivered | — | - |
| P1 | `gh-cfdude-pm-182` | superpowers | epic | archived · delivered | — | relates-to→gh-cfdude-pm-176 |
| P1 | `gh-cfdude-pm-185` | superpowers | epic | archived · delivered | — | - |
| P1 | `per-verb-help-from-flag-registry` | superpowers | epic | archived · delivered | — | - |
| P1 | `reconcile-gate-bypasses` | superpowers | epic | archived · superseded | — | relates-to→comprehensive-code-review-after-0-43-0 |
| P1 | `rules-block-emits-no-help-pointer` | superpowers | epic | archived · delivered | — | relates-to→per-verb-help-from-flag-registry; relates-to→rules-block-hardcodes-claude-slash-commands |
| P1 | `rules-block-writer-destroys-hand-content` | superpowers | epic | archived · superseded | — | relates-to→comprehensive-code-review-after-0-43-0 |
| P1 | `state-json-unparseable-loads-as-empty` | superpowers | epic | archived · superseded | — | relates-to→comprehensive-code-review-after-0-43-0 |
| P1 | `state-write-race-loses-updates` | superpowers | epic | archived · superseded | — | relates-to→comprehensive-code-review-after-0-43-0 |
| P1 | `sweep-covers-operation-pairs` | superpowers | epic | archived · delivered | — | relates-to→gh-cfdude-pm-169 |
| P1 | `test-suite-perf-and-scaffolding` | superpowers | epic | archived · delivered | 10/10 stories | - |
| P1 | `claim-ttl-overflow-crashes-readers` | claude-code | epic | archived · superseded | — | relates-to→comprehensive-code-review-after-0-43-0 |
| P1 | `df-changelog-fragment-conflicts` | claude-code | epic | archived · delivered 🤖 | — | - |
| P1 | `df-conflict-resolution-verification-step` | claude-code | epic | archived · delivered 🤖 | — | - |
| P1 | `df-hierarchy-executor-must-check-readme-for-user-facing-changes` | claude-code | epic | archived · delivered | — | - |
| P1 | `df-live-infra-epic-session-continuity-warning` | claude-code | epic | archived · delivered 🤖 | — | - |
| P1 | `df-readme-stale-since-gate-guard` | claude-code | epic | archived · delivered | — | - |
| P1 | `edd-observe-hardcodes-claude-md` | claude-code | epic | archived · delivered | 1/1 stories | depends-on→rules-block-hardcodes-claude-slash-commands |
| P1 | `edd-user-memory-confound-control-run` | claude-code | epic | archived · delivered | 8/8 stories | blocks→hermes-platform-support |
| P1 | `gh-101-dependency-order-blind-to-blocked` | claude-code | epic | archived · delivered | — | - |
| P1 | `gh-109-tracker-sync-direction` | claude-code | epic | archived · delivered | — | - |
| P1 | `gh-112-backlog-triage-at-intake` | claude-code | epic | archived · delivered | — | - |
| P1 | `gh-113-gate-verdict-records-no-diff-range` | claude-code | epic | archived · delivered | — | - |
| P1 | `gh-115-gates-blind-to-absent-edit` | claude-code | epic | archived · delivered | — | relates-to→gh-113-gate-verdict-records-no-diff-range |
| P1 | `gh-116-conflict-warning-missed-on-burst` | claude-code | epic | archived · delivered | — | relates-to→gh-115-gates-blind-to-absent-edit |
| P1 | `gh-117-sync-blind-to-archived-changes` | claude-code | epic | archived · delivered | — | - |
| P1 | `gh-118-checkbox-signal-unreliable` | claude-code | epic | archived · delivered | — | relates-to→gh-95-stories-optional-and-gate-nothing |
| P1 | `gh-119-deferrals-have-no-path-to-backlog` | claude-code | epic | archived · delivered | — | - |
| P1 | `gh-120-epic-outcome-shipped-vs-killed` | claude-code | epic | archived · delivered | — | - |
| P1 | `gh-121-tasks-verified-against-tree-not-commit` | claude-code | epic | archived · delivered | — | - |
| P1 | `gh-124-rules-block-emits-failing-add-epic` | claude-code | epic | archived · delivered | — | relates-to→gh-114-lane-routing-blind-to-product |
| P1 | `gh-125-release-planning-and-deliberate-exclusion` | claude-code | epic | archived · delivered | — | relates-to→gh-119-deferrals-have-no-path-to-backlog |
| P1 | `gh-126-release-level-cross-spec-gate` | claude-code | epic | archived · delivered | — | depends-on→gh-125-release-planning-and-deliberate-exclusion |
| P1 | `gh-128-openspec-staleness-nudge` | claude-code | epic | archived · delivered | — | relates-to→gh-123-adopt-openspec-1-9-cli-surface |
| P1 | `gh-134-hooks-run-the-installed-plugin` | claude-code | epic | archived · delivered | — | - |
| P1 | `gh-137-release-completion-cascade` | claude-code | epic | archived · delivered | — | relates-to→gh-130-disposition-has-no-correction-verb |
| P1 | `gh-139-command-doc-engine-fallback-ungated` | claude-code | epic | archived · delivered | — | relates-to→gh-134-hooks-run-the-installed-plugin |
| P1 | `gh-140-attribution-write-not-persisted` | claude-code | epic | archived · delivered | — | - |
| P1 | `gh-142-recorded-sha-unreachable` | claude-code | epic | archived · delivered | — | relates-to→gh-140-attribution-write-not-persisted |
| P1 | `gh-151-no-push-detour-verb` | claude-code | epic | archived · delivered | — | relates-to→gh-94-serial-detours-accepted-silently |
| P1 | `gh-156-shipped-markdown-structural-check` | claude-code | epic | archived · delivered | — | - |
| P1 | `gh-165-deferral-assertion-mangled-and-immutable` | claude-code | epic | archived · delivered | — | relates-to→gh-149-add-epic-drops-valueless-flag |
| P1 | `gh-64-sync-duplicate-shipped-plan` | claude-code | epic | archived · delivered | 1/1 stories · 2 disposed | - |
| P1 | `gh-65-autodetour-false-positives` | claude-code | epic | archived · delivered | 6/6 stories | relates-to→autodetour-parser-misses-am-and-f |
| P1 | `gh-66-update-epic-missing-flags` | claude-code | epic | archived · delivered | — | blocks→gh-64-sync-duplicate-shipped-plan |
| P1 | `gh-69-sync-no-done-signal-for-plans` | claude-code | epic | archived · delivered | — | relates-to→gh-64-sync-duplicate-shipped-plan |
| P1 | `gh-79-epic-description-and-notes` | claude-code | epic | archived · delivered | — | relates-to→epic-session-attribution |
| P1 | `gh-95-stories-optional-and-gate-nothing` | claude-code | epic | archived · delivered | — | depends-on→gh-79-epic-description-and-notes |
| P1 | `pm-fix-link-flag-validation-and-update` | claude-code | epic | archived · delivered | — | - |
| P1 | `release-0-43-0-cut` | claude-code | epic | archived · delivered | — | relates-to→archive-gate-reads-what-it-writes; relates-to→gate-verdict-withdrawal; relates-to→comprehensive-code-review-after-0-43-0; carried-from←archive-gate-reads-what-it-writes; carried-from←gate-verdict-withdrawal |
| P1 | `state-lost-update-concurrent-sessions` | claude-code | epic | archived · delivered | — | depends-on→gh-106-conductor-logs-never-gitignored |
| P2 | `archive-gate-reads-what-it-writes` | openspec | detour | archived · delivered | — | relates-to→gate-verdict-withdrawal; resolves-blocker-for→gate-verdict-withdrawal; carried-to→release-0-43-0-cut |
| P2 | `commit-nudge-reads-the-whole-move` | openspec | epic | archived · delivered | — | supersedes→commit-watch-misses-commits; relates-to→gh-cfdude-pm-173; relates-to→gh-cfdude-pm-184; relates-to→gh-cfdude-pm-195; relates-to→gh-cfdude-pm-199 |
| P2 | `conductor-mjs-module-split` | openspec | epic | archived · delivered | — | - |
| P2 | `emitted-commands-run-as-written` | openspec | epic | archived · delivered | — | supersedes→emitted-text-says-hand-edit-state; supersedes→tracker-sync-recipes-broken; supersedes→agent-docs-teach-stale-gate-evidence; relates-to→gh-cfdude-pm-189 |
| P2 | `gate-verdict-withdrawal` | openspec | epic | archived · delivered | — | supersedes→gh-cfdude-pm-192; relates-to→gh-163-gate-verdicts-openspec-only; may-invalidate→archive-gate-reads-what-it-writes; relates-to→handoff-refusal-prints-story-titles-raw; relates-to→archived-delivered-gate2-regression-report; carried-to→release-0-43-0-cut |
| P2 | `gh-cfdude-pm-205` | openspec | epic | untriaged ⚠ no change on disk | ⚠ tasks.md missing | - |
| P2 | `hierarchy-run-has-one-state-writer` | openspec | epic | planned | ⚠ tasks.md missing | relates-to→emitted-commands-run-as-written |
| P2 | `multi-tracker-primary-secondary-support` | openspec | epic | archived · delivered | — | - |
| P2 | `operations-ship-their-inverses` | openspec | epic | archived · delivered | — | supersedes→autonomy-grants-have-no-revoke; supersedes→disposition-references-unvalidated; supersedes→archived-paused-epic-jams-detour-stack |
| P2 | `the-guard-covers-every-write-path` | openspec | epic | archived · delivered | — | supersedes→gate-guard-bash-writes-unguarded |
| P2 | `user-text-never-forges-output` | openspec | epic | archived · delivered | — | supersedes→user-text-unescaped-in-render-brief-integrity; supersedes→handoff-refusal-prints-story-titles-raw |
| P2 | `add-many-drops-input-silently` | superpowers | epic | planned | — | relates-to→comprehensive-code-review-after-0-43-0 |
| P2 | `agent-docs-teach-stale-gate-evidence` | superpowers | epic | archived · superseded | — | relates-to→comprehensive-code-review-after-0-43-0 |
| P2 | `archived-paused-epic-jams-detour-stack` | superpowers | epic | archived · superseded | — | relates-to→comprehensive-code-review-after-0-43-0 |
| P2 | `autonomy-grants-have-no-revoke` | superpowers | epic | archived · superseded | — | relates-to→comprehensive-code-review-after-0-43-0 |
| P2 | `changelog-page-too-long-archive-older-releases` | superpowers | epic | planned | — | - |
| P2 | `commit-gate-tests-working-tree-not-index` | superpowers | epic | planned | — | relates-to→comprehensive-code-review-after-0-43-0 |
| P2 | `commit-watch-misses-commits` | superpowers | epic | archived · superseded | — | relates-to→comprehensive-code-review-after-0-43-0; relates-to→gh-cfdude-pm-199; relates-to→gh-cfdude-pm-173 |
| P2 | `disposition-references-unvalidated` | superpowers | epic | archived · superseded | — | relates-to→comprehensive-code-review-after-0-43-0 |
| P2 | `emitted-text-says-hand-edit-state` | superpowers | epic | archived · superseded | — | relates-to→comprehensive-code-review-after-0-43-0 |
| P2 | `fleet-upgrade-after-release` | superpowers | epic | planned | — | - |
| P2 | `gate-guard-bash-writes-unguarded` | superpowers | epic | archived · superseded | — | relates-to→comprehensive-code-review-after-0-43-0 |
| P2 | `gate-staleness-reads-only-last-attribution` | superpowers | epic | archived · superseded | — | relates-to→archive-gate-reads-what-it-writes; relates-to→archived-delivered-gate2-regression-report |
| P2 | `gh-cfdude-pm-167` | superpowers | epic | untriaged | — | relates-to→gh-117-sync-blind-to-archived-changes |
| P2 | `gh-cfdude-pm-168` | superpowers | epic | archived · delivered | — | relates-to→gh-139-command-doc-engine-fallback-ungated |
| P2 | `gh-cfdude-pm-173` | superpowers | epic | archived · delivered | — | relates-to→df-autodetour-false-positive-conductor-admin-commits |
| P2 | `gh-cfdude-pm-176` | superpowers | epic | untriaged | — | - |
| P2 | `gh-cfdude-pm-177` | superpowers | epic | archived · delivered | — | relates-to→openspec-gate1-guard-on-apply |
| P2 | `gh-cfdude-pm-178` | superpowers | epic | archived · delivered | — | relates-to→sweep-covers-operation-pairs |
| P2 | `gh-cfdude-pm-181` | superpowers | epic | archived · delivered | — | relates-to→gh-cfdude-pm-178 |
| P2 | `gh-cfdude-pm-184` | superpowers | epic | archived · delivered | — | relates-to→gh-cfdude-pm-173 |
| P2 | `gh-cfdude-pm-186` | superpowers | epic | archived · delivered | — | - |
| P2 | `gh-cfdude-pm-189` | superpowers | epic | archived · delivered | — | - |
| P2 | `gh-cfdude-pm-190` | superpowers | epic | untriaged | — | relates-to→gate-verdict-withdrawal |
| P2 | `gh-cfdude-pm-191` | superpowers | epic | archived · delivered | — | relates-to→gh-cfdude-pm-177 |
| P2 | `gh-cfdude-pm-192` | superpowers | epic | archived · superseded | — | relates-to→gh-cfdude-pm-175 |
| P2 | `gh-cfdude-pm-194` | superpowers | epic | untriaged | — | relates-to→lessons-index-cannot-drift-silently |
| P2 | `gh-cfdude-pm-195` | superpowers | epic | archived · delivered | — | relates-to→gh-cfdude-pm-173; relates-to→gh-cfdude-pm-184 |
| P2 | `gh-cfdude-pm-196` | superpowers | epic | untriaged | — | - |
| P2 | `gh-cfdude-pm-198` | superpowers | epic | untriaged | — | - |
| P2 | `gh-cfdude-pm-199` | superpowers | epic | archived · delivered | — | - |
| P2 | `gh-cfdude-pm-200` | superpowers | epic | untriaged | — | - |
| P2 | `gh-cfdude-pm-201` | superpowers | epic | untriaged | — | - |
| P2 | `gh-cfdude-pm-204` | superpowers | epic | untriaged | — | - |
| P2 | `handoff-demand-blind-spots` | superpowers | epic | planned | — | relates-to→comprehensive-code-review-after-0-43-0 |
| P2 | `hooks-not-silent-before-init` | superpowers | epic | planned | — | relates-to→comprehensive-code-review-after-0-43-0 |
| P2 | `lesson-detect-matcher-hardening` | superpowers | epic | planned | — | relates-to→comprehensive-code-review-after-0-43-0; relates-to→gh-cfdude-pm-194 |
| P2 | `mutation-pass-is-a-required-task-item` | superpowers | epic | planned | — | relates-to→sweep-covers-operation-pairs |
| P2 | `suite-twin-and-certification-discipline` | superpowers | epic | later | — | relates-to→functional-assertion-test-split |
| P2 | `tracker-item-dedup-bypassed` | superpowers | epic | planned | — | relates-to→comprehensive-code-review-after-0-43-0 |
| P2 | `tracker-sync-recipes-broken` | superpowers | epic | archived · superseded | — | relates-to→comprehensive-code-review-after-0-43-0 |
| P2 | `user-text-unescaped-in-render-brief-integrity` | superpowers | epic | archived · superseded | — | relates-to→comprehensive-code-review-after-0-43-0; relates-to→handoff-refusal-prints-story-titles-raw |
| P2 | `activity-log-detour-events-lose-epic` | claude-code | epic | planned | — | relates-to→comprehensive-code-review-after-0-43-0 |
| P2 | `agent-outcomes-derivation-unguarded` | claude-code | epic | archived · delivered | — | - |
| P2 | `ai-feedback-loop-github-issues` | claude-code | epic | archived · delivered 🤖 | — | - |
| P2 | `autodetour-parser-misses-am-and-f` | claude-code | epic | archived · delivered | 3/3 stories | - |
| P2 | `capture-pipe-lesson-matcher` | claude-code | epic | archived · delivered | — | relates-to→lessons-index-cannot-drift-silently |
| P2 | `cfdude-pm-repo-governance` | claude-code | epic | archived · delivered | 3/3 children archived | - |
| P2 | └─ `branch-protection-and-pr-workflow` | claude-code | epic | archived · delivered 🤖 | — | - |
| P2 | └─ `ci-test-lint-workflow` | claude-code | epic | archived · delivered 🤖 | — | - |
| P3 | └─ `autonomous-issue-resolution-agent` | decision | epic | archived · delivered | — | - |
| P2 | `dehardcode-openspec-change-root-in-instructions` | claude-code | epic | planned | — | relates-to→gh-86-openspec-lane-silent-zero-progress |
| P2 | `df-autodetour-false-positive-conductor-admin-commits` | claude-code | epic | archived · delivered | — | - |
| P2 | `df-engine-banner-noise-every-invocation` | claude-code | epic | archived · delivered 🤖 | — | - |
| P2 | `df-github-tracker-outward-sync-unsuppressed` | claude-code | epic | archived · delivered 🤖 | — | - |
| P2 | `df-no-mechanical-openspec-gate-enforcement` | claude-code | epic | archived · delivered 🤖 | — | - |
| P2 | `df-orchestrator-committed-release-without-reverifying` | claude-code | epic | archived · delivered | — | - |
| P2 | `df-precommit-hook-too-noisy-on-success` | claude-code | epic | archived · delivered 🤖 | — | - |
| P2 | `df-project-md-diff-triviality-not-mechanically-checkable` | claude-code | epic | archived · delivered 🤖 | — | - |
| P2 | `df-skill-commands-line-drift` | claude-code | epic | archived · delivered 🤖 | — | - |
| P2 | `df-stray-render-artifacts-noise` | claude-code | epic | archived · delivered 🤖 | — | - |
| P2 | `df-update-epic-no-story-toggle-verb` | claude-code | epic | archived · delivered 🤖 | — | - |
| P2 | `df-verify-worktrees-merged-not-just-archived` | claude-code | epic | archived · delivered 🤖 | — | - |
| P2 | `docs-site-cleanup` | claude-code | epic | archived · delivered | 9/9 stories | - |
| P2 | `drift-heal-leaves-claim-on-archive` | claude-code | epic | planned | — | relates-to→comprehensive-code-review-after-0-43-0 |
| P2 | `edd-adapter-discards-agent-stdout` | claude-code | epic | archived · delivered | — | - |
| P2 | `edd-corpus-expansion-nondeterministic-surfaces` | claude-code | epic | queued | 0/3 stories | - |
| P2 | `edd-measures-installed-plugin-not-worktree` | claude-code | epic | archived · delivered | — | relates-to→gh-86-openspec-lane-silent-zero-progress |
| P2 | `edd-run-policy-when-behavior-artifacts-change` | claude-code | epic | planned | — | depends-on→edd-corpus-expansion-nondeterministic-surfaces |
| P2 | `flag-level-doc-gaps` | claude-code | epic | archived · delivered | — | relates-to→per-verb-help-from-flag-registry |
| P2 | `gh-100-link-types-no-known-set` | claude-code | epic | archived · delivered | — | relates-to→gh-70-link-validation-gap |
| P2 | `gh-102-sync-blind-to-issue-comments` | claude-code | epic | archived · delivered | — | - |
| P2 | `gh-103-tracker-refresh-gate-before-specs` | claude-code | epic | archived · delivered | — | depends-on→gh-102-sync-blind-to-issue-comments |
| P2 | `gh-105-feedback-undeclared-gh-dependency` | claude-code | epic | archived · delivered | — | - |
| P2 | `gh-106-conductor-logs-never-gitignored` | claude-code | epic | archived · delivered | — | relates-to→gh-81-project-md-never-clean |
| P2 | `gh-107-snapshot-consumes-warning-into-unread-file` | claude-code | epic | archived · delivered | — | - |
| P2 | `gh-111-activity-log-for-conductor-usage` | claude-code | epic | archived · delivered | — | - |
| P2 | `gh-114-lane-routing-blind-to-product` | claude-code | epic | queued | — | - |
| P2 | `gh-123-adopt-openspec-1-9-cli-surface` | claude-code | epic | archived · killed | — | - |
| P2 | `gh-127-dogfooding-practices-as-product` | claude-code | epic | archived · delivered | — | - |
| P2 | `gh-129-attribution-nudge-at-commit-time` | claude-code | epic | archived · delivered | — | depends-on→gh-104-commit-nudge-fires-on-text-match |
| P2 | `gh-130-disposition-has-no-correction-verb` | claude-code | epic | archived · delivered | — | - |
| P2 | `gh-131-retry-half-unverified` | claude-code | epic | archived · delivered | — | - |
| P2 | `gh-132-lessons-advisor-hook` | claude-code | epic | archived · delivered | — | - |
| P2 | `gh-133-disposition-reverts-backfill-counts` | claude-code | epic | archived · delivered | — | - |
| P2 | `gh-136-add-epic-notes-discarded` | claude-code | epic | archived · delivered | — | - |
| P2 | `gh-138-refresh-warning-counts-terminal-epics` | claude-code | epic | archived · delivered | — | relates-to→gh-103-tracker-refresh-gate-before-specs |
| P2 | `gh-148-propose-spec-association-from-header` | claude-code | epic | archived · delivered | — | relates-to→gh-92-epic-specpath |
| P2 | `gh-149-add-epic-drops-valueless-flag` | claude-code | epic | archived · delivered | — | relates-to→gh-136-add-epic-notes-discarded |
| P2 | `gh-152-flag-value-rule-outside-epic-flags` | claude-code | epic | archived · delivered | — | relates-to→gh-149-add-epic-drops-valueless-flag |
| P2 | `gh-154-openspec-validate-lifecycle-collision` | claude-code | epic | archived · delivered | — | relates-to→gh-123-adopt-openspec-1-9-cli-surface |
| P2 | `gh-159-no-reader-for-gate-guard-state` | claude-code | epic | archived · delivered | — | relates-to→gh-80-gate-guard-refusal-applies-anyway |
| P2 | `gh-161-strip-always-on-duplicated` | claude-code | epic | archived · delivered | — | relates-to→gh-115-gates-blind-to-absent-edit |
| P2 | `gh-162-delegation-drops-stdout` | claude-code | epic | archived · declined | — | relates-to→gh-156-shipped-markdown-structural-check |
| P2 | `gh-163-gate-verdicts-openspec-only` | claude-code | epic | archived · delivered | — | relates-to→gh-113-gate-verdict-records-no-diff-range |
| P2 | `gh-70-link-validation-gap` | claude-code | epic | archived · delivered | — | - |
| P2 | `gh-71-update-epic-positional-id` | claude-code | epic | archived · delivered | — | - |
| P2 | `gh-80-gate-guard-refusal-applies-anyway` | claude-code | epic | archived · delivered | — | - |
| P2 | `gh-81-project-md-never-clean` | claude-code | epic | archived · delivered | — | - |
| P2 | `gh-82-claude-project-dir-overrides-cwd` | claude-code | epic | archived · delivered | — | - |
| P2 | `gh-85-document-mutating-verbs` | claude-code | epic | archived · delivered | — | - |
| P2 | `gh-86-openspec-lane-silent-zero-progress` | claude-code | epic | archived · delivered | — | - |
| P2 | `gh-87-sync-registers-readme-as-plans` | claude-code | epic | archived · delivered | — | - |
| P2 | `gh-90-honor-output-style-contract` | claude-code | epic | archived · delivered | — | - |
| P2 | `gh-91-autodetour-false-minimal-detour` | claude-code | epic | archived · delivered | — | - |
| P2 | `gh-92-epic-specpath` | claude-code | epic | archived · delivered | — | relates-to→gh-69-sync-no-done-signal-for-plans |
| P2 | `gh-94-serial-detours-accepted-silently` | claude-code | epic | archived · delivered | — | - |
| P2 | `github-issue-tracker-sync` | claude-code | epic | archived · delivered 🤖 | — | relates-to→ai-feedback-loop-github-issues |
| P2 | `guards-that-read-engine-source-drift-silently` | claude-code | epic | later | — | relates-to→functional-assertion-test-split |
| P2 | `hooks-json-unknown-keys-warn-every-session` | claude-code | epic | archived · delivered | — | - |
| P2 | `lessons-index-cannot-drift-silently` | claude-code | epic | archived · delivered | — | relates-to→df-skill-commands-line-drift |
| P2 | `no-network-law-test-is-weak` | claude-code | epic | planned | — | relates-to→comprehensive-code-review-after-0-43-0 |
| P2 | `openspec-gate1-guard-on-apply` | claude-code | epic | planned | — | - |
| P2 | `openspec-planning-completeness-instruction` | claude-code | epic | planned | — | relates-to→openspec-gate1-guard-on-apply; supersedes→conductor-skill-orient-reads-spec-inventory; supersedes→autonomy-grant-starts-at-apply-not-propose |
| P2 | `parity-reassessment-per-platform` | claude-code | epic | planned | 0/2 stories | blocks→hermes-platform-support; supersedes→parity-propagation-skill |
| P2 | `platform-switch-orphans-old-rules-block` | claude-code | epic | queued | 0/2 stories | blocks→hermes-platform-support |
| P2 | `pm-plugin-improvements-2026-07-14` | claude-code | epic | archived · delivered | 19/19 children archived | - |
| P0 | └─ `df-plan-hierarchy-includes-archived-children` | claude-code | epic | archived · delivered | — | - |
| P0 | └─ `df-stale-cache-silent-fallback` | claude-code | epic | archived · delivered 🤖 | — | - |
| P1 | └─ `df-hierarchy-no-shared-file-conflict-detection` | claude-code | epic | archived · delivered | — | - |
| P1 | └─ `diffshape-minimal-detour-inference` | claude-code | epic | archived · delivered 🤖 | — | - |
| P1 | └─ `gate-guard-default-on-reconcile` | claude-code | epic | archived · delivered 🤖 | — | - |
| P2 | └─ `category-based-preauthorization` | claude-code | epic | archived · delivered 🤖 | — | - |
| P2 | └─ `changelog-preview-in-sessionstart-nudge` | claude-code | epic | archived · delivered 🤖 | — | - |
| P2 | └─ `dependency-aware-standalone-ordering` | claude-code | epic | archived · delivered 🤖 | — | - |
| P2 | └─ `epic-timestamps-and-staleness` | claude-code | epic | archived · delivered 🤖 | — | - |
| P2 | └─ `fix-archived-no-change-warning` | claude-code | epic | archived · delivered 🤖 | — | - |
| P2 | └─ `honcho-memory-string-print` | claude-code | epic | archived · delivered 🤖 | — | - |
| P2 | └─ `incremental-notify-timing-fix` | claude-code | epic | archived · delivered 🤖 | — | - |
| P2 | └─ `lane-routing-config` | claude-code | epic | archived · delivered 🤖 | — | - |
| P2 | └─ `per-epic-review-mode-override` | claude-code | epic | archived · delivered 🤖 | — | - |
| P2 | └─ `reconciler-structured-writeback` | claude-code | epic | archived · delivered 🤖 | — | - |
| P2 | └─ `remove-epic-verb` | claude-code | epic | archived · delivered 🤖 | 5/5 stories · 1 disposed | - |
| P2 | └─ `verify-state-command` | claude-code | epic | archived · delivered 🤖 | — | - |
| P2 | └─ `autonomy-hierarchy-adoption-investigation` | decision | epic | archived · delivered 🤖 | — | - |
| P3 | └─ `cross-repo-epic-dependency-investigation` | decision | epic | archived · delivered 🤖 | — | - |
| P2 | `release-member-moves-silently` | claude-code | epic | planned | — | relates-to→comprehensive-code-review-after-0-43-0; relates-to→gh-cfdude-pm-178 |
| P2 | `verify-state-false-hand-edit` | claude-code | epic | planned | — | relates-to→comprehensive-code-review-after-0-43-0 |
| P2 | `walk-the-undispositioned-archive` | claude-code | epic | archived · delivered | — | - |
| P2 | `epic-hierarchy-orchestration` | decision | epic | archived · delivered | — | - |
| P2 | `infra-runbook-preflight` | decision | epic | archived · abandoned | — | - |
| P2 | `gh-172-trufflehog-false-positive` | external | epic | queued | — | - |
| P3 | `registry-bound-poison-sweep-practice` | openspec | epic | planned | ⚠ tasks.md missing | relates-to→user-text-never-forges-output |
| P3 | `store-owns-claude-md-managed-block` | openspec | epic | planned | ⚠ tasks.md missing | relates-to→unit-rung-and-fixture-snapshots |
| P3 | `2026-08-03-platform-parity-mechanism` | superpowers | epic | archived · superseded | — | - |
| P3 | `2026-08-14-eval-measures-worktree` | superpowers | epic | archived · superseded | — | - |
| P3 | `archived-delivered-gate2-regression-report` | superpowers | epic | planned | — | relates-to→archive-gate-reads-what-it-writes |
| P3 | `commit-observation-gate2-minors` | superpowers | epic | planned | — | relates-to→commit-nudge-reads-the-whole-move |
| P3 | `disposition-invocation-prints-bare-no-deferrals` | superpowers | epic | planned | — | relates-to→archive-gate-reads-what-it-writes |
| P3 | `gh-cfdude-pm-179` | superpowers | epic | archived · delivered | — | relates-to→gh-165-deferral-assertion-mangled-and-immutable |
| P3 | `gh-cfdude-pm-187` | superpowers | epic | archived · delivered | — | - |
| P3 | `gh-cfdude-pm-206` | superpowers | epic | untriaged | — | - |
| P3 | `gh-cfdude-pm-207` | superpowers | epic | untriaged | — | - |
| P3 | `gh-cfdude-pm-208` | superpowers | epic | untriaged | — | - |
| P3 | `handoff-refusal-prints-story-titles-raw` | superpowers | epic | archived · superseded | — | relates-to→archive-gate-reads-what-it-writes |
| P3 | `status-write-undone-by-heal-reports-updated` | superpowers | epic | planned | — | relates-to→archive-gate-reads-what-it-writes |
| P3 | `sync-registers-ids-add-epic-refuses` | superpowers | epic | planned | — | relates-to→comprehensive-code-review-after-0-43-0 |
| P3 | `autonomy-grant-starts-at-apply-not-propose` | claude-code | epic | archived · superseded | — | - |
| P3 | `code-review-0-43-0-minors` | claude-code | epic | planned | — | relates-to→comprehensive-code-review-after-0-43-0 |
| P3 | `conductor-skill-orient-reads-spec-inventory` | claude-code | epic | archived · superseded | — | - |
| P3 | `cross-session-epic-assignment` | claude-code | epic | archived · delivered | — | depends-on→state-lost-update-concurrent-sessions |
| P3 | `df-contributing-doc-missing-direct-push-recovery` | claude-code | epic | archived · delivered | — | - |
| P3 | `epic-session-attribution` | claude-code | epic | planned | — | relates-to→cross-session-epic-assignment |
| P3 | `gh-104-commit-nudge-fires-on-text-match` | claude-code | epic | archived · delivered | — | relates-to→gh-91-autodetour-false-minimal-detour |
| P3 | `gh-88-record-where-unfinished-tasks-went` | claude-code | epic | archived · delivered | — | - |
| P3 | `gh-89-delegate-discovery-to-subagents` | claude-code | epic | archived · delivered | — | - |
| P3 | `gh-93-verify-specs-coverage` | claude-code | epic | archived · delivered | — | - |
| P3 | `hook-fixtures-couple-to-every-hook-step` | claude-code | epic | later | — | relates-to→functional-assertion-test-split |
| P3 | `init-detects-foreign-agent-instruction-files` | claude-code | epic | planned | 0/1 stories | - |
| P3 | `multi-platform-agent-support` | claude-code | epic | archived · superseded | 3/5 children archived | - |
| P1 | └─ `platform-parity-mechanism` | openspec | epic | archived · delivered | — | - |
| P1 | └─ `rules-block-hardcodes-claude-slash-commands` | claude-code | epic | archived · delivered | — | - |
| P1 | └─ `edd-harness-agent-behavior-testing` | decision | epic | archived · delivered | — | - |
| P2 | └─ `hermes-platform-support` | openspec | epic | queued ⚠ no change on disk | ⚠ tasks.md missing | depends-on→platform-parity-mechanism; depends-on→edd-harness-agent-behavior-testing; depends-on→rules-block-hardcodes-claude-slash-commands |
| P3 | └─ `codex-platform-support` | openspec | epic | queued ⚠ no change on disk | ⚠ tasks.md missing | depends-on→platform-parity-mechanism; depends-on→edd-harness-agent-behavior-testing; depends-on→rules-block-hardcodes-claude-slash-commands; depends-on→hermes-platform-support |
| P3 | `parity-ledger-exemptions` | claude-code | epic | planned | 0/1 stories | - |
| P3 | `parity-propagation-skill` | claude-code | epic | archived · superseded | 0/1 stories | - |
| P3 | `remaining-platform-investigation` | claude-code | epic | planned | — | supersedes→multi-platform-agent-support |
| P3 | `rules-block-cli-fallback` | claude-code | epic | planned | 0/1 stories | depends-on→remaining-platform-investigation |
| P3 | `triage-ignores-non-latin-text` | claude-code | epic | planned | — | relates-to→comprehensive-code-review-after-0-43-0 |
| P3 | `portfolio-architecture-consistency-scan` | decision | epic | archived · delivered | — | depends-on→epic-hierarchy-orchestration |
| P? | `record-answers-for-itself` | openspec | epic | archived · delivered | — | - |
| P? | `2026-07-14-epic-hierarchy-orchestration` | superpowers | epic | archived · superseded | 0/17 tasks | - |
| P? | `2026-07-21-conductor-mjs-module-split` | superpowers | epic | archived · superseded | 0/99 tasks | - |
| P? | `2026-07-26-edd-harness-agent-behavior-testing` | superpowers | epic | archived · superseded | 0/37 tasks | - |
| P? | `2026-07-29-platform-aware-rules-block` | superpowers | epic | archived · superseded | 0/34 tasks | - |
| P? | `2026-08-18-state-write-conflict-guard` | superpowers | epic | archived · superseded | 0/39 tasks | - |
| P? | `alpha` | claude-code | epic | queued | — | - |
| P? | `from-caller` | claude-code | epic | queued | — | - |

## Dependency warnings

> An epic cannot be started until what it waits on moves. Pull the dependency forward, or descope the epic waiting on it.

- ⚠ `rules-block-cli-fallback` (P3) depends on `remaining-platform-investigation` (P3, planned) — the dependency is not queued

## Backlog

> Registered for later, with the rationale that justified registering it.

- `backlog-grooming-pass` (P1, openspec, planned) — Nothing ever re-reads the backlog as a SET after intake. /pm:triage runs once, at registration; after that no surface asks whether an epic still matters, duplicates another, or was already delivered under a different name. The sprint-planning role is unfilled: nobody prunes, dedupes, merges a P2's context into the P0 that supersedes it, or catches an epic whose premise died. Consolidation must record a disposition (--outcome superseded --reason plus a supersedes link), never delete — deletion removes the record of projected work, which is what a disposition exists to preserve. Measured 2026-09-06: all three newly-synced issues overlapped archived epics, and one was a straight re-report of work recorded delivered.
- `hierarchy-run-has-one-state-writer` (P2, openspec, planned) — A hierarchy run has one writer of state.json: children report attribution and gate evidence, the orchestrator records them after merge — From 0.43.0 review E1 and 0.45.0 Gate 1 (emitted-commands lens B). SKILL.md's orchestrator section says children never write state.json while agents/hierarchy-child-executor.md has children record gates and archive; ROOT = CLAUDE_PROJECT_DIR||cwd so parallel children write the main checkout's state concurrently. Open design questions: the managed rules block orders every agent (children included) to attribute each commit; the tasks.md a child is handed carries record-gate-review, attribution and archive tasks; the orchestrator must attribute only the child branch's commits, never a merge or conflict-resolution commit (a merge commit as last entry makes the reviewed headSha stale); evidence carried in the child's DONE prose can be shortened by a terse contract, so Reporting item 1 must cover it; the child's context is discarded after it reports.
- `add-many-drops-input-silently` (P2, superpowers, planned) — add-many silently drops string links, stores target-less and dangling links, and ignores unknown top-level keys — Code review 0.43.0 (B1, B2). add-many.mjs/links.mjs mergeLinks skips non-objects: links ["depends-on:base"] or "depends-on:base" vanish with exit 0; {type:'depends-on',epic:'ghost'} is stored dangling; {type:'blocks'} with no target is stored and cannot render; {parent,…,"epic":[…]} creates only the parent. Spec: a bulk write persists what it accepts or rejects by name. Fix: validate top-level keys and route links through parseLinkFlags-equivalent validation against existing plus batch ids.
- `changelog-page-too-long-archive-older-releases` (P2, superpowers, planned) — Mintlify changelog is 1198 lines / 40 releases on one page: keep ~10 recent in full, index the rest, archive behind evenly-sized pages (NOT calendar months — July alone is 31 of 40), and make the roll-forward mechanical so it cannot drift
- `commit-gate-tests-working-tree-not-index` (P2, superpowers, planned) — The pre-commit suite gate tests the working tree, not the index, and its 'fewer tests than declared' guard cannot fire; CI has no count guard — Code review 0.43.0 (E2). .githooks/pre-commit runs node --test against the checkout: staging a failing test and restoring a passing copy unstaged commits green, and HEAD holds the failing test — required item 2 (the commit is the unit of verification) applied to the suite. The declared count uses the same glob as the runner, so renaming a test out of the glob drops it from both (repro: 1/1 passing with 5 tests present); conductor-09's test for the guard asserts status 0, and mutants flipping the comparison survive. ci.yml has no count guard and tests Node 18 only. Fix: run against the index (stash --keep-index or a worktree of the index), count declared tests across scripts/test/** by content, a fixture that must abort, and the same guard in CI.
- `fleet-upgrade-after-release` (P2, superpowers, planned) — A release reaches a repo only when /pm:upgrade runs there, and nothing tells you which repos are behind. Measured 2026-09-06: 26 pm-managed repos on one machine across three roots, 23 of them stale — the oldest by 35 releases. pm should be able to answer 'which repos on this machine are behind' and emit the per-repo upgrade recipe, without the engine ever leaving the instruction layer (same shape as the tracker sections that tell the agent to run gh). The manual process, its traps and its runner now live in ~/.claude/skills/pm-fleet-upgrade/ — repo-maintenance tooling that every multi-repo pm user is reinventing.
- `handoff-demand-blind-spots` (P2, superpowers, planned) — The handoff demand reads 0/0 once /opsx:archive moves tasks.md, and adding one inline story hides every unticked tasks.md item — Code review 0.43.0 (A1, A2). (1) epic-progress.mjs epicProgress: after the change moves under archive/, tasks.md is not found and archive-as-delivered is accepted with 2 of 3 tasks unticked; archivedTasksPath() exists but is used only for backfilled epics (the gate-integrity spec currently permits reading zero — a spec-level check that cannot fail). (2) Array.isArray(epic.stories) is preferred over the tasks source: tasks.md at 1/3, `--add-story x`, `--story 1 --done --status archived --outcome delivered` archives with PROJECT.md showing 1/1. Fix: read the archived tasks.md when the live one is gone (amend the spec); count tasks and stories together or refuse to switch source.
- `hooks-not-silent-before-init` (P2, superpowers, planned) — Hook verbs print detached-checkout and root-divergence warnings in repos pm never initialised, and nothing tests gate-guard or snapshot dormancy — Code review 0.43.0 (C1, E2). conductor.mjs warnRootDivergence / warnDetachedTree run before any isInitialized() check: snapshot (PreCompact) in a non-pm detached repo prints a 4-line 'about to write .conductor/brief.txt' and writes nothing; commit-nudge prints 'WRITING A DIFFERENT REPOSITORY' when CLAUDE_PROJECT_DIR is a non-pm repo. Breaks hooks/README.md 'silent until /pm:init'. Tests: mutating gate-guard's dormancy return to exit 2 (blocks Edit in every repo on the machine) survives 176 tests; removing snapshot's guard survives 131. Fix: skip both warnings when !isInitialized(); add dormancy tests for gate-guard, snapshot and brief like the existing commit-nudge/write-rules/lesson-advice ones.
- `lesson-detect-matcher-hardening` (P2, superpowers, planned) — lesson-advice: a catastrophic regex hangs every Bash/Edit call ~60s, a typo'd detect key matches every tool call, CRLF frontmatter is silently inert — Code review 0.43.0 (C1, C2). lessons.mjs matchLessons compiles commandMatches with no complexity or input bound: detect {tool:Bash, commandMatches:'^(a+)+$'} on a 31-char command burned 60.4s CPU in the hook, on every Bash/Edit/Write call. An unknown key applies no predicate: {tool:Bash, commandMatch:'gh pr merge'} matched `ls`. CRLF frontmatter (/^---\n/), a tool array and a bad regex are silently inert. Fix: allowlist detect keys, reject nested quantifiers or cap the matched text, accept CRLF, report rejects outside the hook.
- `mutation-pass-is-a-required-task-item` (P2, superpowers, planned) — Required task items demand a call-site sweep and an inverse enumeration, and neither asks whether a new guard CAN fail — make the mutation pass a numbered required item with a durable artifact — Evidence from operations-ship-their-inverses (0.46.0). The change adopted a practice its task list never asked for: after shipping drop-detour, every guard it added was NEUTERED in turn and the suite re-run, with the whole batch saved as red-3.5-mutation-evidence.txt in the change directory. Six mutants; five died; ONE SURVIVED AND WAS A REAL DEFECT — removing drop-detour's own reconcileNeeded recompute left all 17 tests green, because render() runs after the save and the drift heal cleared the flag one write later, which is exactly the intermediate state gate-integrity says the engine cannot produce. Every assertion read the final value, so nothing could tell the two mechanisms apart. The fix was to assert on the drop's OWN stderr. Nothing in the seven required task items would have found it: item 1 enumerates call sites and inverses of the thing being guarded, item 2 verifies the commit, and both gates read a diff. A test that cannot go red is invisible to all of them. Proposal: a numbered required task item — 'prove every guard this change adds can fail' — carrying the batch form (mutate, run, record, restore), the artifact convention (a named file in the change directory, so the gate can read it rather than trust a sentence), and the rule that a SURVIVING mutant is a finding to be explained, not a redundant test to delete. Precedent for the form: docs/lessons/a-guard-can-check-the-wrong-half already states the habit and has no mechanism; measured here, the habit found a defect the whole gate stack missed.
- `suite-twin-and-certification-discipline` (P2, superpowers, later) — The twin-link and certification discipline: a functional test has an assertion twin, and a triggered run is certified by CONTENT — PRACTICE ROUTED BY functional-assertion-test-split (0.47.0), required task item 7. The two disciplines that change earned, with the evidence that made them necessary. (1) TWIN LINK, one direction: every functional id has an assertion file of the same id, enforced by scripts/test/drift.mjs in the pre-commit hook. Evidence: the functional half is the half that can go months unrun, and before 0.47.0 nothing connected the two halves at all -- a change could loosen a functional test and the per-commit gate would never learn it. The 44 twins written by 5.3 are what makes the fast half carry the slow half's behaviour. (2) CERTIFICATION BY CONTENT: a triggered run writes a record under $(git rev-parse --git-common-dir) keyed by contentHash per module, and the hook refuses a certified module's changed content until a fresh run exists. Evidence: docs/lessons/a-one-off-sweep-certifies-only-the-day-it-ran.md -- 0.44.0's one-off sweep checked argv shape once, was archived with its change, and the NEXT release found four remedies the engine printed and then refused. The record is the mechanism that lesson lacked: it is a claim about a pass over NAMED CONTENT, checked on every commit, and it goes stale the moment the content moves rather than the moment the change archives. Cost of the check: the drift script spawns nothing but four git index reads and runs in under a second, so the per-commit gate pays nothing for it. Candidate for promotion into the pm-fleet discipline if a second repo adopts the split.
- `tracker-item-dedup-bypassed` (P2, superpowers, planned) — The one-epic-per-tracker-item guard runs only on add-epic --external-id; --external-url alone, update-epic and add-many skip it — Code review 0.43.0 (B2, E1). add-epic.mjs EPIC_DEDUP_KEYS check sits inside `if (externalId !== undefined)`. Repro: five epics with the same issue URL, each command exit 0. Also: the add-epic duplicate message names external-id when the collision was the URL. Fix: one shared dedup check (URL-keyed) called by add-epic, update-epic and add-many.
- `activity-log-detour-events-lose-epic` (P2, claude-code, planned) — Activity log records epic:null for every detour push/pop — frameEpic reads keys the stack never writes, and the test uses a fake frame — Code review 0.43.0 (C2). activity-log.mjs frameEpic reads f.epic||f.epicId||f.id; detour-stack.mjs writes {pausedEpic, spawnedDetour}. The report's DETOURS byEpic is always empty and activity --epic drops detours. conductor-33.test.mjs asserts against {epic:'e1'}. record-reconcile, set-autonomy, priority change and set-review-mode log only as bare state-write. Fix: read pausedEpic, build the fixture with push-detour, emit named events for those verbs.
- `dehardcode-openspec-change-root-in-instructions` (P2, claude-code, planned) — Instruction text hardcodes openspec/changes/<id>/ in 4 locations across 3 files — agents/reconciler.md:20, agents/reconciler.md:22, skills/conductor/SKILL.md:328, commands/status.md:19. Phrase as 'the change root as OpenSpec resolves it'. NOTE: the page's original citation of scripts/lib/rules.mjs:129 is WRONG at 0.25.0 (that line holds no path literal) — do not edit it
- `drift-heal-leaves-claim-on-archive` (P2, claude-code, planned) — The archive-drift heal archives an epic but leaves its claim, and integrity then blames a hand-edit — Code review 0.43.0 (B2). update-epic clears epic.claim on archive; epic-progress.mjs reconcileArchived (how /opsx:archive epics are usually archived) does not, and update-epic's comment listing removal sites omits the heal. Integrity then reports 'predates that rule or was hand-edited', which is false. Fix: clear the claim in the heal; add the heal to the comment's list.
- `edd-run-policy-when-behavior-artifacts-change` (P2, claude-code, planned) — Practice: run the EDD corpus when a change touches an artifact the AGENT reads (commands/agents/skills/hooks/.claude-plugin + rules.mjs), skip it for engine-internal/test-only changes; document in CLAUDE.md and consider a mechanical reminder
- `guards-that-read-engine-source-drift-silently` (P2, claude-code, later) — Source-reading guards drift silently when the engine's shape moves, and a comment can satisfy one — FRICTION ROUTED BY functional-assertion-test-split (0.47.0), required task item 7. File with /pm:feedback as well: this is a product observation about pm's own test suite, not just this repo's history. EVIDENCE, all measured in 5.6 by mutating a copy of the repository. (1) A guard a COMMENT could satisfy: scripts/test/assert/conductor-13.test.mjs asserted /archiveGate\(/ against the RAW source of lib/update-epic.mjs, and the verb's own prose calls the gate by name -- 'one level down from archiveGate()'s refusal ...' at :561. Aliasing the import and its call site away left the guard GREEN, so the call it claims to watch could be deleted with nothing to say so. Repaired by dropping comment-only lines before matching; the repair is mutation-verified (20/20 unmutated, 19/1 mutated). (2) The repair is not mechanical to make: the FIRST attempt used the same character-level stripper assert-half-has-no-spawn.test.mjs carries, and it DESYNCS on a regex literal holding a quote -- every comment after that point passes through VERBATIM, a false NEGATIVE, which is the opposite of the direction that helper's own docstring claims is the only one it can fail in. (3) 5.6's list is itself evidence of a class: conductor-25's dispatch readers, verb-surface's dispatch-table regex, save-report-surface's call-site scan, conductor-13, emitted-invocations, output-interpolations and conductor-12's cache-busting imports ALL read engine source and ALL had to be re-pointed by hand as 0.47.0 moved the engine's shape (dispatch table indented into main(), frozen constants becoming functions). A product opportunity: a shared, sound source-stripping helper in the test tree, and a lint that flags an assert.match against raw engine source.
- `no-network-law-test-is-weak` (P2, claude-code, planned) — The instruction-layer no-network test misses https/net/tls imports and spawning gh or curl — Code review 0.43.0 (E2). conductor-35.test.mjs scans for fetch(, node:https? and require('https'). Mutants adding `import https from "https"; https.get(…)` or `import net from "node:net"; net.connect(443)` to a lib file survive conductor-35 and -39; node:tls, dgram, http2 and execFile('gh'|'curl') are unchecked. It is the only mechanical guard on a hard architectural law. Fix: allowlist import specifiers (node: built-ins minus net/http/https/http2/tls/dns/dgram, plus relative) and an argv[0] allowlist for spawn/exec.
- `openspec-gate1-guard-on-apply` (P2, claude-code, planned) — Gate 1 is RECORDED but never GUARDED: record-gate-review writes epic.gateReview.gate1, yet nothing blocks an openspec epic entering implementation without it. The archived df-no-mechanical-openspec-gate-enforcement epic shipped only the ARCHIVE/gate2 guard (update-epic.mjs:107) and update-epic.mjs:104 deliberately exempts gate1 from archive — so the guard belongs on the transition INTO apply, not archive
- `openspec-planning-completeness-instruction` (P2, claude-code, planned) — Before treating an openspec epic as ready to apply, pm's rules must direct the agent to confirm the change's planning is complete — stating the OBLIGATION and naming NO command (resolves the page's Tier 3 #2 vs #4 contradiction: #2 proposed baking openspec status --json / isPlanningComplete / applyRequires into pm, which is the 1.9.0-specific coupling #4 forbids and that carried pm through the upgrade unharmed)
- `parity-reassessment-per-platform` (P2, claude-code, planned) — RECURRING: on every new platform, re-run the corpus, reassess ledger exemptions, and compare drift
- `release-member-moves-silently` (P2, claude-code, planned) — release --member silently moves an epic out of another release with no amendment; re-running --defer overwrites the recorded reason — Code review 0.43.0 (A1, A2). releases.mjs member loop overwrites knownEpic(id).release and records nothing on the old release (the --unmember guard's own comment names this harm); `release show r1` then shows 0 members, no amendment, no stderr. Re-running --defer replaces 'depends on X landing' without keeping the prior reason. Fix: refuse or record an unmember amendment on the old release and say so; keep deferral reason history.
- `verify-state-false-hand-edit` (P2, claude-code, planned) — verify-state reports an 'undetected hand-edit' after ordinary engine verbs that save without rendering — Code review 0.43.0 (C2). worktree-hygiene.mjs verifyState compares state.json mtime to the render stamp: `init` → exit 0; `set-activity-log on` → exit 1 claiming a hand-edit; claims and platform recording trip it too. The one hand-edit detector accuses the engine. Fix: compare the state revision recorded in the stamp, not mtime.
- `registry-bound-poison-sweep-practice` (P3, openspec, planned) — Registry-bound poison sweep as a shipped practice: a completeness assertion over every value-bearing flag and non-argv source, so a new value cannot be added without a hostile-input recipe
- `store-owns-claude-md-managed-block` (P3, openspec, planned) — 0.49.0 candidate: make the managed rules block a store-owned artifact (removes the last fsync wall in the assertion half's file rung)
- `archived-delivered-gate2-regression-report` (P3, superpowers, planned) — No integrity check names an archived delivered epic that fails a delivered obligation (Gate 2 not a covering pass, or outstanding work with no handoff) reached by paths no gate sees: record-gate-review recording fail after archive, the heal re-archiving a changed epic with its kept delivered disposition, remove-epic stripping disposition.carriedTo, and disk-side task changes
- `commit-observation-gate2-minors` (P3, superpowers, planned) — commit-nudge observation: Gate 2 minors deferred from 0.45.0 — no cap on commits walked or output size, a partial last reflog line can become the anchor, a retraction row names only the first epic, the detached-tree finish() path is untested, prefix-of-prefix pruned rows retract together
- `disposition-invocation-prints-bare-no-deferrals` (P3, superpowers, planned) — dispositionInvocation() (used by unconsideredOutcomes and the integrity check for engine-stamped unknown outcomes) prints a bare --no-deferrals, suggesting a claim the agent has not made — CLAUDE.md required item 6 says --no-deferrals is a claim, not a default; print a deferral placeholder instead
- `status-write-undone-by-heal-reports-updated` (P3, superpowers, planned) — update-epic --status <non-archived> on an epic whose change directory is archived on disk prints 'updated' while render()'s heal immediately restores archived — a false-success report on the write surface
- `sync-registers-ids-add-epic-refuses` (P3, superpowers, planned) — sync and the archive backfill register ids add-epic refuses, and sync silently archives an active epic on a name collision — Code review 0.43.0 (D1). subcommands.mjs sync/backfillArchive register 'Add Auth', 'x|y', '.hidden', 'My Plan.md' (the id format is enforced only in add-epic); x|y breaks the Epics table and spaces break every emitted update-epic line. An active claude-code epic add-auth is archived with outcome unknown and the active pointer cleared because an unrelated archive/2025-01-01-add-auth exists; sync prints 'synced (0 new…)'. Fix: one id validator shared by all registration paths; the heal matches by recorded spec path, not bare name.
- `code-review-0-43-0-minors` (P3, claude-code, planned) — Minor findings from the 0.43.0 comprehensive code review (batch) — Code review 0.43.0 minors, each reproduced or read by a reviewer: priority never validated (banana stored) in add-epic/update-epic/add-many; saveHookHeal records two conflict entries per skipped hook write (spec says one); worktree-hygiene.mjs isAncestorOfCurrentHead interpolates a sha into execSync (switch to execFileSync); --force described as working on every verb but refused by add-epic/update-epic/claim allowlists and unmentioned in the conflict message; state.json.tmp-* left behind on ENOSPC and not gitignored; remove-epic crashes on an untitled descendant (e.title.slice); record-tracker-refresh accepts a non-date or backwards watermark; handoff refusal wording '2 of 1/3 task(s)'; conductor-09.test.mjs:24 reconcileNeeded assertion cannot fail (the heal clears it); changelog --since garbage dumps everything; untested guards surviving mutation: destructuring outcome read escapes the conductor-13 scan, 0.3.0 and 0.32.0 migration never-overwrite guards, pop-detour's archived-epic refusal, linkOnce dedupe; remedy texts naming refused commands at archive-gate.mjs (record-gate-review without range flags) and integrity delivered-release-epic-left-open (sibling of #189).
- `epic-session-attribution` (P3, claude-code, planned) — Record which session did the work on an epic (opaque agent-supplied identity, append-only trail)
- `hook-fixtures-couple-to-every-hook-step` (P3, claude-code, later) — A fixture that copies the real pre-commit hook breaks on every new hook step — FRICTION ROUTED BY functional-assertion-test-split (0.47.0), required task item 7. File with /pm:feedback as well. EVIDENCE: runHookAgainstFixture (scripts/test/fixtures/helpers.mjs) builds a scratch git repository, copies the REAL .githooks/pre-commit into it, and runs it -- deliberately, so the hook is exercised rather than a stub. 6.4 added a step to that hook (the drift script), and the three fixtures that run it went red for a reason that had nothing to do with what they test: the fixture held a hook but not the script, and the script but not the minimum tree it reads (scripts/lib/, conductor.mjs, the two bucket directories). The fix was to have the fixture construct that skeleton -- which means every future hook step has to be mirrored into a fixture builder, and nothing enforces the mirror. Measured cost: half an hour of diagnosis for a one-line hook insertion, and the failure mode is the worst kind -- three unrelated tests red, all pointing at the hook rather than at themselves. Candidate fix: derive the fixture tree from the hook's own invocations, or give the hook a single well-known entry point so a fixture can stub it by contract.
- `init-detects-foreign-agent-instruction-files` (P3, claude-code, planned) — pm init should detect a pre-existing AGENTS.md / sol.md from a prior agent and read it before initializing
- `parity-ledger-exemptions` (P3, claude-code, planned) — Ledger exemptions (capability genuinely unsupportable on a platform), designed against a real case
- `remaining-platform-investigation` (P3, claude-code, planned) — Scope the three platforms nobody has investigated hands-on: Gemini CLI, Grok Build, and generic AGENTS.md consumers. Codex and Hermes came out of the original multi-platform sweep with hands-on CLI verification (Codex CLI 0.145.0, Hermes v0.19.0) and are separately registered; README.md:207-208 now reads 'Rules block only' for both. These three are still 'Planned' with no investigation behind them, and README.md:205-212 still points all six rows at the superseded umbrella epic — which is wrong for four of them. Deliverable is the same shape the Codex and Hermes work produced: install the CLI, find where it reads project instructions, and decide whether pm's rules block can land there at all before scoping any port.
- `rules-block-cli-fallback` (P3, claude-code, planned) — rules block: CLI fallback for any platform lacking slash commands or hitting a namespace collision
- `triage-ignores-non-latin-text` (P3, claude-code, planned) — triage finds no candidates for non-Latin text — tokenize splits on [^a-z0-9] — Code review 0.43.0 (D1). triage.mjs tokenize: an epic titled in Cyrillic plus the identical ask returns candidates: [], indistinguishable from 'no overlap' at intake's mandatory first step. Fix: a Unicode-aware split (\p{L}\p{N}), or report that zero tokens were extracted.

## Releases

> What each release is for, what is in it, and what was deliberately cut — with the reason.

- `0.27.0`: 21 epics, 4 deferred · cross-spec pass (6 specs) · two independent fresh-context lenses, 2026-08-23 — RECORDED RETROACTIVELY: the review ran by hand before /opsx:apply and returned 5 Critical + 10 Important; all Critical closed and re-reviewed twice, the second round finding 2 more introduced by the fixes. The verb did not exist at the time, so this date is the write date, not the review date — conductor tells the truth — stop the record over-reporting completion: reason-bearing dispositions at four scopes, an archive gate that binds every path, gate verdicts with checkable evidence, tracker direction as explicit configuration, read-only integrity checks, and the gate procedure pm emits as required task items
  - deferred `gh-114-lane-routing-blind-to-product` — Only the call-site half ships in 0.27.0 — mirrored items are routed through suggest-lane instead of a hardcoded --lane claude-code. The primary complaint, that the routing DECISION weighs no product or milestone context, needs a layer this release does not build (design.md 'Not in this release'), so #114 stays open and is not closed when this change archives. (2026-08-25)
  - deferred `gh-66-update-epic-missing-flags` — Partially addressed: update-epic gains --lane and --plan, and the valueless-flag refusals land. The remainder the issue raises — notably its --link complaint — is covered only to the extent epic-annotation's delta spec covers it, so the rest is deferred and #66 stays open when this change archives. (2026-08-25)
  - deferred `gh-64-sync-duplicate-shipped-plan` — Deliberately out of 0.27.0. It is the cause of all 4 dual-lane pairs the new integrity check reports on day one here; the check reports the pair read-only and asserts nothing about intent. A 'deliberate' field would be a fifth disposition scope invented with no motivating case, so #64 remains the place that resolves it. (2026-08-25)
  - deferred `gh-69-sync-no-done-signal-for-plans` — Deliberately out of 0.27.0 — the same known bug as #64 and the other half of the 4 dual-lane pairs the integrity check reports. Excluded for the same reason: this release reports the shape read-only rather than inventing a field to encode intent. (2026-08-25)
- `0.37.0`: 2 epics, 0 deferred — Help that answers the question actually asked: per-verb --help projected from the flag registry, closed enums naming their legal values, and a docs pointer every managed repo receives on its next upgrade (target: 0.37.0)
- `0.38.0`: 3 epics, 0 deferred — The record says what actually happened: the deferral separator stops guessing, a deferral flag outside an archive refuses instead of reporting a phantom write, set-gate-guard reads back, gate verdicts record on any lane, and an attribution can be withdrawn (target: 0.38.0)
- `0.39.0`: 4 epics, 0 deferred — Things that go missing without anything noticing: a shipped file that lost its tail, an agent transcript thrown away, a duplicated helper whose sibling went stale, and a reported stdout loss that did not exist (target: 0.39.0)
- `0.40.0`: 6 epics, 2 deferred · cross-spec pass (5 specs) · re-review after amendment (thorough): the first verdict went stale when 65efef2 amended conductor-record (the shallow-boundary rule) and gate-integrity (the priority-propagation direction). A fresh-context reviewer re-read all five specs against each other AND against the shipped engine, since these specs are now implemented and a spec amended after the code ships can describe behaviour the code does not have. Both amendments verified correct against created-at.mjs:57-75 and dependency-order.mjs:79-99. Returned 1 BLOCK and 5 POLISH. The BLOCK was two specs asserting different sizes for one measured population — gate-integrity said 68 archived epics where epic-disposition and the proposal say 144 — and, underneath it, a conflation: '3 with a passing Gate 2' is the count the MIGRATION stamped delivered, while 9 archived epics carry a passing Gate 2. Fixed by re-measuring once (149 archived, 9 passing Gate 2, 3 migration-derived delivered, all 2026-09-08) and restating the requirement on the relative claim it actually rests on. Three POLISH fixed: the saveState call-site count (69, actually 31) in state-write-guard and design; a scenario whose absolutism a multi-migration upgrade falsifies; and two of four dual-lane collisions named as openspec when they are decision-lane. Two declined with reasons: a live count that drifted 4 to 5 and strengthens its own point, and a chokepoint documented in design and guarded by conductor-08 that does not belong in a capability spec. openspec validate --strict passes after the edits. — The record answers questions about itself. Five defects, one root: .conductor/state.json cannot answer questions about its own contents. Epics gain a registration date and a last-touched date; an epic in a status the engine does not define is reported; the archive can be asked which of its records carry no considered outcome, and an outcome can be recorded as genuinely unreconstructable; every declared-nullable field can be unset and --link appends instead of replacing; and the emitted call-site sweep obliges the inverse operation. Measured across 27 distinct upstreams: 310 archived epics carry outcome unknown and 26 sit in status 'done', a value KNOWN_STATUSES does not contain.
  - deferred `gh-cfdude-pm-167` — sync --dry-run, cut at Gate 1 on a reviewer's recommendation. It was the rider on a release with two spines, and its one real complication was unaddressed: sync calls render() at scripts/lib/subcommands.mjs:648, so the spec's byte-identical-state-file clause would still have rewritten PROJECT.md and .conductor/render-stamp.json. Its own change must also declare the dry-run variant in verb-effects.mjs:73 and cover the archive backfill without consuming the archiveBackfilledAt marker. (2026-09-08)
  - deferred `backlog-grooming-pass` — Depends on the clock this release introduces. Specifying a recurring staleness pass against a field that does not exist yet is how a spec gets written from assumption — the exact failure this release exists to reduce. Follows in 0.41.0 once createdAt and touchedAt are real. (2026-09-08)
- `0.41.0`: 12 epics, 1 deferred — Backlog health and the papercuts 0.40.0's own closeout surfaced: the replay test that fails during every release, the flag parser that silently drops synced issues, the release object that cannot be read back, three deferral flag shapes, a createdAt with no inverse, and hooks.json validating clean. Batched deliberately — 13 releases in 11 days was a 28-repo fleet pass every 20 hours. (target: 0.41.0)
  - deferred `backlog-grooming-pass` — A manual grooming pass WAS performed and its results are recorded — 38 epics read as a set, 3 consolidations, 2 reprioritizations, 2 rescopes, 4 stale README rows repointed, and a lesson about line-number citations rotting. But this epic asks for the CAPABILITY: a repeatable procedure so the next pass is not manual. That is unbuilt, it is an openspec-lane change owing a spec and a Gate 2, and the archive gate correctly refused to let one manual instance stand in for it. Same distinction 0.40.0 learned in the other direction — shipping the walker was not walking it, and here walking it is not shipping the walker. (2026-09-10)
- `0.42.0`: 3 epics, 1 deferred · ⚠ no cross-spec review (2 specs) — The record stops writing where nobody is working, and the backlog items 0.41.0's grooming rescoped. Starts with #175 — pm reads a deployed detached checkout as a workspace because state.json is tracked, so breadcrumbs land in production trees and an upgrade there is discarded by the next deploy, both silently. (target: 0.42.0)
  - deferred `gh-cfdude-pm-192` — Registered during this release's own closeout and not implemented. It is a real gap — a gate verdict has no withdrawal, so one recorded on the wrong epic is permanent — but shipping a new flag unreviewed past a passing Gate 2 is exactly what this project refuses. Follows in the next release with its own gates. (2026-09-12)
- `0.43.0`: 2 epics, 0 deferred · cross-spec pass (2 specs) · two fresh-context lenses (coherence/contradiction; falsifiability/dependency-order), initial + 3 re-rounds; round-3 BLOCKS fixed d046e02; remaining round-3 items adjudicated POLISH (rendering detail, no contradiction) and trimmed from the spec 2b971c9, 2026-09-14 — Gate verdicts get the inverse every other correctable record has, and the shipped withdraw-commit bypass closes with it. Starts with #192. (target: 0.43.0)
- `0.44.0`: 3 epics, 0 deferred · cross-spec pass (7 specs) · two fresh-context lenses (coherence/contradiction; falsifiability/dependency order over apply order argv → state-file → gates): round 1 fail (4 BLOCKS: link-repair contradiction, --force vs persist-or-refuse, 6.10 missed DISPATCH_BASELINE, 6.1–6.4 lacked --verdict); scoped re-review fail (6.9a/6.9b lacked --verdict) fixed and grep-verified; 7 spec files; 2026-09-15 — Fix every Critical from the 0.43.0 comprehensive code review: data-loss paths in the state file and argv handling, and gate bypasses. (target: 0.44.0)
- `0.45.0`: 3 epics, 0 deferred · cross-spec pass (10 specs) · fresh-context lenses, rounds 1-9: r1-r6 as previously recorded (pass at r6); Gate 2 fixes then amended emitted-commands specs, so r7 fail (3 BLOCKS), r8 fail (2 BLOCKS: missing gate-integrity delta, control-character repo wording), r9 narrow pass (delta header and all 17 scenarios verified, both changes validate --strict); 2026-09-17 — Important findings from the 0.43.0 review, first batch: the commit nudge, commands pm emits, and user text in pm's output. Cluster 4 (missing inverses) and #167/#168/#176 are 0.46.0. (target: 0.45.0)
- `0.46.0`: 6 epics, 0 deferred · cross-spec pass (7 specs) · two fresh-context lenses over both changes' spec sets, combined with Gate 1 fix confirmation: round 1 lens A pass (5 POLISH), lens B fail (2 BLOCKS: an engine-invocation exemption that could not fail, a multi-digit fd redirect evading the shape minimum); fixed; narrow confirmation round pass — MODIFIED delta fidelity diffed against the main spec, 117-case regex diff, mutation proofs both ways, drop-detour reachability verified on 0.45.0; 2026-09-17 ⚠ stale — Cluster 4 of the 0.43.0 review: every operation ships its inverse, every stored reference is checked, and the gate guard covers every write path
- `0.47.0`: 0 epics, 0 deferred · cross-spec pass (2 specs) · INLINE review by the applying agent, NOT fresh-context lenses — this run's discipline forbids subagent dispatch, and that limitation is recorded here rather than papered over. Six questions over the release's two spec files (engine-invocation, suite-certification): (1) contradiction — none; the near-miss (one-process assertion half vs two-independent-invocations) is mutual dependency, not conflict. (2) double ownership — none; conductor.mjs's covers referencing the conformance set is a reference, and engine-invocation explicitly declines to restate verb-surface's refusal classes. (3) unmeetable — none in the specs. (4) gaps against proposal What-Changes — one: ci.yml's non-descending syntax loop has no requirement; declined as POLISH. (5) vocabulary forks — two, change-internal: "double" vs "fake", "change-triggered bucket" vs "sweeps bucket"; declined, renaming churns a passed Gate 1 contract for no behaviour change. (6) shared chokepoint — the single-process assertion half with injected streams is load-bearing for BOTH specs; guarded on each side (1.2 exits, 3.5 roots, 5.2 spawns). FINDING raised against the TASK LIST, not the specs: git.mjs's resolvedCommitCache is keyed by VALUE ONLY (unreachedCache by head+commits), so two invocations in one process against two roots share it and a hex abbreviation resolving in one repository and not the other gets the first repository's answer — engine-invocation forbids observing another invocation's values, and no task keys those caches; handled in the gateway work (4.2) rather than by amending a spec that is already correct. Verdict pass: no BLOCK found in either spec file; one BLOCK-level finding against tasks.md, acted on. 2026-09-20 — Split the test suite into a per-commit in-process assertion half and a triggered functional half: every git call behind one injected gateway, every refusal a caught value, every functional result certified by content hash.
- `0.48.0`: 2 epics, 0 deferred · cross-spec pass (4 specs) · inline foreground cross-spec review of the 0.48.0 spec set — no fresh-context subagents were available in this run, so the reviewer is the applying agent, which read both of this change's deltas in full and answered the six questions. NOTE ON THE ENUMERATED SET: the engine enumerated FOUR specs, not two — the extra pair is functional-assertion-test-split/specs/{suite-certification,engine-invocation}/spec.md, the stale LIVE duplicate of the tree ab171b7 archived (task 0.4), which the release-scope enumeration reaches because the live directory was copied rather than moved. Those two are byte-identical to the archived originals and are the same two capabilities this change deltas, so the review covers them; the duplicate is filed separately under 0.4 rather than repaired here. BLOCKS (fixed in this commit): engine-invocation's 'the invocation that used the in-memory store created no file' scenario was unreachable — init() writes CLAUDE.md (rules.mjs:1097) and .gitignore through raw fs, both deliberately outside the store per design D1, so the scenario was narrowed to the store's own footprint and an explicit boundary paragraph was added. CONTRADICTION: none. suite-certification's rungs and engine-invocation's per-call store agree; 'both rungs run in one invocation of one process' does not collide with 'two invocations in one process are independent' — different claims about different things, and no scenario still speaks of the assertion half as one on-disk directory (the modified requirement and all four of its scenarios name the rungs). DOUBLE OWNERSHIP: none. engine-invocation owns the caller's contract and now states the store's boundary; suite-certification owns rung membership and names the store only as the mechanism a unit test obtains a value through, which is the split task 0.2 asked for. UNMEETABLE: the one above, now fixed. GAPS against the proposal's What Changes: none found — the three artifacts I1 added (session-claim.json, commit-observe.json + .lock, write-conflicts.log.prev) and render's three reads (projectMd pre-image render.mjs:306, the stamp's mtime test :356, detoursLog :276) are each carried by a task (1.2, 1.3). VOCABULARY: consistent within the spec set; POLISH declined — the file rung's directory is still named assert/ (the half's own name) while the unit rung's is unit/, so the rungs are named on disk as required but the names are not parallel; renaming is out of this change's scope. CHOKEPOINTS: homeOf() (certification.mjs:63) and the two-glob runner invocation the hook and ci.yml both hand to one runner are each owned by exactly one capability. Recorded AFTER the amendment above, so the recorded hashes cover the amended text. — Make assertions actually assertions: a unit rung over in-memory state and fixture snapshots, so the pre-commit hook is seconds and the dev loop is sub-second
- `0.49.0`: 1 epic, 0 deferred · cross-spec pass (3 specs) · two fresh-context lenses (coherence; falsifiability) + one confirmation: round 1 fail (BLOCKS: 'supported' R1/R2 contradiction over 23/25/27; bounded-wait over-wide; schedule filter unfalsifiable; zero-count CI pass; unfalsifiable version-default scenario; collapsed-half message; unit fs ban vs shim), fixed at 8a73f75; confirmation fail (end clause untested; twin coupling), fixed at 9fa4163; 2026-09-24 — Node support policy: support the oldest non-EOL Node major; CI matrix from Node's schedule; per-file test mode; old-Node workarounds retired

## Dispositions

| Epic | Outcome | Recorded | Why |
|------|---------|----------|-----|
| `conductor-tells-the-truth` | delivered | 2026-08-25T23:12:02.877Z | 0.27.0 shipped: 115 tasks, 20 issues closed, 6 capabilities, 604 tests green |
| `df-plan-hierarchy-includes-archived-children` | delivered | 2026-09-10T02:48:09.194Z | `c39c538` excludes archived children; `d329d06` archives it as fixed in 0.12.1. |
| `df-stale-cache-silent-fallback` | delivered | 2026-09-10T02:48:06.751Z | `9e83963` makes a stale cached engine visible; `43c82bc` archives it as fixed in 0.12.2. |
| `gh-110-gate2-bypassed-by-archive-hook` | delivered · corrected (was delivered) | 2026-08-29T05:14:20.134Z | shipped in 0.27.0 (543a38f): Gate 2 was enforced at the front door and bypassed by the archive hook — 42 of 49 archived openspec epics carried gateReview: null. The gate now binds every path that can reach archived, including the drift heal reached from upgrade, render, commit-nudge and sync — corrected: recorded with --outcome but no --reason, which the verb permits for delivered — so the one epic that named the release's central defect carried no provenance while all 19 of its siblings did. Caught within minutes and uncorrectable until #130 shipped; this repo's own record was the test case |
| `gh-122-gate-records-unreliable-both-ways` | delivered | 2026-08-26T21:23:05.092Z | shipped in 0.27.0 (conductor-tells-the-truth, 543a38f); tracker issue closed |
| `detached-head-is-not-a-workspace` | delivered | 2026-09-12T01:28:09.785Z | Shipped in 0.42.0. A detached HEAD suppresses session bookkeeping (commit watermark, detour log, brief snapshot, session claim, activity log) and a verb that writes says the tree is detached, naming the tag when HEAD is exactly at one. Gate 1 pass at two lenses over the artifacts by path; Gate 2 pass at two lenses over 672d55e..cfb51d9 with five Criticals closed — the sharpest a guard on the FROZEN root while the write re-derived per call, green in the CLI and red on CI. Deliberately does NOT change isInitialized() or stop a mutating verb writing: state.json's tracking is load-bearing, and the warning is the mitigation rather than a refusal. |
| `disposition-backfill-undispositioned-archive` | delivered | 2026-09-08T22:15:44.003Z | The CAPABILITY shipped in 0.40.0 (9a0dfb1): unconsidered-outcomes enumerates the population — engine-stamped AND outcome unknown, excluding both the three epics whose delivered a migration derived from a passing Gate 2 and the epics carrying no disposition at all — and emits a per-epic remedy, with unreconstructable for cases where the evidence is genuinely gone. Shipping the walker is NOT walking it: 66 epics here still carry outcome unknown. That is operator work with a judgement per epic, and it is deferred rather than claimed. |
| `epic-registration-clock` | delivered | 2026-09-08T22:15:42.126Z | Shipped in 0.40.0 (65efef2). createdAt bound to pushEpic, the single creation sink, so it inherits the source scan forbidding bypass rather than an enumeration that had already gone stale once here. touchedAt stamped inside saveState after the no-op early return, excluding both timekeeping fields from the per-record comparison and matching by epic id rather than array position, because remove-epic filters the array. recover-created-at is a re-runnable verb the migration invokes once, not a migration body: a one-shot disk-reading transformation produces a different result per checkout, and two checkouts of one remote on this machine differ. A shallow clone would have fabricated a date for every epic and recorded it as fact; guarded, and absence stays re-attemptable. |
| `every-verb-refuses-what-it-does-not-read` | delivered | 2026-09-15T18:41:26.289Z | Built for 0.44.0 (ships with the release cut; fixes cfdude/pm#187's 0.41.0 regression). One pre-dispatch check reads every verb's declared flags and positionals: a help token in any non-value position prints help and writes nothing (remove-epic e2 --help no longer deletes e2), undeclared flags, surplus positionals, --valueless=value and non-flag-shaped --tokens are refused before any write, argv is handed to verbs in canonical order, init validates --platform before creating anything, hook verbs stay silent in repos without pm, --force is declared once on every mutating verb. Gate 1 two lenses + 2 confirmation rounds; cross-spec pass; Gate 2 two lenses pass (52 mutants killed) plus follow-ups and a docs/test re-review. |
| `functional-assertion-test-split` | delivered | 2026-09-22T06:11:39.187Z | 0.47.0 shipped this change's two spec deltas and every engine change they describe; the epic was archived by render()'s drift heal, which stamps outcome 'unknown' with NO reason, so it read as a live unexplained finding to integrity and to the conductor-15 day-one record. This records the disposition the ordinary end of the documented workflow owes. Found pre-existing (it fails on the pre-1.2 tree too) while certifying 1.2's commit. |
| `gates-bind-to-verified-evidence` | delivered | 2026-09-16T20:28:49.767Z | Built for 0.44.0 (ships with the release cut). record-reconcile answers only an armed, owed detour (never self, unrelated, --no-reconcile or still-paused); reconcileNeeded survives pointer moves, archiving and later pushes and is cleared only by a verdict; removals and link clears that would destroy an owed reconcile's record are refused; a 0.44.0 migration (and every upgrade) stamps link arming. Commit values are resolved to full object names when written (not-a-commit, HEAD and fakes refused), staleness requires headSha to reach every attributed entry, stored values never reach git as options. Gate 1 two lenses + 3 confirmation rounds (legacy rule replaced by a migration); cross-spec pass; Gate 2 two lenses (20 reconcile sequences, 27-repo replay, 0 verdicts reclassified) with injection and mutant fixes, scoped re-review, and a docs re-review. |
| `gh-cfdude-pm-175` | superseded | 2026-09-12T01:28:11.994Z | Superseded by detached-head-is-not-a-workspace, which IS the fix and carries the gates, the commits and the specs. This epic is the tracker mirror of the report — one piece of work registered twice, the shape the 0.41.0 grooming pass resolved seven times. The reporter's diagnosis is preserved in the change's proposal: 'this file exists' was doing double duty as 'this tree is a workspace'; their cheapest suggestion is what shipped; and their --git-common-dir warning held for a stronger reason than they gave — the two trees are separate CLONES, so that probe is blind there rather than inverted. |
| `platform-parity-mechanism` | delivered | 2026-08-04T00:48:57.337Z | — |
| `state-file-refuses-to-guess` | delivered | 2026-09-15T22:29:59.465Z | Built for 0.44.0 (ships with the release cut). An unreadable state.json (conflict markers, truncation, wrong shape) is refused with exit 11 and never replaced, with each hook reporting on its own channel (gate-guard fails closed); saves are serialised by an exclusive lock with fsync, a stale lock broken by dead pid or age under a serialised break, and a non-regular lock path refused instead of opened; claim TTLs are bounded and an unreadable expiry reads expired; write-rules finds its block by whole-line markers, splices literally, keeps CRLF, and refuses an ambiguous arrangement before init/upgrade write anything. Reverses 0.26.0's no-lockfile decision. Gate 1 two lenses + 3 confirmation rounds; cross-spec pass; Gate 2 two lenses (575 stressed writes, 0 lost) with two fix rounds (spin loop, FIFO, detached claim, leaks, 14 mutants) and a docs re-review. |
| `unit-rung-and-fixture-snapshots` | delivered | 2026-09-22T11:09:55.426Z | a store seam (137 loadState/saveState sites, decisions separated from persistence, memory store shipped) plus fixture snapshots moved the assertion half from 72-73 s to 26.50 s and per-unit-test to 0.70-0.87 ms; the sub-15 s acceptance was MISSED (1.77x) and is recorded as a miss — the remaining 21.3 s is durability flushing held by the CLAUDE.md managed-block seam edge (E1), which is a recorded cross-spec boundary not amendable in-apply; fsyncs 12,524 to 3,430; Gate 2 pass at 29e6562 |
| `commit-shas-stored-unresolved` | superseded | 2026-09-15T12:25:06.733Z | Carried into a 0.44.0 openspec change (see its supersedes link); split by spec so each change gets its own Gate 1/Gate 2 and the release a cross-spec review. |
| `comprehensive-code-review-after-0-43-0` | delivered | 2026-09-15T05:12:27.025Z | Independent whole-repo review of pm 0.43.0 (e1820a9) via superpowers:requesting-code-review, fanned out to 10 fresh-context reviewers (5 areas x 2 lenses: gates/record, state/registry, hooks/CLI/autonomy, emitted instructions/render/sync, docs+tests). About 80 raw findings, deduplicated into 30 registered planned epics: 7 Critical at P1 (state.json unparseable-wipe, save race, reconcile-gate bypasses, unresolved commit shas, --help-after-positional writes plus unknown flags, rules-block writer destroying CLAUDE.md content, claim TTL crash), 21 Important at P2, 2 at P3 plus one minors batch, each linked relates-to this epic; new evidence added to gh-cfdude-pm-185 and gh-cfdude-pm-189 (and their GitHub issues). Two Criticals re-verified by the orchestrator in a scratch repo. No code changed: the ask was to log findings. |
| `every-verb-validates-its-argv` | superseded | 2026-09-15T12:24:54.800Z | Carried into a 0.44.0 openspec change (see its supersedes link); split by spec so each change gets its own Gate 1/Gate 2 and the release a cross-spec review. |
| `gh-cfdude-pm-169` | delivered | 2026-09-08T22:15:15.277Z | Shipped in 0.40.0 (8726d8b, 135b22c). Not the single --clear-plan the issue asked for: nullability is now DECLARED on the flag registry and --clear <field> derives its accepted set from it, tested both directions so a nullable row added later with no clearing path fails the suite. --link appends and upserts a reason in place, at all three write paths after Gate 2 found it had landed at one. Six sites documenting replacement changed with it, two of them emitted by the engine at runtime. |
| `gh-cfdude-pm-174` | delivered | 2026-09-08T22:15:13.396Z | Shipped in 0.40.0 (65efef2). epic-in-undefined-status reports any epic whose status is outside KNOWN_STATUSES, naming the consequence a reader would otherwise miss — non-terminal to every archived-status rule, and it permanently absorbs the effective priority of everything depending on it. Reports, never repairs. The reporter also corrected the spec: the priority lift runs from dependent into blocker, the opposite of what three review rounds had read. |
| `gh-cfdude-pm-180` | delivered | 2026-09-10T04:40:08.707Z | Shipped in 0.41.0 (f20003a). The replay now slices its fixture to the release it replays rather than filtering findings out of the whole record, so cross-release contamination is structurally impossible, and the count is replaced by the identity set. Its two siblings were found by the same pass and routed to cfdude/pm#185 rather than folded in. |
| `gh-cfdude-pm-182` | delivered | 2026-09-10T04:40:10.525Z | Shipped in 0.41.0 (c80a948). A flag value may begin with two dashes, via a value-position rule plus --flag=value as the escape. The call-site sweep found the larger half: requireKnownFlags, serving five verbs, was still emitting the original bug verbatim after the parser was clean, invisible to every existing test because each puts the unknown flag last. Two further raw-argv scanners had it too; all four now read one shared predicate. |
| `gh-cfdude-pm-185` | delivered | 2026-09-10T04:39:15.237Z | Half shipped, half declined, both recorded. The totals arm — a frozen day-one document's '16 checks, 11 findings' asserted against a live record — was dropped in 00be074 with the archived document left untouched, which is what this issue argued for. The sibling at conductor-18 is DECLINED as a code change: it is silenced by the in-flight guard whenever an epic is marked active, and it fired 12 times during this release only because the operator never ran set-active. That is an operating failure, addressed by docs/lessons/an-unused-active-pointer-turns-a-true-check-into-noise.md, and weakening a real check to accommodate it would be the wrong trade. |
| `per-verb-help-from-flag-registry` | delivered | 2026-09-01T06:12:56.356Z | Shipped in 0.37.0 (PR #164, squash bac5466). Per-verb --help projected from the flag registry, closed enums naming their legal values, a ghost-flag sweep over all 48 verbs, and a docs pointer every managed repo receives on its next /pm:upgrade. Gate 2 passed with two independent fresh-context reviewers; its one Important finding — add-epic --help advertising a flag add-epic refuses — was fixed before merge. 1069 tests green; docs verified live on pm-plugin.dev. |
| `reconcile-gate-bypasses` | superseded | 2026-09-15T12:25:04.626Z | Carried into a 0.44.0 openspec change (see its supersedes link); split by spec so each change gets its own Gate 1/Gate 2 and the release a cross-spec review. |
| `rules-block-emits-no-help-pointer` | delivered | 2026-09-01T23:19:57.689Z | Shipped in 0.37.0 (185c680, PR #164) alongside per-verb --help, exactly as its relates-to link required — the pointer's first bullet promises a --help that had to exist first. Verified in the emitted block: the 'Getting help with pm' section renders on all three platform variants. The epic was left queued when its two siblings were archived; caught by /pm:sync. |
| `rules-block-writer-destroys-hand-content` | superseded | 2026-09-15T12:25:02.717Z | Carried into a 0.44.0 openspec change (see its supersedes link); split by spec so each change gets its own Gate 1/Gate 2 and the release a cross-spec review. |
| `state-json-unparseable-loads-as-empty` | superseded | 2026-09-15T12:24:56.764Z | Carried into a 0.44.0 openspec change (see its supersedes link); split by spec so each change gets its own Gate 1/Gate 2 and the release a cross-spec review. |
| `state-write-race-loses-updates` | superseded | 2026-09-15T12:24:58.720Z | Carried into a 0.44.0 openspec change (see its supersedes link); split by spec so each change gets its own Gate 1/Gate 2 and the release a cross-spec review. |
| `sweep-covers-operation-pairs` | delivered | 2026-09-08T22:15:11.490Z | Shipped in 0.40.0 (6b8f09e). Required task item 1 now obliges enumerating the inverse of every operation a change adds — in GATE_PROCEDURE_ITEMS[0] lines AND its mustSay claims, plus all three mirrored surfaces the drift guard reads, because the guard iterates mustSay only and amending lines alone would have left every mirror carrying the old rule. Immediately earned its keep: Gate 2 found three more instances of the class inside this very release, two of which both reviewers found independently. |
| `test-suite-perf-and-scaffolding` | delivered | 2026-09-10T02:51:49.232Z | Shipped in 0.24.0 (7be1c4f), which names the split and faster suite for #72. The five stories that read unticked are COMPLETION NOTES miscast as stories — they open 'MEASURED:', 'MEASURED RESULT:', 'GLOB LANDMINE:', 'MUTATION-TESTED', 'AUDIT RESULT:' and record findings the work produced, not work outstanding. That is the exact shape the 0.27.0 audit named when it found four epics archived with 'incomplete' stories that were actually completion notes, and the reason --description exists. One genuine remainder is stated inside story 10 and is low: three doesNotMatch(parseBrief(...)) assertions can still pass vacuously. |
| `claim-ttl-overflow-crashes-readers` | superseded | 2026-09-15T12:25:00.695Z | Carried into a 0.44.0 openspec change (see its supersedes link); split by spec so each change gets its own Gate 1/Gate 2 and the release a cross-spec review. |
| `df-changelog-fragment-conflicts` | delivered | 2026-09-10T02:48:15.987Z | 0.15.0 CHANGELOG: .changesets/<epic-id>.md fragments + a `changesets` engine verb; zero conflicts on first use. |
| `df-conflict-resolution-verification-step` | delivered | 2026-09-10T02:48:18.576Z | 0.15.0 CHANGELOG: mandatory post-resolution marker grep + `node -c` on the merge-conflict ladder. |
| `df-hierarchy-executor-must-check-readme-for-user-facing-changes` | delivered | 2026-09-10T02:48:41.047Z | `79205f9`, 0.17.0 — README drift test + hierarchy-executor README requirement. |
| `df-hierarchy-no-shared-file-conflict-detection` | delivered | 2026-09-10T02:48:11.482Z | `9803a68`/`ca7d3f9` shipped worktree-isolated dispatch + verify-worktrees in 0.12.0; `e731fb5` archives it. |
| `df-live-infra-epic-session-continuity-warning` | delivered | 2026-09-10T02:48:23.407Z | 0.15.0 CHANGELOG: hierarchy-child-executor must answer the session-continuity question in CONCERNS. |
| `df-readme-stale-since-gate-guard` | delivered | 2026-09-10T02:48:32.023Z | 0.15.0 CHANGELOG says "Resolves df-readme-stale-since-gate-guard"; README revamped in `5dec585`. |
| `diffshape-minimal-detour-inference` | delivered | 2026-09-10T02:47:26.057Z | `9dc85bf` auto-detects an unlogged minimal detour from commit diff shape (0.13.0). |
| `edd-observe-hardcodes-claude-md` | delivered | 2026-09-10T02:49:26.862Z | `d93ea84`, 0.25.0 (#76) closes the second platform seam in observe.py. |
| `edd-user-memory-confound-control-run` | delivered | 2026-09-10T02:49:32.249Z | `687b518` (#77) isolates eval runs from operator memory and proves the isolation positively. |
| `gate-guard-default-on-reconcile` | delivered | 2026-09-10T02:47:24.025Z | `f77d774` makes the reconcile-owed guard block unconditionally by default. |
| `gh-101-dependency-order-blind-to-blocked` | delivered | 2026-08-27T23:19:11.691Z | shipped in 0.29.0 (2a42688): effective priority over the depends-on closure, dependency warnings in both surfaces, reorder for manual rank |
| `gh-109-tracker-sync-direction` | delivered | 2026-08-26T21:22:50.755Z | shipped in 0.27.0 (conductor-tells-the-truth, 543a38f); tracker issue closed |
| `gh-112-backlog-triage-at-intake` | delivered | 2026-08-27T23:19:14.972Z | shipped in 0.29.0 (2a42688): triage emits a candidate set and never a verdict; IDF over the backlog itself; declined as a terminal outcome |
| `gh-113-gate-verdict-records-no-diff-range` | delivered | 2026-08-26T21:22:52.332Z | shipped in 0.27.0 (conductor-tells-the-truth, 543a38f); tracker issue closed |
| `gh-115-gates-blind-to-absent-edit` | delivered | 2026-08-26T21:22:53.938Z | shipped in 0.27.0 (conductor-tells-the-truth, 543a38f); tracker issue closed |
| `gh-116-conflict-warning-missed-on-burst` | delivered | 2026-08-26T21:22:55.526Z | shipped in 0.27.0 (conductor-tells-the-truth, 543a38f); tracker issue closed |
| `gh-117-sync-blind-to-archived-changes` | delivered | 2026-08-26T21:22:57.278Z | shipped in 0.27.0 (conductor-tells-the-truth, 543a38f); tracker issue closed |
| `gh-118-checkbox-signal-unreliable` | delivered | 2026-08-26T21:22:58.874Z | shipped in 0.27.0 (conductor-tells-the-truth, 543a38f); tracker issue closed |
| `gh-119-deferrals-have-no-path-to-backlog` | delivered | 2026-08-26T21:23:00.412Z | shipped in 0.27.0 (conductor-tells-the-truth, 543a38f); tracker issue closed |
| `gh-120-epic-outcome-shipped-vs-killed` | delivered | 2026-08-26T21:23:01.965Z | shipped in 0.27.0 (conductor-tells-the-truth, 543a38f); tracker issue closed |
| `gh-121-tasks-verified-against-tree-not-commit` | delivered | 2026-08-26T21:23:03.515Z | shipped in 0.27.0 (conductor-tells-the-truth, 543a38f); tracker issue closed |
| `gh-124-rules-block-emits-failing-add-epic` | delivered | 2026-08-26T21:23:06.660Z | shipped in 0.27.0 (conductor-tells-the-truth, 543a38f); tracker issue closed |
| `gh-125-release-planning-and-deliberate-exclusion` | delivered | 2026-08-26T21:23:08.290Z | shipped in 0.27.0 (conductor-tells-the-truth, 543a38f); tracker issue closed |
| `gh-126-release-level-cross-spec-gate` | delivered | 2026-08-27T16:47:17.753Z | shipped in 0.28.0 (6234f32): record-cross-spec-review, /pm:cross-spec-review, engine-enumerated spec set with per-file hashes, four-state staleness |
| `gh-128-openspec-staleness-nudge` | delivered | 2026-08-27T16:47:20.535Z | shipped in 0.28.0 (6234f32): one shared emitter feeding both the brief and /pm:upgrade; holds mid-change rather than suppressing; adapts to whether a diff will exist |
| `gh-134-hooks-run-the-installed-plugin` | delivered | 2026-08-27T16:47:22.171Z | shipped in 0.28.0 (6234f32) behind PM_ENGINE_DELEGATION, an opt-in naming one checkout; labelled a contributor necessity and documented in CONTRIBUTING.md and the README |
| `gh-137-release-completion-cascade` | delivered | 2026-08-28T00:35:28.728Z | shipped in 0.30.0 (32c940f): integrity check plus a sync step that PROPOSES the disposition; the batch --release verb was deliberately declined and the reasoning recorded |
| `gh-139-command-doc-engine-fallback-ungated` | delivered | 2026-08-28T00:35:30.204Z | shipped in 0.30.0 (32c940f): the CLAUDE_PROJECT_DIR arm deleted across 15 sites rather than gated, with a CI guard over every shipped instruction surface |
| `gh-140-attribution-write-not-persisted` | delivered | 2026-08-27T23:19:17.925Z | shipped in 0.29.0 (2a42688): saveState and update-epic both read back from disk. Root cause of the filed incident remains unestablished; what closed is the class the engine could not distinguish |
| `gh-142-recorded-sha-unreachable` | delivered | 2026-08-27T23:19:20.493Z | shipped in 0.29.0 (2a42688): integrity check distinguishing orphaned from absent, probe-gated so a shallow CI clone is not reported as a disaster |
| `gh-151-no-push-detour-verb` | delivered | 2026-08-29T13:31:44.532Z | shipped in 0.35.0 (1896ce1): push-detour and pop-detour; POP needed it more — as two writes the reconcile obligation is destroyed by the heal meant to preserve it |
| `gh-156-shipped-markdown-structural-check` | delivered | 2026-09-05T09:49:29.001Z | Shipped in 0.39.0 (PR #171, squash b9c5bec). Gate 2 passed with two independent fresh-context reviewers; A found five Important, B found a published arithmetic error in the #162 retraction and README Real Numbers stale by ~2x since 0.27.0. All fixed before merge. 1116 tests + 48 evals green; docs verified live. |
| `gh-165-deferral-assertion-mangled-and-immutable` | delivered | 2026-09-05T07:27:11.816Z | Shipped in 0.38.0 (PR #170, squash e077394). Gate 2 passed with two independent fresh-context reviewers; reviewer A returned FAIL with two Criticals, all nine findings across both reviewers fixed before merge. 1101 tests green; docs merged and verified live. |
| `gh-64-sync-duplicate-shipped-plan` | delivered | 2026-08-29T01:57:09.323Z | shipped in 0.31.0 (64b1adc): a five-rung ladder keyed on a real epic<->plan association, with a tombstone so a removal survives the next sync |
| `gh-65-autodetour-false-positives` | delivered | 2026-09-10T02:49:43.635Z | 0.24.0 `7be1c4f` names the auto-detour fix for #65/#68 (success check + repo-scope check). |
| `gh-66-update-epic-missing-flags` | delivered | 2026-08-29T01:56:33.852Z | shipped in 0.31.0 (64b1adc): --lane and --plan land as in-place writes; a test now fails CI if any source-artifact field is settable at creation but not on the update surfaces |
| `gh-69-sync-no-done-signal-for-plans` | delivered | 2026-08-29T01:56:35.618Z | shipped in 0.31.0 (64b1adc): rung 1 is the done-signal — a claimed plan is skipped status- and lane-blind, with no completion heuristic |
| `gh-79-epic-description-and-notes` | delivered | 2026-08-26T21:22:41.261Z | shipped in 0.27.0 (conductor-tells-the-truth, 543a38f); tracker issue closed |
| `gh-95-stories-optional-and-gate-nothing` | delivered | 2026-08-29T01:56:37.297Z | shipped in 0.31.0 (64b1adc): atomic --add-story and --story <n> --wont-do. NO new refusal: the measurement showed the gate already existed and its remedy was the defect. Mandatory stories declined on 0/148 adoption of the free-text carriers |
| `pm-fix-link-flag-validation-and-update` | delivered | 2026-09-10T02:47:13.519Z | `978ddd4` validated `--link` and added `update-epic --link`; shipped 0.9.3, archived by `91cb87b`. |
| `release-0-43-0-cut` | delivered | 2026-09-15T05:19:19.404Z | pm 0.43.0 cut 2026-09-14: plugin.json + CHANGELOG (d76ee77), PR #202 squash-merged as e1820a9 after green CI with presquash/pr-202 tagged (248 recorded shas reachable), 1393/1393 post-merge. Mintlify synced live via pm-docs PRs #42 and #43 (changelog, commands/epic, commands/status, commands/activity, Real Numbers). Plugin cache updated; fleet pass: 25 repos upgraded, market-intelligence (detached v3.0.1) correctly skipped; highway-snowflake and cfdude-music commits finished by hand; market-intelligence-dev left staged — its own pre-commit suite has 8 failures + 7 errors unrelated to the conductor files. Real Numbers row source corrected in the release-checklist skill (75607a9). |
| `rules-block-hardcodes-claude-slash-commands` | delivered | 2026-09-10T02:49:20.983Z | 0.24.0 `7be1c4f` shipped the platform-aware rules block; the `rules-target` verb exists today. |
| `state-lost-update-concurrent-sessions` | delivered | 2026-09-10T02:50:04.652Z | `152587b`, 0.26.0 (#108) guards state.json against silent lost updates. |
| `edd-harness-agent-behavior-testing` | delivered | 2026-09-10T02:49:16.573Z | `8de2833` (#67) shipped the EDD harness under evals/ as the semantic parity gate. |
| `archive-gate-reads-what-it-writes` | delivered | 2026-09-15T02:21:34.011Z | Built for 0.43.0 (ships with the release cut). update-epic now runs the archive gate after every field write, closing the one-call bypasses via --lane/--attribute-commit/--add-story/--withdraw-commit and the --story --done false refusal; a refused call announces no cleared field. An update to an archived delivered epic that breaks a met Gate 2 or handoff obligation is refused per obligation and prints one runnable invocation recording the disposition it implies. Gate 1 two lenses + cross-spec pass; Gate 2 two lenses, adversarial lens failed on U+2028/U+0085 line forging (3b9504b) and a scoped re-review on an untested detail-line escape (9897cfc). Mintlify task 5.4 carried to release-0-43-0-cut so the site does not document it before the plugin ships. (carried to `release-0-43-0-cut`) |
| `commit-nudge-reads-the-whole-move` | delivered | 2026-09-17T05:13:57.014Z | every live commit since the last observation is reported on both post-call events, with provenance stated, dead commits named, amends replaced, own artifacts and nested bookkeeping never auto-logged, retract-detour as the row's inverse; Gate 2 pass at 5f5c899 |
| `conductor-mjs-module-split` | delivered | 2026-07-21T09:59:09.242Z | — |
| `emitted-commands-run-as-written` | delivered | 2026-09-17T11:31:49.715Z | every command pm emits or ships passes the argv check (Layer A), every printed remedy runs and clears its condition in a fixture (Layer B), tracker recipes run as written, set-tracker validates repo shape incl. GHE, printedId() quotes every printed epic id; Gate 2 pass at 9dcf84f |
| `gate-verdict-withdrawal` | delivered | 2026-09-15T04:25:06.170Z | Built for 0.43.0 (ships with the release cut; resolves cfdude/pm#192). update-epic --withdraw-gate-review <1\|2> --withdrawal-reason moves the whole verdict, superseded included, into withdrawnGateReviews[] — recorded, not erased — and withdrawn is a named state on the archive gate, integrity (archived-with-withdrawn-gate-2, no-Gate-1 detail), PROJECT.md, the brief, the heal and the activity log. Bound by archive-gate-reads-what-it-writes: a withdrawal breaking a met Gate 2 on an archived delivered epic is refused with the correcting invocation. Gate 1 two lenses + cross-spec pass; reconcile vs the detour invalidated with five task amendments; Gate 2 both lenses failed on a padded gate value writing an entry-less withdrawal (fc0561f), scoped re-review pass. Mintlify task 8.7 carried to release-0-43-0-cut. (carried to `release-0-43-0-cut`) |
| `multi-tracker-primary-secondary-support` | delivered | 2026-07-20T01:04:54.194Z | — |
| `operations-ship-their-inverses` | delivered | 2026-09-18T16:59:37.110Z | every operation ships its inverse: set-autonomy --revoke records a revocation (marks only unrevoked matches, never rewrites a stamp) and the re-arm report enumerates what arming restores; --deferral/--carried-to are validated at write (non-empty, known, not self) with two read-time integrity checks whose holder enumeration is value-agnostic; drop-detour ends a jammed detour stack in one saveState (disarms, never removes, the obligation link; clears reconcileNeeded in the same write) and both jam refusals now name it; Gate 2 pass at 499676b |
| `the-guard-covers-every-write-path` | delivered | 2026-09-18T16:52:02.170Z | the reconcile/tracker guard's PreToolUse matcher now covers Bash writes: a closed write-shape list (redirections incl. multi-digit fds, heredocs, in-place editors, tee/cp/mv/unlink/shred/git rm/git mv, record destruction at exact path incl. globs/quotes) with declared limits (both fail-open modes named, no quoting model, undecidable forms pass), an engine-invocation exemption so drop-detour and every emitted remedy stay runnable, and a per-segment scan held by behavioural tests; Gate 2 pass at e6513f1 |
| `user-text-never-forges-output` | delivered | 2026-09-17T18:18:34.323Z | no user-supplied text can forge a line or a table cell: escaped at output (escapeControls, escapeTableCell via tableRow, jsonText for JSON stdout), refused at input for epic/release ids and tracker scopes, skipped at the final rung by sync and backfill, with a registry-bound poison sweep and an in-suite per-interpolation sweep holding it; Gate 2 pass at f02f098 |
| `agent-docs-teach-stale-gate-evidence` | superseded | 2026-09-16T22:37:17.080Z | Carried into a 0.45.0 openspec change (see its supersedes link). |
| `archived-paused-epic-jams-detour-stack` | superseded | 2026-09-18T00:26:29.963Z | carried by the 0.46.0 change `operations-ship-their-inverses`, which holds its requirements, scenarios and tasks; this row would otherwise be a second live record of the same work |
| `autonomy-grants-have-no-revoke` | superseded | 2026-09-18T00:26:28.821Z | carried by the 0.46.0 change `operations-ship-their-inverses`, which holds its requirements, scenarios and tasks; this row would otherwise be a second live record of the same work |
| `commit-watch-misses-commits` | superseded | 2026-09-16T22:37:15.928Z | Carried into a 0.45.0 openspec change (see its supersedes link). |
| `disposition-references-unvalidated` | superseded | 2026-09-18T00:26:29.485Z | carried by the 0.46.0 change `operations-ship-their-inverses`, which holds its requirements, scenarios and tasks; this row would otherwise be a second live record of the same work |
| `emitted-text-says-hand-edit-state` | superseded | 2026-09-16T22:37:16.300Z | Carried into a 0.45.0 openspec change (see its supersedes link). |
| `gate-guard-bash-writes-unguarded` | superseded | 2026-09-18T00:26:30.458Z | carried by the 0.46.0 change `the-guard-covers-every-write-path`, which holds its requirements, scenarios and tasks; this row would otherwise be a second live record of the same work |
| `gate-staleness-reads-only-last-attribution` | superseded | 2026-09-16T20:28:42.020Z | absorbed by gates-bind-to-verified-evidence: its 2026-09-14 Gate 2 lens 2 repro (ancestor attributed after an uncovered descendant) is task 4.1; its 2026-09-15 note (--attribute-commit notasha stored, read unverifiable, uncounted by the regression check) is tasks 2.1 and 4.3 |
| `gh-cfdude-pm-168` | delivered | 2026-09-17T22:52:20.077Z | the command doc resolves the engine from CLAUDE_PLUGIN_ROOT with a newest-cache fallback, so an upgrade no longer re-stamps the old version; verified on 0.45.0 |
| `gh-cfdude-pm-173` | delivered | 2026-09-17T19:46:53.984Z | retract-detour <sha> --reason ships as the verb-shaped inverse of an automatic detour row, and the hook no longer writes one for the active epic's own artifacts; 0.45.0 |
| `gh-cfdude-pm-177` | delivered | 2026-09-10T04:39:17.028Z | Shipped in 0.41.0. See the epic's attributed commit for the change and the CHANGELOG entry for what it does. |
| `gh-cfdude-pm-178` | delivered | 2026-09-10T04:39:18.758Z | Shipped in 0.41.0. See the epic's attributed commit for the change and the CHANGELOG entry for what it does. |
| `gh-cfdude-pm-181` | delivered | 2026-09-10T04:39:22.484Z | Shipped in 0.41.0. See the epic's attributed commit for the change and the CHANGELOG entry for what it does. |
| `gh-cfdude-pm-184` | delivered | 2026-09-17T19:46:54.543Z | the commit hook reads the whole HEAD movement from a reflog anchor and classifies from paths, not a subject prefix; 0.45.0 |
| `gh-cfdude-pm-186` | delivered | 2026-09-10T04:39:24.231Z | Shipped in 0.41.0. See the epic's attributed commit for the change and the CHANGELOG entry for what it does. |
| `gh-cfdude-pm-189` | delivered | 2026-09-17T19:46:55.013Z | unconsidered-outcomes offers delivered only where the archive gate accepts it, and every entry carries deliveredBlockedBy with an ordered remedy per obligation; 0.45.0 |
| `gh-cfdude-pm-191` | delivered | 2026-09-11T08:16:14.039Z | Shipped in 0.42.0. Two readers of the artifact field kept the range-only definition after #177 added it; the predicate itself is correctly range-only and was NOT widened, because two of its callers dereference entry.headSha on the next line. Found by running a Gate 1 with --artifact for the first time — the arm reported the correct sequence as a defect, because its premise (a real verdict predates the work it names) inverts for a Gate 1, whose verdict necessarily post-dates the commit that fixes what the review found. |
| `gh-cfdude-pm-192` | superseded | 2026-09-14T18:09:09.919Z | Superseded at registration by the openspec change gate-verdict-withdrawal, which carries the specs, the gates and the commits. Ended now rather than at close on purpose: #192 was filed because closing #175's tracker mirror as delivered forced gate transcription onto it, and a mirror that stays open until the work ships invites the same transcription. The issue's diagnosis and suggested fix are preserved in the change's proposal and design. |
| `gh-cfdude-pm-195` | delivered | 2026-09-17T19:46:55.466Z | changed paths are compared from the conductor root and read with -z, so a nested conductor's own bookkeeping is never auto-logged; 0.45.0 |
| `gh-cfdude-pm-199` | delivered | 2026-09-17T19:46:55.904Z | the attribution hint lists every candidate epic as a runnable command and decides none; 0.45.0 |
| `tracker-sync-recipes-broken` | superseded | 2026-09-16T22:37:16.675Z | Carried into a 0.45.0 openspec change (see its supersedes link). |
| `user-text-unescaped-in-render-brief-integrity` | superseded | 2026-09-16T22:37:17.453Z | Carried into a 0.45.0 openspec change (see its supersedes link). |
| `agent-outcomes-derivation-unguarded` | delivered | 2026-09-10T04:40:13.979Z | Shipped in 0.41.0 (48baf43). One deliberate literal now sits at the root of the outcome vocabulary and every derivation above it is asserted as a relation. Mutation-tested rather than assumed: dropping 'declined' fails on any record, where of the 124 tests in the two existing drift guards exactly one caught it — and only by accident of this record happening to hold a declined epic. |
| `ai-feedback-loop-github-issues` | delivered | 2026-09-10T02:47:54.663Z | `0edf820` added the /pm:feedback command; commands/feedback.md ships today. |
| `autodetour-parser-misses-am-and-f` | delivered | 2026-08-29T05:18:40.665Z | closed by observation-based commit-nudge in 0.32.0: a HEAD watermark plus reflog classification is flag-form-blind, so -am, -F, editor commits and forms git has not invented yet are covered by construction rather than by three pattern patches |
| `branch-protection-and-pr-workflow` | delivered | 2026-09-10T02:48:00.668Z | 0.15.0 CHANGELOG records main requiring PRs + the `test` check live; `ffb6b60` (#1) is the first PR through it. |
| `capture-pipe-lesson-matcher` | delivered | 2026-09-14T18:08:28.519Z | Shipped on dev at dbf42ee. filter-at-read-time-not-at-capture-time carries a detect matcher for a test run or git commit that starts a command segment and is piped into tail/head/grep/rg. Checked against twelve command shapes after two rounds — the first version false-fired on a search pattern containing 'git commit', the second refused the & in 2>&1. First-line-only by design, so it does not catch a pipe after a multi-line commit message, the shape of the repeat that prompted it; the lesson says so. |
| `category-based-preauthorization` | delivered | 2026-09-10T02:47:46.652Z | `cd0b8f7` added the `category:` `--preauthorize` shorthand; documented in the live rules block. |
| `cfdude-pm-repo-governance` | delivered | 2026-09-10T02:47:58.674Z | Parent epic archived by `24ff9ab` with all three children (branch protection, CI, autonomous-agent research) done. |
| `changelog-preview-in-sessionstart-nudge` | delivered | 2026-09-10T02:47:52.659Z | `2f4d102` inlines top Added-bullet headlines in the SessionStart upgrade nudge. |
| `ci-test-lint-workflow` | delivered | 2026-09-10T02:48:02.699Z | `8773fa5` added .github/workflows/ci.yml, which exists and gates PRs today. |
| `dependency-aware-standalone-ordering` | delivered | 2026-09-10T02:47:40.494Z | `3693032` extends depends-on ordering from hierarchy siblings to the whole top-level queue. |
| `df-autodetour-false-positive-conductor-admin-commits` | delivered | 2026-09-10T02:48:29.779Z | `dba2baa` (#12), 0.16.0 — conductor bookkeeping paths are never auto-logged as a detour. |
| `df-engine-banner-noise-every-invocation` | delivered | 2026-09-10T02:48:50.378Z | `a82c151` (#51) suppresses the engine banner by default in dev/self-hosting context. |
| `df-github-tracker-outward-sync-unsuppressed` | delivered | 2026-09-10T02:48:13.991Z | 0.16.0 CHANGELOG: rulesBlock() suppresses the outward sync section for github-issues (inward-only by design). |
| `df-no-mechanical-openspec-gate-enforcement` | delivered | 2026-09-10T02:48:35.005Z | 0.16.0 added `record-gate-review` and made archive reject an openspec epic lacking a gate2 pass. |
| `df-orchestrator-committed-release-without-reverifying` | delivered | 2026-09-10T02:48:37.199Z | `a268cd1`, 0.18.0 — a mechanical pre-commit hook now runs the full suite before every commit. |
| `df-precommit-hook-too-noisy-on-success` | delivered | 2026-09-10T02:48:44.560Z | `06a7d89` (#50) quiets the pre-commit hook on success. |
| `df-project-md-diff-triviality-not-mechanically-checkable` | delivered | 2026-09-10T02:48:55.143Z | `bcae4da` (#52) added `render --diff-summary`. |
| `df-skill-commands-line-drift` | delivered | 2026-09-10T02:48:20.998Z | 0.16.0 CHANGELOG: a test asserts every dispatch-table key appears in SKILL.md; it caught two real gaps. |
| `df-stray-render-artifacts-noise` | delivered | 2026-09-10T02:48:25.650Z | 0.16.0 CHANGELOG Fixed: writeRenderStamp() skips the rewrite when stateMtimeMs is unchanged. |
| `df-update-epic-no-story-toggle-verb` | delivered | 2026-09-10T02:49:11.736Z | `63309a3` (#53) added `update-epic --add-story` and `--story N --done`. |
| `df-verify-worktrees-merged-not-just-archived` | delivered | 2026-09-10T02:48:27.714Z | `6bf0984` (#49) flags merged-but-uncleaned hierarchy-child worktrees. |
| `docs-site-cleanup` | delivered | 2026-09-10T02:51:26.100Z | All nine stories shipped. The six that read unticked were verified against the LIVE SITE rather than assumed: /industry-frontier-practice returns 200 (story 5); the index title is 'Project Manager (PM) — A Claude Code Project Management Plugin', not the old 'PM: Epic Tracking and Detour Stack' (1 and 3); AGENTS.md, README.md, LICENSE and .atlas-analysis.json all return 404 (6); the nav logo is pm's own pm-plugin-nav-logo-light.png, not a brand.dev guess (2); and the real-numbers table appears twice on /introduction and zero times on the index, so the two pages no longer duplicate (4). The checkboxes were never ticked; the work was done. |
| `edd-adapter-discards-agent-stdout` | delivered | 2026-09-05T09:49:32.280Z | Shipped in 0.39.0 (PR #171, squash b9c5bec). Gate 2 passed with two independent fresh-context reviewers; A found five Important, B found a published arithmetic error in the #162 retraction and README Real Numbers stale by ~2x since 0.27.0. All fixed before merge. 1116 tests + 48 evals green; docs verified live. |
| `edd-measures-installed-plugin-not-worktree` | delivered | 2026-09-10T02:49:37.753Z | `b49f9e7` (#96) measures the worktree, not the installed plugin, and records which tree was measured. |
| `epic-timestamps-and-staleness` | delivered | 2026-09-10T02:47:44.660Z | `206b6d8` stamps startedAt/completedAt and surfaces a staleness indicator. |
| `fix-archived-no-change-warning` | delivered | 2026-09-10T02:47:28.153Z | `260bbd0` — `missing()` excludes archived epics from the no-change-on-disk warning. |
| `flag-level-doc-gaps` | delivered | 2026-09-01T06:12:57.971Z | Shipped in 0.37.0 (PR #164). --description on add-epic/add-many, --diff-summary, --platform, and the write-rules/rules-target verbs documented; two of the four reported gaps turned out to be measurement error and were corrected on #160 rather than 'fixed'. |
| `gh-100-link-types-no-known-set` | delivered | 2026-08-29T10:18:54.273Z | shipped in 0.34.0 (5811c9f): a closed vocabulary in three bands, derived mechanically from what the code reads and guarded in both directions |
| `gh-102-sync-blind-to-issue-comments` | delivered | 2026-08-26T21:22:46.061Z | shipped in 0.27.0 (conductor-tells-the-truth, 543a38f); tracker issue closed |
| `gh-103-tracker-refresh-gate-before-specs` | delivered | 2026-08-26T21:22:47.586Z | shipped in 0.27.0 (conductor-tells-the-truth, 543a38f); tracker issue closed |
| `gh-105-feedback-undeclared-gh-dependency` | delivered | 2026-08-29T08:21:54.299Z | shipped in 0.33.0 (08ff745): gh declared, plus a prefilled URL and email; sync stops rather than reporting a read nobody performed |
| `gh-106-conductor-logs-never-gitignored` | delivered | 2026-09-01T05:22:04.025Z | gitignore coverage shipped: ensureGitignore() runs from migrations and .gitignore carries .conductor/detours.log (line 7), .conductor/session-claim.json and .conductor/activity/. Issue closed COMPLETED 2026-08-20; the epic was never transitioned, which is the drift /pm:sync step 6 exists to catch. |
| `gh-107-snapshot-consumes-warning-into-unread-file` | delivered | 2026-08-26T21:22:49.143Z | shipped in 0.27.0 (conductor-tells-the-truth, 543a38f); tracker issue closed |
| `gh-111-activity-log-for-conductor-usage` | delivered | 2026-08-29T13:31:49.146Z | shipped in 0.35.0 (1896ce1): an activity log that ships with its reader; turns the hand-edit question from a jq sweep into a query |
| `gh-123-adopt-openspec-1-9-cli-surface` | killed | 2026-08-29T10:31:39.259Z | investigated and declined on evidence, no code shipped: status --json carries artifact existence not task counts, doctor is structurally empty here, and openspec validate --archived FAILS pm's own correctly-archived change because it counts raw checkboxes and knows nothing of the pm:lifecycle marker. The two tools compute different quantities from one file and cannot be reconciled by precedence. Surfaced #154 |
| `gh-127-dogfooding-practices-as-product` | delivered | 2026-08-29T10:18:51.205Z | shipped in 0.34.0 (5811c9f): both practices ship in skills/ with .claude/ stubs, plus numbered required task item 7 — pm ships the mechanism, never the corpus |
| `gh-129-attribution-nudge-at-commit-time` | delivered | 2026-08-29T08:22:01.178Z | shipped in 0.33.0 (08ff745): the detector half only — the prose half was discharged by 0.27.0 and the issue was never updated |
| `gh-130-disposition-has-no-correction-verb` | delivered | 2026-08-29T06:57:03.194Z | shipped in 0.32.0 (9334b0e): --correct-disposition: deliberate, self-describing, non-destructive; the prior record survives under superseded |
| `gh-131-retry-half-unverified` | delivered | 2026-08-29T08:21:52.619Z | shipped in 0.33.0 (08ff745): the claim reproduced at HEAD (both sites deleted, suite still green); three neuters now produce three catches |
| `gh-132-lessons-advisor-hook` | delivered | 2026-08-29T10:18:52.733Z | shipped in 0.34.0 (5811c9f): the lessons mechanism ships (shape, frontmatter contract, PreToolUse advisor); the corpus stays in this repo |
| `gh-133-disposition-reverts-backfill-counts` | delivered | 2026-08-29T06:57:04.757Z | shipped in 0.32.0 (9334b0e): root cause was provenance living in the one field an agent overwrites; registeredBy now lives on the epic |
| `gh-136-add-epic-notes-discarded` | delivered | 2026-08-29T08:21:50.945Z | shipped in 0.33.0 (08ff745): no engine fix was needed — --notes already round-tripped; the gap was a guard asserting registration rather than honoring |
| `gh-138-refresh-warning-counts-terminal-epics` | delivered | 2026-08-29T01:56:38.923Z | shipped in 0.31.0 (64b1adc): the count excludes epics that have ended, 60 -> 31 here; the disposition discharges the obligation rather than a second terminal watermark |
| `gh-148-propose-spec-association-from-header` | delivered | 2026-08-29T10:18:58.828Z | shipped in 0.34.0 (5811c9f): verify-specs --headers proposes; the parse rule was chosen empirically against ten real documents, 26 of 26 ids, zero false candidates |
| `gh-149-add-epic-drops-valueless-flag` | delivered | 2026-08-29T10:18:57.337Z | shipped in 0.34.0 (5811c9f): refused on all six registry surfaces from one shared rule; the sweep found it at a fourth surface, record-gate-review --reviewer |
| `gh-152-flag-value-rule-outside-epic-flags` | delivered | 2026-08-29T13:31:46.072Z | shipped in 0.35.0 (1896ce1): the rule binds to the dispatch table, with a verb-level guard reading conductor.mjs's own dispatch object |
| `gh-154-openspec-validate-lifecycle-collision` | delivered | 2026-08-29T14:55:20.865Z | shipped in 0.36.0 (04fdd8a): documented where the marker already is, riding item 3's mustSay so a mirror cannot drop it |
| `gh-159-no-reader-for-gate-guard-state` | delivered | 2026-09-05T07:27:13.909Z | Shipped in 0.38.0 (PR #170, squash e077394). Gate 2 passed with two independent fresh-context reviewers; reviewer A returned FAIL with two Criticals, all nine findings across both reviewers fixed before merge. 1101 tests green; docs merged and verified live. |
| `gh-161-strip-always-on-duplicated` | delivered | 2026-09-05T09:49:30.635Z | Shipped in 0.39.0 (PR #171, squash b9c5bec). Gate 2 passed with two independent fresh-context reviewers; A found five Important, B found a published arithmetic error in the #162 retraction and README Real Numbers stale by ~2x since 0.27.0. All fixed before merge. 1116 tests + 48 evals green; docs verified live. |
| `gh-162-delegation-drops-stdout` | declined | 2026-09-05T09:49:51.385Z | NOT REPRODUCIBLE, and the mechanism I reported was impossible. Delegation returns null for both shapes the test harness produces, and the handoff hands the child the parent's own descriptor via stdio:inherit. 0 reproductions in 34 full-suite runs; 200 delegating runs at 24-way parallelism against a 1MB payload gave one distinct output length every time. I filed it after measuring while two files under test carried uncommitted edits — the lesson it walked into was already in docs/lessons/. Both facts are now tests (conductor-38) so the mechanism cannot be re-derived from the issue text, and the one REAL truncation mechanism found is guarded at 8KB. |
| `gh-163-gate-verdicts-openspec-only` | delivered | 2026-09-05T07:27:15.839Z | Shipped in 0.38.0 (PR #170, squash e077394). Gate 2 passed with two independent fresh-context reviewers; reviewer A returned FAIL with two Criticals, all nine findings across both reviewers fixed before merge. 1101 tests green; docs merged and verified live. |
| `gh-70-link-validation-gap` | delivered | 2026-09-10T02:49:49.282Z | Issue #70 closed COMPLETED; the fix shipped a month earlier in 0.9.3 (`978ddd4`) — duplicate of pm-fix-link-flag-validation-and-update. |
| `gh-71-update-epic-positional-id` | delivered | 2026-08-26T21:22:39.653Z | shipped in 0.27.0 (conductor-tells-the-truth, 543a38f); tracker issue closed |
| `gh-80-gate-guard-refusal-applies-anyway` | delivered | 2026-08-26T21:22:42.876Z | shipped in 0.27.0 (conductor-tells-the-truth, 543a38f); tracker issue closed |
| `gh-81-project-md-never-clean` | delivered | 2026-08-29T08:21:57.625Z | shipped in 0.33.0 (08ff745): SHA dedupe on the trail plus a bookkeeping filter that had never reached its sibling branch |
| `gh-82-claude-project-dir-overrides-cwd` | delivered | 2026-08-29T08:21:59.322Z | shipped in 0.33.0 (08ff745): warns on three required conditions, realpath-compared; warns and never refuses |
| `gh-85-document-mutating-verbs` | delivered | 2026-08-29T08:21:55.963Z | shipped in 0.33.0 (08ff745): all 39 verbs declare their effect, verified by running each read-only verb and hashing content AND mtime |
| `gh-86-openspec-lane-silent-zero-progress` | delivered | 2026-09-10T02:50:06.845Z | `1113924`, 0.25.1 (#97) warns on a missing progress source instead of rendering a silent 0/0. |
| `gh-87-sync-registers-readme-as-plans` | delivered | 2026-09-10T02:50:08.948Z | `1113924`, 0.25.1 (#97) stops registering plans/README.md as an epic. |
| `gh-90-honor-output-style-contract` | delivered | 2026-08-29T14:55:19.380Z | shipped in 0.36.0 (04fdd8a): three bands — RECORDED and PARSED never bend (a reason is a write, field names are a wire format), NARRATED follows the user; reshape-never-drop |
| `gh-91-autodetour-false-minimal-detour` | delivered | 2026-08-29T06:57:07.922Z | shipped in 0.32.0 (9334b0e): no active epic means no work to detour from, so nothing is logged |
| `gh-92-epic-specpath` | delivered | 2026-08-29T06:57:00.173Z | shipped in 0.32.0 (9334b0e): specPath plus verify-specs; the field-family table absorbed it, and the guard that claimed to prove that was itself found vacuous |
| `gh-94-serial-detours-accepted-silently` | delivered | 2026-08-29T10:18:55.790Z | shipped in 0.34.0 (5811c9f): visibility shipped; the gate DECLINED for a structural reason now filed as #151 — no push-detour verb means the engine is absent at the decision |
| `github-issue-tracker-sync` | delivered | 2026-09-10T02:47:56.676Z | `766dd9f` added github-issues inward sync (open issues to untriaged epics), 0.14.0. |
| `honcho-memory-string-print` | delivered | 2026-09-10T02:47:36.337Z | `fc89ff5` added the `honcho-memory` subcommand; verb present today. |
| `hooks-json-unknown-keys-warn-every-session` | delivered | 2026-09-10T04:40:12.239Z | Shipped in 0.41.0 (2078614). hooks.json validates clean against the harness schema; the five comment keys' content moved to hooks/README.md rather than being deleted, because each records a decision that looks like a mistake to anyone who has not read it. Guarded by a closed allowlist, not a blocklist of 'comment', so the next unknown key fails the build too. |
| `incremental-notify-timing-fix` | delivered | 2026-09-10T02:47:48.613Z | `0b20572`; the emitted rules block carries "Notify incrementally, not at the end" today. |
| `lane-routing-config` | delivered | 2026-09-10T02:47:32.265Z | `9120a93` added per-repo lane routing; `set-lane-routing`/`suggest-lane` present today. |
| `lessons-index-cannot-drift-silently` | delivered | 2026-09-12T06:48:21.279Z | Shipped on dev at 88dd9c4. All 23 lessons indexed in docs/lessons/README.md from their own frontmatter, all 23 satisfy the four-field contract (four had title: and no rule:, which adviceText renders as the advice bullet), and scripts/test/lessons-index.test.mjs guards the index, the contract and the detect: shape with six assertions. Three of them were neutered and watched to fail before being trusted. The six bare-regex detect: matchers are grandfathered by name in a closed list rather than fixed here — four of them match file CONTENT, which matchLessons() has no field for, so they need a capability that does not exist. That half is cfdude/pm#194. |
| `per-epic-review-mode-override` | delivered | 2026-09-10T02:47:42.665Z | `2607829` added the per-epic escalation-only review-mode override. |
| `pm-plugin-improvements-2026-07-14` | delivered | 2026-09-10T02:47:21.986Z | Parent epic: `118e273` archived all 15 children, `24ff9ab` archived the parent as all-children-done. |
| `reconciler-structured-writeback` | delivered | 2026-09-10T02:47:30.212Z | `7902ee6` writes the verdict back onto the paused link; `record-reconcile` verb present today. |
| `remove-epic-verb` | delivered | 2026-09-10T02:51:52.853Z | 1d87dcc added remove-epic [--cascade] in 0.11.0; the verb is in today's dispatch list with cascade, link-stripping and active-pointer clearing all present. Five of six stories shipped; the sixth, a fresh-context code review, is recorded --wont-do because no evidence of one exists. |
| `verify-state-command` | delivered | 2026-09-10T02:47:38.402Z | `dd3272a` added `verify-state`; verb present in today's dispatch list. |
| `walk-the-undispositioned-archive` | delivered | 2026-09-10T03:14:36.093Z | Walked all 66. Every verdict cites a commit sha, a CHANGELOG version, an archived change directory or a source line proving the thing exists today: 58 delivered, 7 superseded (plan-file epics sync re-registered after the work had shipped elsewhere), 1 abandoned. unconsidered-outcomes now reports 0 and integrity findings fell 11 to 6. Three verdicts were corrected against each epic's own stated scope, and the three the archive gate refused were resolved on evidence rather than by ticking — docs-site-cleanup's six open stories were verified done against the LIVE SITE. |
| `autonomy-hierarchy-adoption-investigation` | delivered | 2026-09-10T02:47:34.308Z | `d6d6efc` committed docs/feedback/2026-07-15-autonomy-hierarchy-adoption-investigation.md with recommendations. |
| `epic-hierarchy-orchestration` | delivered | 2026-09-10T02:47:15.824Z | plan-hierarchy verb + hierarchy-child-executor agent shipped 0.10.0 (`388edd3`, `fd2ea96`); archived by `85f3e4e`. |
| `infra-runbook-preflight` | abandoned | 2026-09-10T02:47:19.918Z | The epic asked to WIRE infra-runbook preflight into pm's epic/hierarchy preflight. rg for infra-playbook\|infra-runbook across commands/ skills/ agents/ scripts/ hooks/ .claude-plugin/ returns NOTHING — the wiring never landed in pm. f7a2b37 archived it claiming a cross-repo docs change shipped it; that was a mistaken record, and the infra-playbooks commits in that window are unrelated taxonomy work. |
| `2026-08-03-platform-parity-mechanism` | superseded | 2026-09-10T02:50:11.029Z | Tombstone by its own title; shipped as platform-parity-mechanism, PR #78 (`10bbef1`). |
| `2026-08-14-eval-measures-worktree` | superseded | 2026-09-10T02:50:13.056Z | Tombstone; shipped as edd-measures-installed-plugin-not-worktree, PR #96 (`b49f9e7`). |
| `gh-cfdude-pm-179` | delivered | 2026-09-10T04:39:20.669Z | Shipped in 0.41.0. See the epic's attributed commit for the change and the CHANGELOG entry for what it does. |
| `gh-cfdude-pm-187` | delivered | 2026-09-10T04:39:25.960Z | Shipped in 0.41.0. See the epic's attributed commit for the change and the CHANGELOG entry for what it does. |
| `handoff-refusal-prints-story-titles-raw` | superseded | 2026-09-16T22:37:17.828Z | Carried into a 0.45.0 openspec change (see its supersedes link). |
| `autonomy-grant-starts-at-apply-not-propose` | superseded | 2026-09-10T04:37:39.483Z | Consolidated into openspec-planning-completeness-instruction by the 0.41.0 grooming pass. Its premise is ALIVE and unbuilt — the context is merged into that epic's notes verbatim rather than lost — but it shares the survivor's two files, its 'state an obligation, name no command' shape and its completion test, so shipping them separately would be three passes over one emitted block. The survivor was chosen because it is the only one carrying the recorded supersession-risk link to openspec-gate1-guard-on-apply; any other survivor orphans that link. |
| `conductor-skill-orient-reads-spec-inventory` | superseded | 2026-09-10T04:37:37.658Z | Consolidated into openspec-planning-completeness-instruction by the 0.41.0 grooming pass. Its premise is ALIVE and unbuilt — the context is merged into that epic's notes verbatim rather than lost — but it shares the survivor's two files, its 'state an obligation, name no command' shape and its completion test, so shipping them separately would be three passes over one emitted block. The survivor was chosen because it is the only one carrying the recorded supersession-risk link to openspec-gate1-guard-on-apply; any other survivor orphans that link. |
| `cross-session-epic-assignment` | delivered | 2026-08-29T13:31:47.624Z | shipped in 0.35.0 (1896ce1): claim/unclaim/owners — advisory with exactly one refusing surface, a stated TTL, and --steal rather than --force |
| `df-contributing-doc-missing-direct-push-recovery` | delivered | 2026-09-10T02:49:00.577Z | `da2ebfa` (#62) documents the main-moved-under-your-PR rebase recovery in CONTRIBUTING.md. |
| `gh-104-commit-nudge-fires-on-text-match` | delivered | 2026-08-29T06:57:06.318Z | shipped in 0.32.0 (9334b0e): the hook observes a HEAD watermark and the reflog instead of parsing the command text |
| `gh-88-record-where-unfinished-tasks-went` | delivered | 2026-08-26T21:22:44.467Z | shipped in 0.27.0 (conductor-tells-the-truth, 543a38f); tracker issue closed |
| `gh-89-delegate-discovery-to-subagents` | delivered | 2026-08-29T14:55:17.891Z | shipped in 0.36.0 (04fdd8a): an operating rule rather than a gate item — a per-action habit cannot be ticked in tasks.md and would dilute a list whose authority is that every member is a real gate |
| `gh-93-verify-specs-coverage` | delivered | 2026-08-29T06:57:01.719Z | shipped in 0.32.0 (9334b0e): verify-specs ships as a listing verb, not an integrity check — an uncovered design doc can legitimately be true |
| `multi-platform-agent-support` | superseded | 2026-09-08T06:43:24.797Z | Premise dead for 2 of 6 platforms. This epic's premise was 'needs investigation before scoping'; Codex and Hermes were since investigated hands-on (CLI 0.145.0 / v0.19.0), are separately registered as codex-platform-support and hermes-platform-support, and README.md:207-208 now reads 'Rules block only' for both. The umbrella carried no links in state — the parent/child relationship existed only in a design doc — so it was invisible to every dependency surface. Remaining uninvestigated scope re-registered as remaining-platform-investigation. Grooming pass 2026-09-07. |
| `parity-propagation-skill` | superseded | 2026-09-10T04:37:57.415Z | Consolidated into parity-reassessment-per-platform by the 0.41.0 grooming pass. Both were carved out of the shipped platform-parity-mechanism and both wait on the same trigger — the first real port. The survivor's stories are this epic's content, so the two would have been one pass over one document. |
| `autonomous-issue-resolution-agent` | delivered | 2026-09-10T02:48:04.753Z | The epic's own title scopes it to research: 'Needs its own research pass ... BEFORE any design'. f332b36 shipped that pass. The issue-to-PR pipeline was explicitly out of scope, so its absence is not a shortfall. |
| `cross-repo-epic-dependency-investigation` | delivered | 2026-09-10T02:47:50.614Z | `216808f` committed the investigation doc recommending a `link.repo` field. |
| `portfolio-architecture-consistency-scan` | delivered | 2026-09-10T02:47:17.888Z | The epic's own title scopes it to a brainstorm — 'least-scoped of the three, needs its own brainstorm'. That brainstorm shipped: 686d669, docs/feedback/2026-07-15-portfolio-architecture-consistency-scan-brainstorm.md. A brainstorm that ends in open questions is a completed brainstorm, not an abandoned scan. |
| `record-answers-for-itself` | delivered | 2026-09-09T00:04:14.925Z | Shipped as 0.40.0. Five capabilities modified, no new concept added: the clock (createdAt/touchedAt) bound to pushEpic and to saveState's post-no-op path, recover-created-at with its shallow-graft guard, epic-in-undefined-status, unconsidered-outcomes with the unreconstructable outcome, --clear over a declared nullable set, and --link that appends. Gate 1 pass over 88eaadf..19b077b, cross-spec pass re-recorded over five specs after amendment, Gate 2 pass over 996b068..d89f080 across three fresh-context lenses. |
| `2026-07-14-epic-hierarchy-orchestration` | superseded | 2026-09-10T02:49:06.320Z | Sync-registered plan file; `e8c0740` (#63) archives it as a "stale duplicate" of epic-hierarchy-orchestration (shipped 0.10.0). |
| `2026-07-21-conductor-mjs-module-split` | superseded | 2026-09-10T02:49:55.133Z | Plan-file epic sync-registered 2026-07-30 after the work shipped in `2f61c59` (#57); superseded by epic conductor-mjs-module-split. |
| `2026-07-26-edd-harness-agent-behavior-testing` | superseded | 2026-09-10T02:49:59.714Z | Plan-file epic sync-registered; superseded by edd-harness-agent-behavior-testing, which shipped as `8de2833` (#67). |
| `2026-07-29-platform-aware-rules-block` | superseded | 2026-09-10T02:50:02.408Z | Plan-file epic; superseded by rules-block-hardcodes-claude-slash-commands, which shipped in 0.24.0 (`7be1c4f`). |
| `2026-08-18-state-write-conflict-guard` | superseded | 2026-09-10T02:50:15.043Z | Tombstone; shipped in 0.26.0 as state-lost-update-concurrent-sessions (`152587b`). |

## Gate reviews

| Epic | Gate 1 (spec) | Gate 2 (implementation) |
|------|---------------|-------------------------|
| `conductor-tells-the-truth` | pass (84cd1cd..8223508) · spec review over the planning artifacts, four rounds on 2026-08-23 — cross-spec review (5 Critical, 10 Important), two re-review rounds, round 4, then two Gate 1 BLOCKS passes (data-model, tasks); all BLOCKS closed before implementation began at 0204ce0. RECORDED RETROACTIVELY at archive time: the review happened, the verdict was never written down at the time, and reviewedAt is therefore the write date and not the review date ⚠ stale | pass (7829e80..bd2b1e6) · two independent fresh-context reviewers (review mode: thorough) — Gate 2 and Gate 2B; both returned merge-once-group-17-lands, and their BLOCKS are the group-17 tasks now complete. Re-recorded to extend the range over bd2b1e6, which the reviewers did NOT see: it is the arm-1 narrowing this epic's own integrity check surfaced while 17.2's first verdict was being written — self-reported, not covered by either review ⚠ stale |
| `detached-head-is-not-a-workspace` | pass (5 artifacts) · two independent fresh-context lenses (thorough) — A: correctness and testability, 4 Critical 6 Important. B: absent edits and untrue assertions, 2 Critical 6 Important. BOTH independently found the same two Criticals: the proposal's evidence was stale and the cited tree disproved its own central claim, and the write-set requirement was a CLOSED four-item list while its rationale defined a larger one (eight sites exist; session-claim.json and render-stamp.json were both missing, the second measured dirty in production). Lens A additionally caught that 'exits non-zero' would make a non-repository suppress writes, contradicting this change's own scenario, and that the spec's 'mutates' predicate differs from the design's reuse of the shipped '!== read-only' gate on exactly the unknown-verb case the suite asserts against a literal source string. All findings closed. First gate-1 verdict recorded with --artifact paths rather than a fabricated commit range — 0.41.0's capability used for its own purpose. ⚠ unverifiable | pass (672d55e..cfb51d9) · two independent fresh-context lenses (thorough) — A: correctness, spec alignment and whether the tests prove what they claim, 3 Critical 5 Important. B: absent edits and what the change breaks elsewhere, 2 Critical 5 Important. Five Criticals, all closed. THE ONE THAT COULD NOT FAIL LOCALLY: appendEvents guarded the FROZEN ROOT while activityDir re-derives per call, so in-process the guard asked about one tree and the write landed in another — green in the CLI where one root is fixed at startup, and RED ON CI, where actions/checkout leaves HEAD detached at the sha. Reproduced both ways (57/1 detached, 58/0 attached) and fixed at the principle: the probe now takes the root it is asked about and caches per root. THREE VERBS reported writes that suppression removed — claim --repo announced a marker it never wrote and with --steal a takeover that never happened, printing the OTHER session's expiry so the message read as plausible; log-detour discarded the boolean whose docstring says it exists to prevent exactly that; snapshot said 'snapshot written'. The warning also named writes that no longer happen, fixed by declaring detachedNoOp on the effects table the gate already reads rather than a new verb list — which also stopped three stderr lines and a git spawn on EVERY Bash tool call, since commit-nudge is PostToolUse-wired. Four tests proved less than they claimed and were strengthened. Suite 1307/1307, and conductor-33 verified green under a DETACHED root, which is the shape CI runs. |
| `disposition-backfill-undispositioned-archive` | pass (88eaadf..19b077b) · the SAME Gate 1 review as record-answers-for-itself, not a second one: four fresh-context reviewers across three rounds (thorough) read all five delta specs of this change as one artifact set, and this epic is one of the five. Recorded here separately because a verdict is per-epic and this epic carried none, not because it was reviewed again. The sha range is the artifact range, which is what Gate 1 had to review — it reviews specs by path before code exists, and record-gate-review requires a range for a pass (filed as cfdude/pm#177). ⚠ stale | pass (996b068..135b22c) · two independent fresh-context lenses over 996b068..135b22c (review mode: thorough). This is the SAME Gate 2 that covered record-answers-for-itself, recorded here because that change is what implemented this epic and both lenses read the whole diff — not a second review, and not a rubber stamp: lens A returned 2 Important, lens B 2 Critical, and all four were fixed in 135b22c before this verdict was written. |
| `epic-registration-clock` | pass (88eaadf..19b077b) · the SAME Gate 1 review as record-answers-for-itself, not a second one: four fresh-context reviewers across three rounds (thorough) read all five delta specs of this change as one artifact set, and this epic is one of the five. Recorded here separately because a verdict is per-epic and this epic carried none, not because it was reviewed again. The sha range is the artifact range, which is what Gate 1 had to review — it reviews specs by path before code exists, and record-gate-review requires a range for a pass (filed as cfdude/pm#177). ⚠ stale | pass (996b068..135b22c) · two independent fresh-context lenses over 996b068..135b22c (review mode: thorough). This is the SAME Gate 2 that covered record-answers-for-itself, recorded here because that change is what implemented this epic and both lenses read the whole diff — not a second review, and not a rubber stamp: lens A returned 2 Important, lens B 2 Critical, and all four were fixed in 135b22c before this verdict was written. |
| `every-verb-refuses-what-it-does-not-read` | pass (5 artifacts) · re-recorded over artifacts amended after the Gate 1 pass by the 0.44.0 cross-spec review (coherence + falsifiability lenses, round 1 fail 4 BLOCKS, scoped re-review fail 1 BLOCK fixed and verified by grep); Gate 1 history: two fresh-context lenses plus scoped confirmation rounds to pass; 2026-09-15 ⚠ unverifiable | pass (85079e1..67b1e8bc0cab8435a7d05c5bc24b481910ded3bd) · re-recorded at the last attributed commit (task 5.5): code through 7ce3657 reviewed by two fresh-context lenses (both pass, 52 mutants killed) plus follow-ups 4167ba5/b2c5394/7ce3657; delta 7ce3657..67b1e8b (docs dc339e9; help-token sweep test, TTY-safe refusal drain, lesson 67b1e8b) re-reviewed by a fresh-context reviewer: pass, ~60 documented lines run, 5 hook verbs x 200 KB payloads, pty check, sweep mutants killed, 1457/1457; 2026-09-15 |
| `functional-assertion-test-split` | pass (5 artifacts) · two fresh-context lenses (A correctness/testability incl. hook-floor reproduction; B absent edits — all 23 git sites, every process.exit, every consumer of the suite command): round 1 fail (3 C: a silent-pass window between the file move and the hook re-point, twin set-equality contradicting the migration dispositions, a module-scope showPrefix cache breaking under per-call roots; 8 I); fixes applied; confirmation round fail (C1's replacement floor unsatisfiable — declared counted both halves while the runner runs one; 3 I incl. five module-load side effects not four, and a re-measurement: ~0.22-0.24 s/spawn, ~9-10 min serial floor, not 15.7); fixes applied (floor compares what the runner ran against what it was given; every test file has an explicit home; the sweep has a producer and a record); 2026-09-20 ⚠ unverifiable | pass (77c2a71ed8d05fa42ff328b59b2619c4e34b6678..8fe0a3b79621b7e251b47f053febc2a42d9e61ca) · two fresh-context lenses (spec conformance via mutation battery incl. 43-case binary-vs-main equivalence, 22-check cross-contamination probe; end-to-end real binary + real hook): round 1 fail (1 C: CI's piped steps could not go red on a failing suite; 7 I: floor/hook/refresh guards untestable in the firing direction, the assert half could spawn real git, gateway guard failed open, item-2 record incomplete) + 22 missing attributions backfilled; fixes dfb246d..8fe0a3b, every mutant proven dead; 2026-09-21 |
| `gates-bind-to-verified-evidence` | pass (5 artifacts) · re-recorded over artifacts amended after the Gate 1 pass by the 0.44.0 cross-spec review (coherence + falsifiability lenses, round 1 fail 4 BLOCKS, scoped re-review fail 1 BLOCK fixed and verified by grep); Gate 1 history: two fresh-context lenses plus scoped confirmation rounds to pass; 2026-09-15 ⚠ unverifiable | pass (6e1d06746db30ccb01d3dd8246cb5bc63386eae9..f89c58abd12e9d6eb7ccd5a21306a975bbaf6a85) · re-recorded at the last attributed commit: code through e3b65ab passed Gate 2 (two lenses, scoped re-review, two follow-ups); delta e3b65ab..f89c58a (docs f519879 incl. emitted rules/nudge text, lesson d366682) re-reviewed by a fresh-context reviewer: fail on one Important (nudge still taught the old catch-up rule) fixed in f89c58a with affected tests 141/141 and verified by the orchestrator; every other documented behavior reproduced; 2026-09-16 |
| `node-support-policy` | pass (aa48c885fd965d091ebb804a1e379fd79b47bd24..9fa4163fa1ff7012d8f0a19714c6ae9cf2e44947) · two fresh-context lenses (A correctness/testability reproduced on 22/24/26 binaries; B absent-edit sweep) + confirmation: round 1 fail (2 C: zero-count CI pass, unfalsifiable scenario; 9 I), fixed 8a73f75; confirmation fail (2 I), fixed 9fa4163; 2026-09-24 ⚠ stale | — |
| `platform-parity-mechanism` | — | pass (⚠ no checkable evidence) ⚠ unverifiable |
| `state-file-refuses-to-guess` | pass (6 artifacts) · re-recorded over artifacts amended after the Gate 1 pass by the 0.44.0 cross-spec review (coherence + falsifiability lenses, round 1 fail 4 BLOCKS, scoped re-review fail 1 BLOCK fixed and verified by grep); Gate 1 history: two fresh-context lenses plus scoped confirmation rounds to pass; 2026-09-15 ⚠ unverifiable | pass (6f7521bf1e2809e0f0557634ec2e31ac7276a0a7..99f3e286d1f6e778a381b9e62846b1c7481ff971) · re-recorded at the last attributed commit: code through 2498854 passed Gate 2 (two lenses, re-review, orchestrator FIFO/symlink verification); delta 2498854..99f3e28 is docs d619165, lesson ba19925 and four doc corrections from a fresh-context docs re-review (pass: every exit code, refusal excerpt and example run against the engine at HEAD); 2026-09-15 |
| `unit-rung-and-fixture-snapshots` | pass (ab171b7f3edbb77050c3101f89fb5d1ceecf1995..75d34a23a1dbb53251207e1e5e6fac26964c045b) · two fresh-context lenses (A reproduced the fsync premise: 12,524 fsyncs, 8.1x no-fsync control; B swept the 137 call sites and 10 write sites): round 1 fail (2 C: rung definitions stranded 82% of the half, render parity unachievable; 15 I/M), fixes applied; confirmation fail (3 residue: D5's population, a false guard-loudness claim, a missed detours-log read), fixed; 2026-09-21 ⚠ stale | pass (ab171b7f3edbb77050c3101f89fb5d1ceecf1995..29e6562303ec92082ffd209947fa15ada0358384) · two fresh-context lenses (A reproduced the fsync premise: 12,524 fsyncs, 8.1x no-fsync control; B swept the 137 call sites and 10 write sites): round 1 fail (2 C: rung definitions stranded 82% of the half, render parity unachievable; 15 I/M), fixes applied; confirmation fail (3 residue: D5's population, a false guard-loudness claim, a missed detours-log read), fixed; 2026-09-21 |
| `gh-165-deferral-assertion-mangled-and-immutable` | — | pass (977bcea..66bb8ff908ef7d1f0696d2ee8b6ae0d6935c42d5) · two independent fresh-context reviewers (thorough). A: FAIL, 2 Critical — the withdrawal could walk an epic past the archive gate, and withdrawnCommits was write-only (#159's own defect, on the feature fixing it). B: no Critical, 2 Important — --withdraw-commit absent from epic.md's flag table (conductor-13's gate is one-way), and the withdrawal borrowing the disposition's --reason. All 9 findings fixed and re-tested; the reverse registry-to-doc assertion now catches B's finding mechanically. 1101 tests, 0 failures. |
| `archive-gate-reads-what-it-writes` | pass (4 artifacts) · re-recorded over artifacts amended after the first Gate 1 (f7ae09d..2b971c9): amendments reviewed by the 0.43.0 cross-spec rounds (coherence + falsifiability lenses, initial + 3 re-rounds); final trim 2b971c9 removes requirements only, adjudicated by the orchestrator, 2026-09-14 ⚠ unverifiable | pass (9c3b946ad9d6b4b7864ccfadb628bf19921f47f0..492a84b526c148bd80e377ac6a9cf05cb98d2ee0) · re-recorded at the last attributed commit: code reviewed through 9897cfc by two fresh-context lenses + scoped re-review (fixes 3b9504b, 9897cfc); 422a012 docs verified against the engine; 492a84b is a lesson file, the lessons index and task ticks — no code; 2026-09-14 |
| `commit-nudge-reads-the-whole-move` | pass (7 artifacts) · two fresh-context lenses (Gate 1 rounds as previously recorded) plus 0.45.0 cross-spec rounds 1-6 re-reviewing every amendment to these artifacts, final round pass on both lenses with REDs reproduced on 0.44.0; re-recorded over the amended artifacts 2026-09-16 ⚠ unverifiable | pass (f49871af030253dd37d08ffdd5d4f356857a9fae..5f5c899565adad5780870ceadb419328927b6316) · two fresh-context lenses (spec conformance with 38 mutations; adversarial scratch repros): round 1 fail (1 C, 6 I); fixes 304311e..46fa347; scoped re-review pass on both lenses (closure re-runs + 17 mutations), Minors only; re-recorded over post-review commits 4f46b94 (test for re-review Minor M-b, RED shown) and docs/lessons 71dc037..5f5c899; 2026-09-17 |
| `conductor-mjs-module-split` | — | pass (⚠ no checkable evidence) ⚠ unverifiable |
| `emitted-commands-run-as-written` | pass (7 artifacts) · two fresh-context lenses (Gate 1 rounds as previously recorded) plus 0.45.0 cross-spec rounds 1-6 re-reviewing every amendment to these artifacts, final round pass on both lenses with REDs reproduced on 0.44.0; re-recorded over the amended artifacts 2026-09-16 ⚠ unverifiable | pass (b136774f9c426de5312d530ae32c56a190399375..9dcf84f20ddec18e76a61f1b8ec121be0561619e) · two fresh-context lenses per round (spec conformance with mutation testing; adversarial run-as-written): round 1 fail (1 C, 6 I), fixes eca6030..c90258c; re-review 1 split (1 I), fixes 9a986ca..87086fe; re-review 2 split (1 I), fixes 75c1e58..a552d4f; re-review 3 pass on both lenses; re-recorded over docs/lessons commits c3e5a33 and 9dcf84f; 2026-09-17 |
| `gate-verdict-withdrawal` | pass (4 artifacts) · re-recorded over artifacts amended after the first Gate 1 (f7ae09d..2b971c9): amendments reviewed by the 0.43.0 cross-spec rounds (coherence + falsifiability lenses, initial + 3 re-rounds); final trim 2b971c9 removes requirements only, adjudicated by the orchestrator, 2026-09-14 ⚠ unverifiable | pass (dbf42ee615ffad094eef497f7945af2b7b815fcd..a3e617b0bbf316e63a0e6ffde4eefcdcafe202e3) · two fresh-context lenses over 2e91ee2..1f962d9 (spec conformance: fail — padded gate value writes an entry-less withdrawal, untested brief heading; adversarial: fail — same defect), fixed fc0561f; scoped re-review pass (1192-call value probe, mutant-verified heading assertion); a3e617b is a lesson file + index + task tick, no code; 2026-09-14 |
| `multi-tracker-primary-secondary-support` | pass (⚠ no checkable evidence) ⚠ unverifiable | pass (⚠ no checkable evidence) ⚠ unverifiable |
| `operations-ship-their-inverses` | pass (6 artifacts) · two fresh-context lenses (A correctness/testability; B absent edits — call sites, DATA references, inverses): round 1 fail (2 C: the frame-drop contradicted an unmodified gate-integrity requirement, the empty-id check could not fire; 9 I incl. the change violating its own inverse rule for --context/--notify); fixes applied; combined confirmation + release cross-spec round: lens A pass, lens B 2 BLOCKS in the sibling only; narrow confirmation pass; 2026-09-17 ⚠ unverifiable | pass (65d3e7ef0bc4dcf2aba73f55d99968fef95d4bb4..499676bff4c5c490eb553974f42a831645f03145) · two fresh-context lenses (spec conformance with 18 mutants; end-to-end repair-path drives): round 1 fail (2 I: the jam refusals still claimed no verb exists; the new integrity checks prescribed an impossible remedy on superseded history), fixes 97ab488..589cc3e; confirmation pass; re-recorded over docs commit 499676b; 2026-09-18 |
| `the-guard-covers-every-write-path` | pass (7 artifacts) · two fresh-context lenses (A correctness/testability incl. a 49-case shape battery; B absent edits and consequences): round 1 fail (1 C: rm of the conductor record was a total bypass the design waved through; 11 I incl. &>/>\| redirects passing, rg 'a->b' blocked, raw command text in the block message); fixes applied; combined confirmation + release cross-spec round fail (2 BLOCKS: an exemption that could not fail, a multi-digit fd redirect evading the shape); fixed; narrow confirmation pass (117-case regex diff, mutation proofs); 2026-09-17 ⚠ unverifiable | pass (79b050e61f760bb2cdd226045cf0f1c6ea69027a..e6513f1dfc80d0fa6d74a01d23e5287236faab21) · two fresh-context lenses (spec conformance with a 26-mutant battery; end-to-end hook drives across 11 fixtures x 9 payload shapes x ~45 commands): round 1 fail (1 I: git rm destroyed the record; 1 I: per-segment scan untestable; minors), fixes 2610ea4..4b93b88; confirmation pass; re-recorded over docs commit e6513f1; 2026-09-18 |
| `user-text-never-forges-output` | pass (4 artifacts) · two fresh-context lenses (Gate 1 rounds as previously recorded) plus 0.45.0 cross-spec rounds 1-6 re-reviewing every amendment to these artifacts, final round pass on both lenses with REDs reproduced on 0.44.0; re-recorded over the amended artifacts 2026-09-16 ⚠ unverifiable | pass (c8d0d4ef0bc63742bf2ef2000deb753edc2b448e..f02f0988397ab48e5d8f6a4a31fb6331ba9ffa23) · two fresh-context lenses per round (spec conformance with mutation testing; adversarial forging): round 1 fail (8 I), fixes fe35990..bb729c5; re-review 1 fail (2 I), fixes 9458d78..a7fc9b4; re-review 2 split (2 I), fixes adda7b6..7d64996; re-review 3 split (3 I), fixes e55fe4a..d4851ce; re-review 4 pass on both lenses (Minors only; lexical-sweep limits documented with the runtime poison sweep as backstop); re-recorded over the round-4 Minor test a09ef65 and docs/lessons 6937c82..f02f098; 2026-09-17 |
| `gh-159-no-reader-for-gate-guard-state` | — | pass (977bcea..66bb8ff908ef7d1f0696d2ee8b6ae0d6935c42d5) · two independent fresh-context reviewers (thorough) over 977bcea..HEAD; 9 findings, all fixed and re-tested. 1101 tests, 0 failures. |
| `gh-163-gate-verdicts-openspec-only` | — | pass (977bcea..66bb8ff908ef7d1f0696d2ee8b6ae0d6935c42d5) · two independent fresh-context reviewers (thorough) over 977bcea..HEAD; 9 findings, all fixed and re-tested. 1101 tests, 0 failures. |
| `record-answers-for-itself` | pass (88eaadf..19b077b) · four fresh-context reviewers across three rounds (thorough): Gate 1 correctness, Gate 1 absent-edit, cross-spec x2. 13 blocks, then 6, then 6 — all closed. Round 2's Critical was created by two round-1 fixes interacting; three of round 3's blocks existed because earlier fixes had silently no-op'd. Stopped at three rounds deliberately: the KIND of finding converged to wording and doc sites even though the count did not, and the rest is Gate 2's job against real code. ⚠ stale | pass (996b068..d89f080) · two independent fresh-context lenses over 996b068..135b22c (thorough) — A: correctness and spec alignment, 0 Critical 2 Important 5 Minor. B: absent edits, 2 Critical 1 Important 2 Minor. Both independently found the same Critical: the no-op reporting rule was specified over the whole write surface and shipped at one verb, with four siblings confirmed printing success on a no-op save. All closed. THIS RECORD EXTENDS THAT RANGE TO d89f080 on evidence, not by assertion: a third fresh-context lens reviewed 135b22c..d89f080 (the release commit) together with the then-uncommitted documentation and record repair, and returned FAIL with 2 Critical. C1 was this endpoint itself. C2 was a wrong published number — 26 saveState call sites where the shipped scan finds 27 reporting plus 4 exempt of 31; reproduced independently before fixing, and corrected in CHANGELOG.md and on the live docs site. Its Minors also caught three stale counts in engine comments (help.mjs 21/48 to 23/50, verb-effects.mjs forty to fifty, state.mjs 69 to 31) and 16/32 to 17/33 read-only/mutating verbs, all moved by this release's own two new read-only verbs. Its Important flagged the cross-spec verdict as stale; that gate was re-run and re-recorded separately. NOT COVERED BY ANY LENS: the commit that immediately follows this record, carrying those fixes plus the archive. It is deliberately unattributed — docs and the archive move land after Gate 2 by construction, and attributing them would make this verdict stale at the instant the archive gate reads it. The 0.38.0 change epics all carry headSha equal to their last attributed commit; this follows that precedent. |

## Recent detours

| When | SHA | Kind | Epic | Note |
|------|-----|------|------|------|
| 2026-09-15T02:14:06.490Z | `422a012` | DETOUR-COMMIT | `archive-gate-reads-what-it-writes` | docs: describe the archive gate reading the written record and the archived-epic regression refusal |
| 2026-09-15T02:21:18.691Z | `492a84b` | DETOUR-COMMIT | `archive-gate-reads-what-it-writes` | docs(lessons): the agent's tool calls decode backslash-u escape text; route archive-gate-reads-what-it-writes learnings |
| 2026-09-15T02:26:12.695Z | `1a49e86` | DETOUR-COMMIT | `archive-gate-reads-what-it-writes` | chore(openspec): archive archive-gate-reads-what-it-writes |
| 2026-09-15T21:19:45.315Z | `cbe5be3` | AUTO-DETOUR | `state-file-refuses-to-guess` | fix(claims): ask whether the tree being written is detached, not the import-time root |
| 2026-09-16T17:10:12.486Z | `67dbee6` | AUTO-DETOUR | `gates-bind-to-verified-evidence` | fix(reconcile): keep refusing a verdict against a detour epic that does not exist |
| 2026-09-22T09:49:01.066Z | `d557d7b` | MINIMAL | `unit-rung-and-fixture-snapshots` | minimal: dropped a dangling module-eval line (const HOOK = path.join(…ENGINE…)) from scripts/test/unit/conductor-09.test.mjs — it threw ReferenceError at import while node:test still reported 17/17 passing |
| 2026-09-22T11:27:40.282Z | `61d5880` | MINIMAL | `-` | fixed the pre-commit hook: an unresolved rung glob reached node --test, which Node 18 refuses -- CI (PR #217) failed 5 hook tests in scripts/test/functional/conductor-09.test.mjs while the same file passed on Node 26; the hook now resolves its file set from the same two globs, aborts on an empty list, and the assert-half shape guard is re-pointed in the same commit (61d5880) |
| 2026-09-25T01:23:45.766Z | `3256cc2` | AUTO-DETOUR | `node-support-policy` | fix(openspec): restore the 0.48.0 main-spec sync lost to a reset |

## Briefing (what a fresh session sees)

```
⚠ OpenSpec drift — this project's generated instruction files are 1.13.0, the installed CLI is 1.13.2. Run `openspec update` yourself; pm's engine never runs it for you (pm instructs, it does not integrate).
   It rewrites .claude/skills/openspec-* and .claude/commands/opsx/ in place, and local edits there are overwritten silently — never accept it blind.
   Those files are git-tracked here, so `git diff` after the run IS the review — read it before committing.
   ⏸ HOLD until `functional-assertion-test-split`, `node-support-policy` are archived — `openspec update` rewrites the very instruction files that change is being authored against.

CONDUCTOR STATE — where we are and what's next
Progress is claimed completion — ticked checkboxes, not verified delivery.

NOW: `node-support-policy` (openspec, epic, P1) — 22/50 stories · 1 lifecycle

NEXT UP (by priority, then lane):
  • `gh-cfdude-pm-167` (P2, superpowers, untriaged) — —
  • `gh-cfdude-pm-176` (P2, superpowers, untriaged) — —
  • `gh-cfdude-pm-190` (P2, superpowers, untriaged) — —
  • `gh-cfdude-pm-194` (P2, superpowers, untriaged) — —
  • `gh-cfdude-pm-196` (P2, superpowers, untriaged) — —
  (+13 more — see PROJECT.md)
  lanes: openspec 22 · superpowers 61 · claude-code 140 · decision 7 · external 1

DEPENDENCY WARNINGS (an epic cannot be started until what it waits on moves):
  ⚠ `rules-block-cli-fallback` (P3) depends on `remaining-platform-investigation` (P3, planned) — the dependency is not queued

planned: 35 — see PROJECT.md

RELEASES:
  • `0.27.0`: 21 epics, 4 deferred · cross-spec pass (6 specs) · two independent fresh-context lenses, 2026-08-23 — RECORDED RETROACTIVELY: the review ran by hand before /opsx:apply and returned 5 Critical + 10 Important; all Critical closed and re-reviewed twice, the second round finding 2 more introduced by the fixes. The verb did not exist at the time, so this date is the write date, not the review date — conductor tells the truth — stop the record over-reporting completion: reason-bearing dispositions at four scopes, an archive gate that binds every path, gate verdicts with checkable evidence, tracker direction as explicit configuration, read-only integrity checks, and the gate procedure pm emits as required task items
  • `0.37.0`: 2 epics, 0 deferred — Help that answers the question actually asked: per-verb --help projected from the flag registry, closed enums naming their legal values, and a docs pointer every managed repo receives on its next upgrade (target: 0.37.0)
  • `0.38.0`: 3 epics, 0 deferred — The record says what actually happened: the deferral separator stops guessing, a deferral flag outside an archive refuses instead of reporting a phantom write, set-gate-guard reads back, gate verdicts record on any lane, and an attribution can be withdrawn (target: 0.38.0)
  • `0.39.0`: 4 epics, 0 deferred — Things that go missing without anything noticing: a shipped file that lost its tail, an agent transcript thrown away, a duplicated helper whose sibling went stale, and a reported stdout loss that did not exist (target: 0.39.0)
  • `0.40.0`: 6 epics, 2 deferred · cross-spec pass (5 specs) · re-review after amendment (thorough): the first verdict went stale when 65efef2 amended conductor-record (the shallow-boundary rule) and gate-integrity (the priority-propagation direction). A fresh-context reviewer re-read all five specs against each other AND against the shipped engine, since these specs are now implemented and a spec amended after the code ships can describe behaviour the code does not have. Both amendments verified correct against created-at.mjs:57-75 and dependency-order.mjs:79-99. Returned 1 BLOCK and 5 POLISH. The BLOCK was two specs asserting different sizes for one measured population — gate-integrity said 68 archived epics where epic-disposition and the proposal say 144 — and, underneath it, a conflation: '3 with a passing Gate 2' is the count the MIGRATION stamped delivered, while 9 archived epics carry a passing Gate 2. Fixed by re-measuring once (149 archived, 9 passing Gate 2, 3 migration-derived delivered, all 2026-09-08) and restating the requirement on the relative claim it actually rests on. Three POLISH fixed: the saveState call-site count (69, actually 31) in state-write-guard and design; a scenario whose absolutism a multi-migration upgrade falsifies; and two of four dual-lane collisions named as openspec when they are decision-lane. Two declined with reasons: a live count that drifted 4 to 5 and strengthens its own point, and a chokepoint documented in design and guarded by conductor-08 that does not belong in a capability spec. openspec validate --strict passes after the edits. — The record answers questions about itself. Five defects, one root: .conductor/state.json cannot answer questions about its own contents. Epics gain a registration date and a last-touched date; an epic in a status the engine does not define is reported; the archive can be asked which of its records carry no considered outcome, and an outcome can be recorded as genuinely unreconstructable; every declared-nullable field can be unset and --link appends instead of replacing; and the emitted call-site sweep obliges the inverse operation. Measured across 27 distinct upstreams: 310 archived epics carry outcome unknown and 26 sit in status 'done', a value KNOWN_STATUSES does not contain.
  • `0.41.0`: 12 epics, 1 deferred — Backlog health and the papercuts 0.40.0's own closeout surfaced: the replay test that fails during every release, the flag parser that silently drops synced issues, the release object that cannot be read back, three deferral flag shapes, a createdAt with no inverse, and hooks.json validating clean. Batched deliberately — 13 releases in 11 days was a 28-repo fleet pass every 20 hours. (target: 0.41.0)
  • `0.42.0`: 3 epics, 1 deferred · ⚠ no cross-spec review (2 specs) — The record stops writing where nobody is working, and the backlog items 0.41.0's grooming rescoped. Starts with #175 — pm reads a deployed detached checkout as a workspace because state.json is tracked, so breadcrumbs land in production trees and an upgrade there is discarded by the next deploy, both silently. (target: 0.42.0)
  • `0.43.0`: 2 epics, 0 deferred · cross-spec pass (2 specs) · two fresh-context lenses (coherence/contradiction; falsifiability/dependency-order), initial + 3 re-rounds; round-3 BLOCKS fixed d046e02; remaining round-3 items adjudicated POLISH (rendering detail, no contradiction) and trimmed from the spec 2b971c9, 2026-09-14 — Gate verdicts get the inverse every other correctable record has, and the shipped withdraw-commit bypass closes with it. Starts with #192. (target: 0.43.0)
  • `0.44.0`: 3 epics, 0 deferred · cross-spec pass (7 specs) · two fresh-context lenses (coherence/contradiction; falsifiability/dependency order over apply order argv → state-file → gates): round 1 fail (4 BLOCKS: link-repair contradiction, --force vs persist-or-refuse, 6.10 missed DISPATCH_BASELINE, 6.1–6.4 lacked --verdict); scoped re-review fail (6.9a/6.9b lacked --verdict) fixed and grep-verified; 7 spec files; 2026-09-15 — Fix every Critical from the 0.43.0 comprehensive code review: data-loss paths in the state file and argv handling, and gate bypasses. (target: 0.44.0)
  • `0.45.0`: 3 epics, 0 deferred · cross-spec pass (10 specs) · fresh-context lenses, rounds 1-9: r1-r6 as previously recorded (pass at r6); Gate 2 fixes then amended emitted-commands specs, so r7 fail (3 BLOCKS), r8 fail (2 BLOCKS: missing gate-integrity delta, control-character repo wording), r9 narrow pass (delta header and all 17 scenarios verified, both changes validate --strict); 2026-09-17 — Important findings from the 0.43.0 review, first batch: the commit nudge, commands pm emits, and user text in pm's output. Cluster 4 (missing inverses) and #167/#168/#176 are 0.46.0. (target: 0.45.0)
  • `0.46.0`: 6 epics, 0 deferred · cross-spec pass (7 specs) · two fresh-context lenses over both changes' spec sets, combined with Gate 1 fix confirmation: round 1 lens A pass (5 POLISH), lens B fail (2 BLOCKS: an engine-invocation exemption that could not fail, a multi-digit fd redirect evading the shape minimum); fixed; narrow confirmation round pass — MODIFIED delta fidelity diffed against the main spec, 117-case regex diff, mutation proofs both ways, drop-detour reachability verified on 0.45.0; 2026-09-17 ⚠ stale — Cluster 4 of the 0.43.0 review: every operation ships its inverse, every stored reference is checked, and the gate guard covers every write path
  • `0.47.0`: 0 epics, 0 deferred · cross-spec pass (2 specs) · INLINE review by the applying agent, NOT fresh-context lenses — this run's discipline forbids subagent dispatch, and that limitation is recorded here rather than papered over. Six questions over the release's two spec files (engine-invocation, suite-certification): (1) contradiction — none; the near-miss (one-process assertion half vs two-independent-invocations) is mutual dependency, not conflict. (2) double ownership — none; conductor.mjs's covers referencing the conformance set is a reference, and engine-invocation explicitly declines to restate verb-surface's refusal classes. (3) unmeetable — none in the specs. (4) gaps against proposal What-Changes — one: ci.yml's non-descending syntax loop has no requirement; declined as POLISH. (5) vocabulary forks — two, change-internal: "double" vs "fake", "change-triggered bucket" vs "sweeps bucket"; declined, renaming churns a passed Gate 1 contract for no behaviour change. (6) shared chokepoint — the single-process assertion half with injected streams is load-bearing for BOTH specs; guarded on each side (1.2 exits, 3.5 roots, 5.2 spawns). FINDING raised against the TASK LIST, not the specs: git.mjs's resolvedCommitCache is keyed by VALUE ONLY (unreachedCache by head+commits), so two invocations in one process against two roots share it and a hex abbreviation resolving in one repository and not the other gets the first repository's answer — engine-invocation forbids observing another invocation's values, and no task keys those caches; handled in the gateway work (4.2) rather than by amending a spec that is already correct. Verdict pass: no BLOCK found in either spec file; one BLOCK-level finding against tasks.md, acted on. 2026-09-20 — Split the test suite into a per-commit in-process assertion half and a triggered functional half: every git call behind one injected gateway, every refusal a caught value, every functional result certified by content hash.
  • `0.48.0`: 2 epics, 0 deferred · cross-spec pass (4 specs) · inline foreground cross-spec review of the 0.48.0 spec set — no fresh-context subagents were available in this run, so the reviewer is the applying agent, which read both of this change's deltas in full and answered the six questions. NOTE ON THE ENUMERATED SET: the engine enumerated FOUR specs, not two — the extra pair is functional-assertion-test-split/specs/{suite-certification,engine-invocation}/spec.md, the stale LIVE duplicate of the tree ab171b7 archived (task 0.4), which the release-scope enumeration reaches because the live directory was copied rather than moved. Those two are byte-identical to the archived originals and are the same two capabilities this change deltas, so the review covers them; the duplicate is filed separately under 0.4 rather than repaired here. BLOCKS (fixed in this commit): engine-invocation's 'the invocation that used the in-memory store created no file' scenario was unreachable — init() writes CLAUDE.md (rules.mjs:1097) and .gitignore through raw fs, both deliberately outside the store per design D1, so the scenario was narrowed to the store's own footprint and an explicit boundary paragraph was added. CONTRADICTION: none. suite-certification's rungs and engine-invocation's per-call store agree; 'both rungs run in one invocation of one process' does not collide with 'two invocations in one process are independent' — different claims about different things, and no scenario still speaks of the assertion half as one on-disk directory (the modified requirement and all four of its scenarios name the rungs). DOUBLE OWNERSHIP: none. engine-invocation owns the caller's contract and now states the store's boundary; suite-certification owns rung membership and names the store only as the mechanism a unit test obtains a value through, which is the split task 0.2 asked for. UNMEETABLE: the one above, now fixed. GAPS against the proposal's What Changes: none found — the three artifacts I1 added (session-claim.json, commit-observe.json + .lock, write-conflicts.log.prev) and render's three reads (projectMd pre-image render.mjs:306, the stamp's mtime test :356, detoursLog :276) are each carried by a task (1.2, 1.3). VOCABULARY: consistent within the spec set; POLISH declined — the file rung's directory is still named assert/ (the half's own name) while the unit rung's is unit/, so the rungs are named on disk as required but the names are not parallel; renaming is out of this change's scope. CHOKEPOINTS: homeOf() (certification.mjs:63) and the two-glob runner invocation the hook and ci.yml both hand to one runner are each owned by exactly one capability. Recorded AFTER the amendment above, so the recorded hashes cover the amended text. — Make assertions actually assertions: a unit rung over in-memory state and fixture snapshots, so the pre-commit hook is seconds and the dev loop is sub-second
  • `0.49.0`: 1 epic, 0 deferred · cross-spec pass (3 specs) · two fresh-context lenses (coherence; falsifiability) + one confirmation: round 1 fail (BLOCKS: 'supported' R1/R2 contradiction over 23/25/27; bounded-wait over-wide; schedule filter unfalsifiable; zero-count CI pass; unfalsifiable version-default scenario; collapsed-half message; unit fs ban vs shim), fixed at 8a73f75; confirmation fail (end clause untested; twin coupling), fixed at 9fa4163; 2026-09-24 — Node support policy: support the oldest non-EOL Node major; CI matrix from Node's schedule; per-file test mode; old-Node workarounds retired

DISPOSITIONS (recorded outcomes):
  • `conductor-tells-the-truth` — delivered (2026-08-25) — 0.27.0 shipped: 115 tasks, 20 issues closed, 6 capabilities, 604 tests green
  • `df-plan-hierarchy-includes-archived-children` — delivered (2026-09-10) — `c39c538` excludes archived children; `d329d06` archives it as fixed in 0.12.1.
  • `df-stale-cache-silent-fallback` — delivered (2026-09-10) — `9e83963` makes a stale cached engine visible; `43c82bc` archives it as fixed in 0.12.2.
  • `gh-110-gate2-bypassed-by-archive-hook` — delivered · corrected (was delivered) (2026-08-29) — shipped in 0.27.0 (543a38f): Gate 2 was enforced at the front door and bypassed by the archive hook — 42 of 49 archived openspec epics carried gateReview: null. The gate now binds every path that can reach archived, including the drift heal reached from upgrade, render, commit-nudge and sync — corrected: recorded with --outcome but no --reason, which the verb permits for delivered — so the one epic that named the release's central defect carried no provenance while all 19 of its siblings did. Caught within minutes and uncorrectable until #130 shipped; this repo's own record was the test case
  • `gh-122-gate-records-unreliable-both-ways` — delivered (2026-08-26) — shipped in 0.27.0 (conductor-tells-the-truth, 543a38f); tracker issue closed
  (+204 more — see PROJECT.md)

GATE REVIEWS:
  • `conductor-tells-the-truth` gate 1: pass (84cd1cd..8223508) · spec review over the planning artifacts, four rounds on 2026-08-23 — cross-spec review (5 Critical, 10 Important), two re-review rounds, round 4, then two Gate 1 BLOCKS passes (data-model, tasks); all BLOCKS closed before implementation began at 0204ce0. RECORDED RETROACTIVELY at archive time: the review happened, the verdict was never written down at the time, and reviewedAt is therefore the write date and not the review date ⚠ stale · gate 2: pass (7829e80..bd2b1e6) · two independent fresh-context reviewers (review mode: thorough) — Gate 2 and Gate 2B; both returned merge-once-group-17-lands, and their BLOCKS are the group-17 tasks now complete. Re-recorded to extend the range over bd2b1e6, which the reviewers did NOT see: it is the arm-1 narrowing this epic's own integrity check surfaced while 17.2's first verdict was being written — self-reported, not covered by either review ⚠ stale
  • `detached-head-is-not-a-workspace` gate 1: pass (5 artifacts) · two independent fresh-context lenses (thorough) — A: correctness and testability, 4 Critical 6 Important. B: absent edits and untrue assertions, 2 Critical 6 Important. BOTH independently found the same two Criticals: the proposal's evidence was stale and the cited tree disproved its own central claim, and the write-set requirement was a CLOSED four-item list while its rationale defined a larger one (eight sites exist; session-claim.json and render-stamp.json were both missing, the second measured dirty in production). Lens A additionally caught that 'exits non-zero' would make a non-repository suppress writes, contradicting this change's own scenario, and that the spec's 'mutates' predicate differs from the design's reuse of the shipped '!== read-only' gate on exactly the unknown-verb case the suite asserts against a literal source string. All findings closed. First gate-1 verdict recorded with --artifact paths rather than a fabricated commit range — 0.41.0's capability used for its own purpose. ⚠ unverifiable · gate 2: pass (672d55e..cfb51d9) · two independent fresh-context lenses (thorough) — A: correctness, spec alignment and whether the tests prove what they claim, 3 Critical 5 Important. B: absent edits and what the change breaks elsewhere, 2 Critical 5 Important. Five Criticals, all closed. THE ONE THAT COULD NOT FAIL LOCALLY: appendEvents guarded the FROZEN ROOT while activityDir re-derives per call, so in-process the guard asked about one tree and the write landed in another — green in the CLI where one root is fixed at startup, and RED ON CI, where actions/checkout leaves HEAD detached at the sha. Reproduced both ways (57/1 detached, 58/0 attached) and fixed at the principle: the probe now takes the root it is asked about and caches per root. THREE VERBS reported writes that suppression removed — claim --repo announced a marker it never wrote and with --steal a takeover that never happened, printing the OTHER session's expiry so the message read as plausible; log-detour discarded the boolean whose docstring says it exists to prevent exactly that; snapshot said 'snapshot written'. The warning also named writes that no longer happen, fixed by declaring detachedNoOp on the effects table the gate already reads rather than a new verb list — which also stopped three stderr lines and a git spawn on EVERY Bash tool call, since commit-nudge is PostToolUse-wired. Four tests proved less than they claimed and were strengthened. Suite 1307/1307, and conductor-33 verified green under a DETACHED root, which is the shape CI runs.
  • `disposition-backfill-undispositioned-archive` gate 1: pass (88eaadf..19b077b) · the SAME Gate 1 review as record-answers-for-itself, not a second one: four fresh-context reviewers across three rounds (thorough) read all five delta specs of this change as one artifact set, and this epic is one of the five. Recorded here separately because a verdict is per-epic and this epic carried none, not because it was reviewed again. The sha range is the artifact range, which is what Gate 1 had to review — it reviews specs by path before code exists, and record-gate-review requires a range for a pass (filed as cfdude/pm#177). ⚠ stale · gate 2: pass (996b068..135b22c) · two independent fresh-context lenses over 996b068..135b22c (review mode: thorough). This is the SAME Gate 2 that covered record-answers-for-itself, recorded here because that change is what implemented this epic and both lenses read the whole diff — not a second review, and not a rubber stamp: lens A returned 2 Important, lens B 2 Critical, and all four were fixed in 135b22c before this verdict was written.
  • `epic-registration-clock` gate 1: pass (88eaadf..19b077b) · the SAME Gate 1 review as record-answers-for-itself, not a second one: four fresh-context reviewers across three rounds (thorough) read all five delta specs of this change as one artifact set, and this epic is one of the five. Recorded here separately because a verdict is per-epic and this epic carried none, not because it was reviewed again. The sha range is the artifact range, which is what Gate 1 had to review — it reviews specs by path before code exists, and record-gate-review requires a range for a pass (filed as cfdude/pm#177). ⚠ stale · gate 2: pass (996b068..135b22c) · two independent fresh-context lenses over 996b068..135b22c (review mode: thorough). This is the SAME Gate 2 that covered record-answers-for-itself, recorded here because that change is what implemented this epic and both lenses read the whole diff — not a second review, and not a rubber stamp: lens A returned 2 Important, lens B 2 Critical, and all four were fixed in 135b22c before this verdict was written.
  • `every-verb-refuses-what-it-does-not-read` gate 1: pass (5 artifacts) · re-recorded over artifacts amended after the Gate 1 pass by the 0.44.0 cross-spec review (coherence + falsifiability lenses, round 1 fail 4 BLOCKS, scoped re-review fail 1 BLOCK fixed and verified by grep); Gate 1 history: two fresh-context lenses plus scoped confirmation rounds to pass; 2026-09-15 ⚠ unverifiable · gate 2: pass (85079e1..67b1e8bc0cab8435a7d05c5bc24b481910ded3bd) · re-recorded at the last attributed commit (task 5.5): code through 7ce3657 reviewed by two fresh-context lenses (both pass, 52 mutants killed) plus follow-ups 4167ba5/b2c5394/7ce3657; delta 7ce3657..67b1e8b (docs dc339e9; help-token sweep test, TTY-safe refusal drain, lesson 67b1e8b) re-reviewed by a fresh-context reviewer: pass, ~60 documented lines run, 5 hook verbs x 200 KB payloads, pty check, sweep mutants killed, 1457/1457; 2026-09-15
  (+19 more — see PROJECT.md)

HANDOFFS (work carried out of an archived epic):
  • `archive-gate-reads-what-it-writes` carried work to `release-0-43-0-cut` — Built for 0.43.0 (ships with the release cut). update-epic now runs the archive gate after every field write, closing the one-call bypasses via --lane/--attribute-commit/--add-story/--withdraw-commit and the --story --done false refusal; a refused call announces no cleared field. An update to an archived delivered epic that breaks a met Gate 2 or handoff obligation is refused per obligation and prints one runnable invocation recording the disposition it implies. Gate 1 two lenses + cross-spec pass; Gate 2 two lenses, adversarial lens failed on U+2028/U+0085 line forging (3b9504b) and a scoped re-review on an untested detail-line escape (9897cfc). Mintlify task 5.4 carried to release-0-43-0-cut so the site does not document it before the plugin ships.
      `release-0-43-0-cut` inherited it from `archive-gate-reads-what-it-writes`
  • `gate-verdict-withdrawal` carried work to `release-0-43-0-cut` — Built for 0.43.0 (ships with the release cut; resolves cfdude/pm#192). update-epic --withdraw-gate-review <1|2> --withdrawal-reason moves the whole verdict, superseded included, into withdrawnGateReviews[] — recorded, not erased — and withdrawn is a named state on the archive gate, integrity (archived-with-withdrawn-gate-2, no-Gate-1 detail), PROJECT.md, the brief, the heal and the activity log. Bound by archive-gate-reads-what-it-writes: a withdrawal breaking a met Gate 2 on an archived delivered epic is refused with the correcting invocation. Gate 1 two lenses + cross-spec pass; reconcile vs the detour invalidated with five task amendments; Gate 2 both lenses failed on a padded gate value writing an entry-less withdrawal (fc0561f), scoped re-review pass. Mintlify task 8.7 carried to release-0-43-0-cut.
      `release-0-43-0-cut` inherited it from `gate-verdict-withdrawal`

EPIC LINKS:
  • `backlog-grooming-pass` relates-to `epic-registration-clock` — grooming needs a clock to answer 'how long has this sat' — the repeatable pass is blind without createdAt
  • `detached-head-is-not-a-workspace` relates-to `gh-cfdude-pm-175` — this change IS the fix for that issue — the issue is the report, this is the work
  • `detached-head-is-not-a-workspace` supersedes `gh-cfdude-pm-175` — that epic is the tracker mirror of the report; this change is the work
  • `every-verb-refuses-what-it-does-not-read` supersedes `every-verb-validates-its-argv` — the openspec change that carries it
  • `functional-assertion-test-split` relates-to `comprehensive-code-review-after-0-43-0` — root cause of the suite's 15-minute serial floor measured 2026-09-18
  • `gates-bind-to-verified-evidence` supersedes `reconcile-gate-bypasses` — carried here
  • `gates-bind-to-verified-evidence` supersedes `commit-shas-stored-unresolved` — carried here
  • `gates-bind-to-verified-evidence` supersedes `gate-staleness-reads-only-last-attribution` — absorbed — every-entry staleness
  • `state-file-refuses-to-guess` supersedes `state-json-unparseable-loads-as-empty` — carried here
  • `state-file-refuses-to-guess` supersedes `state-write-race-loses-updates` — carried here
  • `state-file-refuses-to-guess` supersedes `claim-ttl-overflow-crashes-readers` — carried here
  • `state-file-refuses-to-guess` supersedes `rules-block-writer-destroys-hand-content` — carried here
  • `unit-rung-and-fixture-snapshots` supersedes `functional-assertion-test-split` — continues its architecture to the unit rung
  • `commit-shas-stored-unresolved` relates-to `comprehensive-code-review-after-0-43-0` — found by it
  • `commit-shas-stored-unresolved` relates-to `gate-staleness-reads-only-last-attribution` — same staleness function, different bypass
  • `every-verb-validates-its-argv` relates-to `comprehensive-code-review-after-0-43-0` — found by it
  • `every-verb-validates-its-argv` relates-to `gh-cfdude-pm-187` — its fix regressed into writing
  • `gh-cfdude-pm-169` relates-to `gh-66-update-epic-missing-flags` — re-reports two surfaces gh-66 closed as delivered — the unset half was never shipped
  • `gh-cfdude-pm-169` relates-to `gh-70-link-validation-gap` — same link surface
  • `gh-cfdude-pm-174` relates-to `epic-registration-clock` — same class — the record cannot answer a question about itself; 174 is status legality, the clock is age
  • `gh-cfdude-pm-182` relates-to `gh-cfdude-pm-176` — found while mirroring #176, whose title is what the parser could not accept
  • `reconcile-gate-bypasses` relates-to `comprehensive-code-review-after-0-43-0` — found by it
  • `rules-block-emits-no-help-pointer` relates-to `per-verb-help-from-flag-registry` — the pointer's first bullet promises a per-verb --help that does not exist yet; shipping the pointer alone would advertise a broken door
  • `rules-block-emits-no-help-pointer` relates-to `rules-block-hardcodes-claude-slash-commands` — same emitter, same family — the block assuming context it should state
  • `rules-block-writer-destroys-hand-content` relates-to `comprehensive-code-review-after-0-43-0` — found by it
  • `state-json-unparseable-loads-as-empty` relates-to `comprehensive-code-review-after-0-43-0` — found by it
  • `state-write-race-loses-updates` relates-to `comprehensive-code-review-after-0-43-0` — found by it
  • `sweep-covers-operation-pairs` relates-to `gh-cfdude-pm-169` — 169 is the defect this rule would have caught; fix and rule ship together
  • `claim-ttl-overflow-crashes-readers` relates-to `comprehensive-code-review-after-0-43-0` — found by it
  • `edd-observe-hardcodes-claude-md` depends-on `rules-block-hardcodes-claude-slash-commands` — observe.py correctly observes what the engine WRITES; the engine hardcodes CLAUDE.md (constants.mjs:14) and writes only there, so parameterizing observe first would abstract over a filename nothing produces
  • `edd-user-memory-confound-control-run` blocks `hermes-platform-support` — the Claude Code baseline cannot be trusted as a comparison target until a control run shows pm's own artifacts carry the behavior
  • `gh-115-gates-blind-to-absent-edit` relates-to `gh-113-gate-verdict-records-no-diff-range` — Gate 2's two failure modes -- 113 is the record being unverifiable, 115 is the review being structurally scoped to miss the absent edit
  • `gh-116-conflict-warning-missed-on-burst` relates-to `gh-115-gates-blind-to-absent-edit` — the test drove the counter one step at a time -- coverage traced the intended sequence rather than the invariant, so the burst case was never exercised
  • `gh-118-checkbox-signal-unreliable` relates-to `gh-95-stories-optional-and-gate-nothing` — the archive gate 95 wants would be backed by a signal with 3 verified false-greens and zero true-reds in an 18-epic sample -- the gate would fire on bookkeeping tasks and stay silent on real ones
  • `gh-124-rules-block-emits-failing-add-epic` relates-to `gh-114-lane-routing-blind-to-product` — same emitted recipe -- 114 is its hardcoded lane, 124 is that it does not run at all
  • `gh-125-release-planning-and-deliberate-exclusion` relates-to `gh-119-deferrals-have-no-path-to-backlog` — same missing concept -- a disposition with a reason -- at a different scope; 119 is a design-doc deferral, 125 is a release-scope exclusion, 120 is an archive outcome. Design once.
  • `gh-126-release-level-cross-spec-gate` depends-on `gh-125-release-planning-and-deliberate-exclusion` — the gate needs a release object to have a set of specs to review across
  • `gh-128-openspec-staleness-nudge` relates-to `gh-123-adopt-openspec-1-9-cli-surface` — same root, opposite halves — 128 is 'are we current with the tool', 123 is 'are we using what the tool now offers'
  • `gh-137-release-completion-cascade` relates-to `gh-130-disposition-has-no-correction-verb` — the 20-epic backfill produced a second instance — one disposition recorded without a reason and no verb to correct it
  • `gh-139-command-doc-engine-fallback-ungated` relates-to `gh-134-hooks-run-the-installed-plugin` — same shape, 14 ungated copies — gating the engine without these leaves the idea behind in every command doc
  • `gh-142-recorded-sha-unreachable` relates-to `gh-140-attribution-write-not-persisted` — same field, opposite halves of its lifecycle — 140 is the write not landing, 142 is what landed not surviving
  • `gh-151-no-push-detour-verb` relates-to `gh-94-serial-detours-accepted-silently` — 94's gate cannot be built until the engine is present at PUSH; 94 shipped its visibility half and declined the gate for this reason
  • `gh-165-deferral-assertion-mangled-and-immutable` relates-to `gh-149-add-epic-drops-valueless-flag` — same family — a write surface that accepts input and does not act on it as stated
  • `gh-65-autodetour-false-positives` relates-to `autodetour-parser-misses-am-and-f` — the remaining slice (paired-repo commit with no parseable -m) is that epic's scope; issue #65 stays OPEN until it lands
  • `gh-66-update-epic-missing-flags` blocks `gh-64-sync-duplicate-shipped-plan` — the plan-association fix needs --plan on update-epic to populate the field for existing epics; without it only new epics can be matched
  • `gh-69-sync-no-done-signal-for-plans` relates-to `gh-64-sync-duplicate-shipped-plan` — same root cause -- dedup keys on the plan's filename-derived id instead of a plan<->epic association, so any epic whose plan is named differently is re-registered forever
  • `gh-79-epic-description-and-notes` relates-to `epic-session-attribution` — an append-only notes trail with {at, actor, text} entries is the same structure session attribution needs -- one field serves both
  • `gh-95-stories-optional-and-gate-nothing` depends-on `gh-79-epic-description-and-notes` — the archive gate fires on false positives until stories stop being the only free-text carrier -- measured: 4 epics archived with 'incomplete' stories that were completion notes
  • `release-0-43-0-cut` relates-to `archive-gate-reads-what-it-writes` — carries its Mintlify task 5.4
  • `release-0-43-0-cut` relates-to `gate-verdict-withdrawal` — carries its Mintlify task
  • `release-0-43-0-cut` relates-to `comprehensive-code-review-after-0-43-0` — runs after this PR merges
  • `state-lost-update-concurrent-sessions` depends-on `gh-106-conductor-logs-never-gitignored` — the conflict guard adds a second .conductor log; shipping it before the ignore gap is fixed would add a second permanently-untracked file
  • `archive-gate-reads-what-it-writes` relates-to `gate-verdict-withdrawal` — split out of its Gate 1 round 2 — the ordering defect is the whole verb, not the withdrawal flags; gate-verdict-withdrawal inherits both requirements
  • `archive-gate-reads-what-it-writes` resolves-blocker-for `gate-verdict-withdrawal` — Gate 1 round 2 showed the archive-gate ordering defect is the whole update-epic verb (lane, attribute-commit, add-story, withdraw-commit bypasses; story-done false refusal; post-archive regressions), not the withdrawal flags — it gets its own change, and the withdrawal spec inherits its two requirements
  • `commit-nudge-reads-the-whole-move` supersedes `commit-watch-misses-commits` — carried here
  • `commit-nudge-reads-the-whole-move` relates-to `gh-cfdude-pm-173` — auto-detour on bookkeeping commits
  • `commit-nudge-reads-the-whole-move` relates-to `gh-cfdude-pm-184` — chore(release) and task-tick commits logged as detours
  • `commit-nudge-reads-the-whole-move` relates-to `gh-cfdude-pm-195` — nested conductor path roots
  • `commit-nudge-reads-the-whole-move` relates-to `gh-cfdude-pm-199` — attribution hint names only the active epic
  • `emitted-commands-run-as-written` supersedes `emitted-text-says-hand-edit-state` — carried here
  • `emitted-commands-run-as-written` supersedes `tracker-sync-recipes-broken` — carried here
  • `emitted-commands-run-as-written` supersedes `agent-docs-teach-stale-gate-evidence` — carried here
  • `emitted-commands-run-as-written` relates-to `gh-cfdude-pm-189` — unconsidered-outcomes emits an invocation the archive gate refuses
  • `gate-verdict-withdrawal` supersedes `gh-cfdude-pm-192` — the tracker mirror of this change — one piece of work registered twice is the shape that produced #192 itself, so it ends at registration rather than at close
  • `gate-verdict-withdrawal` relates-to `gh-163-gate-verdicts-openspec-only` — #163 removed record-gate-review's lane refusal, so any lane can carry a verdict to withdraw; this change's restated heal requirement corrects a spec sentence that has been false since
  • `gate-verdict-withdrawal` may-invalidate `archive-gate-reads-what-it-writes` — Gate 1 round 2 showed the archive-gate ordering defect is the whole update-epic verb (lane, attribute-commit, add-story, withdraw-commit bypasses; story-done false refusal; post-archive regressions), not the withdrawal flags — it gets its own change, and the withdrawal spec inherits its two requirements
  • `gate-verdict-withdrawal` relates-to `handoff-refusal-prints-story-titles-raw` — same defect class — this change prints its new reason escaped rather than add an instance
  • `gate-verdict-withdrawal` relates-to `archived-delivered-gate2-regression-report` — that check must exclude withdrawn-kind epics, which archived-with-withdrawn-gate-2 already reports
  • `hermes-platform-support` depends-on `platform-parity-mechanism` — the structural parity gate and ledger must exist before the first second-platform port
  • `hermes-platform-support` depends-on `edd-harness-agent-behavior-testing` — semantic parity is unprovable without a blessed Claude Code baseline to compare a Hermes run against
  • `hermes-platform-support` depends-on `rules-block-hardcodes-claude-slash-commands` — the rules block must stop hardcoding Claude Code slash-command syntax before pm can claim Hermes support
  • `hierarchy-run-has-one-state-writer` relates-to `emitted-commands-run-as-written` — split out at its Gate 1 — new design, not an emitted-command fix
  • `operations-ship-their-inverses` supersedes `autonomy-grants-have-no-revoke` — carried by this change
  • `operations-ship-their-inverses` supersedes `disposition-references-unvalidated` — carried by this change
  • `operations-ship-their-inverses` supersedes `archived-paused-epic-jams-detour-stack` — carried by this change
  • `the-guard-covers-every-write-path` supersedes `gate-guard-bash-writes-unguarded` — carried by this change
  • `user-text-never-forges-output` supersedes `user-text-unescaped-in-render-brief-integrity` — carried here
  • `user-text-never-forges-output` supersedes `handoff-refusal-prints-story-titles-raw` — carried here
  • `add-many-drops-input-silently` relates-to `comprehensive-code-review-after-0-43-0` — found by it
  • `agent-docs-teach-stale-gate-evidence` relates-to `comprehensive-code-review-after-0-43-0` — found by it
  • `archived-paused-epic-jams-detour-stack` relates-to `comprehensive-code-review-after-0-43-0` — found by it
  • `autonomy-grants-have-no-revoke` relates-to `comprehensive-code-review-after-0-43-0` — found by it
  • `commit-gate-tests-working-tree-not-index` relates-to `comprehensive-code-review-after-0-43-0` — found by it
  • `commit-watch-misses-commits` relates-to `comprehensive-code-review-after-0-43-0` — found by it
  • `commit-watch-misses-commits` relates-to `gh-cfdude-pm-199` — same nudge, different defect
  • `commit-watch-misses-commits` relates-to `gh-cfdude-pm-173` — auto-detour classification
  • `disposition-references-unvalidated` relates-to `comprehensive-code-review-after-0-43-0` — found by it
  • `emitted-text-says-hand-edit-state` relates-to `comprehensive-code-review-after-0-43-0` — found by it
  • `gate-guard-bash-writes-unguarded` relates-to `comprehensive-code-review-after-0-43-0` — found by it
  • `gate-staleness-reads-only-last-attribution` relates-to `archive-gate-reads-what-it-writes` — found by its Gate 2 lens 2; both its guards inherit the hole
  • `gate-staleness-reads-only-last-attribution` relates-to `archived-delivered-gate2-regression-report` — same Gate 2 demand, different bypass
  • `gh-cfdude-pm-167` relates-to `gh-117-sync-blind-to-archived-changes` — fallout of that feature shipping with no preview or subset control
  • `gh-cfdude-pm-168` relates-to `gh-139-command-doc-engine-fallback-ungated` — same command-doc engine-resolution mechanism, different failure
  • `gh-cfdude-pm-173` relates-to `df-autodetour-false-positive-conductor-admin-commits` — the 2026-07 proposal for this exact fix, archived by migration with outcome unknown and no reason
  • `gh-cfdude-pm-177` relates-to `openspec-gate1-guard-on-apply` — both are Gate 1's record being wrong — that one guards the transition Gate 1 should block, this one is the evidence Gate 1 can honestly supply
  • `gh-cfdude-pm-178` relates-to `sweep-covers-operation-pairs` — that epic shipped the inverse-operation rule in 0.40.0; this is a pre-existing instance the rule now catches
  • `gh-cfdude-pm-181` relates-to `gh-cfdude-pm-178` — the sibling instance of the same missing-inverse gap, found in the same Gate 2 review
  • `gh-cfdude-pm-184` relates-to `gh-cfdude-pm-173` — same heuristic — auto-detour classifying a commit that is not a detour; #173 is pm's own bookkeeping, this is a release commit
  • `gh-cfdude-pm-190` relates-to `gate-verdict-withdrawal` — both are gate-verdict records that outlive what they judged — a withdrawn verdict and a failing verdict already answered by a later commit
  • `gh-cfdude-pm-191` relates-to `gh-cfdude-pm-177` — #177 added the --artifact evidence form; this is the reader of the same field that kept the old definition
  • `gh-cfdude-pm-192` relates-to `gh-cfdude-pm-175` — found while closing that epic — transcribing its gates was the wrong call and there was no way back
  • `gh-cfdude-pm-194` relates-to `lessons-index-cannot-drift-silently` — that epic grandfathered the six inert matchers this issue asks the engine to report
  • `gh-cfdude-pm-195` relates-to `gh-cfdude-pm-173` — same heuristic, same allowlist — #173 is files missing from CONDUCTOR_OWN_FILES in a flat repo, this is the same allowlist compared against git-root paths in a nested one
  • `gh-cfdude-pm-195` relates-to `gh-cfdude-pm-184` — same heuristic — a non-detour commit auto-logged as a detour
  • `handoff-demand-blind-spots` relates-to `comprehensive-code-review-after-0-43-0` — found by it
  • `hooks-not-silent-before-init` relates-to `comprehensive-code-review-after-0-43-0` — found by it
  • `lesson-detect-matcher-hardening` relates-to `comprehensive-code-review-after-0-43-0` — found by it
  • `lesson-detect-matcher-hardening` relates-to `gh-cfdude-pm-194` — same matcher surface — malformed detect discarded silently
  • `mutation-pass-is-a-required-task-item` relates-to `sweep-covers-operation-pairs` — the same shape one level over — that item added the INVERSE question to the sweep; this adds the CAN-IT-FAIL question
  • `suite-twin-and-certification-discipline` relates-to `functional-assertion-test-split` — this epic is the practice that change earned
  • `tracker-item-dedup-bypassed` relates-to `comprehensive-code-review-after-0-43-0` — found by it
  • `tracker-sync-recipes-broken` relates-to `comprehensive-code-review-after-0-43-0` — found by it
  • `user-text-unescaped-in-render-brief-integrity` relates-to `comprehensive-code-review-after-0-43-0` — found by it
  • `user-text-unescaped-in-render-brief-integrity` relates-to `handoff-refusal-prints-story-titles-raw` — same defect class, other surfaces
  • `activity-log-detour-events-lose-epic` relates-to `comprehensive-code-review-after-0-43-0` — found by it
  • `capture-pipe-lesson-matcher` relates-to `lessons-index-cannot-drift-silently` — same corpus, same session — that epic made matchers checkable, this one adds one
  • `dehardcode-openspec-change-root-in-instructions` relates-to `gh-86-openspec-lane-silent-zero-progress` — both are store-relocation exposure -- #86 stops the render layer reporting a confident wrong count, this stops the instruction layer pointing an agent at a path that moved. Neither is urgent while openspec store list is empty on both machines
  • `drift-heal-leaves-claim-on-archive` relates-to `comprehensive-code-review-after-0-43-0` — found by it
  • `edd-measures-installed-plugin-not-worktree` relates-to `gh-86-openspec-lane-silent-zero-progress` — both are honesty-of-measurement defects in the eval/render layer -- one measures the wrong artifacts, the other reports a confident wrong count
  • `edd-run-policy-when-behavior-artifacts-change` depends-on `edd-corpus-expansion-nondeterministic-surfaces` — a run-always policy is only worth its cost once the corpus covers more than one scenario -- today it would mostly re-prove typo-routing
  • `flag-level-doc-gaps` relates-to `per-verb-help-from-flag-registry` — --help makes flags discoverable at the CLI; this makes them discoverable in prose — neither substitutes for the other
  • `gh-100-link-types-no-known-set` relates-to `gh-70-link-validation-gap` — same surface — 70 is malformed-format acceptance, 100 is unknown-type acceptance; one validator closes both
  • `gh-103-tracker-refresh-gate-before-specs` depends-on `gh-102-sync-blind-to-issue-comments` — the gate needs a per-epic watermark to answer 'changed since when' precisely
  • `gh-106-conductor-logs-never-gitignored` relates-to `gh-81-project-md-never-clean` — same class — pm generating a permanent working-tree diff; 81 is a tracked file that always differs, 106 is an untracked file that never goes away
  • `gh-129-attribution-nudge-at-commit-time` depends-on `gh-104-commit-nudge-fires-on-text-match` — the nudge belongs in commit-nudge, which has two known false-positive modes; adding an obligation before they are fixed compounds a noisy channel
  • `gh-138-refresh-warning-counts-terminal-epics` relates-to `gh-103-tracker-refresh-gate-before-specs` — 103 built the watermark and its warning; 138 is that warning counting epics with no work left to inform
  • `gh-148-propose-spec-association-from-header` relates-to `gh-92-epic-specpath` — 92 shipped the field; this makes it cheap to populate for a repo that already has design documents
  • `gh-149-add-epic-drops-valueless-flag` relates-to `gh-136-add-epic-notes-discarded` — same shape — a write surface accepting a flag it does not act on; fix them in the shared declaration together
  • `gh-152-flag-value-rule-outside-epic-flags` relates-to `gh-149-add-epic-drops-valueless-flag` — 149 fixed the six registry surfaces; this is where the rule stops and why widening it needs its own registry decision
  • `gh-154-openspec-validate-lifecycle-collision` relates-to `gh-123-adopt-openspec-1-9-cli-surface` — surfaced while investigating 123; the sharpest single reason the CLI cannot be a progress source
  • `gh-159-no-reader-for-gate-guard-state` relates-to `gh-80-gate-guard-refusal-applies-anyway` — same verb, same family — a guard whose observable behavior does not match what a reader concludes from it
  • `gh-161-strip-always-on-duplicated` relates-to `gh-115-gates-blind-to-absent-edit` — the absent-edit class, in the test suite rather than the product
  • `gh-162-delegation-drops-stdout` relates-to `gh-156-shipped-markdown-structural-check` — both are about output that goes missing without anything noticing
  • `gh-163-gate-verdicts-openspec-only` relates-to `gh-113-gate-verdict-records-no-diff-range` — the same unverifiable-verdict defect, scoped to the lanes 113 did not reach
  • `gh-92-epic-specpath` relates-to `gh-69-sync-no-done-signal-for-plans` — same missing association one artifact over -- 69 needs epic<->plan, 92 needs epic<->spec; design the field family once
  • `github-issue-tracker-sync` relates-to `ai-feedback-loop-github-issues` — issues created by the feedback loop are exactly what this syncs back in
  • `guards-that-read-engine-source-drift-silently` relates-to `functional-assertion-test-split` — found by 5.6
  • `lessons-index-cannot-drift-silently` relates-to `df-skill-commands-line-drift` — same defect class — a hand-maintained doc index drifting from the directory or table it claims to mirror, with nothing enforcing it; that one fixed the SKILL.md commands line the same way, with a test
  • `no-network-law-test-is-weak` relates-to `comprehensive-code-review-after-0-43-0` — found by it
  • `openspec-planning-completeness-instruction` relates-to `openspec-gate1-guard-on-apply` — both gate the SAME transition (into implementation) with different evidence -- OpenSpec's planning completeness vs pm's own Gate 1 review. SUPERSESSION RISK: if the two-gate schema fork (page D2) is ever adopted, gate1 becomes an OpenSpec artifact and these two collapse into one; do not build both independently without deciding which is authoritative
  • `openspec-planning-completeness-instruction` supersedes `conductor-skill-orient-reads-spec-inventory` — same two files, same 'state an obligation, name no command' shape, same completion test — one instruction pass, not three
  • `openspec-planning-completeness-instruction` supersedes `autonomy-grant-starts-at-apply-not-propose` — same merge group; the propose/apply boundary is one clause in the same emitted block
  • `parity-reassessment-per-platform` blocks `hermes-platform-support` — Hermes is the first platform that must go through this reassessment, under the ledger gate
  • `parity-reassessment-per-platform` supersedes `parity-propagation-skill` — one procedure, registered twice — the skill file IS this epic's stories written down
  • `platform-switch-orphans-old-rules-block` blocks `hermes-platform-support` — a switched repo would leave stale conductor instructions in the file the previous platform read
  • `release-member-moves-silently` relates-to `comprehensive-code-review-after-0-43-0` — found by it
  • `release-member-moves-silently` relates-to `gh-cfdude-pm-178` — release membership has no read-back
  • `verify-state-false-hand-edit` relates-to `comprehensive-code-review-after-0-43-0` — found by it
  • `codex-platform-support` depends-on `platform-parity-mechanism` — the structural parity gate and ledger must exist before any second-platform port
  • `codex-platform-support` depends-on `edd-harness-agent-behavior-testing` — semantic parity needs a blessed Claude Code baseline to compare against
  • `codex-platform-support` depends-on `rules-block-hardcodes-claude-slash-commands` — the rules block must stop hardcoding Claude Code slash-command syntax
  • `codex-platform-support` depends-on `hermes-platform-support` — Hermes goes first; the second port should reuse whatever the first one teaches about per-platform artifact layout
  • `registry-bound-poison-sweep-practice` relates-to `user-text-never-forges-output` — its 7.x sweep found 12 output sites Gate 2 had not found
  • `store-owns-claude-md-managed-block` relates-to `unit-rung-and-fixture-snapshots` — its E1 stop - the only path to sub-15s
  • `archived-delivered-gate2-regression-report` relates-to `archive-gate-reads-what-it-writes` — the standing-condition half its design deliberately leaves out
  • `commit-observation-gate2-minors` relates-to `commit-nudge-reads-the-whole-move` — Gate 2 round-1 and re-review Minors deferred at its archive
  • `disposition-invocation-prints-bare-no-deferrals` relates-to `archive-gate-reads-what-it-writes` — found by its Gate 1; that change extends the helper but keeps existing callers' output
  • `gh-cfdude-pm-179` relates-to `gh-165-deferral-assertion-mangled-and-immutable` — that fixed the :: parsing bug on one flag; this is the ergonomic gap it left — three adjacent flags for one concept, no two agreeing on shape
  • `handoff-refusal-prints-story-titles-raw` relates-to `archive-gate-reads-what-it-writes` — found by the 0.43.0 cross-spec review; that change quotes values in its own refusal only
  • `status-write-undone-by-heal-reports-updated` relates-to `archive-gate-reads-what-it-writes` — found by its Gate 1 lens A; the check keys on the same isArchived predicate
  • `sync-registers-ids-add-epic-refuses` relates-to `comprehensive-code-review-after-0-43-0` — found by it
  • `code-review-0-43-0-minors` relates-to `comprehensive-code-review-after-0-43-0` — found by it
  • `cross-session-epic-assignment` depends-on `state-lost-update-concurrent-sessions` — dispatch across concurrent sessions is unsafe while state.json has no read-modify-write guard -- assignment would be the feature that makes the lost-update bug routine
  • `epic-session-attribution` relates-to `cross-session-epic-assignment` — attribution records who DID the work; assignment records who SHOULD -- same field family, different lifecycle
  • `gh-104-commit-nudge-fires-on-text-match` relates-to `gh-91-autodetour-false-minimal-detour` — same hook, both false positives — 91 fires on a real commit with no detour, 104 on a non-commit
  • `hook-fixtures-couple-to-every-hook-step` relates-to `functional-assertion-test-split` — found by 6.4/6.6
  • `remaining-platform-investigation` supersedes `multi-platform-agent-support` — carries forward the uninvestigated remainder — Gemini CLI, Grok Build, generic AGENTS.md — after Codex and Hermes were separately registered with hands-on verification
  • `rules-block-cli-fallback` depends-on `remaining-platform-investigation` — the surviving half has NO known triggering platform, so it is unscopeable until that investigation names one — dormant but alive, rather than merged away
  • `triage-ignores-non-latin-text` relates-to `comprehensive-code-review-after-0-43-0` — found by it
  • `portfolio-architecture-consistency-scan` depends-on `epic-hierarchy-orchestration` — C presumes B's hierarchy execution exists to propagate cross-cutting decisions into

💡 1 tracker configured (github-issues) — consider `/pm:sync` this session to pull in any new issues.

LIFECYCLE TASKS (pm): a task that is bookkeeping about the change's own lifecycle — above all the one that archives the change itself, which always qualifies — carries the literal `<!-- pm:lifecycle -->` on its task line. Mark it when the source is authored OR AMENDED; a source predating this capability gets the marker the first time you touch it.

RULES (pm): classify detours before fixing — minimal → fix+commit then `/pm:detour --minimal`; substantial → `/pm:detour` (own proposal + PUSH). After any state change, `/pm:status`. Resume via `/pm:resume` + reconcile gate. Mirror every PUSH/POP to a one-line Honcho memory.

Manage with /pm:status · /pm:next · /pm:detour · /pm:resume, or the `conductor` skill.
```
