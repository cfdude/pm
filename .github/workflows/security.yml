name: Security Scans

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    # Weekly, so newly-disclosed CVEs are caught even when no code changes.
    - cron: "0 6 * * 1"

permissions:
  contents: read
  security-events: write

jobs:
  semgrep:
    name: Semgrep SAST
    # Pinned to a commit SHA (not @main) per the org's own pinGitHubActionDigests convention —
    # a moving branch ref would let a compromised or unexpectedly-changed org workflow silently
    # run in this repo's CI. Bump deliberately: re-check cfdude/.github before updating the SHA.
    uses: cfdude/.github/.github/workflows/security-semgrep.yml@8931286eb4057e4a2829325c1e04e6071ebf3ff4  # main @ 2026-07-30
    permissions:
      contents: read
      security-events: write

  trivy:
    name: Trivy filesystem scan
    uses: cfdude/.github/.github/workflows/security-trivy.yml@8931286eb4057e4a2829325c1e04e6071ebf3ff4  # main @ 2026-07-30
    permissions:
      contents: read
      security-events: write
